Explain, in careful technical detail and at length, the following passage.

Modern inference engines schedule prefill and decode work against a single stream of requests, and the cost of a token depends on which phase it lands in. Prefill is bandwidth bound and batches well across sequences; decode is latency bound and its arithmetic intensity is set by the batch the scheduler can assemble in one wave. A sampler that runs on the host adds a fixed per-token cost that does not shrink as the batch grows, so it shows up as a ceiling on aggregate throughput rather than as a slowdown on any single stream. Modern inference engines schedule prefill and decode work against a single stream of requests, and the cost of a token depends on which phase it lands in. Prefill is bandwidth bound and batches well across sequences; decode is latency bound and its arithmetic intensity is set by the batch the scheduler can assemble in one wave. A sampler that runs on the host adds a fixed per-token cost that does not shrink as the batch grows, so it shows up as a ceiling on aggregate throughput rather than as a slowdown on any single stream. Modern inference engines schedule prefill and decode work against a single stream of requests, and the cost of a token depends on which phase it lands in. Prefill is bandwidth bound and batches well across sequences; decode is latency bound and its arithmetic intensity is set by the batch the scheduler can assemble in one wave. A sampler that runs on the host adds a fixed per-token cost that does not shrink as the batch grows, so it shows up as a ceiling on aggregate throughput rather than as a slowdown on any single stream. Modern inference engines schedule prefill and decode work against a single stream of requests, and the cost of a token depends on which phase it lands in. Prefill is bandwidth bound and batches well across sequences; decode is latency bound and its arithmetic intensity is set by the batch the scheduler can assemble in one wave. A sampler that runs on the host adds a fixed per-token cost that does not shrink as the batch grows, so it shows up as a ceiling on aggregate throughput rather than as a slowdown on any single stream. Modern inference engines schedule prefill and decode work against a single stream of requests, and the cost of a token depends on which phase it lands in. Prefill is bandwidth bound and batches well across sequences. 

Write a long, structured explanation covering scheduling, batching, and sampler placement.