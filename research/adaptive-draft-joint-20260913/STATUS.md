# Status

- 2026-09-13: execution authorized by owner; E4B explicitly excluded. Selected Gemma 4 12B and Qwen3.5-9B.
- Isolated source branch created at `3bb21381848067d922dec1320261846f99ceb29a`.
- Plan registered before first model run. Dedicated 5090 provisioned; CUDA qualification and artifact staging pending.
- First pilot completed: 15/21 subprocess checks passed; 6 Gemma coding-prompt checks failed full-length equality. They agreed on the first 96 tokens but spec emitted 97. The engine's prefix-only gate did not reject this. Original raw receipts retained.
- Fixed the one-shot emission boundary and strengthened the native Gemma gate to reject unequal lengths and token-limit overshoot. A new `r2` namespace reruns all cells before admitting training.
- Recorder ON/OFF output checks completed on all three smoke prompts. Data is diagnostic only; no performance claim or final predictor fit yet.
- Training workload: 88 training, 24 calibration, 24 held-out code-review prompts, split by source file before model execution. Public MT-Bench smoke prompts are excluded from learning.
- Rerun `r2` passed all 21 subprocess checks (Qwen K=1..8 and Gemma K=1,2,4,6,8 on three prompts, plus three traced Gemma runs). Recorder ON/OFF token identity passed. This is greedy CLI qualification for the pilot, not sampled serving qualification or a speed result.
- Own-generation rank learning started on the 5090. Both targets are queued for 4,096-row rank artifacts, requiring at least 16,384 generated training tokens each. Gemma then collects full/static/adaptive-head traces on 6 training, 6 calibration and 6 held-out prompt groups and fits two small acceptance predictors. This first diagnostic has unequal head widths and cannot establish the proposed equal-capacity speedup.
- First learning pilot completed: 22,528 Gemma and 22,525 Qwen generated tokens; both rank artifacts meet the corpus floor. All 54 Gemma trace runs preserved complete target output. Two acceptance-predictor checkpoints fitted; held-out head-aware Brier 0.179607 versus shared-baseline 0.179322, a narrow loss in this pilot.
- All receipts and checkpoints are banked locally. SHA256 verification passed for all 56 learning-stage run logs and the 6,539-row labeled dataset. Formatting and the runtime-flag census passed. See `RESULTS.md` for scope and the remaining joint-controller work.
- Owner requires separate component measurements before combinations. Added an equal-capacity freeze ablation and explicit request timing; registered three independent A/B experiments in `ISOLATED.md`.
- New-host pilot passed 21/21 checks. Head-only baseline completed and banked: 48 pairs, eight held-out groups, six repetitions; 4.41% lower paired request time, 95% descriptive interval 2.58–6.15% reduction. All 100 head-stage raw logs verified. Confidence-only running; depth-only follows after its receipt bank. No joint arm scheduled.
- All three isolated component baselines completed: head request time -4.41%, confidence stopping -4.59%, adaptive depth -4.14%, each against its own fixed control. All 300 component runs passed exact output identity; 144 measured A/B pairs total. Final raw/trace/config/prompt/rank/binary audit passed from the saved worktree receipts. See `ISOLATED-RESULTS.md` and `isolated-audit.json`.
- Rental cleanup confirmed 2026-09-13 11:32:53 UTC: zero remaining instances from this campaign. Repeated SSH acknowledgement timeouts required manual takeover after the final component archive had already been saved and audited. Existing component mechanisms have been measured separately; learned replacement, learned confidence/depth policies, Qwen component replication and pairwise/joint experiments remain pending in `NEXT-ISOLATED.md`.
