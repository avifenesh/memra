# Session C day-twenty-one handoff (the two red gate lines resolved; the door's gates green on A's slice)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged main `124dacc76` (`586a55e47`) and lane A `b967b8d30` (`b4cb89cec`; INDEX.md virtual-base marker hunk dropped, A's day17 row kept). Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; every cell `executed-not-qualified`; no commit on main, no PR.
- Failure gate (`7efab005d`, `tools/kv-host-spill-failure-gate.sh`): the pool-full cell matched the insert-path `skip demote: entry` line, reachable only at `MEMRA_KV_HOST_TENANT_PCT=100`; under the default 50 (`49d1d6f65`) the server refuses BEFORE the copy with the typed share-cap line (suffix `405466cf7`). Gate now asserts the full shape the effective cap produces (bytes and budget) plus `prefix_host_tenant_rejects >= 1`, prints the arm; server unchanged. Green since day 13 only with an out-of-band 100 (lane D `DAY8-CELLS.md:98`).
- Fault gate (`7efab005d`, `tools/kv-host-contract-fault-gate.sh`): promote-reject's `1 of 34 items` now comes from the server's r2 D2H receipt (`d2h_receipt_items`, `reject_total_matches_receipt`); 34/32 on the 27B default/plain, 18/16 on the 9B.
- Verdicts on tree `9be3f7373`, both cards, all arms (`DAY21.md`, `pro-single-day21/`, `rtx5090-day21/`): failure `ALL GREEN` x4, fault `ALL GREEN` x2, identity `ALL GREEN (teeth=0)` x4 per card. Zero `FAIL:`.
- Drivers: `day21-cell.sh` (one gate per named environment, internal or collector lock), `day21-box-run.sh` (collector, bounded retries). Hygiene: fmt, clippy `-D warnings` (release, merged crates), flags census, markers, public boundary 0 new, diff-check: clean.
- NOTHING RUNNING for C: both locks free at close, no server of mine on either card, bundles deleted, `/tmp/spill-c-day21` removed, `/root/wt-c` clean at `9be3f7373`.
- NEXT: the whole-budget arm (`MEMRA_KV_HOST_TENANT_PCT=100`) as a one-boot cell if anyone needs the insert-path line exercised; `docs/TESTING.md:1654` day-16 record may want a pointer; HOSTPREFIX review 2026-10-05 items 1, 2, 3, 5, 6 unchanged; `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04; memra#617, #365 open for the lead.
