# Session C day 75 handoff (2026-09-26): NEED TARGET CARD (DAY75 i16 on the 285K class; DAY74 induce-b on a 9950X and the 285K, >= 98 GiB MemFree; DAY71's default half on BOX15's machine); owner calls C1(c) and C10

- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`, tip pushed at every milestone (hooks on, the announced development mode on engine-source pushes); main `5228ff0cd` (#726) merged into the lane (`2e046ef8b`); no commit on main, no PR.
- DAY64: the first card cell on BOX15 (a Ryzen 9 9950X host) recorded as it reads (`flat`, `flat`, `matches`) with its noise placed (the door's per-boot host-CPU bimodality); the admissibility clause and the rerun `i15b` registered (section 5); the rerun's first attempt on BOX16 void (nvcc segfaulted in a build, no cell ran; section 5a). The lead runs `day64b-box.sh` on BOX14 (the Idaho 285K class) after lane B's sitting, about 18:50Z.
- DAY65: C12's cell `pin` ran on BOX17 (BOX15's machine, run by the lead as registered): `pin_does_not placement_does_not_track`, the L3-domain hypothesis refuted (`DAY65.md` section 2, 190 receipts in `pro-single-day65/`).
- DAY66: C12's cell `freq` ran on BOX18 (BOX15's machine, run by the lead as registered): `clock_does_not_track thp_does_not_track` (`DAY66.md` section 2, 191 receipts in `pro-single-day66/`).
- DAY67: C12's cell `probe` ran on BOX19 (BOX15's machine, run by the lead as registered): `none_tracks` by the strict rule, the reader checked correct; the probe points to a lower effective core clock in the slow state, transient (`DAY67.md` section 2, 190 receipts in `pro-single-day67/`).
- DAY68: C12's cell `eclock` ran on BOX21 (BOX15's machine, run by the lead as registered): `gate_tracks` (`DAY68.md` section 2, 190 receipts in `pro-single-day68/`).
- DAY70: C12's cell `sched` ran on BOX23 (BOX15's machine, run by the lead as registered): `none_tracks` (no run-queue wait, nothing the scheduler accounts on the owner's CPU or its sibling; this kernel accounts no hard-interrupt time; DAY67's DRAM reading corrected in `DAY70.md` section 2 and `DAY67.md`; 189 receipts in `pro-single-day70/`).
- DAY71: one cell `core` for the remaining candidates (the core's TSC, APERF and MPERF around the gate probe via `RDPRU`, interrupts, softirqs, the sibling's idle states, package energy and sensors, the card's PCIe traffic) plus a class verdict from two 9950X machines; the probe change `6bad38150` (decide-by 2026-10-09). Machine `b` ran on BOX24 (run by the lead as registered): `not_reproduced` (19 slow, 1 fast); beside it cycles lost at a full clock (12.4 to 14.2 cycles per step, MPERF over TSC 1.000) and compaction failing to migrate about 70,000 pages a second through every slow door span, none in REF's; 194 receipts in `pro-single-day71-b/` (`DAY71.md` section 2). Section 3: the reader's empty-`/proc/interrupts` defect fixed, `intr_rate` and `migrate_fail` added before BOX15's half. NEED TARGET CARD: `D71_BUILDS="p71=6bad38150" bash /root/wt-c/research/spill-c-20260919/day71-box.sh` on BOX15's machine (one RTX PRO 6000 Blackwell Workstation Edition, root in the container, at least 48 GB MemAvailable, about 15 minutes); then `python3 day71-read.py --class pro-single-day71/core pro-single-day71-b/core`.
- DAY69: OWED C3, the contracts-door packet read up to lane A day 48 and ruling 57 (`DAY69 PACKET LINES checked=32 missing=0 -> PASS`).
- Resync 2026-09-26 after the rig's reboot: tip `9c324e44f` read from OWED and STATE; origin/main is `cf6b82db4` (#727), not merged into the lane yet so queue v9's tree does not move under it. The local RTX 5090 is healthy and shared with lanes A and B under `/tmp/memra-5090.lock`.
- 5090: queues v9, v10 and v11 finished and are read (v11's i15 rerun void with a foreign compute app, pid 279749, colbert-2, on the card and no lock; gap15 rerun inadmissible). RUNNING (mine): queue v12 (`rtx5090-queue-v12-20260926.sh`, pid 1510382, log `target/c-queue/queue-v12.log`): builds `run-gen-i16` into `target/c-bins`, then DAY75's `i16` on the 5090. Remove `target/c-build-wt` (`git worktree remove`) when v12 ends; profiles under `target/c-profiles-day72*`, `target/c-profiles-day75` are by hash in the receipts.
- DAY64 section 5b: `i15b` read as registered (admissible; `i14=flat i15=flat door=i15 vs_ref=loses`; 229 receipts in `pro-single-day64b/`).
- DAY72 (OWED C11): gap15 on BOX29 read as registered: admissible, `partA=cpu_side partB=gpu_stall` (the door's GPU waits on its prefetch path).
- DAY75 (OWED C11): I16 `eeacfaf50` (the door's next-expert prefetch after the current expert's launch), CPU gates green. NEED TARGET CARD: `D75_BUILDS="i15=2243b1fe2 i16=eeacfaf50" bash /root/wt-c/research/spill-c-20260919/day75-box.sh` on a Core Ultra 9 285K host with one RTX PRO 6000 Workstation (nsys; about 35 minutes).
- DAY73 (OWED C12): `compact` ran on BOX31 (a 9950X, as registered): `not_reproduced`, no slow run and no compaction; the BOX30 9950X3D2 diagnostic's one slow run is its only span with compaction (`DAY73.md` sections 2, 2a).
- DAY74 (OWED C12): `induce` read BOX31 `not_run` then `not_induced`, BOX29 `void` (no RDPRU on Intel). `induce-b` (section 4) ready. NEED TARGET CARD: `D74_BUILDS="p71=6bad38150" [D74_RIG=<name>] bash /root/wt-c/research/spill-c-20260919/day74b-box.sh` on a Ryzen 9 9950X host and on the 285K class, each with one RTX PRO 6000 Workstation, at least 98 GiB MemFree at the start (the page-cache eviction of unused files as on BOX31), root in the container (about 25 minutes each). DAY71's default half still waits on BOX15's machine.
- Next: read gap15, compact and the 5090 queue's cells as they land; the improvement DAY72 points to and the remedy DAY73 points to, each registered before code.
