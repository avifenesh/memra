# Session C days 40 to 58 handoff (2026-09-25): BOX8 released; the RTX 5090 requires a reset

- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`, tip pushed at every milestone (hooks on, the announced development mode on engine-source pushes); no commit on main, no PR. `origin/main` has moved (#719, dsv4 only, no file of this lane).
- C1 (the MoE slot cache door, decide-by 2026-10-04): rung 0 attributed on both cards; ten rungs and two fixes with every RTX 5090 verdict in its DAY file (days 43 to 50, 57, 58; I6's default-budget regression fixed on the tuned tree, I8 and I5 fixed as I8f and I5f); the target card's ladder read (`DAY52.md` section 11). Final tree `62e848b1f`. Target card: G1 PASS, G2 PASS, `DAY51 VERDICT rig=pro-single integrity=ok -> door_wins` (decide-b; decide void on a trace term I10 contradicts, `DAY51.md` 1c). REF (the legacy's own prefetch) is faster than the door; reported. The owner's promotion call.
- C4 CLOSED (`DAY54 VERDICT -> moved_at s5=58b814abe`), C6 CLOSED (ALL GREEN both cards), C8 CLOSED (`prime_always_admitted`), C9 closed earlier. C5: target card PASS (attempt 4); two `MEMRA_DSPARK_PARTIAL_RESTORE` defects found and fixed (`c4e18a4e3`, `62e848b1f`); C5b/C5c sequenced after lane A's helper and span work. C3 current through A day 36 plus day 54's answer; A days 37 to 41 in flight.
- The target sitting (BOX8) ran 2026-09-24 22:10Z to 2026-09-25 02:17Z; receipts mirrored file for file into `pro-single-day52/` (`MANIFEST.sha256`, 2,589 files); box scratch, binaries and build targets removed; BOX8 released.
- The local RTX 5090 fell into `GPU requires reset` at 01:25:23Z (bad register reads, Xid 119, Xid 154), 32 s after this lane's last cell ended rc=0 (`rtx5090-fault-20260925/kernel.log`); nothing acted on. Owed on it: DAY51's G1, G2, decide-b on the final tree; C5's attempt 4.
- RUNNING (mine): `/tmp/c53-smoke/queue5.sh` (copy `rtx5090-queue-v5-20260925.sh`, log `/tmp/c53-smoke/queue.log`), polling every 5 s for an idle healthy card (bounded 6 h per cell), then DAY51 hashlock, spec, decide-b and C5's two cells. Safe to stop by killing its pid; its binaries are in `/tmp/c40-bins` (run-gen-final, run-spec-final) and `/tmp/c53-bins` (memra-server-c5). Scratch kept only for it.
- Next: when the card is reset, the queue runs; read each cell with its reader, record in DAY51 and DAY56, commit.
