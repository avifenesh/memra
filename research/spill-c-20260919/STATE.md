# Session C day-twenty-two handoff (Move 1 whole under the door: the fault gate with its floor, the whole-budget arm run, the three gates green on the target card)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged main `3df055601` (#622, #626) and lane A `614c53f71` (A day 18, the promote half) as `cf45be796`; worker.rs conflict resolved keeping BOTH main's `armed()` re-check (revuto round 2 on #622) and A's settle order at `host_demote_prefix_ref` and `host_promote_prefix_hit`; memra-server suite 786 passed. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; every cell `executed-not-qualified`; no commit on main, no PR.
- Tree under every cell: `98170f182`. Target card (`pro-single-day22/`, twelve cells, zero lock retries, no foreign compute app): fault `ALL GREEN` x2 (65 ok, floor line `items=N >= 2` visible, `items=34` / `items=32`), failure `ALL GREEN` x4 share-cap (15 ok) and x2 whole-budget `MEMRA_KV_HOST_TENANT_PCT=100` OFF and ON (14 ok, first run of that arm), identity `ALL GREEN (teeth=0)` x4 with A's promote-off-the-tick lines in the ON logs.
- Local RTX 5090 (`rtx5090-day22/`, 9B, after 8 bounded lock retries behind other sessions' servers, never signalled): fault `ALL GREEN` x2 (65 ok, floor line visible, `items=18` / `items=16`); a foreign `colbert-2` python co-tenant seen in the after snapshots, recorded, not touched; whole-budget arm, failure and identity gates not run locally today.
- Recorded for the lead (`DAY22.md`): under the door at `TENANT_PCT=100` the pool-full demote runs the whole contract (submit, D2H receipt, publish, 160 MB) before the insert-path `skip demote` refusal; deliberate per the `host_bytes > host.budget` comment (ruling 15), reachable only with the share cap disarmed.
- `HOSTPREFIX-DOOR.md` review rows: fault (floor), failure (whole-budget arm as a run receipt), identity (Move 1 whole). Missing list: arena handoff scoped (ruling 28); the DFlash tail slice survives day 20 unanswered (day 20 bounded the standalone tap sink, not the tail image; the export manifest hashes are the identity input, unbound); 3, 5, 6 unchanged.
- Drivers: `day22-cell.sh` (adds the `-pct100-` environment), `day22-box-run.sh` (cell list argument).
- NOTHING RUNNING for C at close: both locks free, no server of mine on either card, bundle deleted both ends, `/tmp/spill-c-day22` removed, `/root/wt-c` clean at `98170f182`.
- NEXT: the whole-budget arm on the RTX 5090 (not run; the 27B receipt stands for the arm); the RTX 5090 pair for the door (review item 5); `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04; HOSTPREFIX decide-by 2026-10-05; memra#617, #365 open for the lead.
