# Session C day-fourteen handoff (ruling 16 landed: the door binds MTP draft-bearing entries)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged main `30e433c4c` (`fbe6c1635`); pushed through the hook (`fbe6c1635`, `62f48ecec`); no commits on main or the lead branch.
- NOTHING RUNNING for C: no tmux on BOX3 (`c14run` exited), no GPU process; `/root/wt-c` detached at `62f48ecec`, receipts `/root/spill-receipts/c-day14/` mirrored to `research/spill-c-20260919/pro-single-day14/` (bundle removed there).
- Code `62f48ecec`: `host_tier_entry_class` (pure; GLM state and the DFlash tail refuse by name), `HostTierDraftSource`, `host_tier_draft_program` (artifact/plan/numeric fold in the MTP head's source and the draft rows' encodings), `HostTierPrograms` + `program(key, class)`, `bind_tier_image` binds `Role::Draft` K/V with checksums, shape blob v2, pinned sum includes the draft plane; FLAGS row, door doc, TESTING rows updated. Census first: `e82363a8b` (`HOSTPREFIX-DOOR.md` "Draft planes").
- Target card (binary `7ffa58cb...`, 600 W, collector, N=1, executed-not-qualified): identity gate under the DEFAULT spec env `ALL GREEN` both arms, 13 verdict lines equal, demote bytes equal (160.7/160.6/161.1/161.1 MB), 23-event prefix sequence equal, 0 refusal lines ON (day 13: `5 FAILURE(S)`); failure gate default `1 FAILURE(S)` both arms, same pre-existing pool-full line (day 13: `6`); plain pairs and serve-smoke (33 lines) unchanged from day 13. `verify-day14.py`: `DAY14 REPLAY: PASS`.
- Findings: draft plane adds ~0.2 MB per entry; the second promote under spec passes verify and is declined by the device protected-share rule identically in every arm and on day 13 (not a door effect); the verify digest is blind to the draft plane (the `Role::Draft` checksums are its receipt; v3 digest is its own slice).
- Local battery on `62f48ecec` green: fmt, 744 + 70 + 2 tests (4 new), clippy -D warnings, check-flags 867, docs-registry census, diff --check.
- NEXT: Option B (D2H through `TransferEngine`, owned planes, ruling 14) with the draft plane as two more segments; the DFlash tail slice needs an export-dir manifest identity and a gate that boots a DFlash drafter.
- OPEN: `MOE-SLOT-CACHE-DOOR.md` items 1, 3 second half, 4, 6 (decide-by 2026-10-04); `HOSTPREFIX-DOOR.md` decide-by 2026-10-05.
