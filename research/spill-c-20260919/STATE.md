# Session C day-thirty-three handoff (the on-tick hash against write-combined memory on the RTX 5090 class: measured)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; the lead's local ref `lane/spill-integ42-20260922` (`af92791f6`) merged clean as `596dded90` (no PR existed, not on origin); pushes in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no commit on main, no PR; no engine change (the `hash-micro` diagnostic bin gains an opt-in `--two-step` arm, day-18 output unchanged without it).
- Pre-registered (`71bd494db`, `DAY33.md` 1) before any run: the question (section D item 6 on this card), H1 (the hash reads the WC destination and the micro-cell rate does not apply), H2 (a cached copy, staging buffer or device bytes), H3 (the destination is not WC despite `PINNED-DEFAULT`), their predictions, two clauses (the FASTEST WC route at 54,800,000 B against the SLOWEST day-31 ON demote `in`, 60.4 ms).
- Run on the local RTX 5090 Laptop GPU, one collector hold on `/tmp/memra-5090.lock` (one lock refusal, zero idle waits; the runner restarted detached before any hold, stated), 56 to 58 C, no compute app before or after, premise `PINNED-DEFAULT ... kind=write-combined flags=4` inside the hold; `rtx5090-day33/hashwc/`, 41 ok, 0 FAIL, admissible.
- Verbatim: `DAY33 HASH-WC VERDICT: 54.8MB cached 11.9 wc 473.8 heap 11.9 wc_copy 224.2 (N=10 each); 160MiB cached 36.6 wc 1450.8 heap 37.2 wc_copy 686.8 (N=10 each); day31 ON in median 48.3 max 60.4 (N=12) in_minus_completion median 21.6 (N=12); H1-single fits=False H1-twostep fits=False -> H1 refuted (no WC read route fits the ON demote's wall time; H2 or H3 stands, separated by the code census, not by this cell); pinned=write-combined; admissible=True`.
- Read: the WC hash rate is size-independent (0.116 GB/s at both sizes); the memcpy route reads the same uncached memory at 0.258 GB/s; neither fits the ON demote (7.8x and 3.7x the slowest); two cached passes (23.7) sit beside the post-completion segment (21.6, max 23.0). H2 against H3 is A day 27's code census, not this cell. The day-31 `D2H receipt` line carries no timing by format; nothing missing, no new boot.
- One reading-script fix after the run (the lock-proof field lives in the collector's `lock.json`, not `ev/LOCK.json`); first output kept as `reading.first.log`; no cell re-run.
- Records: `DAY33.md`, `HOSTPREFIX-DOOR.md` section D item 6 (the 5090 measurement; A's code-side answer lands separately), `research/INDEX.md` row, this file. Checks clean (shellcheck, check-flags, markers, diff --check, cargo fmt, zero em dashes, no host/id/cost).
- Owed, unchanged: the owner's decisions at 2026-09-23 (admission door), 2026-10-04 (MoE slot cache, VMM), 2026-10-05 (contracts door), 2026-10-06 (park door); item 6's promote-side half; the per-plane form of H1 (arithmetic only); the double-park slice (A day 26); the recurrent f32 term off the tick; a tenant-stall cell on the 5090 class.
- Budget about 2.4 agent-hours against 3. NOTHING RUNNING for C at close: no server, no lock, no GPU process; scratch removed.
