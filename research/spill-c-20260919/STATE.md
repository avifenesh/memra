# Session C day-thirty-eight handoff (the per-tick split of day 37's cell and the 9B byte split; no card)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged `origin/main` `0c86309bd` (#652) as `1215dff35`; pushes in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no commit on main, no PR, no engine change, no GPU run.
- Pre-registration `1bb7f4dd7` (`DAY38.md` section 1), pushed before the reader ran: ticks by position after the fire (tick 1 the first gap over 3 x p50 at or after index 23, tick 2 the next), day 37's rule per tick unchanged, `not_defined` when any run lacks the tick; H: option (a) moves the bind pass off the poll tick (tick 2).
- The reading `5746d9685` (`day38-cpu/tick-split.log`): tick 2 demote-on `moved` (`-20.4/2.2`, `-21.0/2.5`), did-demote `moved` (`-21.7/3.2`, `-22.0/3.8`); tick 1 `under_resolution` in every arm and both DiDs; demote-off `under_resolution` on both ticks; `DAY38 HYPOTHESIS ... -> consistent with H`; promote tick 2 `not_defined` (option (a) promote-on stretches one tick, 40 of 40).
- Within each binary, demote tick-2 ON minus OFF `+29.7 unc 3.8` (base) and `+7.0 unc 3.5` (option (a)); the second matches the ledger's `copy_settle` 8.48 / 8.30 in size, not timed.
- The 9B split `f7c2c059c` (`day38-cpu/byte-split.log`, 46 banked logs): KV planes 950,272 B exact, token ids 256 B, heap payloads 53.7 MB; logits in (950,272, 1,099,744) B, conv + ssm + hidden in [52,599,728, 52,699,728) B; conv, ssm and hidden on no banked line (the separating line named, engine code).
- Records: `DAY38.md`, `DOOR-DECISION-PACKET.md` (status, section 4 rows, item 7, section 6, appendix A day 38), `HOSTPREFIX-DOOR.md` section E DAY 38, `research/INDEX.md`; no recommendation.
- Owed: the target card's demote-class tenant-stall cell on the option (a) tree (none has run); the second tick's split into owner-thread segments (not timed); the 9B conv/ssm/hidden split (engine line); the owner's decisions at 2026-09-23, 2026-10-04, 2026-10-05 (contracts door), 2026-10-06; Move 2 owed item 1 (A day 30, A's lane); the double-park slice question; an always-admitted prime arm on the RTX 5090 class (new pre-registration, not run).
- NOTHING RUNNING for C at close: no server, no lock, no GPU process; no worktree created; scratch removed.
