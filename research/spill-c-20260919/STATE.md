# Session C day-thirteen handoff (Option A landed: the HostPrefix contracts door)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged main `5ecfd262c` (`c3154752d`) and `0e7741b76` (`6ec4d29a6`); no commits on main or the lead branch.
- NOTHING RUNNING for C: no tmux on BOX3 (`c13run`, `c13chain2` exited), no GPU process; `/root/wt-c` detached at `b81881dad`, receipts `/root/spill-receipts/c-day13/` mirrored to `research/spill-c-20260919/pro-single-day13/`.
- Code: `ff46abc75` door (`MEMRA_KV_HOST_CONTRACTS`, `HostTierContext` constructor, `ProgramIdentity` at load, `memra_kv::tiered::hostprefix::{tenant_salt, shared_governor}`, FLAGS row decide-by 2026-10-05, `HOSTPREFIX-DOOR.md`); `e7e23dcf4` over-budget image keeps the OFF `skip demote` line; `b81881dad` no tier means no construction, one `[kv-host-contracts]` line.
- Target card (pass 2, binary `9ed02495...`, 600 W, collector, N=1, executed-not-qualified): serve-smoke 33 verdict lines byte-equal OFF/ON; identity gate plain `ALL GREEN` both arms, demote bytes equal (160.5MB, 160.4MB), 1 promote = 1 `verify ok`; failure gate plain `1 FAILURE(S)` both arms (pool-full line pre-empted by the tenant share cap, red on main); lane B reclaim gate identical line and `identity=aa6cc3291b981646` both arms (red on this tree, B's fix absent). `verify-day13.py`: `DAY13 REPLAY: PASS`.
- Findings for the lead: spec serving publishes draft-bearing boundary entries that lane B's surface refuses by name, so under the gates' default env the ON arm fails the feed assertions (identity 5, failure 6) while byte identity holds; equal counts measured with `MEMRA_SERVE_SPEC=0`. `tenant_salt("")` derives (the no-keyring default namespace), the task's "empty refuses" was not implemented, reason in DAY13.
- Local battery on `b81881dad` green: fmt, 739 + 67 + 2 tests (10 new), clippy -D warnings, check-flags 867 names, diff --check, docs-registry census.
- Push: see the report; the perf-ci pre-push arm indicts main's engine files carried by the merges (day-twelve pattern); no skip variable, no `--no-verify`.
- NEXT: lead decision on the spec surface (draft planes into lane B's `bind_tier_image`, or plain-only door), then Option B (D2H through `TransferEngine`, owned planes, ruling 14).
- OPEN: door doc `MOE-SLOT-CACHE-DOOR.md` items 1, 3 second half, 4, 6 (decide-by 2026-10-04); `HOSTPREFIX-DOOR.md` decide-by 2026-10-05.
