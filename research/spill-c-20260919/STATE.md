# Session C day-eighteen handoff (MoeSlotCache door items 1, 3, 4, 6 with their day-18 inputs; the HOSTPREFIX review table and hash micro-cell)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged main `5804cac6a` (`922927619`, #612: the lead's `SubmitGuard` and two fixture cells on memra#586, read not re-derived; #586 closed); `hash-micro` bin `e16bc69e8`; pre-registration `7050170ba` (pushed before any GPU run); receipts and docs after. Every push today in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode (`UNQUALIFIED DEVELOPMENT ... no GPU qualification claimed`, a `log_skip` row each): no qualification claimed, every cell `executed-not-qualified`. No commits on main or the lead branch; no PR opened.
- NOTHING RUNNING for C: BOX3 tmux `c18build`, `c18cells` exited; `/root/wt-c` detached at `e16bc69e8` (`refs/bundle/c18`); receipts `/root/spill-receipts/c-day18/` mirrored to `pro-single-day18/` (bins excluded); lock free, no server. Local: `rtx5090-day18/` (three cells through `/tmp/memra-5090.lock`).
- Item 4 (`overlap`, target card, N=5/arm/order, both orders, one 976 s hold, 36 to 40 C, 189 W peak): `decode_off_s=0.408 decode_on_s=2.343 decode_ratio=5.743 ... door_cost_ms_per_decode_token=60.47 ... install_on_s=74.84 ... -> sync_miss_path_slower`; tape identical; the item is confirmed as the promotion blocker at the 8 GiB budget; the synchronous path is the door's only miss path (no separate arm).
- Item 3 second half (`hashlock`, both cards): `-> hash_lock_refuses` (exit 1, `Error: "experts-via-tier artifact SHA256 mismatch"`, no door line; the same artifact `MATCH` without the door); scale admission: CPU proof only (`memra-gguf expert_banks` 10 passed), no scale-bearing artifact on either rig.
- Item 6 (`serverdoor`, both cards): `-> door_unreachable_in_serving`, `flag_silently_accepted=True` (the server rejects no unknown argument; hygiene finding). Item 1: census + CPU proof (`memra-tier owner_proxy` 4 passed, `WrongOwner` from a spawned thread); no PP gate binary carries the installer, so no native cell.
- HOSTPREFIX door: review table appended to `HOSTPREFIX-DOOR.md` (every owed cell, receipt, verbatim line, state; six missing items). Hash micro-cell both cards: target host cached 77.9 ms per 160 MiB (2.15 GB/s), WC 1698 ms (0.099 GB/s); local host 37.3 / 1431.6 ms. Arithmetic: A's cached demote delta (76 ms) is one hash pass; the promote-share delta (1.4 ms) and day 16's WC delta (130 ms against a 1698 ms pass) are named census questions for the review, not resolved.
- Replay correction stated in `DAY18.md`: `day18-replay.py` v1 counted the bare substring `experts-via-tier` (the refusal line carries it); corrected to the pre-registered bracketed tags before any other result was read. No threshold moved.
- NEXT: nothing owed before the two decide-bys that a cell can produce on these rigs; the remaining items are code (installer in a PP gate binary; arena lease handoff; verify digest v3), an artifact (scale-bearing), or another rig class (the 5090 pair for the HOSTPREFIX door).
- OPEN: `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04 (items 1 to 6 hold their inputs; the decision is the review's); `HOSTPREFIX-DOOR.md` decide-by 2026-10-05.
