# Session C day-sixteen handoff (Option C landed: the promote H2D through the TransferEngine; the WC pair measured; PR #605 review fixes)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged main `1b354be59` (`48ec3f0c5`) then `435a57a75` (`2ad0be159`); census `e7d70a77a`, code `25891baa9`, WC doc and replay `5b03a0a47`, docs `4e7a0a744`, gate matcher fix `75573cad3`, receipts `a902fd2e9`, day docs `36fa5262b`, PR #605 review fixes `4467131f6` (the abort asks the engine via `cancel` and recovers only the accepted items; faults `contract-promote-reject|readyview`; GPU cells and gate cells), review receipts after; no commits on main or the lead branch. Push REFUSED twice, verbatim in `DAY16.md` "Push": by the perf-ci freshness arm on `tier_transfer.rs` (at `a902fd2e9`), then, after main `435a57a75` retired that arm for the #547 content-bound qualification, by `UNQUALIFIED: source inputs changed: ...` (at `a59e33e86`); no skip variable and no `MEMRA_RELEASE_QUALIFICATION_MODE=development` (a logged skip) used; tip unpushed, reported by SHA; the lead runs the battery on integ19.
- NOTHING RUNNING for C once `c16rrun` exits: BOX3 tmux `c16build`, `c16run`, `c16fix`, `c16wc`, `c16rbuild` exited; `/root/wt-c` detached at `4467131f6` (`refs/bundle/c16r`); receipts `/root/spill-receipts/c-day16/` and `c-day16-review/` mirrored to `pro-single-day16/` and `pro-single-day16-review/` (bins and bundles excluded). Lane B held the card before and after my driver; my cells waited through the runner's bounded retries, no holder touched.
- Code `25891baa9`: `CudaTransfers::retain_host` (the one engine addition, the host mirror of `retain_device`); `host_kv_planes_from_contract` (the kv_tier_gate restore sequence: fresh `alloc_u8` destinations registered and retained, source twins, one batch, producer fence, `synchronize`, `Completion::require` against the D2H receipts BEFORE `ready_view`, consumer fence, `retire_source`, `release_producer`, `retire`, `acknowledge`, `take_plane`), `host_promote_contract_abort` (cancel, recover every source per rule 1 with the pointer checked, drain, retire, acknowledge, release fresh planes; nothing discarded), `HostPromoteFailure {Failed, Refused, ReceiptMismatch, Latched}`, `device_entry_from_host(.., tier)`, fault sides `take_fault(demote)`, `contract-promote-presubmit|postpublish`, device ledger at three budgets, the H2D receipt line (its digest equals the D2H line's for the same entry); OFF untouched (`plane_up` intact).
- Target card (binary `cd8e9c11...`, 600 W, collector, N=1, executed-not-qualified, `verify-day16.py`): gputests `6 passed` (B's two, C's four); fault gate `ALL GREEN` on four cells (`faultgate-fix`; the first sitting's `2 FAILURE(S)` were the gate's own matcher, `75573cad3`); identity default and plain `ALL GREEN` both arms, equal demote bytes, `verify ok`, 16/15 events equal, one H2D receipt per ON promote with the D2H's digest; failure `1 FAILURE(S)` both arms (pre-existing pool-full), the digest cell ON names the flip at the H2D and `VERIFY FAILED` follows; tenant fix arm `PASS` both arms, 8 D2H + 2 H2D receipts; serve-smoke 34 lines equal; lane B's evict (busy_overlap_s 21.659 vs 21.661, timing) and newest identical.
- Finding: the day-15 promote delta was the inline demote inside the promote's timing window. WC pair (`WC-DESTINATIONS.md`, `wc-pair2-retry3/`, one 64 s lock hold, 37..51 C, <=492 W, N=5 per arm per order, orders agree within 0.5 ms): demote median OFF 37.8 vs ON 169.2 ms, promote line 12.2 vs 171.7, promote minus inline demote 4.5 vs 33.2; a first-touch step of ~35 ms on the first three demotes in BOTH arms (fresh pinned regions); steady-state demote 6..8 vs 136..140 (~130 ms door cost per 160 MB entry, the split needs the hash micro-cell); the first sitting `wc-pair` was refused by the cell script's own `set -u` trip before any boot. Not a verdict.
- Local battery on `25891baa9` green (`day16-local/`): fmt, 758 + 71 + 515 tests (0 failed), clippy -D warnings (3 crates), check-flags 865, docs-registry census (58 tables, 901 rows), diff --check.
- NEXT before the decide-by (2026-10-05): the WC decision (the engine's allocation flag; a hash-speed micro-cell; the cached-pinned arm measured both rigs), the arena path, the DFlash tail slice, verify digest v3; the pool-full failure-gate line (pre-existing).
- OPEN: `MOE-SLOT-CACHE-DOOR.md` items 1, 3 second half, 4, 6 (decide-by 2026-10-04); `HOSTPREFIX-DOOR.md` decide-by 2026-10-05.
