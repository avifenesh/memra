# Session C day-nineteen handoff (memra#617 fixed; the serverdoor shape re-run; the arena handoff and DFlash tail scoped)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged main `653c997f4` (`05b42ede9`; PR #616 with day 18 was still OPEN, main does not carry day 18 yet); two diff3 base markers inherited from main deleted (`6d09ac60f`, byte-identical to the lead's #616 deletions, ruling 27 census OK on the lane tree). Every push today in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode: no qualification claimed, every cell `executed-not-qualified`. No commits on main or the lead branch; no PR opened; #617 left open with the receipt comment.
- memra#617 fixed (`319f1ad58`): `crates/memra-server/src/argv.rs` `validate`, the first statement of `serve_with` (`lib.rs`), exit 2, the token named, the accepted list printed; `--lane`/`--rate-limit`/`--keys` alone are refused too; every documented shape kept (`--version`, `-V`, `--gen-key`, `--revoke-key`, `--lane`, `--rate-limit`, `--keys`). Tests: `argv::tests` 7 passed; `tests/argv_boot.rs` 7 passed (boots the real binary). `docs/SERVING.md` one sentence. Not run on the target card (owed if the lead wants it before main; argv admission precedes every device statement).
- Cells, local RTX 5090 through the collector (`rtx5090-day19/`): `serverdoor19b` `SERVERDOOR19 rule flag_exit=2 flag_refused=True flag_booted=False flag_ready=False flag_door_lines=0 envon_ready=True envon_request_ok=True envon_door_line=True envon_exit=0 envbad_exit=1 envbad_refused=True envbad_ready=False -> flag_refused; env_door_documented` (`PASS (16 checks)`); `serverdoor19` on the day-18 shape without a host tier: `... envon_door_line=False ... -> FAIL` kept verbatim, cause the door's own no-tier line (`MEMRA_KV_HOST_MB=0`), rule untouched, b cell pre-registered before its run. Day 18 on the same flag arm read `flag_refused=False flag_silently_accepted=True`.
- Arena lease handoff (HOSTPREFIX missing item 1): scoped in `DAY19.md`, WIDER than one bounded change: charge-once alone leaves arena planes `Pinned` and receipt-less (a two-surface door); receipted planes need an arena-backed lease type in `memra-engine` (`tier_transfer.rs` `alloc_host_kind`, `validate`, `PinnedAllocation::drop`) plus the worker's charge-once accounting and a budget ruling (one budget or two). Not started; boot refusal stands.
- DFlash tail slice (missing item 2): pre-registered in `DAY19.md` (third entry class, drafter manifest digest as artifact, `DflashCfg` plan, f32 tail class, tail planes through the contract route); the 27B target and DFlash2 export exist on the local rig; no gate boots the drafter; no cell.
- NOTHING RUNNING for C: no BOX3 access today (no target-card cell owed by the scoped items); local lock released; no server. Receipts: `rtx5090-day19/serverdoor19-retry4/` (ev relocated under the capture dir, stated), `rtx5090-day19/serverdoor19b/`.
- NEXT: the lead's ruling on the arena handoff budget question; the target-card `serverdoor19b` cell if wanted before main; verify digest v3 and the 5090-class pair remain the review table's other missing items.
- OPEN: `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04; `HOSTPREFIX-DOOR.md` decide-by 2026-10-05; memra#617 open for the lead.
