# Session C day 64 handoff (2026-09-25): NEED TARGET CARD; the RTX 5090 still requires a reset

- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`, tip pushed at every milestone (hooks on, the announced development mode on engine-source pushes); main `d6515742f` (#725, integ61) merged into the lane (`13e14fa6b`); no commit on main, no PR. Resynced after the lead's resume: STATE, OWED, the queues' log read.
- DAY63's target cell (run by the lead on BOX13) read into `DAY63.md` section 4: `i13=improves`, the door still `loses` to REF (+0.28 ms per generated token, +0.19 per window token); 187 receipts checked in `pro-single-day63/`.
- DAY64: I14 (`8e7faf4ec`, `83f03d9b7`) and I15 (`2243b1fe2`) landed with their CPU gates; the ladder 2995 to 1997 ns per host-hit prefetched block (`cpu-day64/window.log`); the card cell `i15` registered and prepared (sections 3, 3a). NEED TARGET CARD: `D64_BUILDS="c60=da649107c i13=c9379c051 i14=83f03d9b7 i15=2243b1fe2" bash /root/wt-c/research/spill-c-20260919/day64-box.sh`.
- The local RTX 5090 still `GPU requires reset`; nothing acted on.
- RUNNING (mine): queues v5 (pid 3557213), v6 (3961032), v7 (45460) and v8 (352275, copy `rtx5090-queue-v8-20260925.sh`: DAY64's i15), each behind the one before, all polling for a healthy card, log `/tmp/c53-smoke/queue.log`. Binaries in `/tmp/c40-bins` (`-final`, `-c60`, `-i11`..`-i15`) and `/tmp/c53-bins`; build logs `/tmp/c61-build/`; ladder binaries `/tmp/c63-ladder/`, `/tmp/c64-ladder/`. Safe to stop by killing the four pids; scratch kept only for them.
- Next after the cell: read it into `DAY64.md`; if the door still loses to REF, the next improvement from its split, registered before code.
