# Session C day-twenty-six handoff (the 5090 door gates on the slice-2 tree, the route engaged on this card, the draft-bearing restore census)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged `origin/lane/spill-integ37-20260922` `a3eae3a77` (#638 open: A's Move 2 slice 2, the hit restore off the tick, on main `5df11152f`) as `b74269af5`, clean; `git diff origin/lane/spill-integ37-20260922 -- crates/ tools/` EMPTY. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; every cell `executed-not-qualified`; no commit on main, no PR; no engine change of my own (two client-side drivers).
- Task 1 (`rtx5090-day26/`, 9B, the 27B for the twin, tree `b74269af5`): sixteen cells green: fault default and plain `KV-HOST-CONTRACT-FAULT GATE: ALL GREEN` (67 ok, accounting `1 + 0` / `1 + 2`), failure x4 `ALL GREEN` (15 ok), identity x4 `ALL GREEN (teeth=0)` (12 ok), hit OFF and ON `SPEC-ON-CACHE-HIT GATE: ALL GREEN (qwen)` (61 ok), twin27 OFF and ON `-> PASS` (no `REFUSED`), unit-server 8 passed, unit-engine 2 passed (`d2d_capture_*`, `d2d_restore_*`). Attempt 1 booted twelve cells into `CUDA_ERROR_OUT_OF_MEMORY` while a foreign process held 22176 MiB (kept as `attempt1-oom/`, cause quoted with the compute-apps snapshot); the battery now waits bounded for an idle card.
- The restore route ENGAGED on this card: `identity-plain-on` two restores (`64 tokens, 16 planes (53.6MB)`, seq=6 and 7, landed 0.0 and 28.4 ms, `tick-top poll`) under `teeth=0`, and `fault-plain` four (one per promote cell, 28.4 to 29.0 ms); zero refused, dropped or disabled lines anywhere. Not engaged: the spec arms (draft-bearing, refused by name), the hit gate (boots without `MEMRA_KV_HOST_MB`: `hpx.armed()` false before any class check), the failure and twin gates.
- Task 2: skipped by its pre-registered condition (engagement in task 1, cited in `DAY26.md`); no new cell.
- Task 3: `HOSTPREFIX-DOOR.md` section D item 11, the draft-bearing restore census (refusals by name and where, what the draft plane's off-tick restore needs from `spec_session_from_restored_deferred`, 11 of 14 entries and 12 of 16 hits draft-bearing on both rigs, the hit gate arms no host tier). Correctness table row for the slice-2 tree on the 5090; `research/INDEX.md` day-26 row.
- Drivers: `day26-cell.sh` (day 24's plus the `d2d_` engine filter and the per-cell restore census), `day26-local-battery.sh` (the bounded idle wait).
- NOTHING RUNNING for C at close: the 5090 lock free at my last look, no server of mine on the card, BOX3 not touched today (`/root/wt-c` still detached at `e8667129e`), no bundle or `/tmp` scratch left.
- NEXT: the lead reads the 5090 slice-2 receipts into #638 and the door review; the owner's decide-by 2026-10-05 on `MEMRA_KV_HOST_CONTRACTS` (this lane answers nothing); `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04; the draft-bearing restore and a hit-gate boot with the tier armed stay owed by Move 2 (A).
