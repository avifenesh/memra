# Session C day-twenty-seven handoff (the hit gate's door arm armed and asserted, run on both cards; the review table corrected)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged `origin/main` `58b814abe` (#638 integ37 merged: Move 2 slices 1 and 2 with the review fixes) as `2f8560951`, clean. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; every cell `executed-not-qualified`; no commit on main, no PR; no engine change (two gate-script changes in `tools/spec-on-cache-hit-gate.sh`).
- Task 1 (`5c8c72447`): the hit gate's door ON arm (`MEMRA_KV_HOST_CONTRACTS=1` in its environment) arms the host tier on both boots with the identity gate's budget (`MEMRA_KV_HOST_MB=8192`, an exported value respected) and asserts per boot `[prefix-host] on: budget`, `[prefix-host] contracts door ON`, no latch line, and across the two boots at least one `capture|restore|demote|promote submitted off the tick`; both arms print the entry-class census (`insert (spec-boundary)` against `insert (seed)`, and the identity clause's own namespaces). OFF arm and identity clause unchanged. Door arm defined for the qwen arm only. No new `MEMRA_*` read.
- Runs on `b1e9c75b6`, the gate under its own `flock` (no `--external-lock`; as A day 21): local RTX 5090 (9B) `rtx5090-day27/`: hit-off `SPEC-ON-CACHE-HIT GATE: ALL GREEN (qwen)` 61 ok, hit-on `ALL GREEN` 68 ok; target card (27B) `pro-single-day27/cells/`: the same two lines, 61 and 68 ok. ON arm on both cards: both boots armed and door ON, `7 route submission(s)`: captures on the seeds, off-tick restores `seq=2` / `3` / `5` on the plain twin's r2, r3, g2 hits and `seq=2` on the spec-on np hit (landed after one poll, 2.1 to 2.2 ms), zero refused, dropped or latched lines; identity r1, r2, r3, g1, g2 held with the plain side restored through the door's route and the spec side on draft-bearing entries under the tick program. Census identical on both cards: spec-on 11 draft-bearing + 1 plain entries, twin 2 plain.
- Finding, not against the door: every earlier hit-gate ON arm on both lanes (A target card days 17 to 21 and 5090 days 17, 18; C 5090 days 24, 26) carried the server's own `[kv-host-contracts] ... nothing to route` line and zero `[prefix-host]` lines. Marked in `HOSTPREFIX-DOOR.md` (the day-26 row's note, two new correctness rows, an owed-cell row, item 11's day-27 sentence); the earlier lines stay as receipts.
- Gate hygiene (`c1454a5a4`): `stop()` killed every `memra-server` on the host (`pkill -x`) and the EXIT trap ran it after the lock was released; it now kills only the child of the gate's own `flock` wrapper. Re-runs on that tree, both cards, `rtx5090-day27-stopfix/` and `pro-single-day27-stopfix/` (two bounded lock waits behind lane A's day-22 hold): hit-off `ALL GREEN` 61 ok, hit-on `ALL GREEN` 68 ok, no port squatter.
- Owed, stated: the hit gate has no `--external-lock` (it cannot run under the collector's hold); the draft-bearing restore (the spec side's hits keep the tick program); the owner's decide-by 2026-10-05 on `MEMRA_KV_HOST_CONTRACTS` (this lane answers nothing); `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04.
- NOTHING RUNNING for C at close: no server of mine on either card, both locks free at my last look, BOX3 `/root/wt-c` detached at `c1454a5a4` clean, no bundle or `/tmp` scratch left, `/tmp/day27-battery.pid` removed.
