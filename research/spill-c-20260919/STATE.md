# Session C day 64 handoff (2026-09-25): NEED TARGET CARD (the 285K class); the RTX 5090 still requires a reset

- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`, tip pushed at every milestone (hooks on, the announced development mode on engine-source pushes); main `d6515742f`; no commit on main, no PR.
- DAY64's cell `i15` ran on BOX15 (a Ryzen 9 9950X host, run by the lead as registered): `DAY64 VERDICT rig=pro-single integrity=ok i14=flat i15=flat door=i15 vs_ref=matches`, recorded as it reads (`DAY64.md` section 4, 229 receipts checked in `pro-single-day64/`). Its noise is the door's per-boot host-CPU bimodality on that host (placed from the receipts; the 9950X finding is `OWED.md` C12).
- DAY64 section 5 registers an admissibility clause (every arm's gen-only and window IQR at most 0.005 s) and the rerun `i15b` on the Core Ultra 9 285K class with a placement sampler. NEED TARGET CARD (285K class): `D64_BUILDS="c60=da649107c i13=c9379c051 i14=83f03d9b7 i15=2243b1fe2" bash /root/wt-c/research/spill-c-20260919/day64b-box.sh`.
- The local RTX 5090 still `GPU requires reset`; nothing acted on.
- RUNNING (mine): queues v5 (pid 3557213), v6 (3961032), v7 (45460) and v8 (352275: DAY64's i15, read with `--admissibility` when recorded), each behind the one before, all polling for a healthy card, log `/tmp/c53-smoke/queue.log`. Binaries in `/tmp/c40-bins` and `/tmp/c53-bins`; build logs `/tmp/c61-build/`; ladder binaries `/tmp/c63-ladder/`, `/tmp/c64-ladder/`. Safe to stop by killing the four pids; scratch kept only for them.
- Next after the rerun: read it into `DAY64.md`; if admissible and the door still loses to REF, the next improvement from its split, registered before code.
