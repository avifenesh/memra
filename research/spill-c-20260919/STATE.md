# Session C day 63 handoff (2026-09-25): NEED TARGET CARD; the RTX 5090 still requires a reset

- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`, tip pushed at every milestone (hooks on, the announced development mode on engine-source pushes); main `7bf457384`; the lead integrates `e6217375e` as integ61; no commit on main, no PR. BOX12 is the lead's (integ61's battery); not used again here.
- Days 59 to 62 are recorded (`DAY59.md` to `DAY62.md`): REF qualifies as the target card's naked default (owner's call); the gap to REF is the door's CPU side, the owner demand the largest part; I11 improves, I12 flat, the door still loses to REF; the contracts packet is current through lane A day 41.
- DAY63: the split (`ad73d242c`), I13 changes 1 to 4 (`9bbab60ed`, `e8195cbf3`, `bc0e31d2e`, `c9379c051`), the CPU ladder (3569 to 2940 ns in one window, `cpu-day63/window.log`), the card cell `i13` registered (section 3) with its script, reader and sitting (`day63-box.sh`, section 3a). NEED TARGET CARD: `D63_BUILDS="c60=da649107c i12=117302725 i13=c9379c051" bash /root/wt-c/research/spill-c-20260919/day63-box.sh`.
- RUNNING (mine): queues v5 (pid 3557213), v6 (pid 3961032) and v7 (pid 45460, copy `rtx5090-queue-v7-20260925.sh`: DAY63's i13), each behind the one before, all polling for a reset card, log `/tmp/c53-smoke/queue.log`. Binaries in `/tmp/c40-bins` (`-final`, `-c60`, `-i11`, `-i12`, `-i13`) and `/tmp/c53-bins`; build logs `/tmp/c61-build/`; ladder binaries `/tmp/c63-ladder/`. Safe to stop by killing the three pids; scratch kept only for them.
- Next after the cell: if the door still loses to REF on the target card, the next improvement (the per-record `BankId` lookups, or the three blocks of one expert under one ticket) registered from the cell's split before its code.
