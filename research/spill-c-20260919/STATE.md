# Session C days 40 to 57 handoff (2026-09-24): NEED TARGET CARD for the DAY52 sitting; the RTX 5090 queue running

- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`, tip pushed each milestone (hooks on, `MEMRA_RELEASE_QUALIFICATION_MODE=development` on engine-source pushes); no commit on main, no PR. Base: `origin/main` `d61012658` (#716); main has since moved to `aff5d26b2` (#719, dsv4 only, no file of this lane).
- Ledger: `OWED.md` (C1 to C9, statuses current). Receipts so far this stretch: `DAY40 ATTRIB rig=rtx5090 integrity=ok window_door_ms_per_token=18.25 serial_ms_per_token=20.47 instrument=within_bound top=demand`; `DAY41 9B SPLIT plain_tuples=1 -> PASS`; `DAY42 PACKET LINES checked=68 missing=0 -> PASS`.
- C1 (MoE slot cache door, decide-by 2026-10-04): rungs I6, I9, fill, I1, I2, I8, I5, I7, I4 (days 43 to 50) and I10 (day 57, the fill completes inside the install) landed, each pre-registered with its cell and reader; none of their 5090 cells has run (the card was held by lane B, then lane A, from 19:17Z). Deciding cell `DAY51.md` (sections 1, 1a, 1b; section 2, the final tree, is named from the rung verdicts). Target sitting `DAY52.md` sections 1 to 8, `day52-box.sh` (dry-checked in a sandbox, `day52-cpu/dry-check-driver.log`), build list in section 8.
- C4 `DAY54.md` (target card only), C5 `DAY56.md` (code `dec0ba368`), C6 `DAY53.md` (code `256c3c640`), C8 `DAY55.md`: pre-registered; C5/C6 CPU cells green; their 5090 cells queued; their target cells in the DAY52 sitting (sections 4 to 6).
- RUNNING (mine): `/tmp/c53-smoke/queue.sh` (pid in `ps -eo pid,args | grep "[q]ueue.sh"`; copy `rtx5090-queue-20260924.sh`), log `/tmp/c53-smoke/queue.log`. Order: days 43, 44, 45, 46, 47, 48, 49, 50, 57 cells, then C6 gates (`rtx5090-day53`), C8 (`rtx5090-day55`), C5 (`rtx5090-day56`). Each cell waits for an idle rig (5 s poll, 6 h bound) and runs under the 1200% CPU cap; binaries in `/tmp/c40-bins` (run-gen rungs, run-spec-i4, run-spec-i10) and `/tmp/c53-bins` (memra-server-v3, memra-server-c5), SHA-256 in the DAY files. Safe to stop between cells by killing the queue pid (a running cell ends under its own collector timeout). Receipts land untracked under `rtx5090-day43` to `-day57`; each is read with its registered reader, committed, and the DAY file's results section written.
- Next: read each rung cell as it lands (reader, collector `--validate`, regime), fix or revert any rung that fails its clauses, then name DAY51 section 2's final tree, run DAY51's three cells on the 5090 (`day51-cell.sh`), and give the target sitting its `final=` build.
- Scratch kept while the queue runs: `/tmp/c40-bins`, `/tmp/c53-bins`, `/tmp/c53-smoke`. Everything else of this stretch removed (dry-check logs banked first, SHA-256 matched).
- Process notes: lane B's sccache daemon held `/tmp/memra-5090.lock` about 36 minutes at 17:13Z (not touched); three lanes queue on one 5090, so every 5090 cell here starts in another lane's gap.
