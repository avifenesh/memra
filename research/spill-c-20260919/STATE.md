# Session C day-twenty-four handoff (the 5090 door gates on A's slice-1 tree, the capture cell stopped at its arithmetic, the slice-3 census)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged main `4bb2afb63` (#633) as `5951edf43` and A's `62aa92279` (day 20, Move 2 slice 1) as `fad129042`, both clean; `git diff origin/lane/spill-a-20260919 -- crates/` EMPTY (this lane carries no engine line A's tip does not). Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; every cell `executed-not-qualified`; no commit on main, no PR; no engine change of my own.
- Local RTX 5090 (`rtx5090-day24/`, tree `11df6e653`, 9B for the host-tier and hit gates, the 27B for the twin, idle card, one bounded lock retry): fault-default `ALL GREEN` (65 ok); **fault-plain `2 FAILURE(S)` twice** (63 ok; `presubmit` and `postpublish` "the next demote completes with a D2H contract receipt after the refusal"): the gate's `cell` clause pins the receipt to `seq=1`/`seq=2` and the plain arm's seeds now take slice 1's capture tickets, so the completed `require=ok` demote reads `seq=3`/`seq=4`; a gate assumption slice 1 broke, not an engine failure; A's day-20 target-card fault gate ran the default arm only. Failure `ALL GREEN` x4, identity `ALL GREEN (teeth=0)` x4, hit `ALL GREEN (qwen)` OFF and ON (61 ok), 27B twin `-> PASS` both arms (the integ35 clean-card line to the byte), unit cells `8 passed` and `1 passed`.
- Task 2 (`DAY24.md`): the capture-isolating cell is pre-registered and STOPPED at its arithmetic: no retire-time publish of generated rows exists (publishes are `seed`, `lcp-split`, the spec/dspark/glm5 boundaries); the nearest shape (a warm hit deepened by 64 on-grid tokens) moves 154 MB of D2D off the tick (243 MB at ctx 8192), under C1's 6 ms threshold at any bandwidth above 100 GB/s and under the harness's 1.4 ms resolution above 110 GB/s, against tens of ms both arms share; no box time taken.
- Task 3: `HOSTPREFIX-DOOR.md` section D item 10, the slice-3 and spec-boundary census (what still takes the tick, the bytes on the 27B at the served context, the draft scratch's owner `SpecSession.scratch`, the fence, the borrow's lifetime); no code.
- Drivers: `day24-cell.sh` (day 22 plus hit, twin27, unit cells), `day24-local-battery.sh`. CPU: fmt rc=0, server lib `799 passed; 14 ignored`, tier `74 passed`.
- NOTHING RUNNING for C at close: both locks free, no server of mine on the card, BOX3 not touched today, no `/tmp` scratch.
- NEXT: the lead and lane A decide the fault gate's seq pin (`tools/kv-host-contract-fault-gate.sh` line 203); the owner's decide-by 2026-10-05 on `MEMRA_KV_HOST_CONTRACTS` (this lane answers nothing); `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04; memra#617, #365 open for the lead.
