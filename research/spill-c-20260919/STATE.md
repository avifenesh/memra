# Session C day-thirty-seven handoff (the demote-class tenant-stall cell on the option (a) code, RTX 5090 class)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged `origin/main` `189c91b15` as `d7bf406af` and `origin/lane/spill-integ45-20260922` `298f7d052` as `8b889dcdf` (the option (a) tree); pushes in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no commit on main, no PR, no engine change.
- Pre-registration `a50922b27` (`DAY37.md` section 1), pushed before the first boot: option (a) minus base per arm per order block, `moved` when both blocks are isolated with one sign; the DiD per class; the demote-on arm and the demote DiD are the primary lines.
- The run `adc142c4d` (`rtx5090-day37/`): day 35's cell unchanged, two binaries (`091a931c0` base, `8b889dcdf` option (a)) in ONE hold on `/tmp/memra-5090.lock` via `tier-battery.py --external-lock`, programs p1-base p2-opta p3-opta p4-base, 24 boots, 40 receipts, 40 `REPLAY: PASS`, 19:47:19Z to 20:31:34Z, 51 to 89 C.
- Worst-tick rule: every arm and both DiDs `under_resolution` (`did-demote o1=-2.3/5.8 o2=-6.4/7.3`). Two largest gaps summed: demote-on `moved` (`-21.7/5.2`, `-24.5/8.5`), promote-on `moved`, both DiDs `moved` (`did-demote o1=-24.2/8.2 o2=-29.0/12.7`), OFF and prime controls `under_resolution`.
- Ledger in the cell: `owner_held=40.29` (first three, N=12) and `41.85` (4th on, N=24), `hashed_in=12.9`, 0 parked, 0 reparks, 0 detaches.
- Records: `DAY37.md`, `DOOR-DECISION-PACKET.md` (section 4 rows, item 2's scope, item 7, section 6, appendix A day 37), `HOSTPREFIX-DOOR.md` section E DAY 37, `research/INDEX.md`; no recommendation.
- Option (a)'s code (`45f824a75`, `867655368`) is on `origin/main` since #652 (`0c86309bd`, `rtx5090-day37/ancestry.log`); `0c86309bd` itself not merged into the lane.
- Owed: the target card's demote-class tenant-stall cell on the option (a) tree (none has run); the owner's decisions at 2026-09-23 (admission door), 2026-10-04 (MoE slot cache, VMM), 2026-10-05 (contracts door), 2026-10-06 (park door); Move 2 owed item 1 (A day 30, running in A's lane); the 9B entry's KV byte split; the double-park slice question; an always-admitted prime arm on the RTX 5090 class (new pre-registration, not run); a per-tick split of the summed gaps (reader change, not run).
- NOTHING RUNNING for C at close: no server, no lock, no GPU process; `wt-c37-base` removed; scratch removed.
