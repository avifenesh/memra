# Session C day-thirty handoff (the door's decision packet; the capture share read)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; no `lane/spill-integ40-20260922` existed, so `origin/main` `ebe3fe17d` (integ39, #642) merged as `24597453e` (door-table duplicate row resolved by hand). Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; every cell `executed-not-qualified`; no commit on main, no PR; no engine change (research scripts and records only).
- Task 1: `DOOR-DECISION-PACKET.md`, the owner's input for the `MEMRA_KV_HOST_CONTRACTS` decide-by 2026-10-05: one page (the question, what the door is today, the correctness table, the cost table per card, the open findings, the three outcomes with what each requires, no recommendation) plus an appendix; every number re-read from its receipt file today, untraced none. A draft; nothing created under `docs/decisions/`.
- Task 2 (`55b8da077` pre-registration, run on the target card, `pro-single-day30/`): the capture share of day 28's unread cell, re-read against a cache-ON boot whose 308 MB seed insert is refused by the typed oversize line before any copy (`MEMRA_PREFIX_CACHE_MB=128`; the prime still stops at the 5088 seed boundary). Eight boots, one hold, two passes, N=5 per arm per order, eight `STALL REPLAY: PASS`, admissible. Verdict: `DAY30 CAPTURE-SHARE VERDICT: pass1 refused on-off -0.1 (unc 0.3) under_resolution; pass1 share off +0.6 (unc 0.3) isolated; pass1 share on +1.2 (unc 0.2) isolated; pass1 capture on-off +0.7 (unc 0.3) isolated; pass2 refused on-off -0.1 (unc 0.3) under_resolution; pass2 share off +0.5 (unc 0.3) isolated; pass2 share on +1.2 (unc 0.3) isolated; pass2 capture on-off +0.7 (unc 0.2) isolated; admissible=True`. Base 283.1 / 283.2: day 28's 17 ms was the prime's tail chunk past the seed boundary on a second tick, not a capture cost. 33 to 61 C, 32 to 501 W.
- Records: `DAY30.md`; `HOSTPREFIX-DOOR.md` (section E pointer to the packet; the two capture rows gain the day-30 reading); `research/INDEX.md` row. Checks clean (shellcheck, check-flags, markers, diff --check, gitattributes, zero em dashes).
- Owed, stated: the owner's decision at the decide-by (this lane answers nothing); the promote double-park slice (which change between `0713c1a79` and the integ38 tip moved the demote copy's landing); the recurrent f32 term off the tick (Move 2 owed item 1, the 9 ms); the spec-boundary capture's retire-settle price (A day 24 finding 2); the 5090 class pair; `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04.
- NOTHING RUNNING for C at close: no server of mine on either card; lane A's `a-day25` server on the card and the box lock held by it at my last probe (listing only); BOX3 `/root/wt-c` detached at `55b8da077` clean; no bundle or `/tmp` scratch left on either end.
