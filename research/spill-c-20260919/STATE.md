# Session C day-twenty-eight handoff (the hit gate under the collector's lock; the isolating stall cell; the receipt's price on the 5090)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged `origin/lane/spill-integ38-20260922` `643ecbb28` as `8803f4b6c` (PR #639 still OPEN, `origin/main` not merged). Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; every cell `executed-not-qualified`; no commit on main, no PR; no engine change (one gate script, one fixture, one CI line, research scripts).
- Task 1 (`002507b4b`): `tools/spec-on-cache-hit-gate.sh [--external-lock FD]` in the identity gate's shape: under the collector's hold the gate takes no lock of its own (the `flock -w 300` wrapper is absent, the FD verified into `LOCK.json`, `stop()` addresses `$SERVER_PID` itself); without the flag every boot keeps its own `flock`. Teeth `tools/test_spec_on_cache_hit_gate_lock.sh` (`--lock-self-test FILE`: default arm `probe=held`, external `probe=free`, inode and mtime unchanged, non-numeric FD `REFUSED`) `ALL GREEN (7 ok)`, wired into `ci.yml`'s gate-teeth step; `docs/TESTING.md` bullet. No GPU run of the gate under the collector yet.
- Task 2 (`61dc1a52a` pre-registration, run on the target card, `pro-single-day28/`): restore class ISOLATED by a whole-entry zero-suffix hit intruder (5152-token on-grid seed, no prime): `stall_median` 8.9 / 9.0 OFF against 9.1 ON, `on_minus_off` +0.2 (unc 0.2 / 0.1) both passes; capture class `on_minus_off` +0.7 (unc 0.2) both passes (283.7 against 284.4, nothing evicting at 8192 MB); the prime-only boot read 301.5, 17 ms above the same prime in the cache-on boots, so the pre-registered subtraction reads the wrong sign and the capture arm's own share stays unread (finding, not tuned). Ten boots, one hold, N=5 per arm per order, both orders, 33 to 60 C, 33 to 501 W, ten replays PASS, zero refused/dropped/DISABLED lines.
- Task 3 (`rtx5090-day28/price/`): `D2D-RECEIPT PRICE` on the local RTX 5090, verbatim both orders: `copy_median=0.394 digest_median=0.221 pair_median=0.442 pair_over_copy=1.12` and `copy_median=0.395 digest_median=0.221 pair_median=0.441 pair_over_copy=1.12`; 58 C, P8 before, no compute app; per card, never compared to the target card's rows.
- Records: `DAY28.md`; `HOSTPREFIX-DOOR.md` (hit-gate row's `--external-lock` note; owed rows for the receipt's price on both cards and the isolating cell; cost rows for both; section E clause); `research/INDEX.md` row.
- Owed, stated: the hit gate on a GPU under the collector's hold (the arm exists, no receipt); the capture arm's own on-tick share (the subtraction arm is a different prime); the recurrent f32 term off the tick (owed item 1, the 9 ms); the draft-bearing restore (item 11); the owner's decide-by 2026-10-05 on `MEMRA_KV_HOST_CONTRACTS` (this lane answers nothing); `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04.
- NOTHING RUNNING for C at close: no server of mine on either card, the box lock free and the 5090 lock held by another session's `kernel-check` at my last probe (seen in nvidia-smi's listing only), BOX3 `/root/wt-c` detached at `61dc1a52a` clean, no bundle or `/tmp` scratch left.
