# Session C day-twenty-three handoff (the promote stall cell on main's tree after #627, the door gates on both cards on that tree, the HOSTPREFIX review table in its final form)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged main `0713c1a79` (#627) as `d2b8efbf2`, worker.rs on main's side at both sites; `git diff origin/main -- crates/` EMPTY (no engine line of this lane outside main). Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; every cell `executed-not-qualified`; no commit on main, no PR.
- Tree under every cell: `91b0d4e08` (main's crates plus the day-23 drivers). Target card stall cell (`pro-single-day23/stall/`, one collector hold after five bounded lock retries, OFF then ON boots, replay PASS x4, `day23-stall-reading.py`): promote ON `stall_median=81.9` (day 18: 81.9), promote OFF `85.2` (day 16: 85.0), demote ON `149.5` (day 18: 149.7), demote OFF `117.5` (day 16: 117.5); `server_promote_ms` steady 25.8 to 26.4 (day 18: 25.9 to 26.4); reading `at_or_under_day18, within_wait, on_at_off, off_stable, demote_half_unchanged, off_stable`; the parked-only wait moved nothing measurable.
- Local RTX 5090 (`rtx5090-day23/`, 9B, one bounded lock retry behind another session's server, never signalled): fault `ALL GREEN` x2 (65 ok, `items=18/16`), failure `ALL GREEN` x4 (15 ok), identity `ALL GREEN (teeth=0)` x4 (12 ok): the first door gates on that class on a Move-1-whole tree with the #627 fixes.
- Target card door gates on the same tree (`pro-single-day23-gates/`, twelve cells through the collector, day22-box-run.sh unchanged, zero lock retries): fault `ALL GREEN` x2 (65 ok, `items=34/32`), failure `ALL GREEN` x4 share-cap (15 ok) and x2 whole-budget (14 ok), identity `ALL GREEN (teeth=0)` x4 (12 ok); cell for cell the day-22 verdicts, now on main's crates.
- `HOSTPREFIX-DOOR.md` "Review table ... final form (day 23)": A owed cells, B cost table, C correctness table, D missing (arena handoff ruling 28; DFlash tail slice; verify digest v3; the 5090 class pair; the promote-side census question; the 5090 whole-budget arm), E the decision question stated, not answered.
- Drivers: `day23-stall-cell.sh` (A's shape, both boots in one hold), `day23-box-run.sh`, `day23-stall-reading.py` (thresholds fixed before the run). `day22-cell.sh` and `day22-box-run.sh` reused unchanged for the gates.
- NOTHING RUNNING for C at close: both locks free, no server of mine on either card, `/root/day23.bundle` and `/tmp/spill-c-day23` removed, `/root/wt-c` clean at `91b0d4e08`.
- NEXT: the owner's decide-by 2026-10-05 on `MEMRA_KV_HOST_CONTRACTS` (this lane answers nothing); `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04; the 5090 class pair (review item 5) if that card is ever quiet for 15 minutes; memra#617, #365 open for the lead.
