# Session C days 59 to 62 handoff (2026-09-25): BOX12 released; the RTX 5090 still requires a reset

- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`, tip pushed at every milestone (hooks on, the announced development mode on engine-source pushes); main `7bf457384` (#724, integ60) merged into the lane as `f0e77b8be`; no commit on main, no PR.
- The days 59 to 61 target sitting ran on BOX12 (one RTX PRO 6000 Blackwell Workstation Edition, 600 W) 04:35Z to 05:00Z; receipts in `pro-single-day61/` (635 of 635 files `OK` against the box manifest, `MIRROR-CHECK.txt`); box receipts, binaries, build target and my artifact checksum file removed; BOX12 released.
- C10 (`MEMRA_MOE_PREFETCH=1`, decide-by 2026-10-04): target card gates PASS, `pftime -> pf_wins`, `pfnaked -> pf_flat`: it qualifies as the target card's naked default (the owner's call; the flip is its own change).
- C11 (the door's gap to REF): target card `top=prefetch_ns cpu_side` (the owner demand the largest part); I11 `improves`, I12 `flat` (kept), the tuned door still `loses` to REF (+0.34 ms per generated token, +0.25 per window token, from +0.69 and +0.44). Next: `DAY63.md`, the next improvement, registered from `DAY60.md` section 2 and `DAY61.md` section 3 before its code.
- C3 (the contracts-door packet): current through lane A day 41 and ruling 54 (`DAY62.md`, `DAY62 PACKET LINES checked=63 missing=0 -> PASS`).
- The local RTX 5090 still `GPU requires reset` (since 01:25Z, `rtx5090-fault-20260925/`); nothing acted on.
- RUNNING (mine): queue v5 (`/tmp/c53-smoke/queue5.sh`, pid 3557213: DAY51 hashlock, spec, decide-b, then C5 attempt 4) and queue v6 behind it (`/tmp/c53-smoke/queue6.sh`, pid 3961032, copy `rtx5090-queue-v6-20260925.sh`: DAY59's four cells, DAY60's gap, DAY61's i11). Both poll every 5 s for an idle healthy card, bounded 6 h per cell; log `/tmp/c53-smoke/queue.log`. Binaries in `/tmp/c40-bins` and `/tmp/c53-bins`; build logs `/tmp/c61-build/`. Safe to stop by killing the two pids; scratch kept only for them.
