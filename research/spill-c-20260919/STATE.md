# Session C day-thirty-one handoff (the two RTX 5090 rows the packet listed as not run)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; no integ41 PR or remote branch existed, so the lead's LOCAL `lane/spill-integ41-20260922` `e14c270a6` (11 ahead of `origin/main` `88d3dfd49`, 0 behind) merged clean as `5cef58f09` (the door table is the integ41 side by construction). Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; every cell `executed-not-qualified`; no commit on main, no PR; no engine change (drivers, a reading script and records only).
- Task 1 (`934a6da3a` pre-registration; `rtx5090-day31/pair/`): the door's demote and promote PAIR on the local RTX 5090 Laptop GPU, the 9B, the day-16 `wc-cell` shape (cache 64 MB, host 8192 MB, default spec boot, 64-token draft-bearing entries of 54.8 MB, `18 items`), four boots in ONE collector hold on the `rtx5090` rig (35 s), N=5 per arm per order, both orders, pooled N=10, 54 to 74 C, 9.5 to 169.9 W, no compute app before or after; `WC PAIR REPLAY: PASS (12 checks)`; the write-combined premise printed inside the hold: `PINNED-DEFAULT device="NVIDIA GeForce RTX 5090 Laptop GPU" kind=write-combined flags=4`. Verdict: `DAY31 PAIR VERDICT: demote off 20.0 (N=10) on 57.3 (N=10) on_minus_off +37.3 unc 26.4 isolated; promote off 15.6 (N=10) on 21.1 (N=10) on_minus_off +5.4 unc 25.6 under_resolution; promote_minus_inline off 3.5 (N=10) on -19.3 (N=10) on_minus_off -22.9 unc 7.4 isolated; parked per boot [0, 10, 10, 0]; pinned=write-combined; admissible=True`. Read, not tuned: a first-touch step in both arms (18 to 24 / 57 to 60 on the first three demotes, 5 to 7 / 38 to 40 after); the ON demote's `in` minus its completion figure about 21 to 23 ms per demote; the ON promote-minus-inline negative because the inline demote publishes at a later tick top; the double park on this card (`parked=10, restore_submitted=5` per ON boot).
- Task 2 (`rtx5090-day31/gates/`): the whole-budget failure arm (`MEMRA_KV_HOST_TENANT_PCT=100`), default and plain, OFF and ON: four `KV-HOST-SPILL FAILURE GATE: ALL GREEN` (14 ok, 0 FAIL), `[prefix-host] skip demote: entry 54.8MB > host budget 1MB` (default) / `entry 54.6MB` (plain) twice per boot, the ON arms after the whole Move 1 contract (`demote submitted ... 18 items` / `16 items`, `D2H receipt ... require=ok`, `demote published ... 36.2ms` / `24.9ms`).
- Task 3: `DOOR-DECISION-PACKET.md` gains the two rows (section 3 failure, section 4 pair as three rows), A's `DAY25 DOUBLE-PARK`, `DAY25 DECOMPOSITION` and `DAY25 RETIRE-SETTLE` lines verbatim with ruling 36 in items 3 and 4, the retire settle kept as a statement and dropped as a cost in section 2, items 7 and section 6 updated; no recommendation.
- Records: `DAY31.md`; `HOSTPREFIX-DOOR.md` (section C's 5090 whole-budget row filled, section D items 5 and 9 RESOLVED with this card's cost rows); `research/INDEX.md` row. Checks clean (shellcheck, check-flags, markers, diff --check, gitattributes, zero em dashes).
- Owed, stated: the owner's decision at the decide-by 2026-10-05 (this lane answers nothing); the census question of section D item 6 (which memory the two on-tick hashes read; today's arithmetic beside it); the double-park slice (A day 26's approved proposal 1); the recurrent f32 term off the tick (Move 2 owed item 1); a tenant-stall cell on the 5090 class (none exists); `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04.
- NOTHING RUNNING for C at close: no server of mine on the card, no lock held, no `/tmp` scratch; BOX3 not touched today; the lead's `wt-spill-integ41` worktree and its cargo build seen only in listings.
