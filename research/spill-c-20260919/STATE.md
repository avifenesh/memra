# Session C day-twenty-five handoff (the fixed fault gate live on both cards, the identity gate over the retire-seam settle, the settle's cost cell HOLDS on the target card)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged `origin/lane/spill-integ36-20260922` `64bb0c71d` (#634 still open: the lead's fixed contract fault gate with `receipt_seq_accounts`, the retire-seam settle and the refused-submission fence release, A's slice 1, B day 30, main `4bb2afb63`) as `913095404`, clean; `git diff origin/lane/spill-integ36-20260922 -- crates/ tools/` EMPTY. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; every cell `executed-not-qualified`; no commit on main, no PR; no engine change of my own (one harness fix, `e8667129e`, client-side).
- Task 1, the fixed fault gate live (`rtx5090-day25/`, 9B; `pro-single-day25/cells/`, 27B, one collector hold per cell, six bounded lock retries behind lane A): `KV-HOST-CONTRACT-FAULT GATE: ALL GREEN` default and plain on BOTH cards (67 ok each), the accounting clause verbatim and identical on both: default `receipt seq=1 expected 1 + 0 capture ticket(s) submitted before it = 1`, `seq=2 expected 2 + 0 ... = 2`; plain `receipt seq=3 expected 1 + 2 capture ticket(s) submitted before it = 3`, `seq=4 expected 2 + 2 ... = 4`. Day 24's `2 FAILURE(S)` is closed. Identity default and plain, OFF and ON, target card, over the settle: `KV-HOST-SPILL IDENTITY GATE: ALL GREEN (teeth=0)` x4 (12 ok each).
- Task 2 (`DAY25.md`, pre-registered at `86ef9a4bb` before the run): the retire-seam settle's cost on the target card, one hold, OFF/ON/ON/OFF, N=5 per arm per order, both orders, two arms per boot (`plain` as briefed; `coincide`, a victim's abort in the seed's iteration, because the smoke and the code showed a capture meets the retire seam only when ANOTHER session retires in the seed's iteration). Verdict verbatim: `DAY25 RETIRE VERDICT: admissible=True ... seam-pass1=exercised seam-pass2=exercised -> HOLDS (R1, R2, R3 within 3.0 ms in both arms and both passes; the seam exercised in both passes)`; every ON minus OFF delta 0.1 to 0.7 ms (intruder wall, stall median, two-tick stall), `settled synchronously by a session retire` 10 of 10 in both ON boots, replay PASS x8, `--validate` rc=0, 37 to 58 C, 32.6 to 503.1 W. A first attempt failed at the harness's calibration (the 27B tokenizer's two-token words; kept as `retire-attempt1-calibration/`), fixed and rerun; no rule or claim changed.
- Task 3: `HOSTPREFIX-DOOR.md` fault gate rows name the accounting clause and today's receipts on both cards, identity rows carry `913095404`; `research/INDEX.md` day-25 row; fmt rc=0, check-flags clean, markers OK, `git diff --check` and the public-boundary census before the final push.
- Drivers: `day25-retire-cell.py` (harness, imports A's `stall_cell.py`), `day25-retire-reading.py` (thresholds), `day25-retire-cell.sh`, `day25-box-run.sh`; the gate cells reused `day22-box-run.sh` and `day24-cell.sh`.
- NOTHING RUNNING for C at close: both locks free at my last look, no server of mine on either card, BOX3 `/root/wt-c` detached at `e8667129e` and clean, no bundle or `/tmp` scratch left.
- NEXT: the lead reads the settle cell into the door review (both arms, the mechanism note); the owner's decide-by 2026-10-05 on `MEMRA_KV_HOST_CONTRACTS` (this lane answers nothing); `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04; memra#617, #365 open for the lead; #634 merges under the lead.
