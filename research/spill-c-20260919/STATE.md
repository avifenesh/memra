# Session C day-twelve handoff (census and the lease token identity)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged main `a1bd0be62` (`e9c31d740`, pushed); no commits on main or the lead branch.
- NOTHING RUNNING for C: no tmux, no GPU process, BOX3 `/root/wt-c` untouched today (still at 6defcd604 from day eleven).
- UNPUSHED (this rig): `git push` refused by the perf-ci pre-push arm, verbatim `pre-push: engine files touched after the last perf-ci battery.` (`crates/memra-engine/src/moe_cache.rs`, merge-base `e9c31d740`); no `MEMRA_SKIP_PERF_CI`, no `--no-verify`; the lead pushes. Only the main merge `e9c31d740` is on origin.
- Commits: `468d21016` `HOSTPREFIX-CONTRACT-CENSUS.md` (issue #552 criterion 1 decision input); `69905776f` lease token identity (`ExpertLeaseToken::{record, artifact, epochs}`, `bank::dispatch_id`, `demand` refuses a lease for another record, `with_bytes`/`finish` refuse forged tokens, `admit_banked` asserts the demanded expert before the H2D); DAY12 docs commit.
- Tests: memra-tier 223 pass (5 new unit in `owner_proxy.rs`, 1 new integration); fmt, tier clippy, DOCS_RS engine clippy, flags census, docs registry, diff --check clean; `slru-synthetic.json` re-pinned (2013 rows identical).
- Census headline: bank identity never crossed the proxy seam (fixed), tenant is the artifact digest, epoch constant, no TransferEngine, GPU slots outside the governor, scale planes refused; server `HostTierContext` is never constructed so lane B's sidecar route never runs; tenant string row vs `tenant_salt` unrelated; D2H is a plain copy with no ticket/fence/epochs; by-reference demote vs owned `register_device`; `PinnedHostBuf` vs `CudaPinnedLease`.
- HostPrefix recommendation: Option A (construct `HostTierContext` behind a default-OFF door, `ProgramIdentity` at model load, one `tenant_salt` derivation, server governor) then Option B (pageable-path D2H through `TransferEngine`). Lead rulings needed: who derives `tenant_salt`; borrow discipline for B (move planes out of `&mut PrefixEntry` or a borrowed-source seam); arena path out of scope.
- NEXT: lead ruling on the HostPrefix options; native rerun of the door on the target card before any support-state claim (the new `admit_banked` arm cannot fire while the proxy holds).
- OPEN: door doc items 1 (owner per PP stage), 3 second half (scale admission), 4 (overlap), 6 (serving shape); decide-by 2026-10-04.
