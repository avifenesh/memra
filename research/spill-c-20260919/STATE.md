# Session C days 59 to 61 handoff (2026-09-25): NEED TARGET CARD; the RTX 5090 still requires a reset

- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`, tip pushed at every milestone (hooks on, the announced development mode on engine-source pushes); no commit on main, no PR. Resynced on integ60 (`2dc6cfe50`, main `5d653e851` plus this lane's `942dc5736`).
- C10 (lead item 1, `MEMRA_MOE_PREFETCH=1`): `DAY59.md` registered, decide-by 2026-10-04 in its FLAGS row, cells and reader committed, binaries `c60=da649107c`.
- C11 (lead item 2, the gap to REF): `DAY60.md` (the log-only `--moe-dispatch-clock`, `fec3c582f`; cell `gap`); `DAY61.md` (the CPU profile; I11 changes 1 to 5 `6116cddc2`..`59a5875d4`, 5370 to 3620 ns per host-hit prefetch cycle on the local CPU; change 6 measured flat and reverted, `c70ab06a1` then `a068ee37d`; I12 `117302725`; cell `i11`). Binaries `i11=a068ee37d`, `i12=117302725`.
- The target sitting is ready: `day61-box.sh` (builds by `day61-box-build.sh`), dry-checked (`day61-cpu/dry-check-driver.log`); run as `D61_BUILDS="c60=da649107c i11=a068ee37d i12=117302725" bash /root/wt-c/research/spill-c-20260919/day61-box.sh`. Needs and timing in `DAY61.md` section 2b.
- The local RTX 5090 still `GPU requires reset` (Xid 119 then 154 at 01:25Z, `rtx5090-fault-20260925/`); the lead asked the owner; nothing acted on.
- RUNNING (mine): queue v5 (`/tmp/c53-smoke/queue5.sh`, pid 3557213: DAY51 hashlock, spec, decide-b, then C5 attempt 4) and queue v6 behind it (`/tmp/c53-smoke/queue6.sh`, pid 3961032, copy `rtx5090-queue-v6-20260925.sh`: DAY59's four cells, DAY60's gap, DAY61's i11). Both poll every 5 s for an idle healthy card, bounded 6 h per cell; log `/tmp/c53-smoke/queue.log`. Binaries in `/tmp/c40-bins` (`-final`, `-c60`, `-i11`, `-i12`) and `/tmp/c53-bins`; build logs `/tmp/c61-build/`. Safe to stop by killing the two pids; scratch kept only for them.
- Next: the target sitting's readings into DAY59 to DAY61; then the next improvement (registered from `gap` and `i11`); C3's read-in of lane A days 37 to 41; C5b and C5c once lane A's helper and span code is on main.
