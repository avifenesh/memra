# Session C day-twenty-nine handoff (Move 1 decision cell (i) in the same window; the hit gate under the collector)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged `origin/lane/spill-integ38-20260922` `cd7161bc7` as `f5398f8fb` (PR #639 still OPEN, `origin/main` not merged). Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; every cell `executed-not-qualified`; no commit on main, no PR; no engine change (research scripts and records only).
- Task 1 and 2 (`7349ef932` pre-registration, run on the target card, `pro-single-day29/stall/`): cell (i) of Move 1, door ON in both arms, the day-16 script's `on` boot, arm X today's tree (copy stream) against arm Y the day-16 tree `1646d421b` rebuilt on the box (owner stream; `crates/` = main `653c997f4`, `lines=0`), twenty boots interleaved XYXY../YXYX.. in one hold, N=5 boots per arm per order, a dry boot of Y first (PASS, no patch). Demote `y_minus_x=+43.4 unc=0.6` / `+43.0 unc=0.1` `isolated` (150.0 against 193.4 / 193.1); promote `+13.3 unc=0.4` / `+12.8 unc=0.2` `isolated` (149.5 / 149.7 against 162.9 / 162.5); `DAY29 CELL(i) CLAUSE: NOT MET` (both classes far above idle p99 14.9). 41 `STALL REPLAY: PASS`, zero FAIL, zero bad lines, 33 to 51 C, 32 to 361 W; tenant sha `264b120d487de2c9` in all 41 receipts of both trees.
- FINDING for the lead (not tuned): arm X's promote arm reads 149.6 where the day-18 and day-23 trees read 81.9: on today's tree the hit parks twice (promote published 19.6 ms, then Move 2's restore off the tick, 90.3 ms to re-admission, 100 of 100 runs) and the inline demote publishes after 22.4 ms (day 23: 97.0), so its two on-tick hashes land on one tick and the promote arm's stall equals the demote arm's (150.0). Which slice between `0713c1a79` and the integ38 tip moved the copy's landing is not determined.
- Task 3 (`pro-single-day29/hitgate/`): `tools/spec-on-cache-hit-gate.sh --external-lock 3 qwen` under the collector's hold on the target card, OFF and ON: `SPEC-ON-CACHE-HIT GATE: ALL GREEN (qwen)` both arms (61 and 68 `ok:`), `LOCK.json` owner `collector` in both, `lock: collector's inherited FD 3 on /tmp/memra-gpu.lock (no flock of this gate's own)`; ON door lines 18, restore route lines 8.
- Records: `DAY29.md`; `HOSTPREFIX-DOOR.md` (owed-cell row for cell (i), the two Move 1 cost rows' same-window pair, the hit-gate row's collector run, section E); `research/INDEX.md` row. Checks clean (shellcheck, check-flags, markers, diff --check, gitattributes, zero em dashes).
- Owed, stated: the promote finding's slice (which change moved the demote copy's landing onto one tick); the recurrent f32 term off the tick (Move 2 owed item 1, the 9 ms); the draft-bearing restore (item 11, lane A today); the owner's decide-by 2026-10-05 on `MEMRA_KV_HOST_CONTRACTS` (this lane answers nothing); `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04.
- NOTHING RUNNING for C at close: no server of mine on either card, the box lock held by lane A's `a-day24` collector at my last probe (listing only), BOX3 `/root/wt-c` detached at `7349ef932` clean, `/root/wt-c-day16` removed, no bundle or `/tmp` scratch left on either end.
