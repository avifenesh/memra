# Session C day-fifteen handoff (Option B landed: the pageable D2H through the TransferEngine)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged main `70038ed01` (`ac3789ca6`), lane A `8302f6b0a` (`4cc2e92a6`), main `9ef2f04d6` (`75c531dde`) and main `0175e39d4` (`aefb89d89`, lane B's SLRU removal; `worker.rs` auto-merged, FLAGS resolved row by row); census `16272e9de`, code `5f8d327e2`, docs `8ed05bfc6` (day 15, PR #599 integ17), review fixes `30704905f` + `da52fa885` + `8f8f05a19`; no commits on main or the lead branch.
- NOTHING RUNNING for C: no tmux on BOX3 (`c15build`, `c15run`, `c15review`, `c15merge` exited), no GPU process of C's; `/root/wt-c` detached at `aefb89d89` (`refs/bundle/c15m`); receipts `/root/spill-receipts/c-day15/` and `c-day15-review/` mirrored to `pro-single-day15/` and `pro-single-day15-review/` (bins excluded).
- Code `5f8d327e2`: `HostPlaneBytes::{Pinned, Contract{lease, receipt}}`, `HostTierLedger` (one ledger, two handles), `HostTierContext.transfers` + `inflight` (2 x layers + 2), `HOST_TIER_TRANSFER_EPOCHS` 0/1/1, `host_kv_planes_through_contract` (the kv_tier_gate demote sequence, one batch per demote, planes out of the entry's slots by `Option::take` and back through `take_plane`), `host_contract_abort`, typed `Alloc`/`Refused`/`SourceQuarantined`, `HostDemoteOutcome::SourceQuarantined` (pause sweep drops), `&mut PrefixEntry` on the demote hook and its five callers, pinned-zero residency charge, bind's receipt check (flip fault named), fallible handoff export view; promote H2D, arena, OFF untouched.
- Target card (binary `3d5386e6...`, 600 W, collector, N=1, executed-not-qualified, `verify-day15.py` `DAY15 REPLAY: PASS`, 134 checks): identity default and plain `ALL GREEN` both arms (13 lines each), equal demote bytes, `verify ok`, 16/15-event sequences equal, 2 receipts per ON boot; failure default `1 FAILURE(S)` both arms (the pre-existing pool-full line), digest cell receipt then flip then the named injected difference then `VERIFY FAILED`; lane A tenant fix arm `PASS` both arms (29 lines, 8 demotes equal, 8 receipts); serve-smoke 33 lines equal; lane B evict and newest-turn verdict lines identical (`-> PASS`).
- Review round (PR #599 findings 1 and 2, both real): pre-submit unwinds drop the ops' original DeviceLease handles first (take_plane refused Busy on a registry Rc of three, escalating to quarantine and a dropped device entry); the abort drains the owner stream after record_consumer and never discards retire/acknowledge/release_producer (a swallowed Busy leaked the whole in-flight dimension): typed `TicketLeaked` latches the tier. One-shot `MEMRA_KV_HOST_FAULT=contract-presubmit|contract-postpublish`; GPU cells `option_b_presubmit_refusal_returns_every_plane_and_keeps_the_tier_on`, `option_b_postpublish_refusal_retires_the_ticket_and_keeps_the_tier_on` (2 passed on the card); `tools/kv-host-contract-fault-gate.sh` `ALL GREEN` (presubmit next receipt `seq=1`, postpublish `seq=2`); identity/failure OFF/ON unchanged on the merged binary; `verify-day15-review.py` PASS (46).
- Findings: the contract's pinned destinations are write-combined (`cudarc alloc_pinned` = `CU_MEMHOSTALLOC_WRITECOMBINED`; `PinnedHostBuf::new` is cached), so bind's hash runs at WC speed: N=1 demote 149 vs 281 ms, promote 267 vs 398 ms OFF vs ON, not a claim, Option C measures; two hashes per plane (engine at completion, bind), the receipt digest repeats for a re-demoted entry.
- Local battery on `5f8d327e2` green: fmt, 761 + 71 + 515 tests, clippy -D warnings (3 crates), check-flags 867, docs-registry census, diff --check (`day15-local/`).
- NEXT: Option C (promote H2D through `h2d` / `ready_view` / `KvMaterializer`, bit-identical at `prefix_restore_at`), with finding 1's promote delta as its first cell; the WC destination flag is the engine's call.
- OPEN: `MOE-SLOT-CACHE-DOOR.md` items 1, 3 second half, 4, 6 (decide-by 2026-10-04); `HOSTPREFIX-DOOR.md` decide-by 2026-10-05; DFlash tail slice; verify digest v3.
