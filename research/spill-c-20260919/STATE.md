# Session C day-seventeen handoff (memra#586 repaired with its red/green fixture; the arena cell measured; the WC item closed by pointer)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged main `fa5185d90` (`035773c1b`, #605 to #609); #586 repair `0b55f7b39`; arena pre-registration `0222d24c4`; day docs after. Every push today in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode (`UNQUALIFIED DEVELOPMENT ... no GPU qualification claimed`, a `log_skip` row each): no qualification claimed, every cell `executed-not-qualified`. No commits on main or the lead branch; no PR opened; the day-16 tip (`bea6bd2ff`) went out inside the first push.
- NOTHING RUNNING for C: BOX3 tmux `c17build`, `c17cpubank`, `c17arena` exited; `/root/wt-c` detached at `0b55f7b39` (`refs/bundle/c17`); receipts `/root/spill-receipts/c-day17/` mirrored to `pro-single-day17/` (bins excluded). The card was free for both cells; no holder touched.
- memra#586 (`0b55f7b39`): `worker_loop` released `prefetch_inflight` per `IoJob`; a mirrored projection is two jobs and one charge, so the signed counter went negative under the stats clamp. Fix: the release moves inside the `projection_pending == 1` final-half branch (success or failure); the non-mirrored path unchanged; no ABI change. Fixture `tools/test_cpu_expert_prefetch.sh` + `tools/memra_cpu_expert_prefetch_test.cpp` (the TU included, the signed counter read, the unit's `pread` renamed to hold and fail halves): red on the unfixed source `cpu expert prefetch accounting tests: 9 FAILURE(S)` (`inflight_signed=0` where 3 and 1 were charged), `ALL GREEN` after, locally and on the target rig's host (collector cell `cpubank`, validate rc=0); companion production build clean; `cpu_native_check ALL GREEN` on both; `ci.yml` step after `cpu_native_check`; `docs/TESTING.md` paragraph; #586 commented, left open for the lead's integ.
- Arena cell (`DAY17.md`, pre-registered and pushed before the run, `arena-cell.sh`, `arena-pair.py`): door OFF both arms, pageable against `MEMRA_GLM5_TP_KV_HOST=1`, N=5 per arm per order, both orders, one 50 s lock hold, 33 to 51 C, at most 493 W under 600 W. Verdict verbatim: `-> arena_first_touch_absent; arena_not_slower` (first-touch step 35.4 and 35.8 ms pageable against 0.0 and 0.0 arena; steady-state demote 6.1 against 6.2; promote minus inline demote 4.5 against 0.4; identity ok; `ARENA PAIR REPLAY: PASS (18 checks)`). The step is the fresh pinned region, moved to a 1.3 s boot reserve; the promote share's mechanism is a census item. Not a verdict on the door; not a support state.
- WC item CLOSED BY POINTER: `docs/decisions/PINNED-DESTINATIONS.md` (ruling 23, `PinnedKind::for_device`, cached on the target class, write-combined on the RTX 5090 class and unknown names, no flag); `HOSTPREFIX-DOOR.md` Status and `WC-DESTINATIONS.md` point there; the day-16 pair is the write-combined cost, kept as the record. The review still owes the door's cost with cached destinations (the pair cell on a `for_device` binary) and the hash micro-cell.
- Pre-existing, not fixed, stated: the companion's submit-side exception path in `memra_cpu_expert_prefetch_v2` leaves earlier projections' charges and annex claims behind (`DAY17.md`); the pool-full failure-gate line.
- NEXT before the decide-by (2026-10-05): the cached-destination pair cell and the hash micro-cell (the door's current cost on the target class); the arena lease handoff as a priced item for the lead (not started); the DFlash tail slice; verify digest v3.
- OPEN: `MOE-SLOT-CACHE-DOOR.md` items 1, 3 second half, 4, 6 (decide-by 2026-10-04); `HOSTPREFIX-DOOR.md` decide-by 2026-10-05.
