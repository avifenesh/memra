# Session C day-twenty handoff (memra#365: the standalone DFlash tap bounded; serving was already bounded by #370)
- Lane `lane/spill-c-20260919`, checkout `wt-spill-c`; merged main `a18c936a3` (`6c30f2d39`; INDEX.md marker-vs-row conflict resolved, census OK). Every push today in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; every cell `executed-not-qualified`; no commits on main, no PR, #365 left open with the receipt comment. Ruling 28 (arena handoff scoped until the HOSTPREFIX review) untouched.
- Census (`DAY20.md`): tap = `n_taps x hidden` f32 per prompt token = 102,400 B on the 27B (five taps); `131,070 x 102,400 = 13,421,568,000`. Written per tapped layer per prime chunk (`hybrid_forward.rs` `dflash_tap`), read once by the drafter ingest in 256-row windows, freed after ingest. Serving (`prime_dflash_taps`, chunk sink + 256-row carry) bounded since #370; the whole-prompt sink survived only in standalone `generate_spec_dspark` (qwen) and `generate_spec_dflash` (gemma, capped at 2,048 rows by its window assert).
- Landed `4fd4f3b41` (`dflash.rs` only): `generate_spec_dspark` primes through `prime_dflash_taps` (one 4,096-row chunk sink, 419,430,400 B, plus the 26,214,400-B carry; the monolithic prime's whole-prompt `hiddens` gone too); typed refusal replaces the carry copy's `expect`; CPU test on the chunk bookkeeping. Gate bin `dspark_q38_gate` prints `[dspark-q38-gate] ... spec_sha256=... spec_len=... prime_s=...` (`0e346fe7d`). No kernel, value, flag or serving-path change.
- Before (`rtx5090-day20/tapladder20/`, 27B NVFP4/Q5K + q38 DFlash2, ngen 32): 8,194 and 16,382 tokens `EXACT` (`28/28`); 32,759 OOM on `lib.rs:33856` (GDN scan transient) with the 3.35 GB tap resident; 65,507 the 6,707,916,800-B tap REFUSED (`dflash.rs:3977` then `CUDA_ERROR_OUT_OF_MEMORY`); 131,004 the gate's plain control refused first (`hybrid_forward.rs:6578`).
- After (`rtx5090-day20/tapladder20b-retry2/`): `DAY20 REPLAY tapladder20b: PASS (9 checks) -> identity; bounded`: same `spec_sha256`/length/acceptance at 8,194 and 16,382; 32,759 and 65,507 now `EXACT` with `chunk_tap_bytes=419430400 carry_bytes=26214400`; 131,004 refuses in the plain control as before. Device peaks 21,415 / 21,415 / 22,215 / 23,609 MiB (before 21,607 / 22,759 / OOM / OOM).
- Gates on the changed binary: `cargo test -p memra-engine dflash::` 57 passed; clippy `-D warnings` clean; `SPEC-ON-CACHE-HIT GATE: ALL GREEN (qwen)`; `A4 CONTINUATION GATE: PASS` (`rtx5090-day20/hitgate/`, `contgate/`).
- NOTHING RUNNING for C: no BOX3 access today; local lock released; no server; `/tmp/spill-c-day20` scratch removed.
- NEXT: target-card `tapladder20b` if the lead wants it before main; a 131k control that fits (the gate's plain oracle primes monolithically); the serving 128k/262k cells stay with #370/#377; HOSTPREFIX review 2026-10-05 (arena handoff, DFlash tail slice) unchanged.
- OPEN: `MOE-SLOT-CACHE-DOOR.md` decide-by 2026-10-04; `HOSTPREFIX-DOOR.md` decide-by 2026-10-05; memra#617, #365 open for the lead.
