# Router / aggregator sweep progress — 2026-08-12

Branch: `lane/cx-routersweep`

Scope: docs-only research of channels that can send demand to an independent
OpenAI-compatible inference endpoint. All market facts are revalidated from
current sources; estimates and unresolved claims are labeled.

## Checkpoints

- [x] Create progress ledger before research output.
- [x] Direct provider marketplaces and compute networks.
- [x] Routing gateways and custom-endpoint surfaces.
- [x] Agent tools, directories, and BYO-provider ecosystems.
- [x] Long-tail supplier discovery and live-market evidence capture.
- [x] Saturation pass across endpoint sellers, crypto markets, raw hosts,
  stale/dead leads, and newly launched 2026 surfaces.
- [x] Economics ranking and final synthesis.
- [x] Citation/link audit and final report verification.

## Working notes

- Required output: `research/routersweep-20260812/REPORT.md`.
- Retrieval date for web sources: 2026-08-12 unless a row says otherwise.
- No runtime, generated performance-board, release, merge, tag, push, or
  formatting changes are in scope.
- Direct-market result: Surplus Intelligence has the strongest newly found
  live demand ledger and an existing Q27 order book, but our domain needs
  allowlisting and Q27 moved only $0.00058 in the sampled 24 hours. Swan is
  the only additional channel with a non-negligible conditional revenue case;
  its fields are internally inconsistent and exact-model demand is absent.
  All uncontracted paths retain a $0 evidence floor.
- Foza, Inpherio, Onlist, DGrid, RouteMarket, AntSeed, Coaxiom, Resonara,
  Tangle, and Atlora were added with explicit trust/traffic/terms gates; model
  counts and cumulative counters are not treated as our revenue.
- The closure pass added Surplus, Proxygate, mtok, IDLE, Token Router,
  Routstr, Scalattice, CurrentInference, Switchboard and their smaller peers;
  it also rejected dead/pivoted surfaces against their live origins instead
  of relying on indexed descriptions. Total classified surface count is 111.
- Gateway result: custom endpoints are customer/account scoped, so they are
  compatibility and sales-enablement surfaces rather than demand pools.
- Agent result: Continue Shared Blocks, OpenClaw provider plugins in ClawHub,
  and OpenCode/models.dev are the useful independent-provider discovery paths;
  other clients are tested recipes only.
- Ranked connect set: Q27-only OpenRouter ($6.02 conditional daily revenue per
  midpoint dev-day), Swan ($0.20), Surplus (<$0.00004), then BitRouter ($0
  until a paid request). The older two-model OpenRouter estimate was removed
  because Q35 is not sellable. Operational sequence publishes BitRouter during
  OpenRouter review, before bounded Swan and Surplus canaries.
- Final audit: 111 unique rows and 244 unique external links. Of those links,
  232 returned 2xx/3xx; all 12 exceptions are explicitly classified in the
  report (anti-bot, TLS, dead, or unreachable surfaces). Seven local links and
  three anchors resolve; Pandoc, table-shape, and diff checks pass.
