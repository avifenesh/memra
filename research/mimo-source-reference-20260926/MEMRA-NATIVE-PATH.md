# MiMo-V2.6-Flash-RL source: next Memra GPU text token

Inspected Memra `codex/mimo-qkv-gather-20260926` at `a851f1042a26a9b24864c756f3501501eddf4d66` on 2026-09-26. Target: `XiaomiMiMo/MiMo-V2.6-Flash-RL@3b38d063180c3e4aed9691fdc735f3d10b266ee4`, source MXFP4, exactly two RTX PRO 6000 Blackwell cards. This note specifies the next offline executable rung. It does not admit a server path or make a serving claim.

## What is already in Memra

`crates/memra-gguf/src/model_packs/mimo_v2/mod.rs::{SOURCE_PROFILE,inspect_pinned_source_headers}` binds the pinned source header digest `5ebbdd27e45716b805fc2bdf115c8345b4bfc03c6012b860f76b6b222c758aee` with separate output-head ownership and `TensorConsumption::Refuse`. Its support state is `None`, and it is an explicit inspection profile in `model_packs/mod.rs::ONBOARDING_PROFILES`, outside automatic `for_config` load selection. `config.rs::Arch::MiMoV2` and `model_plan.rs::{compile_attention,layer_uses_moe,router}` carry 48 text layers: dense layer 0, then 47 routed MoE layers with 256 experts and top-8 sigmoid routing. The pinned fixture has nine global layers `[0,5,11,17,23,29,35,41,47]` with KV4, QK192/V128 and 39 sliding layers with KV8, window 128. Both have 64 query heads and rotary width 64; global and sliding RoPE bases differ. `model_plan.rs::MiMoAttentionMath` and `tensor_contract.rs::LayerTensor::{FusedQkv,AttentionSink}` encode four checkpoint QKV shards, a sink only on sliding layers, and V scaling by `0.707` before the KV write.

`source.rs::{SafetensorsSource::find_fp8_mimo_qkv_shards,split_mimo_fp8_qkv_shards}` preserves each source FP8 shard's independent block-128 scale grid. `source.rs::mimo_mxfp4_expert` and `dsv4.rs::dequant_mxfp4_expert` decode source E2M1/E8M0 experts. `memra-reference/src/lib.rs::full_attention` has fused-QKV row order, per-layer RoPE, pre-cache V scaling, and denominator-only learned sink; `memra-reference/src/bin/mimo_v2_checkpoint_runner.rs::run_checkpoint` streams the pinned text trunk. `research/mimo-source-reference-20260926/RESULTS.md` records token ID 42 through all 48 layers with 152,576 finite logits and argmax 220. That 20m40s, roughly 170 GiB RSS result is a **Memra f32 reference diagnostic**, not independent parity.

On GPU, `model.rs::GpuTensor::load_mimo_fp8_qkv_shard`, `bin/mimo_qkv_shard_gate.rs::run`, `mimo_qkv_ffi.rs::Engine::mimo_gather_qkv`, and `cu/mimo_qkv_gather.cu::memra_mimo_qkv_gather_f32` cover source shard residency, projection, and Q/K/V assembly. `bin/mimo_mxfp4_expert_gate.rs::run` checks BF16 decode bits against the scalar source decoder, not routed expert computation. `mimo_sink_ffi.rs::Engine::mimo_sink_decode` and `cu/mimo_sink_attn.cu::memra_mimo_sink_attn_decode_f32` cover bounded f32 full/sliding decode attention with optional sink. The QKV call accepts 1..=128 projected tokens; sink decode accepts a current-token-inclusive contiguous sequence of 1..=4096, with already rotated Q/K and already scaled V. The source-backed QKV and expert results in `RESULTS.md` are component evidence on both cards. The sink gate exists in `bin/mimo_sink_attn_gate.rs`; this note found no complete-model GPU receipt.

## Still-missing executable join

Add a **proposed** offline `crates/memra-engine/src/bin/mimo_source_token.rs`, using `SOURCE_PROFILE` and `inspect_pinned_source_headers` explicitly before allocation. `model_packs/mod.rs::compile_for_source` currently refuses MiMo because inspection profiles are not automatic load packs. `model.rs::Model::load_dense_from_source` also rejects any non-full attention layer, and its layer fields load separate Q/K/V. The MiMo runner therefore needs a plan-driven mixed-attention layer path; reusing a generic loader by renaming QKV or dropping sliding layers would compute another program. Bind all source headers, then consume text trunk, embedding, final norm, and separate LM head for this run. Keep the modal and separate MTP tensors out of text execution without erasing them from the source census.

For each owned layer, load norms, the four native FP8 QKV shard views and their scales, output projection, the layer-appropriate sink, router, and the dense or routed MLP tensors. Project each shard through the checked block-FP8 operand, call `mimo_gather_qkv`, apply that layer's RoPE to Q/K, write scaled V with K to its KV state, and call `mimo_sink_decode` for the current token. Then run attention output, residual, pre-MLP norm, dense layer 0 or top-8 MoE with source MXFP4 experts, and the second residual. The existing MXFP4 **decode** gate does not establish the source's dynamic-FP8 activation, expert GEMM, router combine, or accumulation order. Implement or select those GPU operations only against the pinned numeric program. Finish with the bound final norm and separate 152,576-row head. No text-token runner currently wires this sequence end to end.

For two-card placement, start with contiguous whole-layer stages. `placement.rs::plan_contiguous_stages` accepts per-layer weight and KV costs and legal `ModelPlan::partition_boundaries`; `crates/memra-cli/src/lib.rs::write_checkpoint_placement_candidates` provides header-byte candidates but sets `context_tokens=0` and includes non-text owners in fixed costs. Derive **text-only** physical bytes from the bound `TensorOwner` rows, then charge expansion or mirrors, each stage's workspaces, and the requested KV precision/capacity. Put embedding on stage 0; put final norm and head on stage 1; keep every layer's experts, QKV, sink, and KV on its owning card. `crates/memra-engine/src/pp.rs::{pp_cuts,stage_of}` and its peer-copy boundary are transport precedents, not a MiMo placement receipt. Transfer the hidden row at the cut. Report actual device ordinals, layer range, uploaded bytes, peak allocated HBM, and both GPUs' kernel work. If full text residency does not fit, an explicitly labelled layer-streamed offline token can still prove GPU math; it cannot prove resident two-card fit.

The completed pinned **header-only** ledger counts 73,081 physical entries in 65 files and matches the index `total_size=172,923,364,096` bytes. Text embedding, 48 layers, norm, and separate head total `169,754,520,448` bytes; modal and MTP tensors are excluded. At two concurrent 1,048,576-token requests, the nine global KV4 layers alone require `2 × 9 × 4 × (192+128) × 1,048,576 = 22.5 GiB` at FP8, or 45 GiB at BF16. Adding 128-token windows for the 39 sliding layers gives `24,184,750,080` bytes of assumed FP8 K/V. With 96 GiB assumed per card and complete-layer contiguous PP, the best cut is **before layer 24**: stage 0 `[0,24)` is `96,697,221,504` base bytes; stage 1 `[24,48)` is `97,242,049,024` base bytes. Minimum nominal headroom is `5,837,166,080` bytes, or 5.436 GiB. Only cuts before 24 or 25 fit at this idealized stage. With BF16 K/V for two sessions, the best-cut minimum headroom is **negative 6.568 GiB** under the same assumptions. The FP8 figure is neither comfortable margin nor runtime proof: CUDA context, allocator fragmentation, workspaces, activations, and any weight expansion or mirrors are uncharged. Measure actual per-stage runtime peaks before any production fit or concurrency statement.

**Physical bound for the requested two-session shape:** resident text weights plus assumed FP8 K/V total `193,939,270,528` bytes (180.620 GiB). Against an optimistic nominal 2 × 96 GiB, aggregate slack is `12,219,159,680` bytes (11.380 GiB). No placement can provide more than half of that, `6,109,579,840` bytes (5.690 GiB), as its *minimum* per-card slack without changing residency or precision; the best complete-layer cut gives 5.436 GiB before runtime allocations. FP8 KV quality is unqualified. These are header-byte capacity bounds, not measured memory use or throughput.

The paired reproducibility tool, `research/mimo-source-reference-20260926/source_capacity.py`, checks the pinned index and config hashes and reads each shard header without a tensor payload in remote mode. Its header SHA is `1eaf071f50110e1f72db262dd6ea7ee6d9626fe7015e3a972285dd0e78060793`, distinct from the source pack's digest because it hashes a different representation. Its remote header result matched a separate census. The 96 GiB per-card input is nominal, not a live device reading; no runtime allocation result follows from this header check.

## First truthful comparison

Run one fixed input token, ID 42, through all 48 layers on the two GPUs and produce the complete next-token logit row. Use the same pinned source revision and a separately captured independent model oracle under a declared numeric class; the existing Memra reference TSV is a useful localization control, not that oracle. Predeclare comparison tolerances, compare finite logits and argmax, and retain layer-boundary diagnostics for the first divergence. Bind source, binary, plan, GPU topology, placement, and logit hashes in the receipt. A one-token result exercises current-token attention, sink, MoE, and head but does not prove nonzero-position RoPE, reading prior KV, long context, two-request capacity, or serving. A following two-token continuation is the smallest cached-KV and nonzero-position check.
