# GPU lane queue — door-mining workflow output, 2026-08-13

Generated by a 53-agent workflow: 4 parallel miners over cache-prefill / concurrency / kernel-frontier
/ model-support; 24 candidate doors; each adversarially verified by 2 independent skeptics (one trying
to REFUTE existence, one judging feasibility/value); 14 survivors; ranked into a rig-slotted queue with
paste-ready worker briefs. This is the standing engine backlog. Two doors were explicitly DROPPED with
quoted reasons — do not re-fund them without new evidence.

Lanes already dispatched from this queue: L2 cx-budgetsize (5090), L3 cx-eosclass (5090, promoted after
a FIFTH early-EOS trigger appeared in cachesize attempt 3). Its scheduling finding was folded into
CLAUDE.md (scored GPU work serializes on the PRO pair — shared PIX path).

# GPU LANE QUEUE — memra, 2026-08-13 (planner output)

## 0. VERIFIED METAL STATE (measured, not assumed)

| Slot | Physical | Right now | Lock | Occupant |
|---|---|---|---|---|
| `box1-gpu0` | RTX PRO 6000 SE, `ubuntu@<rented-box-ip>` | 0 MiB / 0% / 27C at 23:47Z, `memra-server` PID 1198315 alive from `/opt/dl-image/nvme/cx-cachesize/memra/target/release/` | `/tmp/memra-gpu.lock` **HELD** (holder tree PID 1154623 bash + nvidia-smi + vmstat samplers) | **cx-cachesize, scored campaign attempt 5**, launched 23:47Z |
| `box1-gpu1` | RTX PRO 6000 SE, same host | 0 MiB / 0% / 27C | `/tmp/memra-gpu-1.lock` **FREE** | **cx-lcprestore** claimed it; staged `/opt/dl-image/nvme/cx-lcprestore` 23:34Z, still in build/code phase |
| `5090` | RTX PRO 5090 Laptop, local | 153 MiB, SM clock **210 MHz** (cap live, `clocks_event_reasons.active=0x0`) | `/tmp/memra-5090.lock` **FREE** | **cx-slrucache** (code) + **cx-fa3softmax** (candidate authorized, PROFILE.md complete) share it |

**HARD SCHEDULING FACT — the single most important item in this document.** `cx-cachesize` has already **thrown away four attempts**, and attempt 1 was excluded *specifically because a separately-locked GPU1 campaign appeared*: `nvidia-smi topo -m` puts both PRO 6000s on a shared **PIX** PCIe path, and the lane ruled that continuing "would violate the campaign's GPU1-idle thermal and I/O regime even though memra itself remained pinned to physical GPU0" (`/home/avifenesh/projects/wt-cx-cachesize/research/cachesize-20260813/raw/attempt1-gpu1-overlap/ATTEMPT-DIAGNOSIS.md`). **Any scored GPU work on `box1-gpu1` during cachesize's window buys a 5th exclusion of the top money lane.** Therefore: `box1-gpu1` is available for **build / compile / non-timed pass-fail correctness cells only** until cachesize releases `/tmp/memra-gpu.lock`. Scored campaigns on box1 **serialize**, they do not parallelize. This contradicts the "two cards = two lanes" assumption in the dispatch premise and must be corrected in the standing doctrine.

Three doors also collapse into one lane each (they are the same mechanism filed thrice): `routed-moe-carried-prime-batch-requal` + `moe-carried-prime-batch-gate` + `moe-carried-prime-batch-unfence` → **L3**; `spec-complete-prefix-entries` + `spec-x-prefix-cache-checkpoint` → **L8**.

---

## 1. RANKING (expected $/agent-day, cache-prefill-concurrency weighted highest)

| # | Lane | Money axis | Effort (agent-days) | Honest expected value | Slot |
|---|---|---|---|---|---|
| **L1** | `cx-capraise` — co-resident pair capacity sweep → raise sold cap from 4 | capacity-per-card | 2–3 | **+$38/day/card Q27 (c4→c16), +$40/day/card Q35 (c4→c40)**, corrected list-price arithmetic. Realized **$0 today** (router at HTTP 402, live keys `rate_limit=1`). Option value + closes a real measurement hole. | `queued-behind-box1-gpu0` |
| **L2** | `cx-budgetsize` — derive prefix budget from geometry + skip counter | cache-prefill-concurrency | 1 | **$0 on the live box** (pinned 4096). Converts a **guaranteed 0% hit rate → hit path** for Q27 on any unconfigured deploy, and makes the failure visible in `/metrics` for the first time. Directly serves #117 self-serve. | `5090` (starts NOW, lock-light) |
| **L3** | `cx-eosclass` — root-cause the Q35/Q27 early-EOS numeric class | correctness guarding live serving | 3–5 | **Unquantified, measure first.** Not a throughput lane. Four independent triggers now observed on the money path. Blocks 3 shipped-but-fenced mechanisms. | `queued-behind-box1-gpu1` |
| **L4** | `cx-ringadmit` — admit un-wrapped ring prefixes (cheap arm only) | capacity-per-card | 1–2 | Restores the **whole existing hit envelope** for ring-ON Step ≤4,639 tokens. **~$0 near-term**: Step is not in `q27-models.toml`, `MEMRA_SWA_RING` is off by default. | `queued-behind-5090` |
| **L5** | `cx-iq4fuse` — same-input IQ4_XS projection fusion | decode-throughput | 2 | **+0.5–1.5% at c=1, plausibly flat at c≥8.** Bit-identical by construction. Two sibling fusions already measured flat/negative on PRO. | `queued-behind-5090` (depth 2) |
| **L6** | `cx-hiradixl2` — whole-entry pinned-host L2 tier | cache-prefill-concurrency | 4–5 | **Unquantified, measure first.** GATED: only fund if SLRU + card-sized budget still leave a measured rehit loss. | `queued-behind-box1-gpu0` (depth 2) |
| **L7** | `cx-iq4pipe` — IQ4_XS load-only software pipeline, K>4096 | decode-throughput | 2 | **≤2.4% of token wall ceiling, realistically <1%** — below the measured 0.9% serve noise floor. Inert on 55–59% of the byte mix. Register-occupancy cliff risk. | `queued-behind-box1-gpu1` (depth 2) |
| **L8** | `cx-specaudit` — spec×cache: the dead-arm audit + threshold re-sweep | decode-throughput | 1 | Full dual-state capture is c≤2-only and **negative at the sold c=4** (0.61x). Fund only the audit: `SPEC_K_CACHED_LONG` is **unreachable** from cache traffic. | queue, no live slot |
| **L9** | `cx-38auto` — Qwen3.8 day-one driver (2 live defects) | model-support | 1 (+GPU rehearsal) | Poll latency ~0 (hermes 4-hourly cron already covers it). Value = 1–3 operator hours + `tools/preflight-38.sh` currently **FAILS on main**. | non-GPU, gap-filler |
| **DROP** | `fa-rows-v4-deep-ladder-port` | — | — | Headline is an 8-row-vs-1-row unit error; `rows_v4` is already faster **per row** than the deep decode kernel, and `ncu27b` prices it at 0.034% of the round. Sibling lane `fadecoderow` already returned NO-GO with a full battery. **Do not fund.** | — |
| **DROP** | `nvfp4-rp-small-grid-multitensor` | — | — | Residual ceiling ~0.6–1.3% dev-rig after the shipped `MEMRA_NVFP4_AUX_DUAL` ate the grid-12 half; sibling measured **-0.024% flat** on the PRO pair; the gain lands on the spec-verify tier that prod disables (`MEMRA_SERVE_SPEC=0`). **Do not fund.** | — |

---

## 2. SLOT MAP

```
box1-gpu0  [LIVE]  cx-cachesize  (scored, attempt 5, 23:47Z)   <- DO NOT DISTURB
             +1    L1 cx-capraise                              <- takes lock on release
             +2    L6 cx-hiradixl2  (CONDITIONAL on L1/cachesize/slru results)

box1-gpu1  [LIVE]  cx-lcprestore (code/build phase; scored window BLOCKED by PIX)
             +1    L3 cx-eosclass  (box1 confirmation arm)
             +2    L7 cx-iq4pipe   (verdict arm; iterate on 5090 first)

5090       [LIVE]  cx-slrucache (code) + cx-fa3softmax (candidate) share /tmp/memra-5090.lock
             +0    L2 cx-budgetsize  <- START NOW, lock-light, adds real parallelism today
             +1    L4 cx-ringadmit
             +2    L5 cx-iq4fuse

no-slot            L8 cx-specaudit (read-only + 1 short c=1/c=2 sweep)
                   L9 cx-38auto    (non-GPU; one GPU rehearsal in a gap)
```

---

## 3. WORKER BRIEFS (paste-ready)

### L1 — `cx-capraise`: co-resident pair capacity sweep, then raise the sold cap
**Slot:** `queued-behind-box1-gpu0` — start the instant `cx-cachesize` releases `/tmp/memra-gpu.lock`. **Effort M (2–3 agent-days), not S.**

**Mechanism.** Sold/advertised concurrency is pinned at 4 in six places while the measured clean knee is c=16 (Q27) and c=40 (Q35) on one PRO 6000. The enforcement point is the per-key `rate_limit` (`crates/memra-server/src/auth.rs`, `effective cap = min(rate_limit, global lane cap)`); the engine ceiling is already 96. **But every knee receipt in the repo is one-model-resident, and the live box co-locates BOTH models in one worker on one card behind one 4 GiB prefix cache** (`/home/avifenesh/projects/bw24/research/servetest-20260812/run_server.sh:50` — `MEMRA_MODELS="qwen/qwen3.6-27b=…,qwen/qwen3.6-35b-a3b=…"`, `CUDA_VISIBLE_DEVICES=0`, one listener on `127.0.0.1:8002`). The pair-resident aggregate knee is **unmeasured**. Measure it first; only then edit the caps.

**Files to touch (all six — the door claimed two):**
- `/home/avifenesh/projects/bw24/deploy/gateway/q27-models.toml:33` (Q27 `concurrency`) and `:59` (Q35 `concurrency`). **Note the asymmetry:** Q27's capacity block carries `prompt_tpm`/`cached_prompt_tpm`/`completion_tpm`/`request_rpm` (lines 28–33); Q35's block (`:58–59`) has **only** `concurrency` — the tpm fields must be **added**, not edited.
- `/home/avifenesh/projects/bw24/deploy/gateway/README.md:50` — "Every externally usable key must have `rate_limit = 4` or narrower."
- `/home/avifenesh/projects/bw24/deploy/glue/keyctl.sh:99` (mint default `rate_limit=1`) and `:124-126` (`((rate_limit < 1 || rate_limit > 16)) && die "rate-limit must be an integer from 1 to 16"` — **this hard-blocks Q35's own c=40 knee**).
- `/home/avifenesh/projects/bw24/deploy/runpod/provision.sh:36` (`MEMRA_KEY_RATE_LIMIT … default 4` — inconsistent with keyctl's default of 1; reconcile).
- `/home/avifenesh/projects/bw24/deploy/gateway/probe.py:805` — asserts `result.headers.get("x-ratelimit-limit") == "4"`; a cap change breaks the public preflight gate unless this is parameterized.
- `/home/avifenesh/projects/bw24/tools/serve-smoke.sh` — add a hit-TTFT p95 assertion so the published envelope is gated, not just documented.

**Measurement protocol.** box1, ONE card, `flock /tmp/memra-gpu.lock` held for the whole campaign, **GPU1 must be idle** (verify `nvidia-smi --query-compute-apps` empty before every scored cell; record the physical card id in every row). Reuse the frozen machinery — do **not** invent a workload: `/home/avifenesh/projects/bw24/research/sellgate-20260812/workload.lock.json` (4,860 prompt / 60 completion, 9:1 hit:miss, `prompt_ids_sha256_canonical_json = eba9dff66d4ebf3f40d6db80298ab9c884fa1bb2e1a6d4ca2c7dadd4f21513fb`) driven through `/home/avifenesh/projects/bw24/research/requal2-20260812/run-box1.sh` + `single_replay.py` + `prefix_gate.py`. Two arms:
1. **PAIR-RESIDENT (the new evidence):** both model IDs in one worker, `MEMRA_CTX=8192`, `MEMRA_PREFIX_CACHE_MB=4096`, `MEMRA_MAX_SESSIONS=96`, `MEMRA_SERVE_SPEC=0` — i.e. the exact live shape. Sweep c = 4, 8, 12, 16, 24, 32, 40, 48 with traffic split across both models.
2. **SINGLE-RESIDENT control** at the same widths, to prove the delta against the frozen one-model receipts.

N≥5 interleaved **inside one lock hold**, width order reversed on even repetitions (cross-run and cross-day comparisons are clock-drift-invalid — LAW 1). State N and thermal regime on every median. `tee` raw first, parse the log second.

**Acceptance criteria (numeric, pre-declared).** Recommend the widest cap per model whose **pair-resident** numbers satisfy ALL of: (a) hit-TTFT p95 ≤ 33 ms for Q27 and ≤ 25 ms for Q35 (from the frozen `coldfix` envelope: c4 19.820 / 10.260 ms, c16 44.87 / 51.45 ms — pick the width inside the sold class, do not just bracket it); (b) all-traffic TTFT p99 ≤ +10% vs the c=4 pair-resident cell (frozen single-resident reference: 2,737 → 2,897 ms = +5.8% at c16); (c) zero admission/session/VRAM defers, zero step-OOM parks, zero prefix-cache refusals; (d) 100% of requests return exactly 60 tokens. Report the corrected list-price arithmetic using the **actual registry price triple** (Q27 `prompt 0.28/M, cached_prompt 0.07/M, completion 2.69/M`; Q35 `0.12/0.03/1.03`) at the 90%-hit mix — **not** all-uncached. Reference values I computed: Q27 $125/day/card at c4 → $163 at c16; Q35 $147 → $187 at c40. **The ~$418/day figure in `research/kneeraise-20260812/RESULTS.md:150-153` bills 90%-hit input at the uncached price and is inflated ~2.5x — do not repeat it.**

**Gates that must stay green.** `kernel-check` ALL GREEN; `run-gen` argmax MATCH on both models; `run-spec` K=1..8 self-consistency PASS; `tools/serve-smoke.sh` 0 failed **including the Q35 mixed c=4 exact-token cell and the "Q35 routed-MoE carried prime batches remain gated" cell**; `tools/serve-stress-gate.sh` c=64; `deploy/gateway/probe.py` green after the header assertion is parameterized; `cargo test` green.

**STOP condition.** Commit to `lane/cx-capraise` under `research/capraise-20260813/` (PROGRESS.md FIRST, commit-or-die). **NO merge, NO tag, NO push, NO board edit, NO `--no-verify`.** **Never touch the live serve box (`<vast-ssh-host>` / Vast 47529373) — box1 only.** The cap change is an **owner call** (two separate lanes already filed it as such): deliver the recommendation + receipts, do not flip the live registry. If the pair-resident knee is materially below the single-resident knee, that is the finding — publish it and recommend no raise.

---

### L2 — `cx-budgetsize`: derive the prefix budget from model geometry + make the refusal visible
**Slot:** `5090`, **starts now**, lock-light (needs `/tmp/memra-5090.lock` only for the two short serving cells; all unit/correctness work interleaves freely). **Effort S (1 agent-day).**

**Mechanism.** `prefix_cache_budget_bytes()` hardcodes `.unwrap_or(256)` MiB (`/home/avifenesh/projects/bw24/crates/memra-server/src/worker.rs:1598-1602`). Against `cx-cachesize`'s just-measured exact device bytes per entry (from the `prefix_cache_bytes` metric delta, both probes reconciling identically): **Q27 = 278,528,000 B at 4,096 tok (265.6 MiB), 301,215,744 B at the sold 4,860-tok shape (287.3 MiB), 400,162,816 B at 8,192 tok (381.6 MiB); Q35 = 103,874,560 / 110,964,480 / 141,885,440 B (99.1 / 105.8 / 135.3 MiB).** So on the naked default **every Q27 entry at ≥4k is refused** at `worker.rs:2540-2545` (`[prefix-cache] skip {why} insert: entry {}MB > budget {}MB`) — a sold model with a guaranteed 0% cross-request hit rate out of the box — while Q35 fits. The doc's "~343 MB" is a **Step 4k example, not a Q27/Q35 receipt**; correct `docs/FLAGS.md:518` and `docs/SERVING.md:391-392` with the measured table.

Second half, and the higher-value half: **there is no counter for the refusal.** I verified `grep -c "prefix_skips\|prefix_refus"` = **0** in both `worker.rs` and `main.rs`. `ReuseMetrics` publishes `prefix_hits/misses/inserts/evictions/hit_tokens/entries/bytes` (`worker.rs:390-401`, surfaced at `main.rs:2133-2149`) but the skip path is a single grep-able stderr line. Monitoring cannot see the money model missing 100% of the time.

**Files to touch.**
- `/home/avifenesh/projects/bw24/crates/memra-server/src/worker.rs:1596-1610` — derive the default: entry bytes = Σ over `LayerKind::FullAttention` layers of (`k_tok_bytes` + `v_tok_bytes`) (`crates/memra-kv/src/lib.rs:217-227,363,521-544`) × `MEMRA_CTX` + fixed recurrent state, × a small documented entry count; clamp against measured free VRAM at boot. `MEMRA_PREFIX_CACHE_MB` stays authoritative when set (keep the `OnceLock` + `pp_host_bounce_active()` live-check semantics intact).
- `worker.rs:390-401` — add `prefix_skips_budget: u64` (+ pinned-skip variant) to `ReuseMetrics`; increment at `worker.rs:2540-2549`; publish in `crates/memra-server/src/main.rs:2133-2149` and add to the operator-body assertions at `main.rs:5572,5705`.
- `worker.rs:3454-3459` — log the derived budget beside `[prefix-cache] on: budget {}MB`, stating whether it was derived or configured; emit **one loud warning on the first budget-skip**.
- `/home/avifenesh/projects/bw24/docs/FLAGS.md:518` and `/home/avifenesh/projects/bw24/docs/SERVING.md:391-392` — replace the 343 MB claim with the measured per-model table and cite `research/cachesize-20260813/`.

**Measurement protocol.** Local 5090, **210–1200 MHz cap stays locked** (verified live: `clocks.current.sm = 210 MHz`, persistence on). All numbers **relative-only**, cap stated on every row, **no absolute-throughput claim**. Model: `/data/ai-ml/hf-models/qwen36-27b-nvfp4-mtp/Qwen3.6-27B-NVFP4-Q4_K_M-mtp.gguf` (the model that actually exhibits the defect; Q35 does not). Two arms, one boot each, `flock /tmp/memra-5090.lock`, arms alternated, N≥3:
- **A (naked default, today):** no `MEMRA_PREFIX_CACHE_MB`. Replay the frozen 4,860-token shared-prefix pattern (request 1 seeds, requests 2–5 should hit). Expected: 0 hits, N skip lines.
- **B (derived default, candidate):** same, no env var. Expected: hit rate > 0, first-hit TTFT in the tens-of-ms class vs multi-second cold.
Also report VRAM used vs free and eviction count at **c=64** under the derived default, so a larger default cannot hide an admission regression.

**Acceptance criteria.** (1) Arm A hit rate = 0 with `prefix_skips_budget == misses` — reproduce the defect before claiming the fix. (2) Arm B: ≥1 insert, hit rate ≥ 4/5 on the replay, `prefix_skips_budget == 0`. (3) Derived default for Q27 @ `MEMRA_CTX=8192` must be ≥ 400,162,816 B × (documented entry count) and must **not** exceed measured free VRAM at boot. (4) `MEMRA_PREFIX_CACHE_MB=4096` explicit still produces byte-identical behavior to today (the live box must not move). (5) c=64 arm: zero admission/VRAM defers, zero step-OOM parks.

**Gates.** `cargo test` green (add unit tests for the derivation at each of the 6 measured geometry points); `kernel-check` ALL GREEN; `run-gen` argmax MATCH; `run-spec` K=1..8 PASS; `serve-smoke` 0 failed. Because this is engine/server code, the full battery applies even though the change is config-shaped.

**STOP condition.** Commit to `lane/cx-budgetsize` under `research/budgetsize-20260813/`. NO merge/tag/push/board. **Coordinate with `cx-slrucache`: see CONFLICT B — you may NOT restructure the insert/evict body.** Your only edit inside `worker.rs:2530-2575` is the counter increment.

---

### L3 — `cx-eosclass`: root-cause the Q35/Q27 early-EOS numeric class
**Slot:** `queued-behind-box1-gpu1`; **iterate on the 5090 first** (`tools/q35-cold-mixed-gate.py` reproduces locally). **Effort H (3–5 agent-days). Correctness lane guarding live serving — this is why it ranks above every kernel lane.**

**Mechanism.** The same failure signature — a **real EOS token selected far short of the frozen 60**, HTTP 200, `finish_reason=stop`, counters reconciling exactly, **no captured CUDA error / OOM / panic** — has now appeared from **four independent code paths on the two sold models**:
1. `Arch::Qwen35Moe`, four carried 2,048-token prime chunks → EOS id **248046** at token **26** (`research/coldfix-20260812/RESULTS.md:51-79`). Serial priming and one whole-fresh batch both reached 60/60. Fenced, not fixed — `worker.rs:74-88` says so verbatim.
2. `MEMRA_MOE_GROUPED=1` on the PRO rig → serve-smoke Q35 mixed c=4 **20/20 requests return 25/60**, `finish_reason=stop`, + 8 "seed failed" hot templates, while `kernel-check`/`run-gen`/`run-spec` all stayed GREEN (merged @`be8109146`).
3. Q35 mixed **c=2**, all five cells invalid: seven cached requests stopped at **17 or 25** tokens (`research/sellgate-20260812/RESULTS.md:118-124`). Fixed for the c=2 rejection cell by `cx-q35bug` (token-counting + keeping `Qwen35Moe` on the generic batched trunk at B=1) — but that was a *containment*, and the class recurred after it.
4. **Q27**, dense, batched 8-wide cache seeding → one 4,860-token hit returned **11 of 60** tokens (`cmpl-5fd64be53904ee8f9032b2e9adeb1372`), counters exact, no error text (`/home/avifenesh/projects/wt-cx-cachesize/research/cachesize-20260813/raw/attempt3-batched-seed-eos/ATTEMPT-DIAGNOSIS.md`). **This is the dense model — the class is not MoE-specific.** The lane's own note: "prefix snapshots inherit the numerical class of the prime configuration that produced them."

`research/p0iso-20260810/RESULTS.md` already established the general shape on Step-3.7: *"an admission-timed transition between two intentionally different floating-point decode classes"* — same prompt, `temperature=0`, seed 3407, same bytes, three stable completion classes **from decode-width history alone**. The hypothesis to test: **prime/decode-width history selects among numerically distinct trunks, and a snapshot freezes whichever class produced it; on some prompts that class puts EOS inside the top-1 margin early.** `tools/argmax-margin-gate.sh` exists and is the right instrument.

**Files to read (do not edit until root cause is quoted).** `crates/memra-server/src/worker.rs:74-100` (`carried_prime_batch_eligible`, `cached_hit_needs_first_token`), `:4745-4762` (the gate site), `crates/memra-gguf/src/config.rs:86-103` (exhaustive arch match — a new variant fails compilation, keep that property), `crates/memra-engine/src/bin/prime_batch_gate.rs:151-210` (the existing `--carried` arm), `crates/memra-server/src/hybrid_forward.rs` B=1-vs-B≥2 trunk split, `tools/q35-cold-mixed-gate.py`, `tools/argmax-margin-gate.sh`, `tools/check-batch-exact.py`, `research/p0iso-20260810/RESULTS.md`, `research/q35bug-20260812/RESULTS.md`.

**Deliverable 1 (the product, even if root cause is not found): the engine gate the code asks for.** `docs/FLAGS.md:503` and `research/coldfix-20260812/RESULTS.md:77-79` both name the missing gate: *repeated real-size carried chunks followed by the serving batched-decode trunk*. Build it as a **first-class battery member** — CLAUDE.md H100 LAW 3: "anything guarding a live lane belongs INSIDE the battery; gates outside rot silently." Extend `prime_batch_gate.rs --carried` to the failing program verbatim (**B=2 tokens=2048 carried=0 → three × B=2 carried=2 → B=2 tokens=1528**), then hand off to the serving batched-decode trunk, with **per-chunk teacher-forced KV identity vs serial prime** plus a 60/60-token completion assertion, on routed-MoE weights AND on dense Q27 with batched seeding. Wire it into `tools/serve-smoke.sh` beside the existing "Q35 routed-MoE carried prime batches remain gated" cell.

**Deliverable 2: the argmax-margin census.** For each of the four triggers, dump the top-2 logit margin at the token where EOS is selected, under both the failing and passing configurations, same prompt/seed/bytes. Quote the margins. If the margin is ~0, the class is a tie-break and the fix is numeric determinism, not a fence. If the margin is large, it is a state-corruption bug and the fence is load-bearing.

**Measurement protocol.** 5090 for iteration (cap locked, correctness only — this lane needs **no** timing claims). box1 confirmation on **one card with the other idle**, `flock` held, `N=5` repetitions of the frozen `sellgate` mixed c=2 and c=4 cells per configuration, all four triggers. `tee` every server log; **quote failure text verbatim, never infer** — a run that died without captured stderr is "died, cause unknown — repro needed."

**Acceptance criteria.** (1) The gate **reproduces at least one** of the four failures at real chunk size on the current default — a gate that cannot reproduce a known failure is not a gate. (2) Root cause stated as a **quoted code path**, or explicitly declared unfound. (3) If unfound: the fence stays, now backed by a battery cell that would catch its regression — **this outcome is a WIN and the lane is scoped to allow it.** (4) Any fence lift requires the gate green on Qwen35Moe **and** dense-Q27-batched-seed, plus N=5 frozen sold-shape requal with 100% 60-token completion.

**Gates.** `kernel-check` ALL GREEN; `run-gen` argmax MATCH ×2; `run-spec` K=1..8 PASS ×2; `serve-smoke` 0 failed incl. Q35 c=4 20/20; c=64 stress; `cargo test`. **Do NOT carry the dense coldhol throughput numbers into a Q35 claim** — Q35's clean knee is already **c=40 / 900% headroom** vs Q27's c=16/300%, its cold prefill is 2.7–3.4x cheaper (c=1 miss TTFT p50 412.8 ms vs 1,406.6 ms), and the coldhol lane priced its own knee-point gain at **+$0.32/day/card**. The throughput upside here is ~0 to low-single-digit; the value is correctness.

**STOP condition.** `lane/cx-eosclass`, `research/eosclass-20260813/`. NO merge/tag/push/board. **CONFLICT D: do not merge anything that changes numeric class while `cx-cachesize`'s scored campaign is open** — its 60 budget boots are on the current binary and would be invalidated.

---

### L4 — `cx-ringadmit`: admit the un-wrapped ring case (cheap arm ONLY)
**Slot:** `queued-behind-5090`. **Effort S/M (1–2 agent-days). Scope discipline is the whole brief.**

**Mechanism.** Three guards blanket-refuse ring sessions: `prefix_snapshot` at `/home/avifenesh/projects/bw24/crates/memra-server/src/worker.rs:2616-2618`, `prefix_restore` at `:2672-2674`, and admission via `ring_prefix_excluded` at `:6100-6106`. There is a **fourth, missed** guard the door did not list: `prefix_fanout_eligible` at `worker.rs:6913-6931` (`c.pos == 0 && !c.has_swa_ring()`). Under the ring, SWA layers cap at `min(max_ctx, 512+4096+31) = 4639` rows while **full-attention layers still allocate `max_ctx`** (`crates/memra-kv/src/lib.rs:521-544`), and `KvRing::physical_range()` is the **identity map with base 0 for T ≤ 4639**. So for prompts up to ~4.6k tokens — the shape live traffic actually sends — a ring-aware entry is **the same size and the same layout as a flat one**, and the refusal is a blanket predicate rather than a geometry constraint.

**Fund arm 1 only:** admit `base == 0 && len <= rows` across all four guards, refuse (loudly, observably) everything else. **Explicitly scope out** the wrapped-entry design and the 262k composition: a 262k ring-aware entry is ~6.1 GB at the live admission coefficients and cannot be inserted under any sane budget, so the "6.0x × hit-envelope" composition in the door text is **arithmetically unreachable and must not be claimed**. Also note the door's "capture recurrent conv/SSM state" clause is a **no-op**: Step-3.7 is all-`FullAttention` (`crates/memra-gguf/src/config.rs:218`) and is the only arch that can carry a ring.

**Measurement protocol.** 5090, cap locked, relative-only. Step-3.7 weights (`/opt/dl-image/nvme/models/step-3.7-flash` on box1 if a local copy is unavailable — then this lane moves to a box1 gap). Arms: ring-ON+cache-refused (today) vs ring-ON+cache-admitted (candidate), prompts at 1k / 2k / 4k / 4,639 / 5,120 (the last must **refuse**), N≥3 interleaved, one lock hold.

**Acceptance criteria.** (1) **Byte identity** on every admitted hit vs the same request served cold — per prompt length, not just in aggregate. (2) Prompts > 4,639 tokens refuse and fall back to today's behavior with an observable log line; **zero fabricated rows**. (3) Hit TTFT drops from the cold class to the tens-of-ms class at ≤4,639 tokens. (4) Session capacity under the ring is unchanged (the cache draws on the same VRAM the ring freed — report the trade explicitly, do not multiply the two receipts).

**Gates.** `kernel-check`; `run-gen` argmax MATCH; `run-spec` K=1..8; `serve-smoke` 0 failed; `tools/tick-invariance-gate.sh`; `cargo test`. Any differing bit = STOP and report verbatim.

**STOP.** `lane/cx-ringadmit`, `research/ringadmit-20260813/`. NO merge/tag/push/board. **State plainly in RESULTS.md that near-term revenue is ~$0** (Step is not in `deploy/gateway/q27-models.toml`; `MEMRA_SWA_RING` defaults off) — this is capability, not income.

---

### L5 — `cx-iq4fuse`: same-input IQ4_XS projection fusion
**Slot:** `queued-behind-5090` (depth 2). **Effort M (2 agent-days), ~20% kernel / ~80% evidence.**

**Mechanism.** Fuse IQ4_XS projections that consume the **same** `quantize_q8_1` activation into multi-tensor launches, executing the arm-1 row body verbatim: Q/K/head-gate (3 launches → 1) and dense/shared gate+up (2 → 1). Precedent form exists: `qmatvec_nvfp4_mmvq_dual_b4_rp` selects the tensor via `grid.y` (`/home/avifenesh/projects/bw24/crates/memra-engine/src/lib.rs:6267-6275`, gated `rp && mcols == 4 && out_f <= 128`). No IQ4_XS equivalent exists — all three dense launch sites hard-wire single-tensor grids (`lib.rs:4090`, `:6015`, `:7521`; kernel at `crates/memra-engine/cu/qmatvec.cu:5387`).

**Honest sizing — do not oversell.** Removes up to **135 of 315** IQ4 launches/token, but 135 of ~1,382 total kernels/token × the 5.90% measured non-GPU launch gap = **≤0.58% ceiling** on the launch-count half. The real mechanism is **small-shape underfill recovery** (64/96/1024-row work at 1.67/2.43/17.69% of card BW launching against an empty machine), worth an ideal ~2.8% of the token wall. **Three mandatory discounts:** (a) the per-shape BW table is **pre-arm-1** and arm-1 moved exactly this kernel (`long_scoreboard` 12.26 → 27.39, L1TEX −36.8%) — LAW 2 requires re-sweeping it; (b) IQ4_XS has no mmvq/batched twin, so at B=8 the 64-row gate becomes 512 CTAs and the underfill mechanism largely dissolves — **the gain concentrates at c=1, not at the cache-hit concurrency that pays**; (c) the closest analogue is **already refuted** at the batched tier (`research/q27-deepdive/RESULTS.md:315-327` LEVER 3, serve c=8 paired mean **−0.20%**, call site reverted; the cited Q8 prior was **−0.04% flat** on 82 SM; the NVFP4 `grid.y` precedent measured **−0.0244% flat** on the 188-SM pair).

**Measurement protocol.** 5090 for iteration (cap locked, relative-only); box1 one-card for the verdict. Standalone `.cu` harness byte-compare at **all 11 Step shapes plus base+4 fallback**; SASS/register receipt (arm 1 moved 42→40 regs; 83.3% theoretical occupancy is the risk budget); then ABBA-interleaved one-lock **N≥8** at **c=1 AND c=8**.

**Acceptance criteria, pre-declared.** Bit-identical output at every shape (non-negotiable — this change is bit-identical by construction, so any diff is a bug). Keep-on-flat rule: land it if c=1 ≥ +0.5% AND c=8 ≥ −0.2% (i.e. no batched regression); **flat at both = NO-GO, the JSONL row is the record, kill the arm and the flag per flags doctrine.** Report the re-swept per-shape BW table regardless — that is a reusable asset.

**Gates.** New `kernel-check` cell; `kernel-check` ALL GREEN; `run-gen` argmax MATCH on the affected models; `run-spec` K=1..8. **Note reach:** the symbol also serves the Q35-A3B / AgentWorld non-expert IQ4_XS trunk and the KAT-Coder trunk (`MEMRA_IQ_FAST` default-on, `docs/FLAGS.md:239`) — so exactness coverage must include Q35, not just Step.

**STOP.** `lane/cx-iq4fuse`, `research/iq4fuse-20260813/`. NO merge/tag/push/board. **CONFLICT E: `cx-fa3softmax` also edits `crates/memra-engine/src/lib.rs` and holds `/tmp/memra-5090.lock` for timed runs — serialize timed arms, never co-run.**

---

### L6 — `cx-hiradixl2`: whole-entry pinned-host L2 tier (CONDITIONAL — do not spawn yet)
**Slot:** `queued-behind-box1-gpu0` (depth 2). **Effort H (4–5 agent-days). GATED.**

**Spawn condition — all three must hold, in order:** (1) `cx-cachesize` publishes the budget knee and shows a **card-sized L1 budget still leaves a measured rehit loss** (its own arithmetic: 4 GiB cached vs ~63–68 GiB free = **16x more L1 available for free via the existing knob**, which dominates any host tier); (2) `cx-slrucache` lands and its re-run of the three evictchurn patterns **still** shows proven-hot entries lost; (3) the raise-L1-budget A/B is published as the **denominator**. Until all three, the correct action is a config change plus one measurement — not an L2 storage ABI.

**What changed since the door was filed.** The reviewers correctly refuted the *original* cited pressure (`research/kneeraise-20260812/RESULTS.md:104-112` — 49 evictions, **495/495 expected hits still landed**, 63.36 GiB free, section titled "Cache size, admission, and gross VRAM are excluded"; and `prefixmoney` cold-arm evictions were of unique-`cache_salt` namespaces that can never re-hit, `cache_concurrency.py:202`). But `research/evictchurn-20260813/` now supplies the nonzero rehit loss they said did not exist: at 782 MiB / 12-of-40 entries, **round-robin = 0/40 reuse hits, all 40 thrashed, 68 evictions**; 80/20 hot-set = **13 proven-hot entries evicted by scan pollution**; hit/miss TTFT **1.470 / 153.260 ms** (~100x per thrashed reuse). That is the workload — but SLRU and a bigger budget are the cheaper first answers, which is why this is depth-2 and conditional.

**Mechanism (if spawned).** Add a default-off L2 host tier under the existing `PoolKey` so an LRU-evicted `PrefixEntry` survives in pinned host memory instead of being destroyed (`worker.rs:2467-2487`, `:2567-2575`; `PrefixEntry` is all `CudaSlice` at `:2186-2204`, no host representation exists). Carry the **complete** `(model artifact/revision, cache_ns, KV layout/version, exact prefix key)` identity — token equality or a content hash is **not** an authorization boundary — and fail closed on any mismatch. Pre-registered gate name **HIRADIX-EXACT-ISO**, four items: (1) snapshot → host encode → host decode → restore reproduces every K/V plane, recurrent plane, `pos`, and boundary-logit bit; (2) namespace A hits through L1 and L2 while B and the default namespace miss in **both** directions, including after eviction/reload; (3) the decoder rejects corrupt / truncated / wrong-model / wrong-KV-layout / wrong-version objects **before** the bounded device-copy path (`worker.rs:2285-2319`); (4) the support matrix says **N/A** for SWA ring and hybrid mid-entry splits rather than falling back to an unsafe truncation.

**Acceptance criteria.** Mandatory baseline arm = **raised L1 budget** (zero restore cost). L2 must beat *that*, not today's 4 GiB config — because an L2 hit pays a real PCIe promotion (~343–500 MB at the measured 54.2 GB/s pinned H2D = **~6–9 ms**) that a bigger L1 does not. Unit arithmetic is excellent per avoided eviction-rehit (~6.3 ms restore vs 5,670 ms cold prefill ≈ 900x) — so the lane must publish the **promotion rate**, which is the entire unknown. Report L2 hit TTFT, promotion rate, and VRAM/host-RAM cost.

**STOP.** `lane/cx-hiradixl2`, `research/hiradixl2-20260813/`. Default-off. NO merge/tag/push/board.

---

### L7 — `cx-iq4pipe`: IQ4_XS load-only software pipeline for K > 4096
**Slot:** `queued-behind-box1-gpu1` (depth 2); iterate on 5090. **Effort M (2 agent-days). Lowest-priority kernel lane that is still worth queueing.**

**Mechanism.** Add a load-only software pipeline to `qmatvec_iq4_XS_dp4a` (`crates/memra-engine/cu/qmatvec.cu:5387-5412`): prefetch the next group's IQ4_XS 64-bit header/quant words and Q8 scale into registers while the current group's dp4a chain executes. Preserve `g = tid; g += blockDim.x` assignment, low/high dp4a issue order, `acc +=` order, and the CTA reduction tree. Arm 2, downstream of the landed arm 1 (dense wide-load + byte_perm, −6.638% on box1).

**Honest sizing.** Only `in_f > 4096` shapes give a thread >1 group: (4096,8192)×12 + (4096,11264)×3 + (4096,12288)×33 = **48 of 315 launches, 1,169,817,600 of 2,872,946,688 weight bytes = 40.7%**. At `in_f=4096`, `nsb=128 = blockDim.x`, so the loop runs **exactly once** and the prefetch branch is **dead code on 55–59% of the byte bill** — it can only cost those shapes registers. Realistic band **~2.4% of the 9.401 ms token wall**, absolute floor-based ceiling 6.38%; the door's **11.0% / +12.35% is the entire family's full-latency-annihilation ceiling across all 315 launches, not arm 2's reach**. Both are priced on **pre-arm-1** NCU rates. And **~2.4% is below the measured serve noise floor** (`research/loaddepth-20260812/RESULTS.md`, N=3 c16 range 170.900–172.488 tok/s ≈ 0.9% spread) — the honest deliverable is a **microbenchmark row**, correctly labelled as `docs/PERFORMANCE.md:552` already does for arm 1: "not an end-to-end decode or tracked-board cell." Also note the 30.17%-of-wall denominator is specific to the **Step-3.7-Flash PP-2 pair, a trial model, not the Q27+Q35 revenue listing**.

**Risk to price into any GO.** Staging a `uint2` header + 2×`int2` quant words + one f32 scale is ~7 live registers. The 40→41 register step costs **48 → 40 warps/SM (−16.7% theoretical occupancy)** on a kernel whose large shapes already achieve 74% of 83.3%. This is the entry price, not something frugality avoids.

**Protocol / acceptance.** Byte-identical output dump at all 11 Step shapes plus base+4 fallback; SASS/register receipt; `kernel-check` ALL GREEN / `run-gen` argmax MATCH / `run-spec` K=1..8 PASS; then interleaved same-lock Nsys **N≥32** plus per-shape NCU. Pre-declared: **flat or negative = NO-GO, kill the arm, the JSONL row is the record.**

**STOP.** `lane/cx-iq4pipe`, `research/iq4pipe-20260813/`. NO merge/tag/push/board.

---

### L8 — `cx-specaudit`: the spec×cache dead-arm audit (fund the audit, NOT the state plumbing)
**Slot:** no live slot; runs read-only + one short c=1/c=2 sweep in a gap. **Effort S (1 agent-day).**

**Do not fund** full dual-state capture. Verified: `spec_gate_defaults(pp2)` returns `(0,1)` for PP-2 and `(2,4)` single-card (`worker.rs:1707-1709`), so spec is policy-refused on PP-2 at any concurrency and above 2 active on single card. The in-code measured table (`worker.rs:1660-1676`, N=3 interleaved) is spec ON/OFF aggregate **374.8/224.5 (1.67x) at c=1, 374.5/347.5 (1.08x) at c=2, 377.3/617.1 (0.61x LOSS) at c=4** — the sold shape is c=4, where spec already loses to batched decode. The one existing combined-shape run is **negative**: `research/cachespec-20260809/RESULTS.md:29-37`, forced spec, 16/16 later requests missed with zero cached tokens, TTFT 9.068 s → 19.860 s. And spec-complete entries would inflate every entry by the draft scratch rows on a budget that already refuses Q27.

**Fund this instead — a real, cheap finding.** `SPEC_K_CACHED_LONG` (`worker.rs:1773`, K=2 for `prompt>=1024 && cached>=1024`, decision site `:1854`) is **unreachable from prefix-cache traffic**: `n_cached` is non-zero only via `spec_resumed` (`worker.rs:6636-6641`, set at `:6454`), and a prefix hit forces spec off (`worker.rs:6143`: `if prefix_on && reused.is_none() && !spec_eligible`). So the shipped K policy carries a cached-long arm no cache hit can ever enter, and the `lcp=` field in the `[spec-k]` log line is decorative on that path. **Deliverable:** the audit written up, plus either a reachability fix or the arm's deletion per flags doctrine, plus a re-sweep of `spec_gate_defaults` thresholds under a **cache-on** traffic population (LAW 2: thresholds calibrated pre-cache are exactly the stale-verdict class). Acceptance: an interleaved N≥3 on-box c=1/c=2 A/B with an acceptance-rate floor, or a documented deletion. **STOP:** `lane/cx-specaudit`, `research/specaudit-20260813/`, no merge/tag/push.

---

### L9 — `cx-38auto`: Qwen3.8 day-one driver — fix the two live defects first
**Slot:** non-GPU; one GPU rehearsal in a gap. **Effort S (1 agent-day) for the defects, M for the driver.**

**Two defects that make the current asset red, found by running it:** (1) `/home/avifenesh/projects/bw24/tools/preflight-38.sh:43` pins `EXPECTED_BRANCH=lane/cx-38-runbook`, a **branch that no longer exists** — running it on `main` yields `PASS=53 WAIT=3 FAIL=1, PREFLIGHT-38: FAIL`, so cron-wrapping it as-is produces a permanently red job. (2) The sighting predicate at `:34,205-207` searches `Qwen3.8-27B` with exact equality on `Qwen/Qwen3.8-27B-FP8`; the 3.8 flagship shipped as `Qwen/Qwen3.8-2.4T-A95B`, so there is a **live false-negative on rename**. Keep `author=Qwen` scoping (HF-wide `search=Qwen3.8-27B` already returns 9 community placeholder repos containing only `.gitattributes` + `README.md`).

**Do not claim a poll-latency win.** An enabled 4-hourly hermes digest already surfaces Qwen releases same-day. The mechanizable head is `H+00:00 → H+00:30` (bind revision, metadata-only fetch, arch-diff + tokenizer diff); `H+00:30` onward is a 28–55 GB download, a release build, and GPU gates — a driver can chain them unattended but cannot compress I/O or GPU time. Honest value: **1–3 operator hours off a 10-hour clock + removal of copy-paste/exit-code risk**, and the owner's own note says 3.8-27B ships with its own MTP head so the day-one cost is already "HOURS, not a day." **Zero dollars measurable; zero GPU perf delta available** — no official artifact exists.

**Acceptance.** Wall-clock rehearsal of the driver against the frozen 3.6 control at `/data/ai-ml/hf-models/qwen36-27b-blk128fp8`, **including fault injection proving the STOP paths fire** (`docs/ONBOARDING.md:539-582` config-diff STOP matrix stays human-decided by design). **STOP:** `lane/cx-38auto`, `research/38auto-20260813/`, no merge/tag/push.

---

## 4. CONFLICTS — what must serialize

| ID | Pair | Collision | Ruling |
|---|---|---|---|
| **A** | `cx-cachesize` (box1-gpu0, scored) **vs any scored box1-gpu1 lane** (`cx-lcprestore`, L1, L3, L7) | **Shared PIX PCIe path.** cachesize already excluded attempt 1 for exactly this and has burned 4 attempts. | **MUST SERIALIZE.** box1-gpu1 = builds and non-timed pass/fail cells only until `/tmp/memra-gpu.lock` releases. Then gpu1 scored lanes go, and *their* protocol must declare whether gpu0 activity is tolerated. Fix the "2 cards = 2 lanes" doctrine. |
| **B** | `cx-slrucache` **vs L2 `cx-budgetsize`** | Both edit `crates/memra-server/src/worker.rs` prefix region: slrucache owns `2186-2600` (eviction policy body), budgetsize wants `1596-1610` (derivation) + `390-401`/`main.rs:2133-2149` (counter) + the increment at `2540-2549`. | **CO-EXIST WITH A SCOPE FENCE:** budgetsize touches **only** the derivation fn, the metrics struct, `main.rs` publication, and a single counter increment — **no restructuring of insert/evict.** Merge order: slrucache first, budgetsize rebases. |
| **C** | `cx-lcprestore` **vs `cx-slrucache`** | Both `worker.rs` prefix path — lookup/restore (`6186-6200`, `best_lcp` `2381`) vs insert/evict (`2467-2575`). Already flagged in lcprestore's brief. | Develop in parallel, **serialize at merge.** Whoever lands second re-runs the other's acceptance table. |
| **D** | L3 `cx-eosclass` **vs `cx-cachesize`** | A numeric-class fix invalidates cachesize's 60 in-flight budget boots. | **L3 must not merge before cachesize's campaign closes**, or cachesize re-scores from zero (attempt 6). |
| **E** | L5 `cx-iq4fuse` **vs `cx-fa3softmax`** | Both edit `crates/memra-engine/src/lib.rs` (launch sites vs `9575-9583`/`9627-9658`), and both need `/tmp/memra-5090.lock` for timed runs. | **SERIALIZE timed arms** — never co-run a timed cell while the other holds a kernel (`nvidia-smi --query-compute-apps` check first). Correctness/build work interleaves freely. |
| **F** | L1 `cx-capraise` **vs `cx-selfserve`** | `docs/FLAGS.md` + `docs/SERVING.md` are hot across selfserve, lcprestore, slrucache, budgetsize, capraise; capraise also owns `deploy/gateway/*` + `deploy/glue/keyctl.sh` + `deploy/runpod/provision.sh`. | Doc-block ownership: **capraise owns the capacity/rate-limit rows only.** Every lane appends, never rewrites another's row. `deploy/` files are capraise-exclusive. |
| **G** | L5/L7 (both IQ4_XS `qmatvec`) | Same kernel family, overlapping register budget; L7's occupancy cliff would confound L5's fusion delta. | **Never co-run or co-merge.** L5 first (bit-identical, cheaper); L7 re-baselines against L5's result. |

---

## 5. STANDING RULES FOR EVERY BRIEF ABOVE

- **No merge, no tag, no push, no board edit, no `--no-verify`, no `git config` change.** The orchestrator ships. Commit to your own lane branch under `research/<lane>-20260813/` with `PROGRESS.md` written **first** (commit-or-die, checkpoint as you go).
- **Never touch the live serve box** (`<vast-ssh-host>` / Vast 47529373 / `api.tiyuvta.ai`). box1 and the local 5090 only.
- **Never hand-edit inside `<!-- PERF-*:START/END -->`** in `README.md` or `docs/PERFORMANCE.md`. Board-moving numbers go through `research/tune-data/current-board.json` + `python3 tools/update-perf-board.py`, and that is the orchestrator's call, not a lane's.
- **Raw output is part of the deliverable.** Commit the per-run JSONL/log next to the summary row. **`tee` first, parse the log second** — never `run-* 2>&1 | parser`.
- **Failure causes are quoted, never inferred.** "OOM" requires a captured `out of memory` / `CUDA_ERROR_OUT_OF_MEMORY` line plus `nvidia-smi` compute-apps at failure time. A run that died without captured stderr is "died, cause unknown — repro needed" and no conclusion may rest on it.
- **Every published median states its N and its thermal regime.** Single runs are labeled single runs. All 5090 numbers are **relative-only** under the 210–1200 MHz cap (verified live) — no absolute-throughput claim, and the cap is never unlocked.
- **LAW 1:** every perf claim is interleaved ×5 **inside one lock hold**; cross-run and cross-day comparisons are clock-drift-invalid. **LAW 2:** thresholds/verdicts calibrated on older cores or kernels must be re-swept when the code under them moves. **LAW 3:** anything guarding a live lane belongs **inside** the battery.
- **Unknown means unknown.** Where the door text carries a number that reviewers refuted (the ~$418/day knee figure; the 6.0x × hit-envelope ring composition; "2.04 → ~0.30 ms" for `rows_v4`; "11.0% of wall" for IQ4 arm 2; the dense coldhol knee/TTFT numbers applied to Q35), the brief above states the corrected value or writes **"unquantified, measure first."** Do not restore the original figure.