# Grouped FP8 wo_a plain projection

Default-OFF process gate, no environment or serving default. The pinned
geometry is eight groups of 1024 output rows and 4096 BF16 input elements.
The shared kernel template changes only group addressing; each output keeps
the same FP8 weight decode, accumulation order and 128-leaf reduction.
BF16, prefill, and t>1 use the original loop.

Matched plain profile counted 344 wo_a launches per token among 666 dense
FP8 projections. The candidate replaces them with 43 grouped launches, not
an attention/TP rewrite. Savings must be measured because CPU launch time
overlaps GPU work.

Paired memcheck component gate passed: all 8192 output rows bit-exact against
the eight-call control, padding guards unchanged, two-group 128-row case
exact, malformed strides refused, actual grouped dispatch delta one.

Frozen component binary:
`24d37a7d9e30e9c010e44f2f382672a853f13e042c44f79aeb2b1eb920cbcc5d`.
Frozen plain performance binary:
`1f6856b7d92b1cecff2fdadfaf5a5dbdb3350bcd35bac24e8a64f896432cd0cf`.
Source baseline: `68c99f2cb` plus this candidate, before checkpoint commit.

`dsv4_plain_perf_gate wo-a` holds the already measured half2 GU/down stores
ON in both arms and changes only wo_a grouping. It checks 43 actual grouped
submissions per decode step in the ON arm, zero in OFF, sampled token tape,
final logits and committed KV, over three ABBA cycles at 256 and 8192 prompt
tokens. The paired component passed and the full model job started on
2026-09-07 at 02:04:54Z. Rate/qualification remain pending.
