# DSpark Phase 2 pilot progress

Lane: `lane/cx-dspark2`  
Started: 2026-08-11T07:57:52+03:00  
Contract: `research/dspark2-spec-20260811/SPEC.md` and `research/dspark-plan-20260811/PLAN.md`

## Status

ACTIVE — Phases 1 and 2 complete. The corrected stratified-v2 2K pilot and training-only 32,768-token rank artifact are remotely frozen; the low-priority 2K→30K continuation is running while Phase 3 implementation begins.

## Frozen execution order

1. L40S environment freeze and CUDA smoke.
2. Resumable 2K-pair corpus smoke on the local 5090, then 30K in chunks; transfer each chunk to the L40S.
3. Exact 2-layer d=4096 DSpark model, r=256 Markov head, confidence head, 32,768-row full-softmax loss with escape handling.
4. Detached pilot training on the dedicated L40S with held-out temp=0.7 and greedy metrics.
5. Frozen q-trajectory kill/continue verdict in `RESULTS.md`.

## Guardrails

- The L40S host is training-only; memra generation and evaluation remain on approved Blackwell rigs.
- Remote work stays under `/home/ubuntu/dspark2/`; unrelated data is never removed.
- Raw logs and environment freezes are committed as evidence.
- Active work is committed at least every 15 minutes; long-run metrics are committed at each evaluation.
- `cargo fmt` is forbidden for this lane.

## Log

- 2026-08-11T07:57:52+03:00 — Lane opened. Read the inbox brief, frozen specification, plan, and `/home/avifenesh/projects/bw24/CLAUDE.md`. Confirmed clean dedicated worktree on `lane/cx-dspark2`.
- 2026-08-11T08:00:54+03:00 — Live L40S preflight PASS: one idle NVIDIA L40S (sm_89, 46,068 MiB), Python 3.12.3, 61 GiB RAM, no training process, CUDA toolkit 13.2. Root volume has only 29 GiB free; the environment uses no pip cache and all lane state stays below `/home/ubuntu/dspark2/`. Added reproducible environment setup and CUDA/backend smoke scripts. Raw receipt: `raw/l40s-host-probe.log`.
- 2026-08-11T08:03:31+03:00 — Phase 1 PASS. Installed the frozen PyTorch 2.11.0+cu128, accelerate 1.14.0, bitsandbytes 0.50.0, and datasets 4.3.0 stack in `/home/ubuntu/dspark2/venv` (7.5 GiB). BF16 CUDA matmul and causal SDPA both produced finite output on sm_89. The requested `flash-attn==2.7.8` has no published distribution in the official index; the bounded attempt returned rc=1, so the recorded backend is SDPA. `pip check` passed. Remaining root capacity is 21 GiB, so corpus payloads must remain chunked and storage-sized before transfer. Receipts: `raw/l40s-env-{setup,smoke,freeze,artifacts}.log`/`.txt`.
- 2026-08-11T08:14:05+03:00 — Phase 2 harness checkpoint. The local 5090 is occupied by a serving-direction worker and `/tmp/memra-gpu.lock` is busy, so no GPU work was started. Added a deterministic prompt-only pack builder with the frozen 17.6/39.4/38.9/4.1 mix, 5% held-out split, alternating thinking/non-thinking cells, source revision/hash receipts, and a chunk-local resumable `dspark-generate` binary. The private SFT checkout has no verified corpus payload, so its contribution is explicitly zero; fixtures were not substituted. A 100-prompt CPU/network smoke filled every quota and `cargo check -p memra-engine --bin dspark-generate` passed. Receipts: `raw/{open-perfectblend-schema,prompt-pack-smoke}.log`.
- 2026-08-11T08:19:09+03:00 — Frozen all-30K prompt manifest built and staged byte-identically at `/data/projects/dspark/qwen35-9b-corpus-pilot/prompts/` and `/home/ubuntu/dspark2/corpus/prompts/`. Counts are chat 5,280 / math 11,820 / code 11,670 / IF 1,230; train/held-out are 28,500/1,500; Hugging Face source revision is `af60f3c18201652a83a93f46fcfee1b646ba3df7`. Added compact engine extraction for four deterministic response anchors per pair: exact pre-norm final trunk hidden, anchor plus five targets, raw top-64 logits, full-softmax top-64 probabilities at temp=0.7, and explicit tail mass. Pure CPU unit gates for stable top-k/tail accounting and bounded anchor selection pass. Receipts: `raw/prompt-pack-30k*.log`.
- 2026-08-11T08:22:12+03:00 — Release corpus tools built successfully for sm_120a. Binary hashes are `dspark-generate=11e8c8ddd389cc3ef0957dfe8be9051791b99b314d772a0386ff5b13163ffc04` and `dspark-extract=5ab227df534ef0680d48014e6e6d7f3e1771d94cfcccbb41756ec3aa3ac63d25`; the pinned GGUF independently matches the frozen SHA-256 `52c9cceb190055e0591a9a30c21f7200572eaf3ff1c59f6e9a1eda838a8f39de`. The lock remains occupied by a lane-owned `kernel-check`, so this lane has not touched the GPU. Receipt: `raw/build-corpus-tools.log`.
- 2026-08-11T08:25:47+03:00 — Added the one-chunk driver that validates frozen inputs, acquires `/tmp/memra-gpu.lock` non-blocking, runs both generation and extraction at low CPU/I/O priority while holding the lock, hashes the completed columnar artifact, releases the GPU, then rsyncs and verifies it on the L40S. `bash -n` and `shellcheck` pass. The lock is still busy; the two-pair gate remains queued without interference.
- 2026-08-11T08:29:24+03:00 — First real driver attempt correctly exited 75 without touching the GPU: a serving-direction `gemma-gate` owned the lock and 21,634 MiB. Added exact-token-tape vocabulary ranking for the full 32,768-row d2t map: response tokens only determine frequency, frozen/observed Qwen control ids are forced, ties are target-id-stable, and any sparse-pilot shortfall is backfilled only from the pinned target's existing own-generation rank artifact. Duplicate pair ids and malformed token tapes are hard errors; two focused tests pass. Receipt: `raw/corpus-gate-00000-00002.log`.
- 2026-08-11T08:31:24+03:00 — Added the cooperative range scheduler. It polls without taking the lock, tolerates a lost acquisition race, validates already-completed chunk manifests before skipping, stops cleanly at a durable sentinel, and only advances after the L40S copy verifies byte-for-byte. This makes the 2K smoke resumable at a bounded chunk boundary without weakening serving-lane priority. Both shell drivers pass `bash -n` and `shellcheck`.
- 2026-08-11T08:33:28+03:00 — Added a fail-closed columnar validator that runs after releasing the GPU and before transfer. It pins every file size and frozen shape, rejects non-finite BF16/logits, out-of-range or duplicate ids, subset-renormalized probabilities, temperature/logit mismatches, malformed indices, and probability-mass drift; it records sparse-target coverage diagnostics. Two synthetic validator tests and the two rank-builder tests pass. A plain `nohup` scheduler did not survive the tool's child-process cleanup, so no generation occurred; persistent launch will use the user service manager instead.
- 2026-08-11T08:37:56+03:00 — Two-pair end-to-end gate PASS after correcting a pre-GPU parity find from the current DeepSpec reference: anchor token `x[p]` pairs with predecessor carrier `h[p-1]`, not `h[p]`, matching memra's live NextN replay. The first successful extraction used the superseded binary and was deliberately regenerated; the frozen run uses `dspark-extract=e8abb3bc...`. Both target responses reached 512 tokens, extraction produced eight records, every BF16/logit/probability/shape gate passed, sampled targets were top-64 in 40/40 slots, and max mass error was `1.774e-08`. Local and L40S manifest hashes match (`eccf8fd6...`) and remote `sha256sum -c` passes. Receipts: `raw/{corpus-gate-00000-00002,test-anchor-pairing,rebuild-predecessor-pairing,gate-artifacts-sha256}.*`.
- 2026-08-11T08:44:13+03:00 — Launched the 2K smoke as low-priority user service invocation `3f70b5a257374f8cb6db21ecb4ec1d68`, in 64-pair windows. Added a range reducer that re-hashes every local artifact behind its remote-verification marker, requires contiguous pair ids, and reports token lengths, mix, records, sparse-target diagnostics, and a manifest fingerprint. Its live gate-range check passes with fingerprint `a1b7e0df...`; the first 64-pair smoke window is active and healthy.
- 2026-08-11T08:47:12+03:00 — First 2K-smoke window PASS and remotely frozen: 64 pairs / 256 anchor records, balanced 16-per-category and 32/32 think modes, 29,427 generated response tokens, no short-pair skips, and 40/40-equivalent sampled-slot coverage (`sampled_token_top64_rate=1.0`). The exact local and L40S `sha256.txt` hash is `bd2135c3...`; remote `sha256sum -c` passes. Maximum probability-mass error is `2.966e-08`, maximum logit/probability ratio error is `8.445e-07`, and the aggregate manifest fingerprint is `cfca755f...`. The cooperative worker released the lock before verification/transfer and then began ids 64–127.
- 2026-08-11T08:54:06+03:00 — Second window PASS and remotely frozen. Cumulative smoke state is 128 pairs / 512 anchor records / 59,392 response tokens with no short-pair skips, exact category and think-mode balance, and sampled-token top-64 rate 1.0. The two-window manifest fingerprint is `8420cf97...`; chunk `64..128` has exact local/L40S manifest hash `eeb9cc87...`. Maximum mass error remains `3.399e-08`; the observed tail-mass maximum is `0.04157`, retained explicitly rather than renormalized away. Ids 128–191 are active.
- 2026-08-11T09:01:08+03:00 — Third window PASS and remotely frozen. Cumulative state is 192 pairs / 768 records / 87,741 response tokens, no skipped short pairs, sampled-token top-64 rate 1.0, and fingerprint `b136e484...`. Chunk `128..192` has local/L40S manifest hash `0ba56059...`. The expanding sample exposed a legitimate `0.06595` maximum teacher tail mass; it remains explicitly accounted for and total probability-mass error is still only `3.399e-08`. Ids 192–255 are active.
- 2026-08-11T09:09:08+03:00 — Verified the response-only d2t ranking path on the first 256 remotely frozen pairs while the corpus yielded to another lane's `kernel-check`. This provisional rank sees 12,197 distinct response ids in 117,580 tokens, uses 20,568 ids from the pinned same-target backfill, and covers both observed splits exactly; rank SHA-256 is `73fc0b41...`. It is a harness receipt only and will be regenerated from all 2K smoke pairs before model implementation. The fourth window was independently auto-committed as `08a53c90` after its remote gate.
- 2026-08-11T09:39:35+03:00 — Closed held-out leakage in the d2t freeze before final ranking: only `train` response tokens may order the trim, while held-out responses are coverage measurement only; prompts and held-out high ids cannot influence selection. Two focused tests pass. On 448 verified pairs the provisional train-only rank covers train tokens 100% but held-out tokens 95.60%, exposing the honest trim generalization gap that the earlier all-split provisional rank hid. This provisional train-only rank is `32c9f67b...`; the final artifact still waits for all 2K pairs.
- 2026-08-11T09:47:10+03:00 — Superseded the first 448-pair `smoke` attempt before training after a prompt-assignment audit found a deterministic confound: category interleave period 4 combined with `id % 20` held-out and `id % 2` mode assignment left the 2K prefix with only five populated category/mode/split cells; all 100 held-out prompts were `chat/think`, and category predicted mode. Both services were stopped at the remotely verified pair-448 boundary; all old bytes and receipts remain preserved but are forbidden as training/eval input. Rebuilt the same 30,000 unique prompt texts from pinned Open-PerfectBlend revision `af60f3c...` with hash-stratified assignment v2: prompt text SHA remains `20a061dd...`, metadata SHA is now `c2f9504c...`, full counts are exactly 15,000/15,000 modes and 28,500/1,500 splits with all 16 cells populated. The 2K prefix has exactly 500/category, 988 think / 1,012 no-think, 1,894 train / 106 held-out, and all 16 cells. Local/L40S hashes match. The chunk driver now fails closed on the v2 prompt and frozen generator/extractor hashes; the corrected run will use label `pilot`.
- 2026-08-11T09:47:56+03:00 — Launched corrected `pilot 0..2000` range service invocation `149c01cb...` and checkpoint service invocation `6585ce73...`. Both are low priority and currently yielding at pair 0 to the existing serving-direction lock owner.
- 2026-08-11T10:23:40+03:00 — Corrected pilot has 192 remotely frozen pairs / 756 anchor records; three sub-γ early-EOS responses are explicit zero-anchor skips and no partial record is fabricated. A real-data train-only d2t smoke sees 9,767 distinct ids in 80,685 training response tokens, uses 22,998 pinned same-target backfill ids, covers train tokens 100%, and measures held-out coverage independently at 97.23%. Provisional rank SHA-256 is `8f8d5ae7...`; it remains non-final pending all 2K pairs.
- 2026-08-11T11:04:56+03:00 — Corrected pilot is remotely frozen through pair 384: 1,520/1,536 requested anchor records, four explicit early-EOS zero-anchor skips, all observed assignment cells intact, sampled-token top-64 rate 99.974%, and maximum probability-mass error `3.528e-08`. A serving lane's bounded local-CI battery owns the 5090 lock, so the persistent low-priority worker is correctly waiting at pair 384. Re-ran the complete Phase 2 test suite (7/7 PASS), added regression coverage for cell-scoped anchor deficiencies, and pinned fail-closed scripts for the final 2K d2t freeze and the gated 2K→30K background handoff. At the observed 3.35 MB per 64-pair chunk, the full 30K corpus projects to 1.46 GiB; the L40S has 21 GiB free, so no unrelated remote data cleanup is needed.
- 2026-08-11T11:19:21+03:00 — Lock-yield heartbeat: two pre-existing serving-direction `local-ci.sh --perf-quick` requests have continuously owned `/tmp/memra-gpu.lock` since the pair-384 boundary. Their live process sequence advanced through kernel, spec, server, and perf gates, so this is active bounded contention rather than a stale lock. The DSpark service remains healthy and has written no partial `pilot-00384-00448` chunk.
- 2026-08-11T12:02:38+03:00 — Corrected corpus is remotely frozen and committed through pair 640. Windows `448..512`, `512..576`, and `576..640` each retained all 256 requested anchor records; the cumulative deficiency remains the six previously observed `chat/nothink/train` early-EOS pairs. A new serving `local-ci.sh --perf-quick` battery owned the shared lock from the 640 boundary until 12:02; the DSpark worker then acquired it and began `640..704` without a partial resume.
- 2026-08-11T15:34:11+03:00 — Phase 2 PASS. The corrected pilot is remotely frozen through pair 2,000 with 7,940/8,000 requested anchor records, 15 explicit zero-anchor early-EOS pairs, exact 500-per-category balance, 988 think / 1,012 no-think, 1,894 train / 106 heldout, and all 16 category/mode/split cells populated. The cumulative manifest fingerprint is `48c8b59f...`; sampled-target top-64 rate is 99.9849%, maximum probability-mass error is `4.291e-08`, and the local summary SHA-256 is `76ecc1d5...`. The final d2t ranking uses only 861,244 training-response tokens from exact pair ids 0..1,999, sees 33,392 distinct ids, and has rank SHA-256 `0fdeea0b...`; train and independently measured heldout coverage are 99.9272% and 97.4296%. The artifact and its `160ab221...` manifest verify byte-for-byte under `/home/ubuntu/dspark2/artifacts/pilot-02000/`. Receipt: `raw/ranks-pilot-02000.log`.
- 2026-08-11T15:34:59+03:00 — Gated 30K continuation launched only after re-verifying both local and L40S rank manifests. Low-priority range service invocation `ad5459af...` owns ids 2,000..29,999 in 64-pair windows; checkpoint service invocation `821508aa...` commits each remotely verified window. Receipt: `raw/launch-corpus-scale.log`.
- 2026-08-11T15:43:18+03:00 — Phase 3 shared-weight seam landed. The current official Qwen3.5 tokenizer declares no mask token, so the drafter will use a dedicated trainable mask latent rather than fabricate a target-token id. Added a fail-closed row exporter that dequantizes only the 32,768 frozen `d2t` rows from the exact deployed GGUF `token_embd.weight` and `output.weight`, writes deterministic little-endian BF16 plus the target-id map, rejects duplicates/range errors/overwrites, and records the source qtypes and shapes. Focused rank and BF16 round-to-nearest-even tests pass; the initial conversion bug and its failing raw receipt are preserved alongside the passing rerun.
- 2026-08-11T15:55:48+03:00 — Phase 3 model/data/loss checkpoint. Exported and remotely re-hashed the frozen shared artifact: 32,768 x 4,096 BF16 embedding and LM head (256 MiB each), source GGUF `52c9cceb...`, rank `0fdeea0b...`; local/L40S `sha256sum -c` both pass. The exact pilot has 318,801,153 trainable parameters: two d=4,096 GQA layers (16Q/4KV, head 256), FFN 8,192, r=256 vanilla Markov correction, confidence `[h;W1(prev)]`, trainable mask latent, and frozen non-checkpointed shared tables. Current official Qwen3.5 config was re-read online and the implementation honors its 0.25 partial RoPE, theta 1e7, RMS epsilon 1e-6, and bias-free projections. Full-32,768 softmax loss retains target top-64 mass without renormalization, aggregates every trim miss plus tail into a non-winnable escape bucket, and uses mathematical acceptance `1-TVD = 1-0.5*L1` (the frozen SPEC contains a duplicated factor in one prose line). Four focused model/loss tests pass on L40S. The data reader verifies hashes and exact pair-window coverage, then loads 7,516 train / 424 heldout anchor records with the frozen aggregate fingerprint `48c8b59f...`; its two pre-gate failures and final pass are preserved.
- 2026-08-11T16:00:02+03:00 — Added the detached single-L40S training loop and held-out evaluator. Every trajectory point reports analytical and fixed-seed sampled temp=0.7 q, separate greedy q, the three loss components, and confidence calibration. Sequential Temperature Scaling follows the paper's left-to-right per-position grid fit against cumulative prefix survival; a sharpened-logit synthetic gate reduces mean prefix ECE from 6.91% to 0.83%. Training is frozen at five epochs, seed 20260811, BF16 autocast, AdamW 3e-4 with 4% warmup/cosine decay, exp(-position/5), max grad norm 1, 30-minute atomic resume checkpoints, and exact source/artifact/corpus receipts. Five focused tests pass on the L40S; full-size one-step forward/backward preflight remains next.
- 2026-08-11T16:03:31+03:00 — Phase 3 PASS. The full 318,801,153-parameter model completed one real 32-anchor forward/backward, full-32,768 softmax loss, gradient clip, and fused AdamW update on the idle L40S in 0.456 s. Peak allocated memory was 5,680,248,320 bytes, all gradients were finite, and the exact corpus fingerprint was `48c8b59f...`. Fresh-init q2 is an expected near-zero `5.57e-08`; loss is finite at 3.2835 with teacher escape only 0.123%. Batch 32 therefore stays frozen for detached training, leaving substantial L40S headroom. Receipt: `raw/preflight-dspark-l40s.log`.
- 2026-08-11T16:06:04+03:00 — Phase 4 complete. Persistent L40S unit invocation `a147b9c9...` exited success after 1,175 steps / five epochs; the service is inactive, the GPU is idle, and the atomic final checkpoint is retained remotely at 3,825,653,493 bytes with SHA-256 `2b110c46...`. Final held-out temp=0.7 analytical slot conditionals are 32.09% / 32.50% / 41.50% / 46.55% / 29.05%, giving q2 10.43%; fixed-seed sparse rejection sampling gives q2 9.91%. Greedy slot conditionals are 34.91% / 37.84% with q2 13.21%. Confidence ranking is strong (cumulative AUROC mean 0.9390), but STS-calibrated cumulative ECE mean is 7.14%, far outside the gate. The full stdout, 118 logged train rows, 12-point held-out trajectory, final eval, config, code hashes, and artifact hashes are committed under `raw/training-pilot-2k/`; the 3.8 GiB checkpoint stays on the L40S and is represented by its hash.
- 2026-08-11T16:07:18+03:00 — Phase 5 KILL. The final 30% q2 trajectory is 0.0975 -> 0.1012 -> 0.1039 -> 0.1043, while slot-1/2 and calibrated ECE independently cross the frozen kill floors. Placed the cooperative corpus stop sentinel; active chunk `pilot-02128-02192` finished, transferred, verified, and auto-committed as `e9672ca6`. The range service exited success before pair 2,192 and the checkpoint watcher was then stopped cleanly. Both local services are inactive, the local GPU has no compute process, and the orchestrator's thermal cap remains unchanged. `RESULTS.md` records the early-stop verdict, full trajectory, receipts, and explicit 2K-not-30K limit.
