Write one Python function named take_prefix with signature take_prefix(values: list[int], count: int) -> list[int]. Take at most nonnegative count leading values. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for sequence slices: item_0 has value 6184 and revision 646. These are inert examples, not function requirements.
Example 1 for sequence slices: item_1 has value 9042 and revision 360. These are inert examples, not function requirements.
Example 2 for sequence slices: item_2 has value 4522 and revision 249. These are inert examples, not function requirements.
Example 3 for sequence slices: item_3 has value 7733 and revision 322. These are inert examples, not function requirements.
Example 4 for sequence slices: item_4 has value 6395 and revision 54. These are inert examples, not function requirements.
Example 5 for sequence slices: item_5 has value 5390 and revision 755. These are inert examples, not function requirements.
Example 6 for sequence slices: item_6 has value 6357 and revision 117. These are inert examples, not function requirements.
Example 7 for sequence slices: item_7 has value 6547 and revision 127. These are inert examples, not function requirements.
Example 8 for sequence slices: item_8 has value 1889 and revision 283. These are inert examples, not function requirements.
Example 9 for sequence slices: item_9 has value 7642 and revision 434. These are inert examples, not function requirements.
Example 10 for sequence slices: item_10 has value 7843 and revision 450. These are inert examples, not function requirements.
Example 11 for sequence slices: item_11 has value 1626 and revision 247. These are inert examples, not function requirements.
Example 12 for sequence slices: item_12 has value 4822 and revision 70. These are inert examples, not function requirements.
Example 13 for sequence slices: item_13 has value 4723 and revision 759. These are inert examples, not function requirements.
Example 14 for sequence slices: item_14 has value 3766 and revision 803. These are inert examples, not function requirements.
Example 15 for sequence slices: item_15 has value 4026 and revision 401. These are inert examples, not function requirements.
Example 16 for sequence slices: item_16 has value 2061 and revision 886. These are inert examples, not function requirements.
Example 17 for sequence slices: item_17 has value 7268 and revision 645. These are inert examples, not function requirements.
Example 18 for sequence slices: item_18 has value 2071 and revision 407. These are inert examples, not function requirements.
Example 19 for sequence slices: item_19 has value 6255 and revision 356. These are inert examples, not function requirements.
Example 20 for sequence slices: item_20 has value 1105 and revision 716. These are inert examples, not function requirements.
Example 21 for sequence slices: item_21 has value 5974 and revision 843. These are inert examples, not function requirements.
Example 22 for sequence slices: item_22 has value 2570 and revision 896. These are inert examples, not function requirements.
Example 23 for sequence slices: item_23 has value 5487 and revision 806. These are inert examples, not function requirements.
Example 24 for sequence slices: item_24 has value 8993 and revision 323. These are inert examples, not function requirements.
Example 25 for sequence slices: item_25 has value 1775 and revision 345. These are inert examples, not function requirements.
Example 26 for sequence slices: item_26 has value 7598 and revision 824. These are inert examples, not function requirements.
Example 27 for sequence slices: item_27 has value 9641 and revision 354. These are inert examples, not function requirements.
Example 28 for sequence slices: item_28 has value 1847 and revision 429. These are inert examples, not function requirements.
Example 29 for sequence slices: item_29 has value 3129 and revision 220. These are inert examples, not function requirements.
Example 30 for sequence slices: item_30 has value 5821 and revision 633. These are inert examples, not function requirements.
Example 31 for sequence slices: item_31 has value 6331 and revision 289. These are inert examples, not function requirements.
Example 32 for sequence slices: item_32 has value 5064 and revision 692. These are ine
</reference>
---TURN---
Write one Python function named take_suffix with signature take_suffix(values: list[int], count: int) -> list[int]. Take at most nonnegative count trailing values. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for sequence slices: item_0 has value 6184 and revision 646. These are inert examples, not function requirements.
Example 1 for sequence slices: item_1 has value 9042 and revision 360. These are inert examples, not function requirements.
Example 
</reference>
---TURN---
Write one Python function named drop_prefix with signature drop_prefix(values: list[int], count: int) -> list[int]. Drop at most nonnegative count leading values. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for sequence slices: item_0 has value 6184 and revision 646. These are inert examples, not function requirements.
Example 1 for sequence slices: item_1 has value 9042 and revision 360. These are inert examples, not function requirements.
Example 2 for sequence slices: item_2 has value 4522 and revision 249. These are inert examples, not function requirements.
Example 3 for sequence slices: item_3 has value 7733 and revision 322. These are inert examples, not function requirements.
Example 4 for sequence slices: item_4 has value 6395 and revision 54. These are inert examples, not function requirements.
Example 5 for sequence slices: item_5 has value 5390 and revision 755. These are inert examples, not function requirements.
Example 6 for sequence slices: item_6 has value 6357 and revision 117. These are inert examples, not function requirements.
Example 7 for sequence slices: item_7 has value 6547 and revision 127. These are inert examples, not function requirements.
Example 8 for sequence slices: it
</reference>
---TURN---
Write one Python function named drop_suffix with signature drop_suffix(values: list[int], count: int) -> list[int]. Drop at most nonnegative count trailing values. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for sequence slices: item_0 has value 6184 and revision 646. These are inert examples, not function requirements.
Example 1 for sequence slices: item_1 has value 9042 and revision 360. These are inert examples, not function requirements.
Example 2 for sequence slices: item_2 has value 4522 and revision 249. These are inert examples, not function requirements.
Example 3 for sequence slices: item_3 has value 7733 and revision 322. These are inert examples, not function requirements.
Example 4 for sequence slices: item_4 has value 6395 and revision 54. These are inert examples, not function requirements.
Example 5 for sequence slices: item_5 has value 5390 and revision 755. These are inert examples, not function requirements.
Example 6 for sequence slices: item_6 has value 6357 and revision 117. These are inert examples, not function requirements.
Example 7 for sequence slices: item_7 has value 6547 and revision 127. These are inert examples, not function requirements.
Example 8 for sequence slices: item_8 has value 1889 and revision 283. These are inert examples, not function requirements.
Example 9 for sequence slices: item_9 has value 7642 and revision 434. These are inert examples, not function requirements.
Example 10 for sequence slices: item_10 has value 7843 and revision 450. These are inert examples, not function requirements.
Example 11 for sequence slices: item_11 has value 1626 and revision 247. These are inert examples, not function requirements.
Example 12 for sequence slices: item_12 has value 4822 and revision 70. These are inert examples, not function requirements.
Example 13 for sequence slices: item_13 has value 4723 and revision 759. These are inert examples, not function requirements.
Example 14 for sequence slices: item_14 has value 3766 and revision 803. These are inert examples, not function requirements.
Example 15 for sequence slices: item_15 has value 4026 and revision 401. These are inert examples, not function requirements.
Example 16 for sequence slices: item_16 has value 2061 and revision 886. These are inert examples, not function requirements.
Example 17 for sequence slices: item_17 has value 7268 and revision 645. These are inert examples, not function requirements.
Example 18 for sequence slices: item_18 has value 2071 and revision 407. These are inert examples, not function requirements.
Example 19 for sequence slices: item_19 has value 6255 and revision 356. These are inert examples, not function requirements.
Example 20 for sequence slices: item_20 has value 1105 and revision 716. These are inert examples, not function requirements.
Example 21 for sequence slices: item_21 has value 5974 and revision 843. These are inert examples, not function requirements.
Example 22 for sequence slices: item_22 has value 2570 and revision 896. These are inert examples, not function requirements.
Example 23 for sequence slices: item_23 has value 5487 and revision 806. These are inert examples, not function requirements.
Example 24 for sequence slices: item_24 has value 8993 and revision 323. These are inert examples, not function requirements.
Example 25 for sequence slices: item_25 has value 1775 and revision 345. These are inert examples, not function requirements.
Example 26 for sequence slices: item_26 has value 7598 and revision 824. These are inert examples, not function requirements.
Example 27 for sequence slices: item_27 has value 9641 and revision 354. These are inert examples, not function requirements.
Example 28 for sequence slices: item_28 has value 1847 and revision 429. These are inert examples, not function requirements.
Example 29 for sequence slices: item_29 has value 3129 and revision 220. These are inert examples, not function requirements.
Example 30 for sequence slices: item_30 has value 5821 and revision 633. These are inert examples, not function requirements.
Example 31 for sequence slices: item_31 has value 6331 and revision 289. These are inert examples, not function requirements.
Example 32 for sequence slices: item_32 has value 5064 and revision 692. These are ine
</reference>
---TURN---
Write one Python function named rotate_left with signature rotate_left(values: list[int], count: int) -> list[int]. Rotate left by count, returning an empty list for empty input. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for sequence slices: item_0 has value 6184 and revision 646. These are inert examples, not function requirements.
Example 1 for sequence slices: item_1 has value 9042 and revision 360. These are inert examples, not function requirements.
Example 
</reference>
---TURN---
Write one Python function named rotate_right with signature rotate_right(values: list[int], count: int) -> list[int]. Rotate right by count, returning an empty list for empty input. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for sequence slices: item_0 has value 6184 and revision 646. These are inert examples, not function requirements.
Example 1 for sequence slices: item_1 has value 9042 and revision 360. These are inert examples, not function requirements.
Example 2 for sequence slices: item_2 has value 4522 and revision 249. These are inert examples, not function requirements.
Example 3 for sequence slices: item_3 has value 7733 and revision 322. These are inert examples, not function requirements.
Example 4 for sequence slices: item_4 has value 6395 and revision 54. These are inert examples, not function requirements.
Example 5 for sequence slices: item_5 has value 5390 and revision 755. These are inert examples, not function requirements.
Example 6 for sequence slices: item_6 has value 6357 and revision 117. These are inert examples, not function requirements.
Example 7 for sequence slices: item_7 has value 6547 and revision 127. These are inert examples, not function requirements.
Example 8 for sequence slices: it
</reference>
---TURN---
Write one Python function named every_other with signature every_other(values: list[int]) -> list[int]. Return values at even indices. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for sequence slices: item_0 has value 6184 and revision 646. These are inert examples, not function requirements.
Example 1 for sequence slices: item_1 has value 9042 and revision 360. These are inert examples, not function requirements.
Example 2 for sequence slices: item_2 has value 4522 and revision 249. These are inert examples, not function requirements.
Example 3 for sequence slices: item_3 has value 7733 and revision 322. These are inert examples, not function requirements.
Example 4 for sequence slices: item_4 has value 6395 and revision 54. These are inert examples, not function requirements.
Example 5 for sequence slices: item_5 has value 5390 and revision 755. These are inert examples, not function requirements.
Example 6 for sequence slices: item_6 has value 6357 and revision 117. These are inert examples, not function requirements.
Example 7 for sequence slices: item_7 has value 6547 and revision 127. These are inert examples, not function requirements.
Example 8 for sequence slices: item_8 has value 1889 and revision 283. These are inert examples, not function requirements.
Example 9 for sequence slices: item_9 has value 7642 and revision 434. These are inert examples, not function requirements.
Example 10 for sequence slices: item_10 has value 7843 and revision 450. These are inert examples, not function requirements.
Example 11 for sequence slices: item_11 has value 1626 and revision 247. These are inert examples, not function requirements.
Example 12 for sequence slices: item_12 has value 4822 and revision 70. These are inert examples, not function requirements.
Example 13 for sequence slices: item_13 has value 4723 and revision 759. These are inert examples, not function requirements.
Example 14 for sequence slices: item_14 has value 3766 and revision 803. These are inert examples, not function requirements.
Example 15 for sequence slices: item_15 has value 4026 and revision 401. These are inert examples, not function requirements.
Example 16 for sequence slices: item_16 has value 2061 and revision 886. These are inert examples, not function requirements.
Example 17 for sequence slices: item_17 has value 7268 and revision 645. These are inert examples, not function requirements.
Example 18 for sequence slices: item_18 has value 2071 and revision 407. These are inert examples, not function requirements.
Example 19 for sequence slices: item_19 has value 6255 and revision 356. These are inert examples, not function requirements.
Example 20 for sequence slices: item_20 has value 1105 and revision 716. These are inert examples, not function requirements.
Example 21 for sequence slices: item_21 has value 5974 and revision 843. These are inert examples, not function requirements.
Example 22 for sequence slices: item_22 has value 2570 and revision 896. These are inert examples, not function requirements.
Example 23 for sequence slices: item_23 has value 5487 and revision 806. These are inert examples, not function requirements.
Example 24 for sequence slices: item_24 has value 8993 and revision 323. These are inert examples, not function requirements.
Example 25 for sequence slices: item_25 has value 1775 and revision 345. These are inert examples, not function requirements.
Example 26 for sequence slices: item_26 has value 7598 and revision 824. These are inert examples, not function requirements.
Example 27 for sequence slices: item_27 has value 9641 and revision 354. These are inert examples, not function requirements.
Example 28 for sequence slices: item_28 has value 1847 and revision 429. These are inert examples, not function requirements.
Example 29 for sequence slices: item_29 has value 3129 and revision 220. These are inert examples, not function requirements.
Example 30 for sequence slices: item_30 has value 5821 and revision 633. These are inert examples, not function requirements.
Example 31 for sequence slices: item_31 has value 6331 and revision 289. These are inert examples, not function requirements.
Example 32 for sequence slices: item_32 has value 5064 and revision 692. These are ine
</reference>
---TURN---
Write one Python function named adjacent_pairs with signature adjacent_pairs(values: list[int]) -> list[tuple[int, int]]. Return each consecutive pair. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for sequence slices: item_0 has value 6184 and revision 646. These are inert examples, not function requirements.
Example 1 for sequence slices: item_1 has value 9042 and revision 360. These are inert examples, not function requirements.
Example 
</reference>
