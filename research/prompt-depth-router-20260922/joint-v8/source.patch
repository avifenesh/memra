diff --git a/crates/memra-engine/src/spec/prime.rs b/crates/memra-engine/src/spec/prime.rs
--- a/crates/memra-engine/src/spec/prime.rs
+++ b/crates/memra-engine/src/spec/prime.rs
@@ -531,12 +531,13 @@
                 .as_deref()
                 .is_some_and(|c| c.draft_mask_enabled()),
             self.sess.collect_confidence
-                || self
-                    .sess
-                    .confidence_cutoff
-                    .as_ref()
-                    .map(|cutoffs| cutoffs.iter().any(|c| *c > 0.0))
-                    .unwrap_or(p_min > 0.0),
+                || self.sess.depth_experiment.as_ref().is_some_and(|policy| {
+                    matches!(policy, MtpDepthExperiment::Learned(learner) if learner.has_confidence_policy())
+                })
+                || self.sess.confidence_cutoff.as_ref().is_some_and(|cutoffs| {
+                    cutoffs.iter().any(|c| *c > 0.0)
+                })
+                || p_min > 0.0,
             embd_gpu,
             qt,
             rb,
diff --git a/crates/memra-engine/src/learned_depth.rs b/crates/memra-engine/src/learned_depth.rs
--- a/crates/memra-engine/src/learned_depth.rs
+++ b/crates/memra-engine/src/learned_depth.rs
@@ -428,8 +428,8 @@
         let Some(confidence) = &mut self.confidence else {
             return Ok(false);
         };
-        if position >= 3 || !q.is_finite() || !(0.0..=1.0).contains(&q) {
-            return Err("invalid offered confidence position or probability");
+        if position >= 3 || !q.is_finite() || !(0.0..=1.0).contains(&q) || q == 0.0 {
+            return Err("missing or invalid offered confidence probability");
         }
         let started = Instant::now();
         let chance = confidence.model.accepted_chance(
