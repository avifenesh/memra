Write one Python function named totals_running with signature totals_running(values: list[int]) -> list[int]. Return the inclusive running sums. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for prefix totals: item_0 has value 7004 and revision 135. These are inert examples, not function requirements.
Example 1 for prefix totals: item_1 has value 2449 and revision 565. These are inert examples, not function requirements.
Example 2 for prefix totals: item_2 has value 1366 and revision 145. These are inert examples, not function requirements.
Example 3 for prefix totals: item_3 has value 2265 and revision 876. These are inert examples, not function requirements.
Example 4 for prefix totals: item_4 has value 9726 and revision 343. These are inert examples, not function requirements.
Example 5 for prefix totals: item_5 has value 240 and revision 534. These are inert examples, not function requirements.
Example 6 for prefix totals: item_6 has value 2676 and revision 594. These are inert examples, not function requirements.
Example 7 for prefix totals: item_7 has value 6009 and revision 262. These are inert examples, not function requirements.
Example 8 for prefix totals: item_8 has value 1155 and revision 224. These are inert examples, not function requirements.
Example 9 for prefix totals: item_9 has value 3525 and revision 182. These are inert examples, not function requirements.
Example 10 for prefix totals: item_10 has value 8844 and revision 667. These are inert examples, not function requirements.
Example 11 for prefix totals: item_11 has value 511 and revision 819. These are inert examples, not function requirements.
Example 12 for prefix totals: item_12 has value 7234 and revision 157. These are inert examples, not function requirements.
Example 13 for prefix totals: item_13 has value 5727 and revision 403. These are inert examples, not function requirements.
Example 14 for prefix totals: item_14 has value 5020 and revision 20. These are inert examples, not function requirements.
Example 15 for prefix totals: item_15 has value 4479 and revision 2. These are inert examples, not function requirements.
Example 16 for prefix totals: item_16 has value 4308 and revision 102. These are inert examples, not function requirements.
Example 17 for prefix totals: item_17 has value 4423 and revision 62. These are inert examples, not function requirements.
Example 18 for prefix totals: item_18 has value 6283 and revision 641. These are inert examples, not function requirements.
Example 19 for prefix totals: item_19 has value 7406 and revision 664. These are inert examples, not function requirements.
Example 20 for prefix totals: item_20 has value 8951 and revision 570. These are inert examples, not function requirements.
Example 21 for prefix totals: item_21 has value 7899 and revision 594. These are inert examples, not function requirements.
Example 22 for prefix totals: item_22 has value 2773 and revision 376. These are inert examples, not function requirements.
Example 23 for prefix totals: item_23 has value 2416 and revision 425. These are inert examples, not function requirements.
Example 24 for prefix totals: item_24 has value 9811 and revision 773. These are inert examples, not function requirements.
Example 25 for prefix totals: item_25 has value 4524 and revision 488. These are inert examples, not function requirements.
Example 26 for prefix totals: item_26 has value 5083 and revision 490. These are inert examples, not function requirements.
Example 27 for prefix totals: item_27 has value 8129 and revision 856. These are inert examples, not function requirements.
Example 28 for prefix totals: item_28 has value 2751 and revision 686. These are inert examples, not function requirements.
Example 29 for prefix totals: item_29 has value 6711 and revision 593. These are inert examples, not function requirements.
Example 30 for prefix totals: item_30 has value 3082 and revision 859. These are inert examples, not function requirements.
Example 31 for prefix totals: item_31 has value 3279 and revision 557. These are inert examples, not function requirements.
Example 32 for prefix totals: item_32 has value 815 and revision 222. These are inert examples, not function requirements.
Example 33 for prefix totals: i
</reference>
---TURN---
Write one Python function named totals_between with signature totals_between(values: list[int], start: int, end: int) -> int. Sum the valid half-open slice from start to end. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for prefix totals: item_0 has value 7004 and revision 135. These are inert examples, not function requirements.
Example 1 for prefix totals: item_1 has value 2449 and revision 565. These are inert examples, not function requirements.
Example 2 fo
</reference>
---TURN---
Write one Python function named totals_before with signature totals_before(values: list[int], index: int) -> int. Sum values strictly before a valid insertion index. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for prefix totals: item_0 has value 7004 and revision 135. These are inert examples, not function requirements.
Example 1 for prefix totals: item_1 has value 2449 and revision 565. These are inert examples, not function requirements.
Example 2 for prefix totals: item_2 has value 1366 and revision 145. These are inert examples, not function requirements.
Example 3 for prefix totals: item_3 has value 2265 and revision 876. These are inert examples, not function requirements.
Example 4 for prefix totals: item_4 has value 9726 and revision 343. These are inert examples, not function requirements.
Example 5 for prefix totals: item_5 has value 240 and revision 534. These are inert examples, not function requirements.
Example 6 for prefix totals: item_6 has value 2676 and revision 594. These are inert examples, not function requirements.
Example 7 for prefix totals: item_7 has value 6009 and revision 262. These are inert examples, not function requirements.
Example 8 for prefix totals: item_8 has value 115
</reference>
---TURN---
Write one Python function named totals_first_at_least with signature totals_first_at_least(values: list[int], threshold: int) -> int | None. Return the first running-sum index meeting threshold, else None. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for prefix totals: item_0 has value 7004 and revision 135. These are inert examples, not function requirements.
Example 1 for prefix totals: item_1 has value 2449 and revision 565. These are inert examples, not function requirements.
Example 2 for prefix totals: item_2 has value 1366 and revision 145. These are inert examples, not function requirements.
Example 3 for prefix totals: item_3 has value 2265 and revision 876. These are inert examples, not function requirements.
Example 4 for prefix totals: item_4 has value 9726 and revision 343. These are inert examples, not function requirements.
Example 5 for prefix totals: item_5 has value 240 and revision 534. These are inert examples, not function requirements.
Example 6 for prefix totals: item_6 has value 2676 and revision 594. These are inert examples, not function requirements.
Example 7 for prefix totals: item_7 has value 6009 and revision 262. These are inert examples, not function requirements.
Example 8 for prefix totals: item_8 has value 1155 and revision 224. These are inert examples, not function requirements.
Example 9 for prefix totals: item_9 has value 3525 and revision 182. These are inert examples, not function requirements.
Example 10 for prefix totals: item_10 has value 8844 and revision 667. These are inert examples, not function requirements.
Example 11 for prefix totals: item_11 has value 511 and revision 819. These are inert examples, not function requirements.
Example 12 for prefix totals: item_12 has value 7234 and revision 157. These are inert examples, not function requirements.
Example 13 for prefix totals: item_13 has value 5727 and revision 403. These are inert examples, not function requirements.
Example 14 for prefix totals: item_14 has value 5020 and revision 20. These are inert examples, not function requirements.
Example 15 for prefix totals: item_15 has value 4479 and revision 2. These are inert examples, not function requirements.
Example 16 for prefix totals: item_16 has value 4308 and revision 102. These are inert examples, not function requirements.
Example 17 for prefix totals: item_17 has value 4423 and revision 62. These are inert examples, not function requirements.
Example 18 for prefix totals: item_18 has value 6283 and revision 641. These are inert examples, not function requirements.
Example 19 for prefix totals: item_19 has value 7406 and revision 664. These are inert examples, not function requirements.
Example 20 for prefix totals: item_20 has value 8951 and revision 570. These are inert examples, not function requirements.
Example 21 for prefix totals: item_21 has value 7899 and revision 594. These are inert examples, not function requirements.
Example 22 for prefix totals: item_22 has value 2773 and revision 376. These are inert examples, not function requirements.
Example 23 for prefix totals: item_23 has value 2416 and revision 425. These are inert examples, not function requirements.
Example 24 for prefix totals: item_24 has value 9811 and revision 773. These are inert examples, not function requirements.
Example 25 for prefix totals: item_25 has value 4524 and revision 488. These are inert examples, not function requirements.
Example 26 for prefix totals: item_26 has value 5083 and revision 490. These are inert examples, not function requirements.
Example 27 for prefix totals: item_27 has value 8129 and revision 856. These are inert examples, not function requirements.
Example 28 for prefix totals: item_28 has value 2751 and revision 686. These are inert examples, not function requirements.
Example 29 for prefix totals: item_29 has value 6711 and revision 593. These are inert examples, not function requirements.
Example 30 for prefix totals: item_30 has value 3082 and revision 859. These are inert examples, not function requirements.
Example 31 for prefix totals: item_31 has value 3279 and revision 557. These are inert examples, not function requirements.
Example 32 for prefix totals: item_32 has value 815 and revision 222. These are inert examples, not function requirements.
Example 33 for prefix totals: i
</reference>
---TURN---
Write one Python function named totals_differences with signature totals_differences(values: list[int]) -> list[int]. Return each later value minus its predecessor. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for prefix totals: item_0 has value 7004 and revision 135. These are inert examples, not function requirements.
Example 1 for prefix totals: item_1 has value 2449 and revision 565. These are inert examples, not function requirements.
Example 2 fo
</reference>
---TURN---
Write one Python function named totals_running_max with signature totals_running_max(values: list[int]) -> list[int]. Return the inclusive running maximum at each position. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for prefix totals: item_0 has value 7004 and revision 135. These are inert examples, not function requirements.
Example 1 for prefix totals: item_1 has value 2449 and revision 565. These are inert examples, not function requirements.
Example 2 for prefix totals: item_2 has value 1366 and revision 145. These are inert examples, not function requirements.
Example 3 for prefix totals: item_3 has value 2265 and revision 876. These are inert examples, not function requirements.
Example 4 for prefix totals: item_4 has value 9726 and revision 343. These are inert examples, not function requirements.
Example 5 for prefix totals: item_5 has value 240 and revision 534. These are inert examples, not function requirements.
Example 6 for prefix totals: item_6 has value 2676 and revision 594. These are inert examples, not function requirements.
Example 7 for prefix totals: item_7 has value 6009 and revision 262. These are inert examples, not function requirements.
Example 8 for prefix totals: item_8 has value 115
</reference>
---TURN---
Write one Python function named totals_balance_indices with signature totals_balance_indices(values: list[int]) -> list[int]. Return indices where the sum before equals the sum after, excluding the value at that index. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for prefix totals: item_0 has value 7004 and revision 135. These are inert examples, not function requirements.
Example 1 for prefix totals: item_1 has value 2449 and revision 565. These are inert examples, not function requirements.
Example 2 for prefix totals: item_2 has value 1366 and revision 145. These are inert examples, not function requirements.
Example 3 for prefix totals: item_3 has value 2265 and revision 876. These are inert examples, not function requirements.
Example 4 for prefix totals: item_4 has value 9726 and revision 343. These are inert examples, not function requirements.
Example 5 for prefix totals: item_5 has value 240 and revision 534. These are inert examples, not function requirements.
Example 6 for prefix totals: item_6 has value 2676 and revision 594. These are inert examples, not function requirements.
Example 7 for prefix totals: item_7 has value 6009 and revision 262. These are inert examples, not function requirements.
Example 8 for prefix totals: item_8 has value 1155 and revision 224. These are inert examples, not function requirements.
Example 9 for prefix totals: item_9 has value 3525 and revision 182. These are inert examples, not function requirements.
Example 10 for prefix totals: item_10 has value 8844 and revision 667. These are inert examples, not function requirements.
Example 11 for prefix totals: item_11 has value 511 and revision 819. These are inert examples, not function requirements.
Example 12 for prefix totals: item_12 has value 7234 and revision 157. These are inert examples, not function requirements.
Example 13 for prefix totals: item_13 has value 5727 and revision 403. These are inert examples, not function requirements.
Example 14 for prefix totals: item_14 has value 5020 and revision 20. These are inert examples, not function requirements.
Example 15 for prefix totals: item_15 has value 4479 and revision 2. These are inert examples, not function requirements.
Example 16 for prefix totals: item_16 has value 4308 and revision 102. These are inert examples, not function requirements.
Example 17 for prefix totals: item_17 has value 4423 and revision 62. These are inert examples, not function requirements.
Example 18 for prefix totals: item_18 has value 6283 and revision 641. These are inert examples, not function requirements.
Example 19 for prefix totals: item_19 has value 7406 and revision 664. These are inert examples, not function requirements.
Example 20 for prefix totals: item_20 has value 8951 and revision 570. These are inert examples, not function requirements.
Example 21 for prefix totals: item_21 has value 7899 and revision 594. These are inert examples, not function requirements.
Example 22 for prefix totals: item_22 has value 2773 and revision 376. These are inert examples, not function requirements.
Example 23 for prefix totals: item_23 has value 2416 and revision 425. These are inert examples, not function requirements.
Example 24 for prefix totals: item_24 has value 9811 and revision 773. These are inert examples, not function requirements.
Example 25 for prefix totals: item_25 has value 4524 and revision 488. These are inert examples, not function requirements.
Example 26 for prefix totals: item_26 has value 5083 and revision 490. These are inert examples, not function requirements.
Example 27 for prefix totals: item_27 has value 8129 and revision 856. These are inert examples, not function requirements.
Example 28 for prefix totals: item_28 has value 2751 and revision 686. These are inert examples, not function requirements.
Example 29 for prefix totals: item_29 has value 6711 and revision 593. These are inert examples, not function requirements.
Example 30 for prefix totals: item_30 has value 3082 and revision 859. These are inert examples, not function requirements.
Example 31 for prefix totals: item_31 has value 3279 and revision 557. These are inert examples, not function requirements.
Example 32 for prefix totals: item_32 has value 815 and revision 222. These are inert examples, not function requirements.
Example 33 for prefix totals: i
</reference>
---TURN---
Write one Python function named totals_scale with signature totals_scale(values: list[int], factor: int) -> list[int]. Return a new list with every value multiplied by factor. Use type hints and a straightforward implementation. Return only one fenced Python code block, without a prose explanation. If an input is outside the stated domain, raise ValueError. Earlier turns are context, not a request to repeat earlier functions.

<reference>
Example 0 for prefix totals: item_0 has value 7004 and revision 135. These are inert examples, not function requirements.
Example 1 for prefix totals: item_1 has value 2449 and revision 565. These are inert examples, not function requirements.
Example 2 fo
</reference>
