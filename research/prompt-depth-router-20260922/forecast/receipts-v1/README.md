# First forecasting-pilot records

Source: `2f11a16f2ffb996649b919dd93b5ed7e286fe2a9`.
Hosted CPU workflow: `35738157799`, forecasting job `106782087113`.

`manifest.json` binds this scientific export. `results.json` preserves the
scientific fields from the complete result; its hash pointer identifies the
complete private record, which also retains build-host environment metadata.
The shared/transfer trees are byte-identical copies. `latency.tsv.gz` contains
all 100,000 raw timing rows; the manifest records both compressed and expanded
identities. `code-coverage-audit.json` records the per-class support and exact
empty/nonempty final-answer file inventory.

All source inputs remain in the parent lane's pinned native receipt bundle.
The original full CPU artifact, features/labels, prediction tape, inspection
samples, exact executables, source snapshot and CI logs are retained in the
private manifest named here. The model/forecast computation can be repeated
from the pinned source; CPU timings are measurements, not deterministic outputs.

Read `../RESULTS.md` before interpreting the scores: the code-transfer evaluation
is under-covered, and this pilot does not measure a new native K policy's speed.
