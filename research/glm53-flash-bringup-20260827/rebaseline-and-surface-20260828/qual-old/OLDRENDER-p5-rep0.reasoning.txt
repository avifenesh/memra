The user is providing an update on their technical environment and preferences. Let me parse this:

1. **memra (bw24 successor)** - replaces llama as their default inference tool. It works on Blackwell 50 series and H100, beats llama and vllm on supported models. New inference ideas go to memra.
2. **llama** - no longer their default. They use memra now.
3. **Inference work** - current work is FULL, no need for more inference work. What inference world is missing: better ways to keep the full benchmark correct without re-running the whole bench every change. (This is about validation efficiency - keeping bench state valid across incremental changes)
4. **ML interests** - model side: improving training, making models smarter, new generation of models addressing what current LLMs are missing.
5. **eigen** - under re-work, frozen. Don't touch it until they finish and confirm it's actually better. They'll report back with current pains after.
6. They want to update the itch skill and their preferred work.

So my update should emphasize:
- memra isthe go-to for inference (not llama)
- Focus: ML model side (training improvements, model intelligence, next-gen model architectures)
- Inference: full, don't add more. Only care about bench state management (keeping full bench valid without full re-runs)
- eigen: frozen, hands off until user signals otherwise

Let me also check memory for existing knowledge... The user references "itch skill" - Ishould update AGENTS.md memory. I don't have specific prior memory about llama/bw24/eigen in the folders I've seen, but I should write this update to fold/update concise memory.

I'll write a concise replacement update. Ishould keep entries short:

```markdown
<agp_review>
default inference stack: memra (bw24 successor). runs on Blackwell 50-series + H100. beats llama & vllm on supported models. memra is the default — llama is legacy/secondary. new inference work aims at memra.

focus: ML model side — improving training, making models smarter, next-gen model architectures that fill gaps current LLMs miss.

inference: work full, no new threads. only gap in inference ecosystem: keeping full benchmark state valid without re-running entire bench after each change (bench-state management / incremental validation).

eigen: frozen during rework. do not modify or build on it. wait for user signal when rework lands and pain-points exist for real contribution.
</agp_review>
```

Hmm, should I keep the AGENTS.md format header? The system prompt says AGENTS.md "should be plain text, not markdown" — so I should avoid the heavy markdown? Actually looking at instructions: "AGENTS.md file is the standard place to store guidance for coding agents ... should be plain text, not markdown". Wait, it actually says, but instructions state "should be plain text, not markdown" — so I'll write plain text without markdown formatting.

Let me write plain text:

```
priority for new work: ML model side — training improvements, making models smarter, next-gen model architectures that close gaps current LLMs have.

default inference tool: memra (bw24 successor). works on Blackwell 50-series and H100. beats llama and vllm on supported models. memra is the default inference path; llama is no longer default. route inference ideas to memra.

inference work: full, no need for more. only remaining inference gap: keeping full benchmark correctness without re-running the entire bench after every change (incremental bench-state validation).

eigen: frozen while user reworks it. do not use or build on eigen. wait for user to finish and report current pains before touching it.
```

I should place this as a complete update of "preferred work areas" / "current priorities" section.