Got it — updated picture of your current state:

**Inference stack:**
- Current inference work is **full** — don't bring you new inference projects.
- **Memra** (bw24 successor) is your default inference path, not llama. Works on BW50 series + H100, beats llama and vLLM on supported models. **New inference ideas → memra**, not llama.
- Only open inference pain: keep benchmark results correct across changes **without rerunning the full bench every time** (incremental/validated bench updating).

**ML interest (the actual focus):**
- Model-side work: better training, making models smarter, next-gen models that fill what current LLMs are missing.

**Eigen:**
- Frozen mid-rework. Not in use. Don't suggest changes or work on it until you re-show it as improved — then you'll state the current pain.

So going forward: model training/quality gaps = active interest, memra = inference default, inference itself closed, eigen paused.

Two clarifications so I respond usefully next time:
1. On the bench-only-if-needed problem — are you thinking caching paired with fingerprinting (code changes → which bench items could be affected), or periodic spot-validation of caches?
2. For "what current LLMs are missing" — do you have a concrete shortlist (e.g., persistent memory, true multi-step reasoning, sample efficiency), or is that what you want to explore?