check those node-MainThread is open on :4173
listening on every interface — reachable from your network, not just this machine
2h
Open
quiet
source
python3 is open on :4177
listening on every interface — reachable from your network, not just this machine
2h
node-MainThread is open on :4321
listening on every interface — reachable from your network, not just this machine
2h
an unknown process is open on :5601
listening on every interface — reachable from your network, not just this machine
2h
an unknown process is open on :7443
listening on every interface — reachable from your network, not just this machine
2h
python3 is open on :7680
listening on every interface — reachable from your network, not just this machine
2h
ttyd is open on :7681
listening on every interface — reachable from your network, not just this machine
2h
an unknown process is open on :8443
listening on every interface — reachable from your network, not just this machine
2h
an unknown process is open on :9443
listening on every interface — reachable from your network, not just this machine
2h
Op