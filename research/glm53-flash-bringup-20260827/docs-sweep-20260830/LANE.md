# lane/glm5-docs-sweep — hermes review findings vs banked receipts (2026-08-30)

Charter: every glm5 doc/product-surface claim the hermes review flagged as drifted is
verified against the CURRENT bringup head (base origin/lane/glm53-flash-bringup @
a53342541) and corrected ONLY toward what receipts prove. Every correction cites its
receipt; numbers are quoted, never paraphrased.

## Verdict table

| # | finding | verdict | what was done / found |
|---|---|---|---|
| 1 | `/v1/models` publishes trained 1M ctx for glm5 while the 3-card resident shape cannot prime 1M | **fixed-here (engine-side leak)** | The endpoint sources `context_length` from `lm.model.cfg.context_length` (the checkpoint's TRAINED figure; worker.rs caps probe) — NOT from models.toml and NOT from `MEMRA_CTX` (`MEMRA_CTX` is only the default per-request allocation; a client `max_ctx` can allocate up to trained). The deployment's only hard cap below trained is the `MEMRA_MODEL_METADATA` envelope (`max_prompt_length + max_output_length`), which admission enforces (`apply_model_request_limits`) but which did NOT flow into any catalog body — so a metadata-pinned deployment still advertised the raw trained value. Fix: `published_context_length()` in main.rs caps the claim by the envelope on ALL THREE catalog views (v1 / openrouter / openmodels); test `catalog_context_claim_is_capped_by_the_deployment_envelope` (glm5-shaped: trained 1,048,576, envelope 131,072 published; half-envelope and wide-envelope arms). Config side unchanged and still required: darklanes must pin the envelope per deployed shape. Receipt: `research/glm5-prefix-latent-20260830/box-window/WINDOW-STATUS.md` — "Deep prime FAILED at wall 427.9 s: `[engine-error] class=Overloaded prefill error: layer 31: DSA k-pool selection failed: DriverError(CUDA_ERROR_OUT_OF_MEMORY)`", per-card peaks 65048/77688/**97242** MiB, "the 3-card RESIDENT recipe is NOT a 1M-context config." Also noted in docs/SERVING.md's Provider-Monitor section. |
| 2 | FLAGS.md `MEMRA_DSA_INDEX_RING` claims glm5 never chunks | **fixed-here** | Stale: `hyper_prime_ranges` (hybrid_forward.rs:894) now DELEGATES the mHC prime to `prime_chunk_ranges` on both walks (single-engine + ppN), `MEMRA_HYPER_BATCH` batches decode, and the spec verify is a batched t=K+1 walk. The row's incident narrative was recast as dated history ("on the 2026-08-28 head glm5_next did not chunk") with the current-behavior correction inline; the drain design and the sizing lesson stand unchanged. |
| 3 | FLAGS.md prefix-cache row sells skip-prefill for glm5 | **fixed-here (row) / already-fixed (flag)** | `MEMRA_PREFIX_LATENT` row was already honest (default OFF, restored-suffix defect class named). The generic `MEMRA_PREFIX_CACHE_MB` row's "A NEW session ... skips that prefill" carried no latent exception — added: `StatePlan::LatentKvCache` models (glm5_next) refuse capture loudly (`[prefix-cache] snapshot failed (latent (MLA/DSA) KV planes are not carried by prefix entries); prefix not cached`, `cached_tokens` 0 always — worker.rs:5044) and pay a FULL prime per request. Receipt: `spec-battery-20260830/` re-receipted the refusal on the deployed 3-card shape. |
| 4 | CENSUS.md claims indexer RoPE the 5.3 reference never applies | **fixed-here** | `Glm5NextTextIndexer.forward` (modular_glm5_next-ref.py:749/772) computes `q = wq_b(q_resid)`, `k = k_norm(wk(x))` — NO rotary; the file never reads `indexer_rope_interleave` (grep: zero hits; the only reference rotary is the vision tower's `Glm5NextVisionRotaryEmbedding`). The engine's kpool transcription (`mla_kpool_indices`, hybrid_forward.rs:6637) likewise applies none. Corrected CENSUS.md AND the same stale claim in `crates/memra-gguf/src/config.rs` ("the indexer has its own rope" comment): the config key is checkpoint metadata, dead for this arch. |
| 5 | MEMRA_KDA_CHUNKED row + kda.rs advertise an unpaid A/B after the L3 HOLD | **kda.rs fixed-here; FLAGS row not-on-this-head** | Receipt: `launch-diet-20260830/WINDOW-20260830.md` §4 prefill census — "kda (incl. scan 160 ms) \| 222 \| 3.4% \| confirms L3's ATTRIBUTED-NEGATIVE: scan ~2.4%" (mla-prefill-attn 75.8% owns the wall; raw `box-receipts-20260830/census-analysis.txt`). kda.rs header rewritten: the chunked twin is SHELVED, ATTRIBUTED-NEGATIVE, no A/B owed, algebra stays banked. The `MEMRA_KDA_CHUNKED` FLAGS row does NOT exist on this head — it lives only on the unmerged branch `lane/glm5-kda-chunk-scan` (e69ed0600), whose row still states the pre-census flip condition ("Flip condition: interleaved x5 box A/B on the serving card class"). Adding the row here would catalog a flag with zero read sites on this head (the coverage audit forbids exactly that), so the correction is recorded here for whoever revives or retires that branch: its flip condition is PAID and NEGATIVE. |
| 6a | BRINGUP.md headlines "THE SERVING SHAPE IS 2-CARD" | **fixed-here** | Heading re-marked WAS/SUPERSEDED (history kept verbatim); new section "THE SERVING SHAPE IS 3-CARD RESIDENT (owner ruling, 2026-08-30)" with the chain: r92 falsified by its OFF arm (all C2/C3 rows 408-dead; "No value of this knob yields dev0 residency AND multi-GiB headroom"), s23 aborted at the boot gate (`FATAL: worker init failed ... CUDA_ERROR_OUT_OF_MEMORY`; "the bytes do not exist"), 2-card SLRU closed by OWNER RULING verbatim "we are not fast enogh ffor 2 card slru", 3-card resident adopted (PP_STAGES=3 SPLITS=15,30; VRAM at ready 54547/65651/68085 MiB). Receipts: `research/glm5-prefix-latent-20260830/box-window/WINDOW-STATUS.md` + out-plx-off-{r92,s23,slru}/. Also banked there: L3 attributed-negative correction to the 2-card section's stacking plan, and the 1M-not-a-3-card-claim receipt. |
| 6b | MLA_TC default ON — verify row | **already-fixed** | `MEMRA_MLA_TC_PREFILL` row reads "DEFAULT ON (owner acceptance 2026-08-30, on the two-box A/B receipts...)" with the interleaved x5 two-box numbers (TTFD -62..-69%, prefill 619-724 -> 1629-2255 tok/s, zero argmax flips across 20 boots) and receipts pointer. No edit needed. |
| 6c | spec + dflash rows default OFF with the three-way verdict | **fixed-here** | Both rows still claimed "NO real-artifact or serving-box receipts yet" / "the flip needs the box three-way comparison ... no serving-shape numbers exist yet" and pinned the flip to "the 2-card serving shape". Rewritten with the paid receipts: spec-battery NO-FLIP (OFF **35.4** vs native ON **27.5** tok/s = 0.777x, TTFT 5.5-6.1x worse, 70/70 byte identity, loop-law 0/252) and the 3way verdict (DFlash2 = drafter of record: acc/cycle 1.907 vs 1.452 K3 greedy +31.3%, sampled 1.889 vs 1.365 +38.4%, K5 2.290 +55.5%, TTFT near-constant vs O(prompt), -3520 MiB head-engine VRAM, 66/66 byte identity; best arm K=1 **34.98 vs 35.41 = 0.988x**, no K crosses `tok/cycle > (31.6 + 20.1*K)/28.24`; flip condition = fixed round cost < ~28.24 ms; mature-loop port named). K-policy note added to the SPEC row: cached>=1024 rows unreachable on glm5 (snapshots refused), K=3 everywhere, c>2 shed protective (K=3 pinned through c=4 costs 24% aggregate). Receipts: `spec-battery-20260830/`, `3way-decision-20260830/`. |
| 7 | HF card pins the 1M wall to the mint and calls long context unsolved | **draft-for-owner** | `HF-CARD-DRAFT.md` in this directory: replacement "Context" section quoting the 1m-demo receipts (1,035,357 tokens primed through the serving surface on PP4, greedy prefill 161.28 tok/s / 6,419.8 s wall, sampled twin 161.26 at 0.01% agreement, decode 15.7 at 1.035M depth, depth ladder 16k->1M, error census 0 — `1m-demo-20260829/LANE.md`, branch lane/glm53-1m-demo @ 315a1080fb), the monolithic-prime attribution of the old wall + its chunk-schedule fix, and the honest boundaries kept (4 cards not fewer — the 3-card kpool OOM receipt; TTFT at 1M is 107 minutes; bench claims only). DFlash2 attribution block included as CONDITIONAL (inco approval requires a visible DFlash 2 mention IF it serves; spec is default OFF today so the block is explicitly held back). OWNER PUBLISHES; nothing was pushed to HF. |

## Also found while sweeping (not in the findings list)

- `crates/memra-gguf/src/config.rs` carried the same indexer-rope claim as CENSUS.md
  ("the indexer has its own rope") — corrected in the same breath (finding 4's receipt).
- `docs/SERVING.md`'s Provider-Monitor bullet ("Text `max_context_length` ... come from
  the loaded model") described the pre-fix sourcing — updated alongside finding 1.
- The 3way LANE.md's prior-receipts bullet quotes spec-battery byte identity as "56/56";
  the spec-battery LANE.md's own verdict line is "70/70 served-path spec-vs-plain
  byte-identity (56 stage-1 + 14 trim spot-check)" — the 56 is the stage-1 subset, not a
  contradiction; FLAGS.md quotes the 70/70 form with its decomposition.
- NOT changed: `MEMRA_GLM5_MTP` row's "DEFAULT OFF BY DESIGN ... NOTHING consuming it
  yet" rationale predates the 3way but stays accurate (the head loads only for spec, and
  spec is no-flip; DFlash2 as drafter of record makes the head even less loaded, stated
  in the DFLASH row).

## House checks

- fmt: run on touched crates. clippy: memra-server + memra-gguf all-targets, zero new.
- New unit test green: `catalog_context_claim_is_capped_by_the_deployment_envelope`.
- Engine files touched (main.rs code; kda.rs/config.rs comments) -> local-ci per house
  rule before push; results recorded in the push commit chain.
- Public-boundary: no box identity, no provider ids, no prices in any edit.
