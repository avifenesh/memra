Let me parse what the user is asking for. They have some kind of autonomous agent system (sounds like they're building a scheduled agent pipeline — possibly with something like OpenClaw/Clawdbot/Moltbot-style cron agents, or a custom setup with hourly agents that wake up). They mention:

1. "active research is a good one" — apparently referring to a previous suggestion (maybe a previous conversation about agent prompts/schedules). They want to replace "the digest" with a "twice a day researched thing."

2. Add the MCP for search (they provided the config JSON for open-websearch MCP server).

