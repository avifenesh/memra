Box state left behind (sbox bench box), 2026-08-28 end of the decode-attribution lane.

RUNNING: memra-server, card 0, with the brief's serve invocation PLUS MEMRA_ST_PINNED=1 —
the one change proven byte-identical (six boots, two prompts, two transports, three slot counts,
all producing sha fd006d0d50eb59b5 on the p5 greedy cell):

  MEMRA_MOE_RESIDENT=0 MEMRA_MOE_SLOTS=12000 MEMRA_ST_PINNED=1 \
  CUDA_VISIBLE_DEVICES=0,1 MEMRA_COMPAT=openai \
  MEMRA_MODELS="zai/glm-5.3-flash=$HOME/models/glm53-nvfp4" MEMRA_ADDR=127.0.0.1:18400 \
  MEMRA_CTX=8192 MEMRA_MAX_SESSIONS=4 NVIDIA_TF32_OVERRIDE=0 MEMRA_SPILL_STATS=1 \
  ./target/release/memra-server

  Verified after the restart: 20.362 tok/s median (3 reps) on p5 greedy, sha fd006d0d50eb59b5,
  i.e. the A2/A2R arm reproduced a sixth time. VRAM 79.6 GB on card 0, GPU util 96%, card 1 idle.
  MEMRA_BF16_MMV is NOT enabled: it is a numeric-class door and is the owner's call.

  Deliberately NOT enabled at 14000 slots even though A4 measured 25.9: that arm carries
  BF16_MMV, and 14000 slots on the f32 trunk would not fit (0.80 hard VRAM cap).

  A COLD first request after a restart differs in bytes from a prefix-restored one
  (67d9e60b9cc4aa63 vs fd006d0d50eb59b5). That is fresh-prefill vs restore numerics, a
  pre-existing engine property visible in every arm here, not something this lane introduced.
  Every cell above compares restore-to-restore, which is why the shas are stable within arms.

NO leftover bench processes. The harness lives at ~/probe.py ~/steady.py ~/sea.py ~/cell2.sh
~/arm4.sh ~/collect.sh ~/prompts.json and is banked in this directory; the raw per-arm outputs
are ~/s-*.log (banked as results-*.jsonl) and boot logs ~/cell-*.log (trimmed into
boot-extracts.txt). Nothing was deleted from the box.

NO ENGINE CODE WAS CHANGED IN THIS LANE and the served binary is the one that was already
running. The repo diff is docs/FLAGS.md (one table row) plus this new receipts directory.
