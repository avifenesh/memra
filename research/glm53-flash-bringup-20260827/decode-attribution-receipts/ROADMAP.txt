GLM-5.3-Flash BASE decode — the road to the owner's ~90 tok/s (no drafter), 2026-08-28

Every row below is ms/token on the measured decomposition (real prompt p5, 203 prompt tokens,
192 completion tokens, greedy = the instrument, steady state, card 0 only).

  step                                                        staging  roofline  launch   total   tok/s
  0  as served today (mmap staging, 12000 slots)                84.1     15.9     17.1    117.1     8.5
  1  + MEMRA_ST_PINNED=1   MEASURED, BYTE-IDENTICAL            16.1     15.9     17.1     49.1    20.3
  2  + MEMRA_BF16_MMV=1    MEASURED, numeric-class door        16.3     10.1     16.6     43.0    23.3
  2b + the 9.7 GB BF16 frees -> 14000 slots  MEASURED           11.8     10.1     16.7     38.6    25.9
     (p7: 27.7 tok/s. This is the best config that fits on ONE card: 3.0-3.2x the served baseline,
      and the sampled arm matches it at 25.9 — the product shape, not just the instrument.)
  3  + full expert residency across BOTH cards  PROJECTED       0        10.1     16.7     26.7    37.4
     (the intercept is MEASURED: A3 26.70 ms and A4 26.75 ms once their staging is subtracted,
      so step 3 is not a model — it is the arms' own zero-staging limit.)
  4  + fused MoE epilogue (sigmoid + pre-clamp + macro)         0        10.1     ?       ?       ?
     LANDED as MEMRA_MOE_FUSED_EPI (default OFF), lane/glm53-epilogue. The launch delta is now
     MEASURED, not projected: nsys over the gate's two arms counted 2698 -> 1933, and the -765
     decomposes exactly to -15 per engaged token-layer x 51 with nothing else moving. At the
     real n_used=8 that structure is 49 -> 4 per token-layer, i.e. 2058 -> 168 per token across
     the 42 MoE layers. NO ms/token or tok/s claim: the rig is correctness-only by law, and the
     interleaved sampled A/B on the serving card class has not run yet. The two arms are
     bit-identical (0/N elements differ on all 11 rows).

     CORRECTION to this row's first draft: the 42 per-layer router DtoH + stream syncs are NOT
     recoverable by the epilogue and never were. The sigmoid router is ALREADY a device kernel;
     what crosses the bus is the tiny [t, n_used] sel/w, and the host needs `sel` because it
     DRIVES ADMISSION. The zero-DtoH path needs a device pointer table, which exists only for a
     fully resident layer (layer_dev_row / prewarm_layer) or a load-time slab. One glm5 MoE
     layer is 288x3 = 864 blocks against ~285 slots per layer at the serving recipe's 12000, so
     no layer can be fully resident on this placement. The sync removal is therefore DOWNSTREAM
     OF STEP 3, not part of step 4. Source: lane/glm53-epilogue finding 1, 2026-08-28.

     The KDA (~600), mHC pre/post (~540) and MLA chains are NOT touched by the epilogue, so the
     residue still needs its own launch-structure arc — and CUDA graph capture, the usual
     answer, is refuse_hyper'd on this residual topology.

  90 tok/s = 11.1 ms/token. Steps 1-3 are arithmetic on measured constants. Step 4 is the
  biggest single remaining piece but is NOT on its own sufficient; the honest claim is
  "residency + BF16 trunk + launch-structure work reaches the target, and the fused epilogue
  is the largest item in the third bucket".

STEP 3 — what "full expert residency" actually requires (source-level sizing)
  171.2 GB of routed experts vs 2 x 96 GB. Card 0 alone holds at most ~57-66 GB of them
  (12000-14000 SLRU slots = 33-39% of the 36,288 blocks), so single-card residency is
  arithmetically impossible; the second card is the only route, and it is idle at 3 MiB today.
  With MEMRA_BF16_MMV the trunk halves to 13.9 GB, and an even 21/21 layer split puts ~85.6 GB
  of experts + ~7 GB of trunk on each card — inside 96 GB, but only with the BF16 trunk.
  So step 2 is not merely 6 ms; it is what makes step 3 fit.

  ALREADY DONE (no work needed):
    - hybrid.rs MoePlacement::plan/should_reside is ALREADY per-device and keys on pp::pp_cuts,
      so the loader shards and decides residency per card the moment the PP door opens.
    - pp.rs BoundarySlot buffers are lazily sized from the caller's `n`, so tx()/rx() already
      accept a stream state of streams*n_embd rather than n_embd. No slot-sizing change.
    - pp.rs already documents "plain eager decode only (generic arm N-stage)" — and glm5 is
      routed EAGER-ONLY, so it is exactly the arm PP supports.

  BLOCKED BY ONE EXPLICIT REFUSAL:
    hybrid_forward.rs:1382, decode_step_hyper():
      if pp_cuts(...).is_some() { return Err("MEMRA_PP_SHARD is set, but the hyper-connection
      trunk walk is single-engine: the sharded stage handoff is unwired for this residual
      topology") }
    and decode.rs:723 routes hyper BEFORE the ppN door at decode.rs:738.

  THE WORK:
    1. decode_step_hyper_ppn — mirror of decode.rs:1339 decode_step_h_ppn (~120 lines):
       hyper::expand on stage 0, per-stage layer ranges through rt.engine(s), tx/rx of the
       [streams, n_embd] state at each fence, hyper::collapse + output_norm + lm head on the last.
    2. The prefill twin for forward_hyper / the monolithic prime (this model has no batched prime).
    3. Per-stage KDA recurrent state + MLA KV placement (pp::new_cache already owns this contract
       for the generic arm; the KDA/kpool state needs auditing against it).
    4. A bit-identity gate in the ppn-gate class: logits byte-identical to the unsplit walk at
       every N/knob combination, plus the greedy tape.
    Not a one-increment change, but every load-bearing piece except the two walks already exists.

  ALSO IN THE WAY OF A CLEAN DEFAULT: hybrid.rs:216 computes exact_expert_bytes only for
  `match src.gguf()`. A safetensors model has no whole-model expert census, which is why
  MEMRA_MOE_RESIDENT=0 is forced in this serving recipe and why MEMRA_ST_PINNED cannot yet
  become capacity-keyed by default.
