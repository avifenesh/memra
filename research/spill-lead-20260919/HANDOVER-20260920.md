# Handover — generic spill / tiered-KV pre-work lead (2026-09-20, session move)

Lead alias `@agent-c07799`. Repo `avifenesh/memra`. This file is provider-neutral; hosts, instance ids and
prices live in the gitignored `LANE-LOCAL.md` in the lead worktree (copy it out-of-band to the new machine).

## Where things are

| surface | state |
|---|---|
| `main` | `f79b3e57` at handover (moves several times a day — always fetch + replay before merging) |
| merged this program | #518 `61be8b0d`, #519 `b3487a03`, #560 `7326f0e1`, **#563 `cf4f317e6`** (days 7–8: native `CudaTransfers`, `memra_tier::conformance`, experts-via-tier MATCH, device-publish rows) |
| **open PR #568** | `lane/spill-integ5-20260920` @ `db16dc53` → days 8–9: **ACTIVE-8K G1 PASS** (VMM reclaim), banked experts under 8/4 GiB pressure, collector child containment, `h2d-probe --copies`, `pro-single` rig profile, `docs/decisions/KV-PHYSICAL-RECLAIM.md`. CI jobs all green on `eb010c87`+; two `revuto` review findings fixed (`a799cf5d`, `eb010c87`); the bot re-reviews per push. **Needs a replay on current `main` before merge** (INDEX.md is the usual conflict — union of rows). Merge with `gh pr merge 568 --merge` on green; owner rule: no human review needed. |
| lane branches | `lane/spill-{a,b,c,d,e,f}-20260919` (tips in each lane's `STATE.md` / `DAY*.md`); lead `lane/spill-integ5-20260920`; older `lane/spill-integ{,2,3,4}-*`, `lane/spill-lead{,2}-*` are merged history — delete after #568 merges |
| records | `research/spill-lead-20260919/{FREEZE*.md,INTEGRATION-DAY{2,3,4,6,8,9}.md,PR5xx-REVIEW*.md,BOX-ACCESS.md,HANDOVER-20260920.md}`; lane reports `research/spill-{a..f}-20260919/DAY*.md`; `research/INDEX.md` rows |
| rig | **BOX3**: one RTX PRO 6000 Blackwell (target card class), fixed-price spot, real block-device root (virtio; NVMe ancestry provider-claimed), nvcc 13.2 at `/usr/local/cuda/bin`; bootstrap receipt `/root/spill-receipts/box3-bootstrap-*/BOOTSTRAP.json` (31 steps green); `/root/memra-spill` built at integ5 `b1982864`; artifacts `/root/artifacts/{Qwen3.8-27B-NVFP4-Q5K-mtp.gguf,Qwen3.6-35B-A3B-UD-IQ4_XS.gguf}` + `.sha256` (both MATCH). The bid-market 5090 (BOX2) was destroyed after four host-stops; its receipts remain valid 5090 evidence. **Never compare timing across boxes.** |
| access | ssh master socket `~/.ssh/cm/box3` + keeper `~/.ssh/cm/keep-box3.sh` (machine-local; recreate on the new machine from `BOX-ACCESS.md`); the same ed25519 keypair is authorized on the box from both machines |

## Gate status (page 08 §E)
- **G1**: `ACTIVE-8K G1 PASS` on the 5090 (VMM planes: released = reacquired = 201,326,592 B, bit-identical). 32k: bit-identical, **one-granule residual unclassified → not PASS** (16k residual 0; VA-free / ctx-sync probes return 0). Criterion (a)–(d) is fixed in `docs/decisions/KV-PHYSICAL-RECLAIM.md`. Target-card rerun in flight (B day 10).
- G0, G2–G7: unchanged/pending. G2 scored N≥5 campaign was lead-authorized on BOX3 (D day 10).
- Every cell so far is `executed-not-qualified` development evidence.

## Lanes in flight at handover (dispatched ~15:20–15:25 UTC on BOX3; each was told to push + write `STATE.md`)
- **A day 8** — native conformance on the target card; O_DIRECT storage cells on the real block device (label `block-device ext4 (virtio; NVMe ancestry provider-claimed, not proven)`); pread baseline for the io_uring decision → `IO-BASELINE.md`.
- **B day 10** — BOX3 baselines 8k/32k; `--case active --kv-allocator vmm` 8k/32k + pooled control; residual classification probe (VA reservation free vs keep → `va-reservation-page-table`?); allocator injection `Cache::new_with_allocator`.
- **C day 9** — BOX3 baseline + banked experts (default and 8 GiB); `MoeSlotCache` wiring behind the door via the owner-thread proxy registry (must keep `Engine: Sync`); `BUDGET-REFUSAL.md` (`MEMRA_MOE_SLOTS` clamps to 8).
- **D day 10** — review the lead's `pro-single` profile + tests; **G2 scored campaign** (N=5 AB + 5 BA, 5 sizes, both directions, one lock, 250 ms telemetry, medians with N + regime); `pp-transport-smoke`; `--validate` over BOX3 receipts.
- E (contracts/docs) and F (rig ops) idle since their last seals (`4d89a243`, `7644c41f`).
If the parent session dies, these agents die with it; their pushed lane tips + `STATE.md` + `/root/spill-receipts/*` + `/root/wt-*` on BOX3 are the resume points. Relaunch with `LANE-PROMPT-TEMPLATE.md`.

## Standing rulings made by the lead (keep)
1. One numeric program per request; VMM planes chosen over paged block pool because kernels/addresses stay identical. Pool trim rejected by measurement.
2. Never relax a gate's bar after seeing a result; classify residuals by diagnostic or label not-PASS.
3. Shared-file ownership: lanes add only their `pub mod` line / `[[bin]]` fragment in a lane-local `*-FRAGMENT.md`; the lead installs them. Conflicts: union for additive lines, owner's file for code.
4. `memra_tier::conformance` is a public module (the contract crate ships the suite); no feature/self-dev-dep (publish census refuses internal dev-deps).
5. Legacy shell gates run under the collector's inherited lock (`--external-lock`), never wrapped twice.
6. Refusal token contract: final line `REFUSED: <reason>` (or `kv-tier-gate: REFUSED: …`), exit 2; generic `Error:` + exit 2 is *failed*.
7. Toolchain: repo pins Rust 1.97.1; newer lints (1.98 `chunks_exact_to_as_chunks`) are not churned.
8. New `MEMRA_*` env read → FLAGS.md row in the same commit; CLI doors (`--kv-allocator vmm`) need no FLAGS row but carry `decide-by:` (2026-10-04) in their design doc.
9. Receipts: raw logs are hash-bound; never "fix" whitespace inside them (`git diff --check` noise in `research/**/raw` is expected).
10. Public boundary: no provider names in a role, hosts, ids, cities, prices in tracked files.

## Next steps (in order)
1. Replay #568 on current `main`, run the CPU battery (below), push, merge on green; ff local `main`; delete merged integ/lead branches + worktrees.
2. Collect A/B/C/D day-10 results (lane `STATE.md`/`DAY*.md`); relaunch dead ones from on-disk state; integrate onto `lane/spill-integ6-*` from `main`; PR; merge.
3. G1: if B classifies the residual as driver mapping metadata on both cards → `ACTIVE-32K G1 PASS (…classified)`; then allocator injection lands the door in `Cache` construction. Decide-by 2026-10-04: promote to the naked default for the tiered materializer or delete the door.
4. G2: D's scored table (N=5, regime) → `docs/PERFORMANCE.md` only via `current-board.json` + `tools/update-perf-board.py` if it ever becomes a published number; today it stays in `research/`.
5. C: land `MoeSlotCache` door (default OFF) once `Engine: Sync` holds; then the Hy3/Step ladder cells per page 08.
6. M1 storage: block-device cells on BOX3 upgrade the evidence class; NVMe-proven cells still need a bare-metal/passthrough rig (F's `NVME-DECISION.md`: NO-GO on unprovable VMs).
7. Only after G0–G3 hold on the target card: the first V4.1-specific work (page 08 §D order). DSv4-0731 stays paused.

## CPU battery (lead, before every PR)
`cargo fmt --all -- --check` · `cargo test -p memra-tier -p memra-kv --offline --no-fail-fast` · `DOCS_RS=1 cargo clippy -p memra-engine -p memra-server -p memra-tier -p memra-kv -p memra-gguf --offline --target x86_64-unknown-linux-gnu --all-targets -- -D warnings` · `bash tools/check-flags.sh` · `bash tools/workspace-publish-census.sh` · `bash tools/docs-registry-census.sh` · `python3 -m pytest -q crates/memra-tier/tests/battery/` · `python3 tools/update-perf-board.py --check` · `git diff --check` (raw receipts excepted). `tools/test_sft_scale.py` fails 5/10 on pristine main — unrelated, don't absorb.
