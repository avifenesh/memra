finish=length content_nonempty=False
---
Got it, let's tackle this GitHub issue step by step. First, I need to understand what the bug is: the JetBrains Marketplace listing for the AgniX plugin has two broken links: the Documentation link 404s, and the Source Code link points to an old, stale repo URL.

First, let's start with investigation. First, I should check the current repo structure, right? Wait, the issue is in the agent-sh/agnix repo, issue #1099. First, let's confirm the current state of the repo's marketplace metadata. Oh right, JetBrains Marketplace listings for plugins are usually configured either in the plugin's `plugin.xml` (for IntelliJ plugins) or in the `gradle.properties`/`build.gradle.kts` if it's a Gradle-built plugin, or sometimes in a separate `marketplace.yaml` file? Wait no, for JetBrains plugins, the marketplace metadata is often set via the `plugin.xml`'s `<idea-plugin>` tags, or via the `com.jetbrains.plugins` block in Gradle, or if they use the JetBrains Marketplace publishing workflow, it might be in the `github/workflows` publish config, or a `marketplace.json` file.

Wait first, let's triage: this is a bug, not a question, so no need to ask the reporter for more info unless there's ambiguity. First, let's check the current source of truth for the plugin's metadata. Let's assume the AgniX repo has a standard IntelliJ plugin structure. Let's first check the existing documentation link: the 404 means the current URL is wrong. Let's see, if the docs were moved, say from `agent-sh/agnix-docs` to `agent-sh/agnix/tree/main/docs`? Or maybe the old docs repo was archived? Wait also the source code link is stale: maybe the repo was moved from a personal account to the `agent-sh` org, so the old URL was something like `github.com/olduser/agnix` instead of `github.com/agent-sh/agnix`.

First step: Investigate the root cause. Let's check the repo's plugin configuration files:
1. First, check `plugin.xml` for any `<url>` or `<documentation>` tags that set the marketplace links.
2. Check `build.gradle.kts` or `gradle.properties` for any marketplace-related properties like `pluginDocumentationUrl`, `pluginRepositoryUrl`.
3. Check the GitHub Actions publish workflow (if they use one to push to JetBrains Marketplace) for any hardcoded URLs.
4. Check if there's a `marketplace.yaml` or similar config file in the repo root.

Wait, let's say after investigation, we find:
- The stale source code link is set in `build.gradle.kts` line 42: `pluginRepositoryUrl.set("https://github.com/old-agnix-dev/agnix")` which is the old personal repo, now archived.
- The documentation link is set in `plugin.xml` line 12: `<url>https://agent-sh.github.io/agnix-docs/</url>` which was the old GitHub Pages site that was taken down when the docs were moved to the main repo's `docs/` folder, so the new URL is `https://agent-sh.github.io/agnix/docs/`.

Also, let's confirm the new correct URLs:
- Source Code: `https://github.com/agent-sh/agnix` (current org repo)
- Documentation: `https://agent-sh.github.io/agnix/docs/` (new live GitHub Pages URL for the docs)

Next, since this is a code/config change needed, we should create a feature branch, make the fix, then open a PR. Let's outline the steps:
1. Create a new branch from `main` named `fix/marketplace-links-1099`
2. Update the stale URLs in the relevant config files:
   a. In `build.gradle.kts`: replace the old `pluginRepositoryUrl` value with the current org repo URL
   b. In `plugin.xml`: replace the old `<url>` tag value with the new live documentation URL
3. Validate the changes: run the plugin build to make sure the config changes don't break the build, and confirm the new URLs are correct by checking they resolve.
4. Commit the changes with a clear commit message referencing the issue: `fix: update stale JetBrains Marketplace links for issue #1099`
5. Push the branch and open a PR against `main` with a description of the fix, linking back to issue #1099.
6. Also, if the plugin is already published with the old links, note that the fix will apply to the next marketplace release, and if an urgent hotfix is needed, we can trigger a patch release.

Wait also,