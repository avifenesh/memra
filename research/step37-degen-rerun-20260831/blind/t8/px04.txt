finish=length content_nonempty=False
---
Got it, let's tackle this GitHub issue step by step. First, the issue is about the JetBrains Marketplace listing for Agnix having two broken links: a 404 on the documentation link, and a stale source code repo URL.

First, I need to triage the issue first: it's a bug, not a question, so no need to ask for clarification. Next, I need to investigate the root cause. Let's start by checking the Agnix repo's current structure first, right? Wait, first, let's confirm the current state of the project:
1. First, check the existing JetBrains Marketplace listing metadata: that's usually in the `plugin.xml` file for JetBrains plugins, right? Or sometimes in the `gradle.properties` if it's a Gradle-based plugin, or the `publish` configuration.
2. First, find the documentation link: the 404 means the current URL is pointing to an old docs location. Let's check what the current docs URL is for Agnix—probably the GitHub Pages for the repo, or a dedicated docs site. Wait, the repo is agent-sh/agnix, so let's check if they have docs set up: maybe the old docs were at `https://agent-sh.github.io/agnix` or a separate docs repo, but now they're in the main repo's `/docs` folder, or moved to a new URL.
3. Then the source code link: the stale URL is probably an old repo, like maybe it was previously hosted under a different org or repo name, and now it's `https://github.com/agent-sh/agnix` which is the current one.

Next, implement the fix on a branch, as requested. Let's outline the steps:
1. First, create a new branch from main, e.g., `fix/jetbrains-marketplace-links`.
2. Locate the file that defines the JetBrains Marketplace listing metadata. For most JetBrains plugins built with Gradle, this is either:
   a. `src/main/resources/META-INF/plugin.xml` (the core plugin descriptor, which has `<idea-version>`, `<name>`, `<description>`, and also the `<vendor>` with links, or explicit `<url>` tags for docs/source)
   b. Or the `gradle.properties` file if using the `org.jetbrains.intellij` plugin's publish configuration, which has `pluginRepositoryUrl`, `pluginGroup`, etc., but more likely the links are in `plugin.xml` or the `publish` task config in `build.gradle.kts`/`build.gradle`.
3. Fix the documentation link: first confirm the correct, live docs URL. Let's check the agent-sh/agnix repo: if they have GitHub Pages enabled, the docs are likely at `https://agent-sh.github.io/agnix/` or if they use a docs folder, maybe `https://github.com/agent-sh/agnix/tree/main/docs` but no, the marketplace docs link should be the public docs site, not the repo folder. Wait, if there's no dedicated docs site, maybe the README is the main docs, but no, the issue says the docs link returns 404, so the current link is broken. Let's assume the current broken docs link is something like `https://agent-sh.github.io/agnix-docs` (old URL) and the correct one is `https://agent-sh.github.io/agnix/` (current GitHub Pages for the repo).
4. Fix the source code link: the stale URL is probably something like `https://github.com/old-org/agnix` or `https://github.com/agent-sh/agnix-old`, update it to the current canonical repo URL `https://github.com/agent-sh/agnix`.
5. Also, check if there are any other stale links in the plugin metadata, like the issue tracker link, to make sure they're all correct.
6. Then, validate the changes: run the plugin build to make sure the metadata is correctly packaged, no syntax errors in `plugin.xml`.
7. Then, clean up: make sure no dead code is added, only the two link fixes.
8. Then, summarize the changes: what was fixed, where, the new URLs, and that the branch is ready for PR.

Wait, also, need to make sure that the changes are minimal, no rewrites, just fix the two links. Also, if the plugin uses a separate `marketplace` config file, check that too, but most of the time it's in `plugin.xml`.

Also, let's confirm the triage: this is a straightforward bug, no questions needed, just fix the stale links in the plugin's marketplace metadata.

Wait, also, let's make sure to check if the documentation link is actually live first. Let's say we check `https://agent-sh.github