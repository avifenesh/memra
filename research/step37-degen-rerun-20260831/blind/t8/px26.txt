finish=length content_nonempty=False
---
Got it, let's tackle this GitHub bug report step by step. First, I need to follow the instructions: investigate, triage if it's a question, implement on a branch if code is needed, summarize.

First, let's start with triage. First, let's confirm what the issue is: the JetBrains Marketplace listing for Agnix has two broken links: 1) Documentation link returns 404, 2) Source Code link points to a stale repo URL.

First, step 1: Triage & Investigation
First, let's check the current state of the Agnix repo's marketplace configuration. Wait, first, let's confirm if this is a bug (not a question, it's a clear bug report of broken links). First, let's identify the root cause:
- First, the stale source code URL: likely the repo was moved or renamed, and the marketplace listing metadata still points to the old URL.
- The 404 documentation link: likely the docs were moved to a new path (e.g., from /docs to /wiki, or to a separate docs repo, or the URL was updated in the repo but not synced to the marketplace listing).

Wait, first, let's check the actual issue link: https://github.com/agent-sh/agnix/issues/1099. Let's first confirm the current repo structure: let's assume the Agnix repo is at https://github.com/agent-sh/agnix (current), and the stale URL was maybe https://github.com/agent-sh/agnix-legacy or something? Wait no, let's check the marketplace listing config: JetBrains Marketplace listings for plugins usually pull metadata from the plugin's `plugin.xml` or a separate `marketplace.yaml` file in the repo, right? Or if it's a Gradle-based plugin, the `gradle.properties` or the `publish` task configuration.

Wait, first, let's do the investigation properly:
1. First, check the current Agnix repo's plugin metadata files:
   - Look for `src/main/resources/META-INF/plugin.xml` (standard for JetBrains plugins)
   - Look for `marketplace.yaml` or `jetbrains-marketplace.yml` if present
   - Look at the `build.gradle.kts` or `pom.xml` for marketplace publish configuration that sets the source code and documentation URLs.

2. Check the current broken links:
   - First, test the existing documentation link from the marketplace: let's assume the current broken doc link is https://agent-sh.github.io/agnix/docs (404) and the correct one is https://agent-sh.github.io/agnix/ (or https://docs.agnix.dev, whatever the current correct URL is).
   - Test the existing source code link: assume the stale URL is https://github.com/agent-sh/agnix-old which returns 404, correct is https://github.com/agent-sh/agnix.

3. Check if there's a separate docs repo: maybe the docs were moved to https://github.com/agent-sh/agnix-docs, so the link should point there, or to the GitHub Pages site.

Then, next: is this a question? No, it's a clear bug report of broken links, so no triage as a question needed. Code change is needed: update the marketplace metadata in the repo to point to the correct URLs.

Then, implement on a branch: let's create a branch `fix/jetbrains-marketplace-links` from main, make the changes, then open a PR.

Wait, also, need to make sure that after updating the links, we validate them: test that the new doc link returns 200, the new source code link returns 200, and that the changes are in the correct metadata file that JetBrains Marketplace reads when syncing plugin listings.

Also, need to check if there are any other places where these stale URLs are referenced: like README.md, website, etc., but the bug is specifically about the marketplace listing, so focus on the metadata that controls the marketplace listing.

Wait, also, let's confirm: JetBrains Marketplace pulls the source code and documentation links from the plugin's `plugin.xml` `<idea-version>` or `<vendor>` tags? No, wait, actually, for plugins published via the JetBrains Marketplace, the links are set either:
1. In the `plugin.xml` under `<project>`? No, wait, no: when you publish a plugin via Gradle using the `org.jetbrains.intellij` plugin, you can set `marketplace` properties in `gradle.properties` or in the `publish` task:
   - `marketplace.documentationUrl`
   - `marketplace.sourceCodeUrl`
   Or if using the `plugin.xml`, there are `<url>` tags for documentation and source code? Wait no, let me check: actually, the standard is that the