finish=length content_nonempty=False
---
Got it, let's tackle this GitHub issue step by step. First, I need to understand what the bug is: the JetBrains Marketplace listing for the Agnix plugin has two broken links: the documentation link 404s, and the source code link points to a stale repo URL.

First, I should start by investigating the root cause. Let's break down what I need to check:
1. First, confirm the current state of the Agnix repo: where is the actual current repo hosted? The issue says the source code link is stale, so first check if the repo moved, or if the link in the marketplace listing is outdated.
2. Check the documentation: where is the current documentation hosted? Is it moved from the old URL that's 404ing?
3. Check the Agnix repo's own metadata: look at the README, any marketplace configuration files (like in .github/, or plugin.xml for JetBrains plugins, since Agnix is a JetBrains plugin right? Wait, Agnix is the agent tool for JetBrains IDEs, right? So the plugin's marketplace listing is configured either in the plugin's build config (like in build.gradle.kts or plugin.xml) or in the GitHub repo's release/marketplace settings.

Wait, first, let's do the investigation steps first, then triage, then implement the fix on a branch, then summarize.

First, investigation:
- Step 1: Check the current Agnix repository URL. The issue is from agent-sh/agnix, so that's the current repo? Wait the issue says the source code link references a stale URL. Let's check: maybe the old repo was under a different org/user, like maybe it was previously under a personal account before moving to agent-sh? Or maybe the link in the marketplace config points to an old GitHub URL like github.com/old-user/agnix instead of github.com/agent-sh/agnix.
- Step 2: Check the documentation link: the 404 is probably because the docs were moved from, say, old-org.github.io/agnix to agent-sh.github.io/agnix, or moved to a docs site like agnix.dev/docs, or moved to the repo's wiki, or to a /docs folder in the repo with GitHub Pages enabled.
- Step 3: Check the plugin's marketplace configuration: for JetBrains plugins, the marketplace listing metadata is usually defined in the plugin's build script (e.g., build.gradle.kts for Gradle-based plugins) or in a plugin.xml file, with tags like <vendor> and <url> for documentation and source code. Alternatively, if the listing is managed via the JetBrains Marketplace dashboard, the links are set there, but usually they're pulled from the repo's metadata if it's published via GitHub Actions.

Wait, let's confirm: first, let's look at the agent-sh/agnix repo's current structure. Let's assume we checked:
- The current source code URL is https://github.com/agent-sh/agnix, so the stale link is probably pointing to an old URL like https://github.com/agnix-dev/agnix or https://github.com/olduser/agnix.
- The current documentation is hosted at https://docs.agnix.dev (or https://agent-sh.github.io/agnix), and the old link was pointing to https://old-org.github.io/agnix which is now 404.

Next, triage: this is a straightforward bug, not a question. It's a configuration issue in the plugin's marketplace listing metadata, no code logic changes needed, just updating the URLs in the correct configuration file.

Then, implement the fix on a branch:
1. Create a new branch from main: fix/marketplace-links-1099
2. Locate the file that defines the marketplace listing metadata. For Agnix, which is a Gradle-based JetBrains plugin, this is in build.gradle.kts, in the `jetbrainsPlugin` block, where `documentationUrl` and `sourceCodeUrl` are set.
3. Update the `documentationUrl` from the old 404ing URL to the current docs URL (e.g., https://docs.agnix.dev)
4. Update the `sourceCodeUrl` from the stale repo URL to the current https://github.com/agent-sh/agnix
5. Also check if there are any other references to the old URLs in the repo (like README.md, plugin.xml, .github/release.yml) to make sure there are no other stale links, but the core fix is in the marketplace config.
6. Run the plugin build to make sure the config changes don't break the build (since it's just URL updates, no code changes, build should pass)
7. Commit the changes with a clear commit message: "fix: update stale marketplace documentation and source code URLs (closes #1099)"
8.