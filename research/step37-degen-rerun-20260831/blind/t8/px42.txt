finish=length content_nonempty=False
---
Got it, let's tackle this GitHub bug issue. First, I need to first understand what the bug is: the JetBrains Marketplace listing for the agnix plugin has two broken links: docs link 404, source code link points to a stale repo URL.

First step, triage: this is a confirmed bug, not a question, because it's a report of broken public-facing links in the plugin's marketplace metadata. No code fix needed for the plugin itself, right? Wait, wait—wait, where are these links stored? For JetBrains Marketplace plugins, the listing metadata (including docs and source code links) is usually set either in the plugin's build configuration (like in the Gradle/Maven build file for the plugin, or in the marketplace listing settings directly on the JetBrains Marketplace portal, or in the plugin's repository metadata like the plugin.xml or the marketplace descriptor file).

Wait first, let's confirm the context: the repo is agent-sh/agnix, which I assume is a JetBrains IDE plugin. First, let's check what the current links are, first. Let's first look at the issue #1099 to see what the reporter said, but since I can't access live GitHub, I'll base it on the bug description: docs link 404, source code is stale.

First, triage: This is a non-code metadata bug, no code changes required for the plugin runtime, but we need to fix the marketplace listing metadata. Wait, but sometimes the links are defined in the plugin's repository configuration, like in the build.gradle.kts or plugin.xml, or in the .github/marketplace.yml file? Wait no, JetBrains Marketplace has its own listing, but often the source links are pulled from the repository's README or the plugin's build config if it's auto-synced.

Wait first, let's outline the steps I would take, since I can't actually modify the repo directly, but I can outline the exact implementation, then create a PR, right? Wait the user said implement on a branch if code is needed. Wait, if the links are defined in the plugin's build configuration (common for JetBrains plugins built with Gradle), then we need to update those, then the next marketplace sync will pick up the new links. If they are set directly in the marketplace portal, then we need to update them there, but if the repo has a configuration file that's used to sync to the marketplace, we fix it there.

Wait first, let's do the triage first:
1. Triage: This is a confirmed bug report, not a question. The issue is broken public links in the JetBrains Marketplace listing for the agnix plugin, which impacts user trust and ability to access documentation and source code. No runtime code changes are needed, only metadata updates to the plugin's listing configuration.

Then, investigation:
First, check where the marketplace links are defined for the agnix plugin:
- First, check the plugin's build configuration (likely build.gradle.kts or build.gradle in the repo root, since most JetBrains plugins use Gradle) for the `marketplace` block, which often defines `documentationUrl` and `sourceCodeUrl` properties that are synced to the JetBrains Marketplace listing automatically.
- If no such config exists, check if there's a `.github/marketplace.yml` or similar sync config, or if the links are set manually in the JetBrains Marketplace portal.

Wait, let's assume the standard case for a Gradle-based JetBrains plugin: the build file has a marketplace section with the old URLs. Let's say the stale source code URL is pointing to an old repo like `https://github.com/old-org/agnix` instead of the current `https://github.com/agent-sh/agnix`, and the docs URL is pointing to an old docs site like `https://old-agnix-docs.net` which is now 404, and the current docs are at `https://agent-sh.github.io/agnix-docs/` or in the repo's README/wiki.

Then, implementation steps:
1. Create a new branch from main, e.g. `fix/marketplace-links-1099`
2. Update the plugin's build configuration (e.g. `build.gradle.kts`) to set the correct URLs:
   - `documentationUrl` to the current valid docs URL (either the GitHub Pages site, or the repo's README if that's the official docs, or the correct docs domain)
   - `sourceCodeUrl` to the current canonical repo URL `https://github.com/agent-sh/agnix`
3. If there's a separate marketplace sync config file (like `.github/marketplace.yml`), update the links there too if present.
4. Check for any other references to the old stale URLs in the repo (like README.md, CONTRIBUT