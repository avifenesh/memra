finish=length content_nonempty=False
---
Got it, let's tackle this GitHub bug. First, I need to understand what the issue is: the JetBrains Marketplace listing for Agnix has two broken links: docs 404, source code points to a stale repo URL.

First step: Investigate the issue. Let's start by checking the Agnix repo's current structure, find where the JetBrains plugin metadata is stored (usually it's in a plugin.xml, or a marketplace-specific config file, or the GitHub repo's README that's pulled for the marketplace, or the plugin's build configuration that sets the links for the marketplace listing).

Wait, first, let's confirm the current state of the repo. Let's assume the Agnix repo is at https://github.com/agent-sh/agnix. First, check the plugin's manifest: for JetBrains plugins, the marketplace links are usually set in the `plugin.xml` under `<idea-plugin>` tags, specifically `<vendor>` with `<url>` for source code, and there's a separate documentation link field, or it's pulled from the repo's README, or set in the `build.gradle.kts`/`pom.xml` for the plugin build.

Wait, also, the issue says the source code link is stale. Let's check: maybe the repo was renamed? Or the old URL was something like `https://github.com/agent-sh/agnix-old` and now it's the current one? Also the docs link: maybe the docs were moved from `https://agent-sh.github.io/agnix` to `https://docs.agnix.sh` or something, or the old docs folder was removed.

First, triage: this is a confirmed bug, not a question. It's a broken marketplace listing that affects user trust and ability to find docs/source. Code change is needed to update the links in the plugin's marketplace metadata.

Next, implement on a branch. Let's create a branch `fix/jetbrains-marketplace-links` from main. Then, find where the links are defined. Let's say the links are in `build.gradle.kts` under the `jetbrains` plugin configuration, or in `plugin.xml`:

Wait, typical JetBrains plugin metadata for marketplace links:
In `plugin.xml`, you might have:
```xml
<idea-plugin>
  ...
  <vendor>
    <url>https://github.com/agent-sh/old-agnix-repo</url> <!-- stale -->
  </vendor>
  <description>
    ... documentation at https://agent-sh.github.io/agnix/old-docs ... <!-- 404 -->
  </description>
</idea-plugin>
```
Or in Gradle, the `jetbrains` plugin extension sets these:
```kotlin
jetbrains {
  marketplace {
    documentationUrl = "https://agent-sh.github.io/agnix/old-docs" // 404
    sourceCodeUrl = "https://github.com/agent-sh/old-agnix-repo" // stale
  }
}
```

So first, update those URLs to the correct ones:
- Source code URL: update to the current repo URL `https://github.com/agent-sh/agnix`
- Documentation link: update to the current docs URL, let's say the repo now has docs in the `docs/` folder deployed to GitHub Pages at `https://agent-sh.github.io/agnix/`, or if there's a separate docs site, use that. Wait, also check if the repo's README has the correct links, because sometimes the marketplace pulls the description from the README, so we need to update the README links too if they're stale.

Also, verify that the new links work: test that the source code URL redirects correctly to the repo, test that the docs URL returns 200, not 404.

Then, after making the changes, run the plugin build to make sure there are no errors from the updated config. Then commit the changes to the branch, push it, and open a PR against main with a clear description of the fix.

Wait, also, check if there are any other places where the stale URLs are referenced: like in the plugin's about page, in the release notes, in the repo's README, etc. Clean those up too if needed, but the core fix is the marketplace metadata.

Also, let's make sure we don't break anything: the change is only updating two URL strings, so no functional code changes, just metadata, so low risk.

Then, summarize what was done: investigated the root cause (stale URLs in plugin marketplace metadata and repo README), created a fix branch, updated the source code and documentation links to the current correct URLs, validated the new links return 200, pushed the branch and opened a PR, no functional changes so no test regressions.

Wait, also, let's make sure to address the exact issue: the two broken links. Let's confirm the root