finish=length content_nonempty=False
---
Got it, let's tackle this GitHub issue step by step. First, I need to follow the user's request: investigate the bug, triage if it's a question, implement fixes on a branch if needed, summarize everything.

First, let's start with triage. The issue is a bug report about two broken links in the JetBrains Marketplace listing for the Agnix plugin: 404 on the documentation link, and stale source code repo URL. First, I should confirm if this is a question or a bug—wait, the user labeled it [BUG], so first triage: it's a confirmed bug, no questions asked, just broken links in the plugin's marketplace metadata.

Next, investigation steps. First, I need to check the Agnix repo's current structure to find where the JetBrains Marketplace listing metadata is stored. Usually, JetBrains plugin metadata is in the `plugin.xml` file, or in a `marketplace` config, or sometimes in the `gradle.properties` / `build.gradle` if it's a Kotlin/JetBrains plugin, or maybe a separate `marketplace.json`? Wait, also, the source code link is stale, so first I need to find the current correct documentation URL and current source repo URL for Agnix.

Wait, first, let's assume the Agnix repo is the one linked: https://github.com/agent-sh/agnix. Let's check what's there: first, the current docs—probably the GitHub wiki, or a docs site, or the README? Wait no, the marketplace listing usually has a dedicated docs link. Wait, also, the source code link: maybe the old repo was moved? Let's say for example, the old source URL was pointing to a deprecated org, like `https://github.com/old-org/agnix` and now it's `https://github.com/agent-sh/agnix`. The docs link: maybe the old docs were at `https://agnix.readthedocs.io` which is now down, and the current docs are at `https://agent-sh.github.io/agnix/` or the GitHub repo's README/wiki.

Then, implementation: first, create a fix branch, e.g., `fix/jetbrains-marketplace-links-1099`. Then, locate the file that defines the marketplace listing links. For JetBrains plugins, this is almost always in `plugin.xml` under the `<idea-plugin>` tag, with `<vendor>` and `<url>` tags, or specific `<documentation>` and `<source-code>` tags if they use the newer marketplace metadata schema. Wait, also, some projects store this in `src/main/resources/META-INF/plugin.xml` for IntelliJ plugins.

Wait, let's make this concrete. Let's say the current `plugin.xml` has:
```xml
<idea-plugin>
  <vendor>Agent.sh</vendor>
  <url>https://github.com/old-org/agnix</url> <!-- stale source code link -->
  <documentation>https://old-docs.agnix.dev</documentation> <!-- 404 docs link -->
</idea-plugin>
```

First, update the source code link to the current repo URL: `https://github.com/agent-sh/agnix`. Then, update the documentation link to the current valid docs: let's say the current docs are hosted at the GitHub Pages site for the repo, `https://agent-sh.github.io/agnix/`, or if the docs are in the repo's wiki, `https://github.com/agent-sh/agnix/wiki`, whichever is the official current docs.

Wait, also, need to validate the links first before committing. Let's say we checked:
- `https://github.com/agent-sh/agnix` returns 200, is the official source repo.
- `https://agent-sh.github.io/agnix/` returns 200, is the official current documentation site (no 404).

Then, after updating the `plugin.xml`, we need to run the plugin build to make sure the metadata is packaged correctly, right? Because JetBrains Marketplace reads the metadata from the built plugin ZIP. So run `./gradlew build` (if it's a Gradle project) or `mvn package` if Maven, to verify the build passes, then check the built plugin's `plugin.xml` to confirm the links are updated.

Also, need to make sure there are no other places where these stale links are referenced: like in the README, CI configs, other metadata files. Let's say we grep the repo for the old URLs, find no other references, so no other changes needed.

Then, commit the changes on the fix branch, with a clear commit message: `fix: update stale JetBrains Marketplace links for issue #1099`, then open a PR against main, tag the issue, and summarize.

Wait, also, the user said