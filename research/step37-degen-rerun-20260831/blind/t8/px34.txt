finish=length content_nonempty=False
---
Got it, let's tackle this GitHub bug issue. First, I need to start by investigating the reported bug: the JetBrains Marketplace listing for Agnix has a broken documentation link (404) and a stale source code repo URL.

First step: Triage the issue first. It's a clear bug, not a question, so no need to ask for more info. The root cause is almost certainly that the Agnix project moved its docs and/or source repo, and the JetBrains Marketplace listing metadata wasn't updated to match.

Wait, first, let's confirm what the current correct URLs are for Agnix, right? Let's assume (since I can check public info) that Agnix's current official docs are at https://agnix.sh/docs and the source repo is now at https://github.com/agent-sh/agnix-core instead of the old stale one, right? Wait no, wait the issue is from agent-sh/agnix repo, so let's make that accurate.

First, I'll create a fix branch off main, let's call it fix/marketplace-listing-links. Then, what files need to be changed? The JetBrains Marketplace listing metadata for Agnix is usually stored in the plugin's build configuration, right? For a JetBrains plugin, that's typically in the `plugin.xml` file, or sometimes in the `gradle.properties` if it's a Gradle-based plugin, or in the `pom.xml` for Maven. Wait, Agnix is a Kotlin/JetBrains plugin, so likely Gradle.

Wait, also, sometimes the Marketplace listing has a separate metadata file, but no—usually the links are pulled from the plugin's manifest (plugin.xml) or the repository's README that's synced to the Marketplace? No, wait no, the Marketplace listing has editable fields: Documentation URL, Source Code URL, etc. But if the project is using GitHub's automatic sync for the Marketplace listing, those fields are pulled from the repository's `README.md` or the plugin's manifest. Wait, but the bug says the links are stale, so either the manifest has old URLs, or the repo's metadata wasn't updated, or the Marketplace listing's custom fields are outdated.

Wait, let's structure this properly:

1. First, Triage: Confirm this is a confirmed bug, not a question. No additional info needed from the reporter. Root cause: The Agnix plugin's JetBrains Marketplace listing has hardcoded/unsynced stale URLs for documentation and source code, likely from a previous repository/docs host migration.

2. Then, create the fix branch: `git checkout -b fix/marketplace-listing-links main`

3. Then, identify the files to update:
   a. First, check the plugin's manifest file (usually `src/main/resources/META-INF/plugin.xml` for JetBrains plugins) for the `<idea-plugin>` tags that set the documentation and source code URLs. Those are usually `<url>` tags? Wait no, the Marketplace uses specific fields: the `plugin.xml` has `<vendor>` with `<url>` for the vendor site, but the documentation and source code links for the Marketplace listing are either pulled from the repository's GitHub metadata (if the repo is linked to the Marketplace plugin page) or set in the plugin's build configuration (e.g., `gradle.properties` has `pluginDocumentationUrl` and `pluginSourceCodeUrl` properties for the Gradle JetBrains plugin build plugin).
   b. Also, check the repository's root `README.md` to make sure the links there are correct, since the Marketplace often syncs those if auto-sync is enabled.
   c. Also, check if there's a separate `marketplace-metadata.json` or similar if the project uses custom Marketplace sync.

Wait, let's make this concrete. Let's say we find that in `gradle.properties`, the old URLs are set:
Old values:
```
pluginDocumentationUrl=https://old-agnix-docs.netlify.app
pluginSourceCodeUrl=https://github.com/agent-sh/old-agnix-repo
```
We need to update those to the current correct URLs:
```
pluginDocumentationUrl=https://agnix.sh/docs
pluginSourceCodeUrl=https://github.com/agent-sh/agnix
```
Wait, but also, if the Marketplace listing has custom overrides, we need to update those too, but since we're working in the repo, we can only update the source of truth for the links, which is the plugin's build config and README.

4. Then, also check the `README.md` at the root of the repo: make sure the badges and links to docs and source code are correct, no stale URLs there.

5. Then, run the project's build to verify nothing breaks: since it's a Gradle project, run `./gradlew build` (or `gradlew.bat build`