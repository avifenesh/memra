finish=length content_nonempty=False
---
Got it, let's tackle this GitHub issue step by step. First, I need to understand what the bug is: the JetBrains Marketplace listing for Agnix has two broken links: documentation link 404s, and source code link points to a stale repo URL.

First, let's start with triage: this is a clear bug report, not a question, so no need to ask for clarification first. The root cause is almost certainly that the repository was moved/renamed, or the docs were moved to a new location, and the marketplace metadata wasn't updated.

First step: I need to check the current Agnix repo structure first, right? Wait, the issue is in agent-sh/agnix, so first let's confirm the current valid URLs:
1. First, check what the current source code repo URL is. Let's see, agent-sh/agnix is the repo, so the correct source code link should be https://github.com/agent-sh/agnix, right? Wait, maybe the old link was pointing to a different org or repo name, like maybe it was under a different user before, or a fork?
2. Then check the documentation: where is the current docs hosted? Let's see, if the repo has a docs folder, or uses GitHub Pages, or maybe it's hosted on a separate docs site? Wait, let's check: if the repo has a README with docs link, or a docs directory, or maybe it's on https://agent-sh.github.io/agnix/ or something? Wait no, wait let's first verify the current state of the repo to confirm the correct URLs.

Wait, first, let's outline the steps I would take, since I can't actually access GitHub directly, but I can outline the exact implementation, then create a branch, make the changes, test, then summarize.

Wait, first, triage: This is a confirmed bug, no clarification needed. The issue is that the JetBrains Marketplace plugin listing metadata has two outdated links:
- Stale source code URL (points to an old/removed repo)
- Documentation URL that returns 404 (points to a moved/deleted docs location)

First, let's confirm the correct current URLs by inspecting the agent-sh/agnix repository:
1. Source code: The canonical source is https://github.com/agent-sh/agnix (the repo the issue is filed in, so that's the correct source link, the old one was probably pointing to a previous location like a personal fork or old org)
2. Documentation: Let's check the repo's README first—wait, if the repo uses GitHub Pages, the docs would be at https://agent-sh.github.io/agnix/, but if not, maybe the docs are in the repo's /docs folder, or hosted on a separate site? Wait, alternatively, if the previous docs were on a different domain, let's check: wait, maybe the old docs link was https://agnix.agent-sh.dev/docs or something? No, wait, let's assume that first we check the repo's existing links: if the README has a docs link that works, that's the correct one. Wait, alternatively, if the project's docs are now hosted at https://docs.agnix.dev/ or whatever, but let's make sure we get the correct live URLs first.

Wait, but first, the JetBrains Marketplace listing metadata is usually stored in the plugin's build configuration, right? For a Gradle-based JetBrains plugin (which Agnix is, I assume, since it's a JetBrains Marketplace plugin), the marketplace metadata is defined in the build.gradle.kts or build.gradle file, under the `pluginXml` or `publish` configuration, specifically the `<idea-plugin>` XML's `<vendor>` and `<url>` tags, or the `gradle-intellij-plugin`'s `publishPlugin` block parameters like `website`, `repository`, `description` etc.

Wait, right! For JetBrains plugins built with the Gradle IntelliJ Plugin, the marketplace listing fields are set in the build script, so that's where we need to make the changes.

So first step: Create a fix branch from main, e.g. `fix/marketplace-links-1099`

Then, first, verify the correct URLs:
1. Source code repository: Confirm that https://github.com/agent-sh/agnix is the active, public repo (it is, since the issue is filed there). The old stale link was probably something like https://github.com/old-org/agnix or https://bitbucket.org/.../agnix which is no longer accessible.
2. Documentation link: Let's check the repo's README.md—wait, let's say the current valid docs link is either:
   a. If using GitHub Pages: https://agent-sh.github.io/agnix/
   b. Or if docs are in the repo: