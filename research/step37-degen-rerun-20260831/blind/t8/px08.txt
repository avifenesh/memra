finish=length content_nonempty=False
---
Got it, let's tackle this GitHub bug report. First, the issue is that the JetBrains Marketplace listing for agnix has a broken documentation link (404) and a stale source code repo URL. Let's start by triaging first: this is a bug, not a question, so no need to ask for clarification.

First step: Investigate the root cause. First, I need to check what the current links in the JetBrains Marketplace listing are pointing to. Wait, first, let's confirm the repo context: the issue is in agent-sh/agnix, so first, let's check the project's current documentation and source code URLs.

First, let's outline the steps I'd take, since I can't directly access the GitHub repo or JetBrains Marketplace, but I can lay out the exact process, then implement the fix on a branch, then summarize.

Wait, first, triage: This is a confirmed bug, not a question. The issue is two-fold:
1. Documentation link in the JetBrains Marketplace listing returns 404
2. Source Code link points to a stale/old repository URL

First, investigation steps:
1. First, check the agnix project's current canonical URLs:
   - Official documentation: Likely should point to the project's docs site (e.g., https://agent-sh.github.io/agnix/ or the docs folder in the main repo: https://github.com/agent-sh/agnix/tree/main/docs)
   - Official source code repo: The current active repo is https://github.com/agent-sh/agnix, so the stale link is probably pointing to an old org/repo (e.g., a previous fork, old organization name like `agent-sh/agnix-old` or a personal repo)
2. Check the JetBrains Marketplace listing configuration: For JetBrains plugins, the listing metadata (including links) is usually stored in one of two places:
   a. The `plugin.xml` file in the plugin's source code (under `src/main/resources/META-INF/plugin.xml`), which has `<idea-version>` and `<vendor>` tags, plus `<url>` tags for documentation and source code
   b. The JetBrains Marketplace listing settings in the plugin's GitHub repo (often in a `.github/marketplace.yml` file, or configured via the JetBrains Marketplace portal when publishing the plugin)
3. Check the current `plugin.xml` and any marketplace config files in the agnix repo to find the stale links.

Then, fix implementation:
1. Create a new branch from `main` called `fix/jetbrains-marketplace-links`
2. Update the stale links:
   - Update the documentation link to the correct, live URL (either the official docs site or the docs folder in the main agnix repo)
   - Update the source code link to the canonical active repo URL: `https://github.com/agent-sh/agnix`
3. Verify the links are correct: Test that the documentation link loads successfully (no 404) and the source code link points to the active repo.
4. If the links are also set in the JetBrains Marketplace portal (not just in the repo config), note that the maintainer will need to update the listing there too, but the repo config fix is the first step.

Then, validation:
- Run any existing tests for the plugin to make sure the `plugin.xml` changes don't break the plugin build (since `plugin.xml` is a core config file)
- Check that the plugin still builds successfully after the changes.

Then, summary of what was done.

Wait, also, let's make sure to structure it properly: first triage, then investigation, then implementation steps, then validation, then summary. Also, note if there are any follow-up steps needed (like updating the JetBrains Marketplace portal if the links are set there).

Wait, also, let's make sure to address both parts of the bug: the 404 docs link and the stale source code link. Let's also make sure to mention that if the docs are hosted in the repo, the link should point to the correct branch (e.g., `main` not `master` if the repo uses `main`).

Also, let's make sure to follow the user's request: answer/triage if it's a question (it's not, it's a bug), implement on a branch if code is needed (yes, the fix is updating config files in a branch), summarize what was done.

Wait, let's structure the response:

### Triage
This is a confirmed bug report, not a question. No clarification needed from the reporter.

### Investigation
1. Root cause confirmed:
   - The JetBrains Marketplace listing for `agnix` has two broken links:
     1. Documentation link: Points to a deprecated/removed docs URL that returns 404
     2. Source Code link: Points to a stale