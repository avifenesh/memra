ANON=nx19 TURN=8 CHARS=4406 FINISH=length REPEAT60=1
======================================================================
Got it, let's break down what the user is asking here. First, they want me to take care of a GitHub task related to JetBrains Marketplace listing, documentation link returning 404, source code link issues, stale repository URL, and references to GitHub.com/agent-sh/agnix/issues/1099. Also, they mention investigating, handling, answering, implementing on a branch, and summarizing.

First, I need to clarify the steps here. Let's start by outlining the process to address this GitHub task. First, I should note that I don't have direct access to the GitHub repo, but I can outline the exact steps to resolve these issues, provide concrete fixes, and a summary.

First, let's list the issues identified:
1. JetBrains Marketplace listing has a documentation link returning 404.
2. Source code link references a stale repository URL (github.com/agent-sh/agnix/issues/1099).
3. Need to investigate the issue, handle the answer, implement fixes on a branch, and summarize.

First, step 1: Investigate the root cause. Let's break that down:
- Check the JetBrains Marketplace listing's documentation link: likely points to an old docs URL that's no longer valid (e.g., moved from GitHub Pages to a new docs site, or the repo was renamed).
- Check the source code link: the URL points to an issue (issues/1099) instead of the repository root, or the repository was renamed/moved, so the URL is stale.
- Verify the current repository URL: check if the repo is still at github.com/agent-sh/agnix, or if it was renamed (e.g., to a new name) or moved to an organization.

Step 2: Fix the stale links. Concrete steps:
a. For the JetBrains Marketplace listing:
   - Log in to the JetBrains Marketplace developer portal.
   - Navigate to the plugin listing (for the relevant plugin, e.g., the one associated with this repo).
   - Update the "Documentation" link to the current valid docs URL (e.g., if docs are now at docs.agent.sh, or the repo's GitHub Pages URL if still using that).
   - Update the "Source Code" link to the current repository root URL (not an issue link) — e.g., if the repo is still at github.com/agent-sh/agnix, use https://github.com/agent-sh/agnix, not the issues/1099 link.
b. For the repository itself:
   - If the repository was renamed, update all internal links in README.md, docs, and any other files that reference the old URL.
   - If the issue link (issues/1099) was mistakenly used as the source code link, replace it with the repo root URL.

Step 3: Implement fixes on a branch (as requested):
- Create a new branch from main (e.g., fix/marketplace-links-1099) to avoid breaking the main branch.
- Make the changes to the repository files (README.md, docs, etc.) to update stale links.
- Commit the changes with a clear commit message (e.g., "fix: update stale repository and documentation links for JetBrains Marketplace listing (ref #1099)").
- Push the branch to GitHub, then create a pull request to merge into main, with a description of the changes and reference to issue #1099.

Step 4: Verify the fixes:
- Check that the documentation link no longer returns 404 by visiting the updated URL.
- Check that the source code link points to the correct repository root.
- If the JetBrains Marketplace listing updates are live, verify the links there as well.

Step 5: Summarize the changes:
- List all updated links, the branch used, the pull request created, and verification steps taken.

Wait, but the user mentioned "answer-triage if it is a question, implement on a branch if code is needed, summarize what you did." So also, if this is a triage question, we need to confirm the issue: the 404 is due to stale links, the fix is updating the links to the current repo and docs URLs.

Also, the user mentioned "stale repository URL" — so maybe the repo was moved, so we need to check the current URL. For example, if the repo was renamed from agnix to something else, we need to update all references.

Wait, also, the user said "Source Code link references stale repository URL — https://github.com/agent-sh/agnix/issues/1099" — that's a link to an issue, not the repo, so that's definitely wrong. The source code link should point to the repo root, not an issue.

So putting this all together, the response should outline the investigation steps, the concrete fixes, the branch implementation, and the summary. Also, note that if the user provides access to the repo or the Jet