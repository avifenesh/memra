ANON=mx07 TURN=8 CHARS=4442 FINISH=length REPEAT60=1
======================================================================
Got it, let's tackle this. First, the user is asking to handle a GitHub task related to a bug in the JetBrains Marketplace listing, where documentation links return 404, source code links reference a stale repository URL, and there's an issue number 1099 on the agent-sh/agnix repo. Also, there's a mention of implementing a branch, code needs, and summarizing work done.

First, I need to break down the problem:
1. The bug is that JetBrains Marketplace listing has broken links: docs 404, source code points to a stale repo URL.
2. The related issue is agent-sh/agnix#1099.
3. Need to investigate, handle the answer/triage, implement a branch, address code needs, summarize work.

First, step 1: Investigate the issue. Let's start by checking the issue on GitHub (agent-sh/agnix#1099) to understand the reported problem. Then, check the current JetBrains Marketplace listing to see the broken links.

Wait, but since I can't access external links directly, I'll outline the steps to take, and then provide a concrete fix plan, plus a summary of what would be done.

First, investigation steps:
- Open issue agent-sh/agnix#1099 to read the full report: confirm the exact broken links, what the correct URLs should be, any context about the stale repo (maybe the repo was moved, renamed, or the URL was updated).
- Check the current JetBrains Marketplace listing for the plugin/extension in question: navigate to the listing, inspect the "Documentation" and "Source Code" links to confirm they return 404 or point to the stale URL.
- Check the repository's current state: confirm the correct repository URL (maybe it's now under a different org or name, e.g., moved from agent-sh/agnix to a new repo, or the source code is in a subdirectory).
- Check if there's a branch already created for this fix, or if we need to create a new one.

Next, triage:
- Confirm the bug is valid: yes, links are broken.
- Determine the fix: update the documentation link to the correct URL (maybe the docs are now in the repo's wiki, or a separate docs site), update the source code link to the current correct repository URL.
- Check if there are other related issues: maybe other links in the listing are also broken, or other plugins have the same problem.

Then, implementation:
- Create a new branch from the latest main branch, e.g., `fix/jetbrains-marketplace-links-1099`.
- Locate the file that configures the JetBrains Marketplace listing: this is usually in the plugin's `build.gradle.kts` or `plugin.xml` file, or in a separate `marketplace` config file, depending on the project structure. For JetBrains plugins, the listing metadata is often in the `gradle.properties` or a `META-INF/plugin.xml` file, or in the `build.gradle.kts` under the `jetbrains` plugin configuration.
- Update the documentation URL to the correct one: e.g., if the docs are now at `https://agent-sh.github.io/agnix/docs/`, update the `documentationUrl` field.
- Update the source code URL to the correct repository URL: e.g., if the repo is now at `https://github.com/agent-sh/agnix-new`, update the `sourceCodeUrl` field.
- Also, check if the issue number is referenced in the changelog or release notes, and update if needed.

Then, validation:
- Build the plugin locally to ensure the changes don't break the build.
- Test the updated links: open the new URLs to confirm they work, point to the correct content.
- If possible, update the JetBrains Marketplace listing via the JetBrains Marketplace portal (if the project has access) to reflect the new URLs, or submit a PR to the repository that configures the listing, so the next release will have the correct links.

Then, cleanup:
- Remove any dead code or temporary files created during the fix.
- Commit the changes with a clear commit message: `fix: update JetBrains Marketplace links for issue #1099`.
- Push the branch and open a PR against the main branch, referencing issue #1099.

Then, summarize the work done:
- Investigated issue #1099, confirmed broken documentation and source code links in the JetBrains Marketplace listing.
- Created a fix branch, updated the relevant configuration files to point to the correct URLs.
- Validated the links work, built the plugin successfully.
- Opened a PR with the fix, ready for review and merge.

Wait, but the user mentioned "answer/triage if it is a question, implement on a branch if code is needed, summarize what you did." So I need to structure the response