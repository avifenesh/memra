ANON=mx08 TURN=8 CHARS=4488 FINISH=length REPEAT60=1
======================================================================
Got it, let's tackle this step by step. First, the user is asking to handle a GitHub task related to a bug in JetBrains Marketplace listing, documentation link returning 404, stale repository URL, etc. First, I need to break down the problem.

First, let's parse the user's request: The GitHub task involves issues with JetBrains Marketplace listing where the documentation link returns 404, the source code link references a stale repository URL (https://github.com/agent-sh/agnix/issues/1099). We need to investigate, handle the answer/triage, implement on a branch if code is needed, summarize what was done.

First, let's start by outlining the steps to resolve this:

1. **Initial Investigation & Triage**:
   - First, check the current JetBrains Marketplace listing for the plugin/extension in question.
   - Verify the documentation link: navigate to the provided URL, confirm it's returning 404.
   - Check the source code link: confirm it's pointing to the stale `https://github.com/agent-sh/agnix/issues/1099` URL, which is likely outdated.
   - Identify the correct, current repository URL for the project. If the project has moved, find the new canonical repo.
   - Check if the documentation has moved to a new location (e.g., new repo's wiki, GitHub Pages, separate docs site).

2. **Determine Fix Scope**:
   - If the issue is only in the Marketplace listing metadata (no code changes needed), we can update the listing directly via JetBrains Marketplace dashboard (if we have maintainer access) or submit a PR to the repository that manages the Marketplace listing (if it's open-source).
   - If the source code link is stale, update it to the current repository URL.
   - If the documentation link is stale, update it to the current documentation location.
   - If code changes are needed (e.g., if the listing metadata is stored in a config file in the repo), create a dedicated branch for the fix.

3. **Implementation**:
   - If using a branch: create a new branch (e.g., `fix/marketplace-listing-links`) from the main branch.
   - Update the relevant files: either the Marketplace listing config (e.g., `marketplace.json`, `plugin.xml` for JetBrains plugins) or the repository's README/metadata files that feed into the listing.
   - Update the documentation link to the correct, working URL.
   - Update the source code link to the current, active repository URL (replace the stale `https://github.com/agent-sh/agnix/issues/1099` with the correct repo URL).
   - If the documentation is missing entirely, create or update the documentation in the repo (e.g., update the wiki, add a `docs/` folder, or enable GitHub Pages) to ensure the link is valid.

4. **Verification**:
   - Test the updated links: confirm the documentation link no longer returns 404, the source code link points to the correct repo.
   - If updating the Marketplace listing directly, verify the changes are reflected in the public listing.
   - If submitting a PR, ensure all CI checks pass, and the changes are reviewed and merged.

5. **Summary**:
   - Document the triage findings: confirmed 404 on documentation link, stale source code URL pointing to `https://github.com/agent-sh/agnix/issues/1099`.
   - Document the fix: updated links to current repository and documentation, created PR/branch if needed, verified links are working.
   - Note any follow-up actions: if the documentation was missing, created new docs; if the repo was moved, updated all references to the new URL.

Wait, but the user mentioned "answer/triage if it is a question, implement on a branch if code is needed, summarize what you did." Also, the user's message has some typos: "triage if it is a question" probably "triage the issue", "implement on a branch if code is needed", "summarize what you did".

Also, the user mentioned "JetBrains Marketplace listing" — so this is likely for a JetBrains plugin. JetBrains Marketplace listings can be managed via the [JetBrains Marketplace Dashboard](https://plugins.jetbrains.com/plugin/list) if you're a maintainer, or via a repository if the listing is auto-generated from repo metadata.

Also, the stale URL is `https://github.com/agent-sh/agnix/issues/1099` — that's an issue link, not a repo link, so that's definitely stale. The correct repo URL is probably `https://github.com/agent-sh/agnix` (without the `/issues/1099` part), but we need to confirm if that repo is still active, or if it's moved to a new location.

Also, the documentation link returning 404