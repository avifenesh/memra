ANON=mx02 TURN=8 CHARS=4550 FINISH=length REPEAT60=1
======================================================================
got it, let's tackle this step by step. First, the user wants me to handle a GitHub task related to a bug in JetBrains Marketplace listing, documentation link returning 404, stale repository URL, etc. First, I need to break down the requirements:

1. First, understand the problem: There's a bug (issue #1099) on GitHub related to JetBrains Marketplace listing where documentation links return 404, source code links reference stale repository URLs (https://github.com/agent-sh/agnix/issues/1099). The task is to investigate, handle the answer/triage, implement on a branch, and summarize what was done.

Wait, first, let's parse the user's request correctly. The user said: "Take care of this GitHub task: [Bug] JetBrains Marketplace listing. Documentation link returns 404, Source Code link references stale repository URL — https://github.com/agent-sh/agnix/issues/1099. Investigate, handle it, answer triage, implement on a branch, code if needed, and summarize what you did."

First, I need to outline the steps I would take to resolve this GitHub issue, since I don't have direct access to the repo, but I can provide a structured plan, then a sample implementation, and a summary.

First, step 1: Investigate the issue. Let's break down the bug report:
- Symptom 1: Documentation link in JetBrains Marketplace listing returns 404.
- Symptom 2: Source Code link references a stale repository URL (the link points to https://github.com/agent-sh/agnix/issues/1099, which is probably outdated).
- Root cause possibilities:
  a. The documentation URL in the JetBrains Marketplace listing metadata is incorrect or points to a moved/deleted resource.
  b. The source code URL in the repository metadata (like README, package.json, or the JetBrains plugin descriptor) is outdated, pointing to an old repo or issue tracker.
  c. The repository was renamed or moved, and all references weren't updated.

Step 2: Triage the issue. First, check the current state of the repo:
- Verify if the documentation link in the JetBrains Marketplace listing is indeed broken (try accessing it).
- Check the source code URL in the repo's metadata (e.g., README.md, plugin.xml, build.gradle, package.json) to see if it's pointing to the correct repo.
- Check if the repository was moved: https://github.com/agent-sh/agnix — is this the correct current repo? Or was it moved to a different org/user?

Step 3: Implement fixes on a branch. Let's outline the fixes:
a. Fix the documentation link: Update the JetBrains Marketplace listing metadata to point to the correct documentation URL (e.g., if docs were moved to a new location, update the link in the plugin's descriptor or the marketplace listing settings).
b. Fix the stale source code link: Update all references to the old repository URL to the new correct one. For example, if the repo was moved from agent-sh/agnix to a new location, update the source code link in the README, plugin descriptor, and any other metadata files.
c. If the issue is that the documentation was deleted, restore it or update the link to point to the correct location.

Step 4: Test the fixes. After making changes on a branch:
- Verify that the documentation link no longer returns 404.
- Verify that the source code link points to the correct, active repository.
- If applicable, update the JetBrains Marketplace listing with the new metadata.

Step 5: Summarize the changes. Provide a clear summary of what was done, including the root cause, fixes applied, and verification steps.

Now, since the user mentioned "implement on a branch, code if needed", I should provide a sample of what the code changes might look like. For example, if the stale URL is in a README.md file, the fix would be to replace the old URL with the new one. If it's in a plugin descriptor (like plugin.xml for JetBrains plugins), update the <url> or <source-code> tag.

Also, the user mentioned "answer triage" — so I should provide a triage assessment: priority (high, since it's affecting marketplace listing), affected components (documentation link, source code link), root cause (stale URL references), and proposed fix.

Wait, but let's make sure to structure this properly. Let's start with the triage, then investigation steps, then implementation, then summary.

Also, the user said "take care of this GitHub task" — so I should present this as a complete plan that can be executed, with concrete steps and sample code changes.

Wait, but let's check if there's any ambiguity. The bug report link is https://github.com/agent-sh/agnix/issues/1099