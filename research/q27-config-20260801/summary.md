# q27 shipping config — mirror-budget matrix on the board shape (2026-08-01)

Micro-sprint decision run: pick the SHIPPING config for the q27 board row from the
round-49 mirror-budget matrix. Box: <bench-instance> H100 (<h100-box-ip>), GPU 3
dedicated, tree = ~/arc4 (q4k-f16-mirrors lane binary, 49b: Q4_K+Q5_K f16 mirrors +
KQRP). Model: Qwen3.6-27B-Q4_K_M.gguf (NVMe, page-cached — 17s wall per full run).

## Method

Board shape exactly: `MEMRA_PROMPT_FILE=research/e2e/prompts/board-2048.txt` (2048
tokens), `MEMRA_NGEN=512`, `MEMRA_SPEC_K=3 MEMRA_SPEC_HPOST=1 MEMRA_SPEC_PMIN=0.3`.
One run-spec invocation per data point gives the real (prefill prime, plain decode,
spec decode) triple. Fresh process per run; nvidia-smi VRAM guard between configs
(vram-guard.log: 0 MiB before/after every run — clean teardown, no residue).
Phase 1 = one run per config; runs proved cheap (~17s), so N=3 was completed on ALL
three configs (interleaved A,B,C x3, same-session, ~08:57-09:03Z — covers both
plain- and spec-axis top-2 readings). All 9 runs: SELF-CONSISTENCY PASS.

Configs:
- **A** default: budget 32768 MB + KQRP on — 183 f16 mirrors (22.4GB), 449 KQRP tensors
- **B** raised: `MEMRA_PP_F16_BUDGET_MB=43008` + KQRP on — 265 mirrors (32.7GB), 449 KQRP
- **C** full-f16: `MEMRA_KQRP=0 MEMRA_PP_F16_BUDGET_MB=50688` — 288 mirrors (35.6GB), 96 q8rp

e2e = 512/(512/decode + 2048/prefill); the prefill term is that run's measured prime
seconds. Table generated by parse_matrix.py (per-run JSON rows + medians = verdict.txt).

## Verdict (N=3 medians, board-2048, NGEN=512)

| cfg | prefill tok/s | plain | spec K=3 | acc % | e2e plain | e2e spec | x vLLM (plain) | x vLLM (spec) |
|-----|---------------|-------|----------|-------|-----------|----------|----------------|---------------|
| A default+KQRP    | 4934.9 | 89.14 | 105.31 | 62.0 | 83.13 | 97.01  | 1.140x | 1.331x |
| **B 43008+KQRP**  | **6564.1** | **89.08** | **107.05** | 61.0 | **84.49** | **100.49** | **1.159x** | **1.378x** |
| C fullf16, KQRP off | 8031.4 | 76.84 | 101.28 | 60.7 | 74.01 | 96.43  | 1.015x | 1.323x |

**WINNER: B — `MEMRA_PP_F16_BUDGET_MB=43008` + KQRP on (default).** It wins BOTH e2e
axes and is never behind on any column that feeds the board. The matrix confirms the
mission's prior: decode dominates the board shape (512/decode = 4.7-5.8s vs prime
0.25-0.42s), so C's prefill crown (8031, +22% over B) buys back only ~57ms while KQRP-off
costs ~0.9s of plain decode (89.1 -> 76.8, the round-48 +15% lever inverted). A vs B:
same decode (KQRP identical), B's extra 10GB of f16 mirrors is pure prefill upside
(+33%) — B >= A everywhere.

## Winner gate

run-gen argmax at config B (board-2048, NGEN=64): **MATCH**, logit maxdiff 5.124e-1
(rungen-argmax-winner-bud43008-kqrp.log) — bit-same maxdiff as the round-49 lane's
43008 gate, same binary, consistent.

## VRAM

Peak sample 76277 MiB / 81559 MiB (vram-samples.log, 5s cadence over the phase-1
window; matches round-49's recorded 43008-budget peak). Guard log: GPU 3 at 0 MiB
between every pair of runs.

## Projected board row

q27 board-2048/512: **e2e 84.5 tok/s plain (1.16x vLLM 72.9), 100.5 tok/s spec K=3
(1.38x)** at the shipping config `MEMRA_PP_F16_BUDGET_MB=43008` (KQRP on, no other
flags — per flags doctrine the budget is machine-specific serving config for this
81.5GB H100, not a default bump).

Thermal regime: all 9 matrix runs + the argmax gate interleaved same-session on
dedicated GPU 3, single box, no cross-day denominators. N=3 per config.

## Files

- matrix.sh / phase2.sh — the run scripts (fresh process per run, VRAM guard)
- matrix-{A,B,C}-*-r{1,2,3}.log — 9 raw run-spec logs (the evidence)
- rungen-argmax-winner-bud43008-kqrp.log — winner argmax gate
- parse_matrix.py / verdict.txt — mechanical table + winner
- vram-guard.log / vram-samples.log — between-config guard + 5s peak samples
- matrix-phase{1,2}.out — driver stdout (rc=0 x9)
