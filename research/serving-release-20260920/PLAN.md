# Required serving release cells (#526)

The first component validates recorded OpenAI chat SSE instead of trusting HTTP 200, output text, or a success summary. It requires a terminal finish reason and DONE, rejects errors/truncated frames/wrong model or choice/late output, and checks requested terminal usage. Reasoning-only output remains valid. Native SSE has a separate contract; it will not be sent through the chat validator.

Current CPU validation: 16 wire and accounting tests pass. Coverage includes native SSE with its named done event, native blocking token snapshots, blocking chat, the actual Memra finish-usage plus usage-only copy, malformed counts, and complete client-attempt accounting. The log is in `raw/wire-and-accounting-cpu.log`. No server, model or GPU qualification has run for this issue.

The full gate will use a reviewed manifest with required cell IDs per configured model and execution route. Required scenarios cover short and long inputs, stream completion, cold and restored transitions, concurrency, overload recovery, cancellation recovery, drain, and worker failure. Every attempted request gets a final classified record, including errors, truncation and respawn; performance summaries must retain failure/censoring counts. Missing required scenarios refuse qualification.

Reuse existing serving/cache/stress controls where their assertions, binary binding and lifecycle handling are adequate. Existing smoke streaming accepts data plus DONE without requiring a finish reason; the new validator closes that gap. Existing stress checks already require finish reason and DONE, but their orchestration and binding must be reconciled with the new manifest and per-card lease protocol.

Coordinate with #547 using `memra-serving-release-v1`: reviewed manifest BlobRef, tested commit/source input digest/build record/server binary binding, exact cell identities, model/numeric/topology/raw evidence, and actual completed leases. The integration seam is `validate_serving_run(run, evidence_reader, required_manifest, expected_bindings)` returning derived verdicts. A future sealed-record v2 makes required serving extensions explicit; the native build recipe remains v3. This work owns new serving modules/manifests/tests; #547 owns its input view and release workflow until its changes are integrated.

A real native run and independent review remain required before this gate can be treated as complete. Preserve model-specific hardware scope and explicitly report unsupported routes; do not promote a one-token health probe or old receipt into qualification.

The accounting helper retains every scheduled request, rejects missing/duplicate/unknown observations, and derives clean success, typed errors, refusal, truncated responses, invalid responses, transport errors and client cancellation from raw bodies. It reports clean-request latency alongside the complete outcome denominator. A client cancellation is deliberately not yet server-confirmed cancellation; recovery, admission, worker generations and lifecycle predicates remain to implement. The process-ownership utility is being implemented independently in disjoint files.

Worker-generation accounting now parses raw health bodies and requires samples completed before, and started after, every request. It rejects missing boundaries, decreasing generations and server-process identity changes. Wire outcomes remain intact; requests whose observation interval crosses a generation change are excluded from clean timings, with observed respawn and affected-request counts reported separately. Five CPU controls pass in `raw/generation-accounting-cpu.log`. This conservative bracket does not claim the specific request caused or survived a panic. The actual runner must establish process/port ownership and collect the samples.

The loopback HTTP collector now enforces finite connect/read/wall budgets, interrupts slow-drip headers and bodies at the wall deadline, keeps bounded partial bodies, checks Content-Length and chunked-transfer completion, and records cancellation without claiming worker cleanup. Nine real local-socket controls pass in `raw/http-capture-cpu.log`; no model or GPU was used. Chunk timestamps measure client-observed body bytes, not model TTFT. The final runner must verify that the port belongs to its owned server before making requests and persist these raw observations.

Independent review TC526-WIRE-1 found that Python integer-digit limits and an oversized native elapsed integer could escape per-attempt error classification. The added unchanged regression fails on the old parser (three numeric cases) and passes after numeric JSON failures are normalized and elapsed time is bounded without overflowing float conversion. Invalid schedules still raise. Mixed clean/malformed/clean schedules retain all three rows. The combined wire/generation/HTTP suite now passes31 tests; red, green and combined logs are preserved under `raw/`. Generation accounting separately passed independent review at e20670cf1. Wire repair and HTTP source review remain pending.

The owned-process supervisor is implemented with explicit argv/env/cwd, an isolated server session, autonomous deadlines, PID/birth identity checks, raw output, original exit/signal records, Linux subreaping, and failure receipts when supervision is lost. Parent review found premature TERM of child workers during drain. The corrected helper signals the primary first; orphan cleanup and deadline escalation remain bounded. A fresh Linux regression against the hash-verified originally reviewed helper fails with PREMATURE_TERM/INFLIGHT_LOST; the identical regression and all15 Linux process tests pass on the repaired helper, with no platform skips. Source hashes and raw logs are banked under `raw/process-linux-*`; `raw/process-drain-before.patch.gz` reconstructs only the original CPU control from the repaired helper. This proves CPU process lifecycle behavior, not native GPU drain or listener ownership. Complete serving orchestration and release qualification remain pending.

HTTP review TC526-HTTP-01/02/03 found a fast-response cancellation window, lenient standard-library chunk termination, and a test peer that could close before consuming the request body. The peer now drains that body; final capture samples pending cancellation; strict chunk hooks require complete size lines, data delimiters and trailer termination. The same controls fail14 cases on the old collector and pass after repair. All12 HTTP tests also pass on Linux/Python3.12. The first combined macOS run exposed a fixture-only `/var` versus `/private/var` path alias; resolving the temporary root fixes that assertion without changing the supervisor. Original failure logs remain preserved. Final combined validation:49 tests pass on Linux with no skips; macOS passes48 with one explicit Linux-only skip. All eight source/test hashes match the Linux capture before and after. See `raw/combined-linux-green.*` and `raw/combined-cpu-after-http-repair-fixed-path.log`. Independent HTTP re-review remains pending.

Three raw artifacts whose formats contain significant trailing spaces are stored as deterministic gzip files instead of altering their bytes. `raw/raw-evidence-packaging.json` binds compressed and uncompressed hashes; decompress before inspecting the failure logs or applying the original-control patch. All original contents remain byte-identical.

Listener ownership and group orchestration now run together on Linux. The listener module proves the primary process owns the only matching socket inode in an unrestricted procfs view, rejects shared/reuseport/foreign listeners and records before/after PID and namespace evidence;18 tests pass, including four real Linux controls. The capture runner consumes an explicit bounded plan, hashes binary/artifact files before and after, records every planned request and raw response in content-addressed blobs, and derives wire/generation accounting without promoting capture to qualification. Five Linux capture tests pass, including a real owned server handling serial/concurrent requests with a truncated peer retained in the counts, and a foreign-port refusal with no connection to the foreign responder. All source and log hashes are banked in `raw/listener-linux-green.*` and `raw/runner-linux-green.*`. No model weights or GPU were used. Required scenario predicates, manifest selection, release bindings and native qualification remain.

The tracked required-cell manifest declares11 scenarios per externally required model/route scope: short/long inputs, four wire contracts, cache restore, concurrency, overload, queued/prime/decode cancellation, drain, and worker failure/recovery. Coverage validation rejects absent/duplicate/skipped/substituted cells and returns source-policy requirements rather than receipt-supplied ones. Four CPU tests pass in `raw/manifest-cpu.log`. This establishes required coverage only: actual scenario predicates, evidence bindings and native execution are still required before release qualification. The caller must derive required scopes from the reviewed release roster/program rather than the capture itself.

Independent capture review found four integration failures: unjoined clients after cancellation, lost completed serial observations, incomplete health replies entering generation timing, and clean exit masking an overall timeout. The repair owns/cancels/joins all client workers, flushes completed/cancelled raw rows during unwinding, rejects incomplete health brackets while retaining wire accounting, and requires the actual supervisor stop reason. Requests/probes now retain explicit method/path and observed server identity. A worker-final event is used because an interrupted join on older CPython is not reliable completion evidence.

The first repair runs exposed unstable procfs scans while unrelated compiler helpers exited. Original run2/run3 failures and a bounded diagnostic are preserved. The collector now retries only the read-only ownership proof for a bounded total three seconds, keeps every failed observation, and still requires the unchanged complete listener proof; inference requests are never retried. The health fault fixture targets explicit request IDs instead of counting startup calls, and the timeout fixture allows startup before inducing the actual overall expiry. The final run4 passes all10 Linux capture tests, including real no-late-HTTP cancellation, truncated health, and supervisor timeout controls; source/log identities are in `raw/runner-repair-attempt4.*`. macOS passes five CPU controls and explicitly skips five Linux controls. Independent repair re-review remains pending; no native/release qualification follows.

Pure completed-group and drain predicates now derive their outcomes from raw captures, wire/generation accounting and the owned lifecycle receipt;25 CPU test groups pass in `raw/scenario-policy-cpu.log`. Drain requires inflight terminal completion, typed new-admission503 with retry headers, healthy-draining200/readiness503, the actual TERM, and clean bounded process exit. It does not require an impossible health call after listener shutdown. Generation evidence is limited to sampled intervals; unobserved tails have no clean generation latency. Buffered response bytes may finish reading after the exit observation within the fixed drain allowance, without claiming post-exit server work. These predicates still need independent review and actual runner/model execution; they do not implement all11 required scenarios.

Capture re-review found a remaining launch interruption: a native thread could start before being added to cleanup tracking. Registration now precedes `start()`, and genuine unstarted allocation failures are distinguished from interrupted native launches. The unchanged new regression fails on the previous collector and passes on the repair; all12 capture controls pass on Linux with exact committed dependencies (concurrent policy/producer work was excluded from this snapshot). The actual launch-interruption cancellation row is retained before return, and truly unstarted workers are never joined. Raw red/green/source identities are in `raw/launch-repair-*`. Independent re-review is required; the separate drain-policy findings remain open.

The capture launch repair now has independent source GO at dcd749f8b; the original launch regression and real Linux cancellation checks pass. Separate drain receipt timing/census repairs remain in progress.

Required short/long/wire/concurrent completion predicates now consume a trusted manifest cell and independently frozen request program. They rederive complete wire and worker-generation observations, enforce the short exact answer and actual token bounds, require all four terminal wire contracts, and require at least three overlapping client request intervals for concurrency. That overlap does not claim simultaneous GPU execution. A received config/oracle cannot serve as the trusted program: the later release binding layer must bind it to the actual collector schedule. All58 pure CPU test groups pass using committed dependencies, including seven new scenario groups with refusal mutations; concurrent drain work was excluded from the snapshot. Raw source/log identities are in `raw/completion-policy-*`. Independent scenario review, remaining cancellation/cache/fault schedules, release bindings and native model execution are still required.

The cache-restore predicate now requires a serial cold-control/seed/restored program with explicit native prompt IDs and separate cold/restored cache salts. Cold and restored programs may differ only in cache namespace; the restored result must match actual token IDs, text, stop reason and output count while reporting reuse of the whole seed prefix. Six before/after metrics counters must match the complete three-request denominator. Seven new refusal test groups cover false cache hits, changed output/program, missing attempts, generation changes and false counter summaries. All65 combined pure CPU groups pass on committed dependencies; `raw/cache-policy-*` preserves the source/log identities. This is a predicate implementation with fixture evidence, not a native cache qualification or proof that the frozen program was actually sent. Independent review and collector/release bindings remain required.

The drain receipt repair records a post-syscall signal bound and actual cleanup-finish time. Every observed child now has either a supervisor wait record or an identity-bound disappearance observation; a child reaped by its primary is not assigned an invented exit status. Predicates require those observations to precede cleanup and the frozen drain deadline. Three controls accepted by the old policy now refuse.

The full Linux CPU integration first ran128 tests: all changed process/policy controls passed, and one unchanged positive IPv6 listener test failed when an unrelated process closed a descriptor during its procfs snapshot. That failed run is preserved. The positive live-listener fixtures now mirror the collector's existing bounded read-only retry and still require a complete proof; negative ownership controls and production listener code are unchanged. Fresh full integration passes130 tests with no skips and identical source hashes before/after, including actual primary-reaped-child and adopted-orphan controls, both scenario adapters, HTTP capture and owned-process cleanup. Raw attempts and repair controls are in `raw/drain-repair-*`. CI now runs the CPU suite with the complete procfs visibility its listener proof requires. Independent repair review remains pending; this does not qualify native models or a release.

Independent cache review identified an incorrect assumption in the first predicate: continuation-pool reuse contributes to global cached-token accounting but bypasses the prefix-cache probe. Its valid restored result was rejected by requiring all cached tokens to appear in prefix-specific counters. The repaired predicate retains exact global prompt/cached/computed deltas and the complete token/text/parity checks; prefix counters remain separately recorded, monotonic diagnostics without a claim about the supplying tier. The unchanged source-derived continuation control fails on f78a5633f and passes after repair; all eight cache predicate groups pass. Original evidence and red/green sources/logs are retained under `raw/cache-tier-repair-*`; independent re-review remains required.

Independent review now approves the corrected cache predicate at14f75c67a, with the unchanged original regression and20 additional controls. All hosted codeCI and GitGuardian checks pass at that checkpoint.

The queue-overload recovery predicate adds complete peer/refused/recovery request accounting, exact source queue429 error codes and retry headers, completed peer intervals spanning refusals, subsequent clean recovery and unchanged worker-generation brackets. It does not accept quota exhaustion, worker-unavailable503 or client failures as a queue-overload demonstration. Halley supplies61 passing CPU checks, including28 new cases, under `raw/overload-policy-*`. Client interval overlap does not prove simultaneous server/GPU execution. Independent review and actual collector/release bindings are still required.

The group-capture evidence reader now consumes an externally trusted index digest, frozen request plan and artifact identities. It validates canonical content-addressed blob references, rechecks every declared payload hash, binds the recorded process launch/limits/cleanup and saved listener observations, reconstructs raw responses and recomputes complete wire/generation accounting. It does not authenticate the external release record, infer GPU/source/build provenance or qualify the scenario manifest; failed requests remain in the capture denominator. Six pure corruption/substitution controls and the actual Linux collector/replay integration pass; all18 affected evidence/capture controls run without skips with before/after source hashes unchanged (`raw/evidence-reader-linux-*`). Independent review remains pending. Overload predicate ae630be56 meanwhile has independent source GO (28 tests plus86 independent controls).

Reader review AD526-EVIDENCE-1 identified internally consistent but impossible timing: whole groups after cleanup, or overlapping requests under a trusted serial schedule, were accepted. The reader now binds the complete startup proof to the process start, requires sequential group boundaries before the collector's capture_complete stop, and enforces serial request order/nonoverlap. It uses the completed startup proof as its lower bound because ready.monotonic is a supervisor loop-entry sample, not a post-acceptance timestamp. The separate drain policy that allows buffered response reads after exit is unchanged. Both chronology regressions fail on d85b3f1d and pass after repair; all20 affected evidence/capture controls pass on Linux without skips, with unchanged source hashes. Original failures and raw proofs are in `raw/evidence-chronology-*`; independent re-review remains required.

Hosted CI then exposed the same unrelated descriptor churn before a live shared-socket negative reached its intended reason. The full165-test run recorded one failure because the fixture expected the shared-owner refusal but received the earlier incomplete-inventory refusal. The negative fixture now retries only that exact transient observation for a bounded total window. It still requires its original refusal reason, fails immediately on unexpected acceptance or any other error, and never counts persistent inventory errors as success. Production ownership logic and deterministic negative tests are unchanged. An unchanged mocked invocation of the actual live negative fails on the previous test and passes after repair; two new controls guard the retry limits and immediate failure. All42 affected listener/evidence/capture tests pass on Linux without skips with before/after source hashes equal. Hosted failure excerpt, full-log hash, regression and Linux evidence are preserved under `raw/listener-negative-*`; independent review and fresh CI remain required.

Optional per-group metrics capture and evidence decoding now preserve both raw `/metrics` observations around all scheduled requests, inside the health/listener brackets. The trusted plan decides whether metrics are required. The reader rejects missing/substituted references, foreign owners/routes/IDs, incomplete framing and invalid chronology, while retaining non-200 statuses and malformed JSON as raw policy inputs. It never grants cache success from capture completion or prefix counters. Real Linux captures verify metrics round-trip alongside failed-request accounting.

The affected capture/evidence/HTTP/cache suites pass all **52 tests on Linux with zero skips**, with all 25 source/fixture hashes unchanged before and after. macOS ran the same 52 tests: 43 passed and 9 Linux-only tests skipped. The first Linux package omitted the tracked serving manifest and produced eight missing-file errors; that preparation failure remains in `raw/metrics-linux-001.*`. The unchanged Python source passed after the fixture was included (`raw/metrics-linux-002.*`). No model or GPU ran, and native serving qualification remains pending.

`serving_group_gate.evaluate_group_capture` now connects the evidence reader to the existing short/long/wire/concurrent completion and cache predicates. It requires one complete captured group, binds its cell ID, process identity, scheduling mode, exact request payloads and order to the independently reviewed program, and then applies the scenario checks. It cannot select one successful group from a larger capture. Cache responses require actual captured metrics; unsupported lifecycle scenarios refuse explicitly. Source/build/hardware/lease provenance and complete required-suite coverage still belong to the outer release binding layer.

All 36 focused group-gate/completion/cache/evidence tests pass locally with no skips. The nine new controls cover all five supported scenarios, changed payloads and JSON types, reordered/omitted/duplicated requests, broader capture scope, process/mode/cell reassignment, independent answer and usage requirements, missing artifact/digest bindings, missing cache metrics and unsupported lifecycle claims. These use sealed synthetic byte bundles, not a model or GPU. Source hashes and raw output are preserved in `raw/group-gate-cpu.*`; independent review and full native serving qualification remain pending.

Cancellation now has an owned diagnostic collector for queued/prime/decode scenarios. It preserves every planned request and raw response, records cancellation timing, checks peer/recovery completion and refuses incomplete controls. It remains explicitly unqualified for native cancellation: current runtime telemetry does not establish request-specific phase, cancellation observation and completed resource retirement. A successful reconnect or global idle status cannot substitute for those records.

Independent review found and closed three fixture defects: decoded target server errors must fail the diagnostic; EOF-before-cancel requires deterministic ordering; and the typed-error barrier must wait for the entire captured body across arbitrary read fragmentation. The repaired source passes 38 pure tests plus a complete local suite of 49 tests: 48 pass and one Linux-procfs-only test skips. All ten actual macOS HTTP/process fixtures completed with clean server exits; the reviewer verified 168 captured payload hashes and the ordering records. Earlier source failures and sandbox environment denials remain preserved in the private audit archive. Public test logs, source hashes and bounded fixture summaries are in `raw/cancel-repair-local-io.*` and `raw/cancel-fragments-pure.*`. Linux ownership and model/GPU qualification are still separate requirements.


The request lifecycle producer and actual prime-advance observers now have source review and Linux CPU evidence: the frozen `aa25e935` runtime passed strict server Clippy and 799 server tests, with eight explicit native/manual ignores. The pure log parser at `7b130073` has independent source approval after closing P2 AD526-TRACE-1; 53 parser/wire tests and the retained original failure cases pass in normal and optimized Python. It reconstructs request binding, phase, actual quantum returns, HTTP drop, receiver close and explicit retirement site, while retaining unrelated traces in its census. The [parser evidence](lifecycle-parser-20260921/README.md) keeps the old failures and current raw controls. These additions close the earlier missing-observation and parsing components; the phase-driven collector, complete peer/recovery denominator, outer source/binary/controller/lease binding and native C4 matrix still require integration and validation.


Phase-driven cancellation capture and immutable replay now have independent source approval and [Linux CPU/real-IO evidence](phase-capture-linux-20260922/README.md) at `96ec779a`. The Event.set timing race, late watcher mutation, observed truncation, body-limit omission, stale phase chronology and missing-path error handling are fixed. All123 tests pass in both Python modes with no skips; six additional actual Linux listener/process/socket replays use synthetic lifecycle producers. Every negative capture remains in the preserved archive. Source/build/model/controller/physical-GPU lease binding, the remaining required scenario orchestration and native serving qualification are still pending.
