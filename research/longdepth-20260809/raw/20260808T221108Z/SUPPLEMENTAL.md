# Supplemental context-control run

This run began under the original context-by-depth-by-policy plan. Before its 6K cells, the hourly
orchestrator steering supplied live corruption receipts at both 131072 and 262144 contexts and
replaced the remaining matrix with 262144-only depth by true-spec by temperature cells.

The four completed 2048-token cells remain useful controls: all eight generations were clean, and
the context pairs were byte-identical at temperature zero. Both `policy` cells logged zero
`[spec-acc]` lines because cross-device PP-2 defaults to no spec admission; they must not be
described as MTP-on evidence.
