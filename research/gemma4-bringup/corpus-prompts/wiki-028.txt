--- title: NVIDIA GH200 Grace Hopper Superchip type: entity tags: [hardware, gpu, cpu, superchip, nvlink-c2c, hopper, grace, hbm3e, coherent-memory] sources: [] updated: 2026-06-29 --- # NVIDIA GH200 Grace Hopper Superchip The NVIDIA GH200 is a "superchip" that fuses an Arm-based **Grace CPU** (72 Neoverse cores) and a **Hopper GPU** onto a single module, joined by **NVLink-C2C**, a cache-coherent chip-to-chip interconnect rated at **900 GB/s** of total CPU↔GPU bandwidth — roughly **7x** the 128 GB/s of a PCIe Gen5 link on a conventional x86 server.[^1] The defining architectural feature is a single per-process page table shared by CPU and GPU threads: both can address all system-allocated memory wherever it physically resides, so workloads no longer copy buffers back and forth across PCIe.[^1] The GPU side carries HBM3 or HBM3e memory; NVIDIA quotes up to **624 GB** of combined fast CPU+GPU memory and **4 petaFLOPS** of H200-class GPU compute for the higher-end configurations, and markets the part for giant-scale AI and HPC.[^1] Note the HBM-side device in current GH200 parts is an **H200** (not the base H100), which is why the 900