--- title: Eigen memory — tools type: source tags: [eigen-memory, tools, agent-sh] updated: 2026-06-30 raw: raw/eigen-memory/tools-049e191f/MEMORY.md --- # Eigen memory — tools Eigen's `tools` memory tracks the [[tools]] / agent-sh harness packages (`@agent-sh/harness-*`): git/upstream sync rules, itch+pi Bedrock model routing, and the deepest record of the **harness-websearch** design — engines, fallback/RRF, dedupe, output format, tests, and the dual TS+Rust release pipeline. ## Key facts - Git/upstream sync (s17): `git fetch` -> `git log --oneline HEAD..@{u}` -> summarize upstream delta before pull/rebase; rebase/pull only when clean and safe, else report expected conflicts. Example: behind `e0dc5f6` (`chore: version packages`), harness-write+harness-tools 0.6.0, `multiedit`->`multi_edit`, spent `.changeset/` removed — clean rebase no conflicts. - itch + pi (Bedrock): "no fable 5 its frozen, use fallback to sonnet" -> map frozen `global.anthropic.claude-fable-5` -> `PI_FROZEN_FALLBACK` -> `us.anthropic.claude-sonnet-4-6` (`_to_pi_model()`). Pass Bedrock ids verbatim with inference-profile prefix; bare `anthropic.claude-*` -> 400 "on-demand throughput isn't supported". harness-websearch >=0.7: keyless websearch under pi needs no `SEARXNG_URL`. Pi headless `--mode json` = JSONL (`message_update`, `tool_execution_*`), not `claude` stream-json. - harness-websearch design (2026-06-14): zero-config keyless default (v0.4+) **Mojeek -> Marginalia -> Wikipedia** (`WebSearchSessionConfig::auto`);