--- title: Eigen memory — projects type: source tags: [eigen-memory, projects, eigen, speculative-decoding, mtp] updated: 2026-06-30 raw: raw/eigen-memory/projects-86e3b29e/MEMORY.md --- # Eigen memory — projects Eigen's `projects` memory is the master `~/projects` working file — the densest architecture record in the set. It covers the [[eigen]] harness internals (daemon, sessions, models/routing, TUI, agent loop, memory/dream, Telegram, Codex provider), plus per-project sections for [[latent-bridge]], the Pearl miner / GEMM / noising internals, Qwen3.6 MTP/spec-decode theory, [[revuto]], [[itch]]/[[atrium]]/[[sxc]], and Agor. It is the cross-cutting "how everything actually works on this box" file. ## Key facts - Eigen daemon IS the app (`eigen daemon`, `~/.eigen/daemon.sock` line-JSON); windows are views (`eigen attach`). Sessions `~/.eigen/daemon/sessions/<id>.jsonl` + `.meta.json`; `transcript.Save` atomic with `.bak` rotation. Prod = empty instance; dev = `EIGEN_INSTANCE=dev` (`daemon-dev.sock`). FIRM RULE: never kill/restart production daemon from agent work. Public repo `github.com/avifenesh/eigen` (MIT, main); CI = `make gate` + `make race`; CI installs ripgrep (symbols_test skips when rg absent). - Models/routing: active `config.json` = provider `converse`, model `us.anthropic.claude-opus-4-8`, perm auto, max_tokens 500000, judge `openai.gpt-5.5`, effort max, route true. **claude-fable-5 REMOVED** (persistent 500). Failover chain `{glm-5.2, opus-4-8}`,