--- title: Benchmarking Rigor type: concept tags: [benchmarking, perf, variance, methodology, oom, napi, llm-judge, retrieval-eval] sources: [perf-claim-n5-median, glide-mq-bench-protocol, bench-no-oom-rules, bench-on-dev-cloud-compute, napi-define-properties-slower, ffi-crossing-count-dominates, valkey-udp-rdb-sync, research-2026-06-29-aiml-unified-embed-rerankers-ceilings, research-2026-07-04-aiml-grpo-variance-war, bw24-session-2026-07-03, rig5090-chassis-thermal-budget] updated: 2026-07-11 --- # Benchmarking Rigor The owner's standing rules for trustworthy performance measurement, hardened across [[valkey-glide]] NAPI work and GPU inference. The through-line: a perf claim is worthless without enough independent runs to separate signal from variance, and a "win" that regresses the normal case is not a win. ## Baseline and the normal case Establish a long-term baseline **first**, before any change. The baseline must defend the **normal case** (values <1KB, set/get 20/80, high QPS) — a finding only counts with no regression there. Large/array payloads are secondary; never let a big-payload win mask a normal-case regression. No gain -> no change, unless the win is provable in the compiled binary (fewer instructions / eliminated malloc visible in asm or alloc trace). ## N>=5, medians, and variance Run each condition with **N>=5 separate process launches**, compare **medians**, report min..max. N=1 is worthless: a memset patch faked **1.7-2.1x at N=1** but was