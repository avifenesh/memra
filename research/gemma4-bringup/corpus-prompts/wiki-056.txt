--- title: Codex memory — research, FFI & systems ideation type: source tags: [codex-memory, research] updated: 2026-07-02 raw: raw/codex-memory/MEMORY.md --- # Codex memory — research, FFI & systems ideation CODEX's memory records the owner's exploratory/research-phase threads where the deliverable is sharp comparative judgment, not shipped code: screening local-inference product ideas for a real wedge, narrowing a local-only diff-scoped security reviewer, validating an FFI/encoding-addon performance premise, building the ferrings io_uring transport scaffold, and the standalone memory-controller-model training project with its blocked day-scale launch. The recurring discipline is to prove or kill an idea against real engine/boundary capabilities before building. ## Key facts - (NEW) ferrings = from-scratch Rust/NAPI io_uring Node transport (`/home/avifenesh/projects/ferrings`). Scaffold proven on both surfaces (`cargo check/test/clippy --all-targets -- -D warnings` + `npm test`/`build:debug`/`build`); `index.d.ts` + `ServerInfo` counters are runtime truth. `useRecvBundle` is a provided-buffer-ring path only (rejected for ZCRX mode). Starvation hardening (`-ENOBUFS` = starvation signal not terminal close; `recover_recv_buffer_starvation`, `resubmit_tcp_recv_multi`) is INCOMPLETE — resubmit path must compile per-receive-flow before relying on it. Corroborates [[ferrings]] and touches [[ffi-napi-overhead]]. - (NEW 2026-07-02) ferrings TLS + `v0.2.49`: TLS support implemented