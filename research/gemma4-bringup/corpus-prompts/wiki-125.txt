--- title: SuperInfer — SLO-Aware Rotary Scheduling for Superchips type: concept tags: [inference, serving, kv-cache, scheduling, slo, gh200, nvlink-c2c, offloading, transport] sources: [] updated: 2026-06-29 --- # SuperInfer — SLO-Aware Rotary Scheduling for Superchips SuperInfer (MLSys 2026, arXiv 2601.20309) is an LLM serving system built for GPU-CPU "Superchips" — specifically the NVIDIA GH200, where a Hopper GPU and a Grace CPU are joined by NVLink-C2C, a cache-coherent interconnect rated at up to 900 GB/s, roughly an order of magnitude faster than PCIe (~32-64 GB/s).[^1] The paper's framing problem is the standard one for memory-bound decode: under high request rates the per-request KV cache grows until it exhausts HBM, and existing systems then suffer severe head-of-line (HOL) blocking and miss tight Time-To-First-Token (TTFT) and Time-Between-Tokens (TBT) SLOs. Prior SLO-aware schedulers (Sarathi-Serve, Llumnix-style reordering) only cut queuing delay and are still capped by on-device memory; prior offloading systems (FlexGen, NEO, FastDecode, CachedAttention) expand effective capacity but are SLO-unaware — they react to memory pressure rather than latency urgency, so swapped requests blow their TBT budget — and were designed for PCIe bandwidth,