--- title: Eigen memory — eigen (d969c230) type: source tags: [eigen-memory, eigen, llm-agent-memory, valkey-glide] updated: 2026-06-30 raw: raw/eigen-memory/eigen-d969c230/MEMORY.md --- # Eigen memory — eigen (d969c230) The smaller of two eigen-project stores (dir `eigen-d969c230`): a 39-line slice of [[eigen]] notes focused on session identity, eigen config/infrastructure (Splade, Eigencloud server API, rmcp version handling), the prod->dev session-copy procedure, PR-review preferences, the valkey-glide multi-key MIGRATE review, and Node.js `vm.runInContext` GUI-test gotchas. Overlaps the larger consolidated eigen store but is its own canonical file. ## Key facts - Production main chat title is **`main-session`** (daemon id **s8**) — use that exact label in migrations/handoffs. - Eigencloud / server API: durable local state in SQLite under `~/.eigen`; call `https://api.eigencloud.xyz` only via agent skills/tools + `EIGEN_SERVER_TOKEN`; grow thin proxy/wrapper tools in the daemon. - rmcp `#[tool_handler]` servers: OMIT the `version` field so advertised version tracks `CARGO_PKG_VERSION`; `version = env!("CARGO_PKG_VERSION")` as a macro arg does NOT compile — guard with a parity unit test after version bumps. - Prod->dev session copy: copy `<id>.jsonl` + `<id>.meta.json`; `eigen --instance dev daemon stop` then `eigen dev` from a terminal NOT