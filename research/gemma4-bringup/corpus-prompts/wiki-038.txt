--- title: GPU Kernel Abstraction Landscape type: concept tags: [gpu, cuda, kernel-dsl, blackwell, triton, cutlass, mojo, hipkittens, layout-algebra, amd] sources: [research-2026-06-04-gpu-kernel-abstraction-landscape, research-2026-05-27-blackwell-stack-moat] updated: 2026-06-29 --- # GPU Kernel Abstraction Landscape This page surveys the sprawling field of GPU kernel-authoring tools on Hopper and Blackwell — cuTile, Triton and its siblings, CuTe-DSL, ThunderKittens/HipKittens, Mojo, the layout algebras — and the one structural lesson that organizes all of them. ([[cuda-tile-ir]] drills into cuTile specifically; [[blackwell-architecture]] covers the silicon that forces the shape.) The deep finding: the entire landscape converges on the same kernel shape not by fashion but because Blackwell's silicon forces it — and **portability is a property of the tile abstraction, not of any layout algebra. Schedules must be re-derived per architecture, and "Blackwell" is itself two different machines.** ## Blackwell forces the kernel shape Three independent PTX/SASS microbenchmark studies arrive at one universal endpoint: **single producer issues async matmul → accumulator lands in TMEM → separate consumer drains it → pair two SMs to beat the bandwidth wall.** `tcgen05.mma` is single-thread CTA-scoped (the CuTe MMA atom ThrID drops from