# WP-B day-2 milestone — CPU implementation, native runtime gates pending

Repository **avifenesh/memra**; branch `lane/spill-b-20260919`; worktree `../wt-spill-b`.
Code checked: **`7f1a61a09a968ad1386c8c0b5899d8b650c7e27c`**. Later receipt-only
commits do not change the checked code. This lane is open for lead integration;
no push, PR, tag, main integration, deployment, model execution or support promotion.

## Commits and ownership

- `4a7c3f12`: preserve the pre-existing, expected Cargo-generated day-1 sha2 lock edge.
- `5ee50c01`: `--no-ff` merge of lead `259ff819`; **no conflicts**, Cargo.lock auto-merged.
- `b505e8d8`: single shared quota governor in `memra-tier::tier`, bounded queue/dirty
  backlog, priority/deadline/tenant fairness, mandatory headroom and explicit pin release.
- `f64406af`: frozen-schema migration, hierarchy, native q8_0/q5_1 CPU materializer,
  full-route pure prefix policy, identity sidecar implementation and conformance tests.
- `7f1a61a0`: inactive HostPrefixEntry sidecar field + both production/test constructors.

The root lock was regenerated by `cargo check -p memra-kv --offline`; its lane diff
replaces memra-kv's sha2 dependency with memra-tier. Only the allowed `pub mod tier;`
export was added to the shared lib. No shared contracts, schedules, fixtures, root
manifest or other lane implementation was edited. The KV control plane stays in
memra-kv; only the shared governor lives in memra-tier so A/C/D need not depend on KV.

`record.rs` and tier shared names are imports/re-exports, not duplicate structs.
Canonical JSON v1 and homogeneous state/src_gen/dst_gen tickets are accepted as frozen.
`KvBacking<T>` is a narrow native directory/descriptor mapping seam, not another
physical ObjectStore, TransferEngine or payload cache. Source images keep their own
physical provenance/charge; device consumption requires the sealed transfer ReadyView.

## Files and substance

- `crates/memra-tier/src/tier/{mod,governor}.rs`, shared export in `src/lib.rs`.
- `crates/memra-kv/Cargo.toml`, root `Cargo.lock`.
- `crates/memra-kv/src/record.rs`, `src/tiered/{mod,integration,policy,tests}.rs`.
- New `src/tiered/{materializer,hostprefix}.rs` and six-line server `worker.rs` attachment.
- This receipt, `HOSTPREFIX-EXTENSION.md`, updated `CELLS.md`, historical-document banners,
  raw `day2-verification.log`, and bounded reproducer `verify-day2.py`.

The CPU materializer implements the frozen trait, derives native geometry from existing
KvLayer fields and tests retained exact q8_0 K/q5_1 V bytes, including multipage odd tails.
No selectable f16/FP8 path, quantization, attention reduction or CUDA code was introduced.
The host sidecar defaults unbound; no runtime caller binds a complete native image yet.
Legacy promotion/tenant/LRU/handoff algorithms and payload bytes are unchanged. Full
continuation state and native attention consumption remain explicit gates, not implied
by the byte fixture. Details/functions are in `HOSTPREFIX-EXTENSION.md`.

## Conformance / exact command results

All required commands actually ran. Full stdout+stderr is retained in
[`day2-verification.log`](day2-verification.log), before this summary.
Reproducer: `python3 research/spill-b-20260919/verify-day2.py`.

| Command | Result |
|---|---|
| `cargo fmt --all -- --check` | exit 0; no output |
| `cargo check -p memra-kv -p memra-tier --offline --all-targets` | exit 0; `Finished dev profile` |
| `cargo test -p memra-kv -p memra-tier --offline` | exit 0; memra-kv **40 passed, 0 failed, 0 ignored**; frozen contracts **36 passed, 0 failed, 0 ignored**; compile-fail docs **3 passed, 0 failed, 0 ignored** |
| `cargo clippy -p memra-kv --offline --all-targets --no-deps` | exit 0; no memra-kv warning |
| `cargo clippy -p memra-tier --offline --all-targets --no-deps -- -D warnings` | extra check, exit 0 |
| `git diff --check` | exit 0; no output |
| `bash tools/check-flags.sh` | exit 0; `runtime literal reads=864`; `no uncovered runtime names` |
| `python3 crates/memra-tier/tests/contracts/fixture_reference.py --check` | extra check, exit 0; `8 payload + 3 canonical wire SHA256 pins match` |
| `git diff --exit-code 259ff819 -- crates/memra-tier/src/contracts.rs crates/memra-tier/tests/contracts` | exit 0; no output (frozen sources unchanged) |

Existing macOS dependency warning is retained, not silently repaired out of scope:
`crates/memra-gguf/src/source.rs:20:5: unused import: std::os::fd::AsRawFd`.
No new warning in changed crates on the final Clippy runs.

The exact frozen `conformance::tier_cancel` schedule is path-imported and run against B's
Hierarchy. The lead did **not** export a generic BudgetGovernor schedule: its governor
schedule lives in a concrete test tied to `support::Governor`. B's own test replays
that exact headroom/foreign/double-release/pin operation/assertion sequence against B's
Governor; no frozen schedule was edited. Additional B tests cover all quota dimensions,
real simultaneous CPU admission (eight threads/barrier/shared mutex), queue limits,
deadline expiry, no bypass, tenant turn-taking and dirty-backlog refusal. This distinction
was sent to the lead rather than silently claiming the concrete fake was our implementation.

Two iterative test/tool findings were corrected before final receipt: a fairness test
incorrectly expected a previously served tenant before a never-served tenant (fixture
reset corrected); Clippy reported modulo/counter-loop style warnings (owned code corrected).
Source inspection also tightened rejected-descriptor reclamation, materializer instance
identity, source-provenance retention and bounded fairness metadata. No red gate was skipped.

Frozen ordinary SHA-256 pins (not runtime domain hashes):

- contracts.rs: `8d65f2ae27f481f82b52bcac0e8c53ce8c5d929e1c6f735ae26241aa97bef7e6`
- fixtures.json: `1b722a5eb6022b60bf7c62817c3ce773ea6459131af395a7df73dcff41aff18b`

## CELLS delta and numbered blockers

CELLS now distinguishes executed CPU rows from the unchanged GPU ladder. B1 metadata,
conformance, quota/lifetime, host identity and native-byte binding rows pass in CPU scope;
B4's fixture-only cost rule passes. **No GPU row advanced.**

1. **Native GPU/server build and binding:** no local nvcc/GPU. The full server/engine
   build was not run; the worker's six-line field wiring is source-inspected only.
   Existing CUDA KvLayer buffers/len/base addresses, real fences and attention calls
   must replace the CPU NativeKvImage binding before B2/B3 can run.
2. **Actual HostPrefixCache/A integration:** complete Qwen recurrent/auxiliary state,
   physical source-image leases, A's real ObjectStore/TransferEngine descriptors and
   scheduler tickets remain to bind. Metadata leases alone never authorize DMA or
   hold payload memory. The sidecar stays unbound and no async runtime is enabled.
3. **Hardware/artifact window:** one bounded read-only development SSH attempt returned
   exit 255, `Connection closed by UNKNOWN port 65535`. No inventory was returned;
   no second attempt, serving-instance interaction or remote state change. The lead
   still owns immutable artifact authorization and non-serving PRO windows. All real
   8k/32k/128k/262144 active/prefix, corruption/churn/spec and integrated pressure gates
   remain pending; local 5090 evidence is not substituted for the target gates.
4. **Measured policy/defaults:** chunk frontier, real calibrated recompute costs, route
   costs and serving-load fairness remain unmeasured. No flags/defaults/board numbers
   or format support claims were added. No FLAGS/KERNELS fragments are needed.

Cost: approximately **0.4 agent-hours for this continuation**, CPU-only (about twenty
minutes including re-entry/reads). Previous session time was not measured here. This is
one milestone inside the **10 agent-day WP-B budget**, not completion of that budgeted
work; no fixed hours/day conversion is assumed. The active worktree/branch are retained
for the lead's next integration milestone, not abandoned or closed.
