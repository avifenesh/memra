# WP-B day 23 checkpoint: memra#427 fixed on main by #614 (the tier bound), the lane's scope dropped, the whole battery green on main's mechanism on both cards
- Branch lane/spill-b-20260919 on origin/main 653c997f4 (#614 merged as 180ca38aa after lead ruling 26; #613 merged as aeb4d0771 before it). Pushes in the announced development mode (`UNQUALIFIED DEVELOPMENT`, logged; no qualification claimed anywhere).
- Mechanism on main: `Engine::small_m_tier_max()` = 16 inside verify-exact, `PRIME_MIN_T - 1` outside; `matmul`/`matmul_pre` bound the batched small-m tier with it; `matmul_decode_exact*` keep `2..=16`. The lane's `prefill_rows` scope, `batched_tier_admits` and the `prime_layers` arm are removed (a second mechanism for the same behaviour; main's covers every caller, so the day-22 scope gap at `prime_layers_gemma`/`step35_prime_cache_batch` is closed by construction). Record: `PRIME-MIN-T-DECISION.md`, "Superseded by #614" (both mechanisms, the two sites read: step35 `attn_gate` out_f 64/96 could reach the tier at `total = 16`; gemma width-safe by artifact census only).
- Left in the tree against main: the one-arm `qwen-a4-width-walk` diagnostic (+ its `[[bin]]` entry) and one TESTING.md pointer to it in main's continuation-gate paragraph.
- Local 5090 on the merged tree (`rtx5090-day23/`): walk `WIDTH WALK width 16 vs 17: 0 of 497 tensors differ` (no scope); seven-arm table every split `ok`, digests identical to day 22 (`9280 + 16: logits_sha=14ab5f8b365dbd71 ok`, chunk-32 `14ab5f8b365dbd71`, chunk-16 `bafe0e0a09a3d0a4`); `kernel-check` `ALL GREEN (109 cells, 10 skipped)`; hit gate `SPEC-ON-CACHE-HIT GATE: ALL GREEN (qwen)` (drafter absent, WARNING branch); twin gate `... V1=ok V2=ok V3=ok V4=ok V5=ok V6=ok -> PASS`; run-gen MATCH (+ batched-prime MATCH) on std/probe/16-token/4112-token; run-spec K=1..8 `=== SELF-CONSISTENCY PASS ===` on probe/16/4112 with the embedded NextN drafter.
- Target card (`pro-single-day23/`, mirrored from `/root/spill-receipts/b-day23/`): the 9B artifact staged from the verified local copy (SHA-256 `52c9cceb...` = five repo receipts) into the lane's own dir, `/root/artifacts` untouched; `kernel-check` step35 manifest alone and both manifests `ALL GREEN (109 cells, 6 skipped)` with `DUAL-BATCHED-AUX ... OK` (day-22 binary and merged binary); run-spec and run-gen verdicts and counts equal the local card's; the seven-arm table digest for digest.
- Owed: the step35 continuation split (16 vs 17 rows) on a rig holding a Step-3.7-Flash GGUF (none on either rig; a confirmation under #614, not a gap). Not run: the original task 3's base-versus-fixed run-gen (ruling 26 narrowed it).
- memra#427: receipts commented, left open for the lead to close with the integ. memra#445 sections 2 and 3: mapped on day 22, unchanged.
- Open: `prefix_fanout_groups` takes the raw in-batch LCP (inspection only). Decide-by 2026-10-04 for the --kv-allocator vmm door unchanged.
- RULE (lead, 2026-09-21): never signal a process you did not start on this rig; identify by cwd before any kill, and if it is not your worktree, leave it.
