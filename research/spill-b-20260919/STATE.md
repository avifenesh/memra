# WP-B day 24 checkpoint: memra#476 fixed on the lane (the predictive book reads the physical cost's terms), memra#524 gap written, fault gate pre-registered
- Branch lane/spill-b-20260919 on origin/main e2e9e294a (#616 merged as fb0c0a9a0 at day start). Pushes in the announced development mode (`UNQUALIFIED DEVELOPMENT`, logged; no qualification claimed anywhere).
- memra#476: `admit_predict::RequestCharge::from_physical_cost(cost, ctx(C), A, D, ctx(P+L+8)).total()` at both predictor sites (verdict: cold cost; booking: the final restore/eager-adjusted `cost` the real book takes). Before: `kv_hat = ctx(P+L+8) + A`, so `W(P)` and `D` were in the real book and not the predictive one. Local 5090 before/after (`rtx5090-day24/`): 4th concurrent admit `booked_bytes=615855840` vs `booked_real=5156718304` before (8.37x); after `5152798432` vs `5156718304`, difference `3 x 1306624` = the context bracket exactly; `A: max |residual~| = 479880 bytes` (1 MB line grain); C PASS both; pre-registered B FAIL both (`2550136832 > floor 1610612736`: the pool's retained high-water at inflight 0; in-flight only 822879944, inside floor), recorded, not relaxed.
- CPU tests: 4 in admit_predict.rs, `predictive_charge_books_the_physical_cost_terms_on_every_path` and `readiness_follows_the_boot_calibration_probe` in worker.rs. FLAGS.md rows for `MEMRA_ADMIT_PREDICT_SHADOW`/`ENFORCE` state the new `fixed`.
- Named, not done: release `W` from both books at prime completion (outstanding-only); the physical book charges the shared prime slab per request (over-books 4-way concurrency by ~5.6 GB on the 9B); the enforcing door on the fuller charge needs its own cell against a budget arm; the retained-prefix arm did not trigger on this card (CPU test only).
- memra#524: `run_boot_calibration` (17172) precedes `ready_tx.send(Ok` (17311) and `mark_ready` (17316); the `Engine ready` line the issue cites is engine construction. Gap: the probe-skipped paths (MEMRA_ADMIT_CALIBRATE=0, plain-only serving, MEMRA_ADMIT_RESERVE_MB set, probe failure) and shapes the B=1 probe did not walk; no `phase=warming`. Measured: ready to first completion 1437/1426 ms at a 500 ms poll, warm `prime_ms=75.8/76.0`. No readiness code change (nothing to wait for on the skipped paths without adding work); `tools/health-fault-gate.sh` arms (a)-(f) pre-registered in DAY24.md section 5.
- Left in the tree against main: the `qwen-a4-width-walk` diagnostic bin and one TESTING.md pointer (day 23), plus the day-24 change above.
- Open: `prefix_fanout_groups` takes the raw in-batch LCP (inspection only). Decide-by 2026-10-04 for the --kv-allocator vmm door unchanged.
- RULE (lead, 2026-09-21): never signal a process you did not start on this rig; identify by cwd before any kill, and if it is not your worktree, leave it. Day 24: the cell's own server is identified by cwd == the lane worktree before SIGTERM (`run-day24-cell.sh`).
