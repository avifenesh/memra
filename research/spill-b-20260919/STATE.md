# WP-B day 33 checkpoint: memra#680 fixed under the door and green on the local RTX 5090; the target-card part is owed (BOX3 lost)
- Branch lane/spill-b-20260919 on origin/main c3eb41d12. Pushes in the announced development mode (`UNQUALIFIED DEVELOPMENT`, logged; no qualification claimed). Engine change under the default-OFF door only; no default moved.
- Pre-registration `9491bc5d9` committed 14:08:50Z and pushed; the first boot started 14:09:07Z (DAY33.md 1).
- Diagnosis (DAY33.md 1.1, worker.rs at c3eb41d12): the burst was admitted by the generic VRAM gate (`admission_required` 23548 against one `admission_headroom` reading, `sufficient` 27939). The door acts only in the defer branch (24024, 24161) and printed only defers and refusals (24211). The reading counts driver free plus pool-cached (27840), not resident prefix entries, and cannot see the prefill workspace of sessions admitted earlier in the burst (it grows at prime time; the cost model's own note, 2598 to 2603). The charge does use the learned residual (`estimate` 2739, `observe` 2763). A prefill OOM is answered terminally as 503 (25757, 26223; `is_cuda_oom` promotion 1539); the step-OOM park (28120) covers decode only.
- Fix `30a5ab697`, armed only: every headroom reading of the admission block is reduced by `pending_prime`, the workspace still-priming sessions owe; every admission prints `verdict=admit` with `pending_prime=`; a prefill CUDA OOM on a pre-emission session parks and requeues. Unarmed the term is 0 and `less_pending(0, _)` is the identity. CPU: `--lib` 883/0, clippy `-D warnings` clean, fmt clean, 8 new tests plus the extended park census.
- 5090 (DAY33.md 2.3): red on main at G2 (8192, B = 64): 19 x 200, 10 x 429, 35 x 503 prefill OOM; at R32 2 x 503. Green: G2 36 x 200, 28 x 429, no OOM (twice); R32 19 x 200, 13 x 429, no OOM. G-NOOM and G-BOOK PASS on every green ON boot; V-ID-FIX, V-ID and V-OFF 16/16; card verdict GREEN. G1 (2048) was not red on main.
- Gate `tools/admit-mem-burst-gate.sh` (G2): RED on main (34 x 503), ALL GREEN twice on the fix; wired into `tools/local-ci.sh` (`MEMRA_CI_ADMIT_MEM_BURST=0` skips; FLAGS and TESTING rows). About 7 minutes on the 9B.
- Target card NOT RUN: the BOX3 spot instance was lost after the 14:51:38Z launch (host key changed at the same address; lead confirmed the VM is gone). Owed: red-R64, red-off, green-R64, green-off on a replacement card, 0.1 agent-day plus about 1.5 h of card time.
- Open from earlier days: the door decision (owner, decide-by 2026-10-07); `--kv-allocator vmm` decide-by 2026-10-04; `MEMRA_KV_PARK_COMPACT` decide-by 2026-10-06 with its deciding cell; memra#476 boot pre-grow booking point (owner); outstanding-only `W` release at prime completion; DAY24's step-OOM and client-disconnect fault arms; the `[spec-vg]` predictive gap on MoE + linear families; darklanes `lane/budget-journal-order-20260922` at `20f8140e8` (owner, money path) and memra#464 guard seed (0.5 agent-day).
- RULE (lead, 2026-09-21): never signal a process you did not start on this rig; the driver stops only a server it spawned whose cwd is this cell's scratch dir.
