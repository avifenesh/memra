# WP-B day 26 checkpoint: memra#539 census from the code, allocated-over-used cell on both cards, KV-RESIDENCY-DESIGN.md
- Branch lane/spill-b-20260919 on origin/main 8e94a480b (#619 merged at day start as `1c66ff10e`). Pushes in the announced development mode (`UNQUALIFIED DEVELOPMENT`, logged; no qualification claimed). Day 25 is being integrated as integ29. No engine change today.
- Census (DAY26.md 1): `request_ctx_cap` (worker.rs:21705) gives `prompt+max_tokens+8` bounded, the server context (MEMRA_CTX or the checkpoint's 262,144) when `max_tokens` is omitted, `prompt+8192+8` only under the OFF `MEMRA_ADMIT_BY_MEMORY` door (decide-by 2026-09-23); every session cache is born at that cap (`new_cache_planned`/`new_session(.., ctx_cap)`, worker.rs:25336, 24718) with K and V planes of `max_ctx` rows per full-attention layer (memra-kv lib.rs:2892-2905), never grown or trimmed; admission books `ctx_cap+56` rows more than allocated (`need`); a hit is a D2D copy into a fresh cap-sized cache, the entry stays; retired caches park in the reuse pools at `cache.max_ctx` (no TTL, 2 per pool per namespace, `MEMRA_KV_PARK_COMPACT` default OFF).
- Bytes per token: 1,856 B per full-attention layer (q8_0 K + q5_1 V at kv_dim 1024); 9B 14,848 plain / 16,704 spec; 27B (= Qwen3.8 class) 29,696 / 31,552; recurrent layers fixed per session.
- Cell (DAY26.md 2), both cards, N=5 per arm per order, both orders, executed-not-qualified: target card 27B at the checkpoint context, open arm ratios median 129.26 / 71.90 / 41.61 (L0/L1/L2; 8,271,167,488 B allocated per request), bounded 1.00 everywhere, warm deferred 134.02 / 72.12 / 41.15 with `cached=0` (entries LRU-evicted inside the 800 MB budget derived `at MEMRA_CTX=8192`); concurrency at 262,144: 8/7/7 open vs 49 (69 in BA)/35/27 bounded from 82.76 GB idle free. Local 9B at MEMRA_CTX=65536, order AB: open 11.30 / 10.73 / 6.62 (the model reasons 2.4k to 5k tokens), bounded 1.00, warm deferred 11.79 / 10.62 / 6.35 `cached=0`; 8/6/5 vs 18/11/8 sessions.
- Finding recorded, not fixed: the prefix-cache budget derivation prints `at MEMRA_CTX=8192` when MEMRA_CTX is unset while the request cap resolves to the checkpoint's 262,144; 43 of 45 spec-boundary entries were evicted before their continuation on the target card.
- Design (KV-RESIDENCY-DESIGN.md): (a) the admission-by-memory door as the bounded open default, 0.5 agent-day; (b) grow-on-demand VMM planes, 3 to 4; (c) paged KV, 10 to 15; (d) COW prefix sharing on (c), 3 to 5. Recommended (a) then (b), (c)/(d) only on the shared-prefix workload's evidence. Owner decides.
- Open from earlier days: outstanding-only `W` release at prime completion; the physical book charges the shared prime slab per request; the enforcing door on the fuller charge needs its own cell; `prefix_fanout_groups` raw LCP (inspection only); decide-by 2026-10-04 for the --kv-allocator vmm door; DAY24's step-OOM and client-disconnect fault arms pre-registered only.
- RULE (lead, 2026-09-21): never signal a process you did not start on this rig; the driver signals only a server it spawned whose cwd is this tree.
