# WP-B day 30 checkpoint: memra#423 named and fixed on the deployment side of the seam; memra#464 gap written
- Branch lane/spill-b-20260919 on origin/main da1f59bf6 (#631) at `a88791f92`. Pushes in the announced development mode (`UNQUALIFIED DEVELOPMENT`, logged; no qualification claimed). No memra engine code change today (one stale doc comment in `metering.rs` corrected); no default changed.
- memra#423 (DAY30.md 1, 2): the budget journal, `balance_after_micro`, the boot backfill and the `/admin` listener are NOT in this tree (extracted 2026-08-29, FLAGS.md rows 304 to 306); they live in darklanes `serving/darklanes-metering/src/ledger.rs` behind memra's `Metering`/`Receipt` seam. Rows ARE in causal order (one `BudgetState` mutex, the journal file inside it). The chain breaks because the stamp was the AVAILABLE balance: `admit` takes each worst-case hold with no row, so every settle row stamped settled minus the OTHER in-flight holds; breaks cancel at quiescence and the live gap is the outstanding holds. Sequence diagram in DAY30.md 2.
- Cell, local 5090, darklanes-serve on the 9B, synthetic tenant, N=16 concurrent plus one credit in flight (DAY30.md 3, 4): BEFORE `JOURNAL-ORDER CELL arm=before N=16 rows=17 chain_violations=17 order_violations=0 conservation=ok cancel_sum=0 -> FAIL`; AFTER (twice, pre-fmt and committed tree) `... chain_violations=0 order_violations=0 conservation=ok cancel_sum=0 -> PASS`.
- Fix, darklanes `lane/budget-journal-order-20260922` at `20f8140e8`, pushed NOT merged (owner decides; a money path): `tenant_reserved` per tenant at admit/settle/drop, every row stamps `balance_after + tenant_reserved_after` (the SETTLED balance); no field, kind, file, flag, thread or numeric change; 4 tests from the diagram (3 red before), suite 92/92, clippy clean.
- Owner decisions stated (DAY30.md 5): the stamp now means settled (admin reply and snapshot stay available); source-file top-ups still have no row (second chain break, 0.2 agent-day, format decision); audits cut at the first boot on the fixed binary; pre-existing fmt drift in darklanes `admin.rs`/`capture.rs` untouched.
- memra#464 (DAY30.md 6): the backfill's only evidence is journal membership; a carried row and a crash-unbilled row are byte-identical to it; today's fix repairs the seed arithmetic but adds no id to the guard. Two guard-only seeds offered (the `.accounted` sidecar, or zero-amount debit rows per carried id); 0.5 agent-day; commented on #464.
- Open from earlier days: the admission-by-memory door decide-by 2026-09-23 (KV-RESIDENCY-DESIGN.md (a), 0.5 agent-day); `--kv-allocator vmm` decide-by 2026-10-04; `MEMRA_KV_PARK_COMPACT` decide-by 2026-10-06 with its deciding cell; memra#476 boot pre-grow booking point (owner); outstanding-only `W` release at prime completion; the enforcing door on the fuller charge; DAY24's step-OOM and client-disconnect fault arms; the `[spec-vg]` predictive gap on MoE + linear families.
- RULE (lead, 2026-09-21): never signal a process you did not start on this rig; the driver stops only a server it spawned whose cwd is this cell's scratch dir.
