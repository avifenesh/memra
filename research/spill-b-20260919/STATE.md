# WP-B day 22 checkpoint: memra#427 named and fixed on the lane (the 16-row prime chunk runs the wide chunk's program), receipts (a) to (f) on the local 5090, table and kernel-check on the target card
- Branch lane/spill-b-20260919 on origin/main 5804cac6a (day 21 merged via #612; merge 051bbfee3). Pushes in the announced development mode (`UNQUALIFIED DEVELOPMENT`, logged; no qualification claimed). The day-22 agent died mid-run (API outage) after the fix cells F1..F4 and before writing them up; resumed 19:02Z, receipts committed as they were (de4fb332b), then finished.
- The named operation: `Engine::matmul`/`matmul_pre` at m = 16 for a quantized `out_f < 128` projection (Qwen3.8: GDN `ssm_beta`/`ssm_alpha`, NVFP4 [5120, 48], all 48 GDN layers) takes the batched MMVQ tier `(2..=16)` (32-thread warp reduce); at m >= 17 the `grid.y = m` dp4a kernel. `WIDTH WALK width 16 vs 17: 96 of 497 tensors differ; tensor names: {"ssm_alpha", "ssm_beta"}`; `48 vs 17: 0 of 497`.
- The fix (89c693be0, `fix:`): RAII scope `Engine::prefill_rows_scope` armed at the top of `HybridModel::prime_layers`; `batched_tier_admits() = verify_exact_on() || !prefill_rows_on()` on the tier in `matmul` and `matmul_pre`. No new MEMRA_* read, no kernel, no third program; verify-exact keeps precedence. The continuation gate counts a differing 16-row split (tightening). Decision record: `PRIME-MIN-T-DECISION.md` (shape (b), the schedule-side fold, rejected: it leaves the engine invariant broken at width 16).
- Receipts on the fix binaries (local 5090, `rtx5090-day22/`): (a) `fix-walk-retry4` `scope=prime width 16 vs 17: 0 of 497 tensors differ` (bare scope unchanged, 96); (b) `fix-gate` seven arms all `ok`, `A4 CONTINUATION GATE: PASS`, `9280 + 16: logits_sha=14ab5f8b365dbd71 ok`, chunk-32 one-call `14ab5f8b365dbd71` (was `35bd15f063bfd5ba`); (c) `fix-kc` `ALL GREEN (109 cells, 10 skipped)`; (d) `fix-hitgate` `SPEC-ON-CACHE-HIT GATE: ALL GREEN (qwen)` (drafter absent, WARNING branch); (e) `fix-twin` `... V1=ok V2=ok V3=ok V4=ok V5=ok V6=ok -> PASS`; (f) `base-gate` (fix hunks reverted): default 9296 `14ab5f8b365dbd71` = fix, 9297 `e641952cac19e526` = fix, chunk-32 `35bd15f063bfd5ba` vs fix `14ab5f8b365dbd71`: the fix moved only the 16-row chunk program.
- Target card (`pro-single-day22/`): see DAY22.md section 2 (the same seven-arm table on the fix binary built on the box, kernel-check with the 27b manifest's one cell absent for want of the 9B artifact, the step35 manifest alone beside it).
- Not armed, stated: `prime_layers_gemma` and `step35_prime_cache_batch` do not take the scope; owed to the integ before main: `run-gen` on a 16-token and a t = 16 mod 4096 prompt, `run-spec` K=1..8, `docs/TESTING.md` listing the 16-row split as a verdict.
- memra#445 sections 2 and 3: mapped as a comment (section 2 open behind #151's gemma snapshot layout; section 3 open, needs a gemma sampled-spec ladder at c=1/4/8 on the target card reading the `[spec-k]` source lines).
- Open: `prefix_fanout_groups` takes the raw in-batch LCP (inspection only). Decide-by 2026-10-04 for the --kv-allocator vmm door unchanged.
- RULE (lead, 2026-09-21): never signal a process you did not start on this rig; identify by cwd before any kill, and if it is not your worktree, leave it.
