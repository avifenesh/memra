# WP-B day 29 checkpoint: the 27B twin `V3=FAIL` on the 5090 named and typed, the 5090 door gates green in both arms
- Branch lane/spill-b-20260919 on origin/main 45c4c3cd2 (#628) at `f0dc5d0d8`. Pushes in the announced development mode (`UNQUALIFIED DEVELOPMENT`, logged; no qualification claimed). No engine change today; no default changed; the only code change is the twin gate (`tools/prefix-newest-turn-fits-gate.py`).
- A's day-18 27B twin `V3=FAIL` (DAY29.md 1, 3): repro 3 of 3 on the local RTX 5090, gate unchanged, door OFF, A's line to the byte, with a 1390 MiB co-tenant (another session's python) on the card on every 1 Hz sample. Named: the cache-on boot's turn-2 `[admit-oom] reclaim-on-defer` released one cohort-shaped parked plain session (`3272 x 29696 + 313187668 = 410352980` B) while the cache-off boot kept its counterpart until its own turn-3 reclaim took another; the two boots crossed the 2 GiB admission floor on different turns because the card had 1.46 GB less free than day 17 (the co-tenant) and the cache-on boot carries 0.93 GB of cache. Not a door delta, not the day-27 budget change (the gate configures its budget), not an accounting defect (every settle balances). The clean-card run arrived on its own (the co-tenant left mid-battery): `evictions=9 -> PASS`, A's day-17 values to the byte.
- Gate change (DAY29.md 4): V3 unchanged (clause, form, 64 MiB slack); when the two boots' `reclaim-on-defer` parked-session releases differ on any window the gate REFUSES (`REFUSED: V3 premise: ...`, exit 2) naming windows, releases, budget and the card at each boot (now sampled into the receipts), and keeps the would-be verdict in `summary.json`. CPU test `test-day29.py` 13 ok. Fault arm on the card (self-owned 1390 MiB child under one collector hold): DAY29.md 6.
- 5090 door gates on tree `1dce64df0`, both arms (DAY29.md 5): identity default and plain `ALL GREEN (teeth=0)` x4 (default OFF, the arm A never got, included), failure `ALL GREEN` x2, fault `ALL GREEN` (65), hit `ALL GREEN (qwen)` x2, unit cells `8 passed`, 9B twin `REFUSED` (cohort shape, both arms), 27B twin `-> PASS` both arms on the clean card. Target card: 27B twin `-> PASS` both arms on the patched gate, premise rows equal.
- Open from earlier days: the admission-by-memory door decide-by 2026-09-23 (KV-RESIDENCY-DESIGN.md (a), 0.5 agent-day); `--kv-allocator vmm` decide-by 2026-10-04; `MEMRA_KV_PARK_COMPACT` decide-by 2026-10-06 with its deciding cell; memra#476 boot pre-grow booking point (owner); outstanding-only `W` release at prime completion; the enforcing door on the fuller charge; DAY24's step-OOM and client-disconnect fault arms; the `[spec-vg]` predictive gap on MoE + linear families (named for the lead).
- Same-card note for the lead: the 27B at the 5090's 24 GB with the twin gate's 1 GiB budget sits within one co-tenant of the admission floor; the 96 GB card cannot cross it at this shape. The 9B twin refusal (cohort shape) stands from A's day 17.
- RULE (lead, 2026-09-21): never signal a process you did not start on this rig; the driver stops only a server it spawned whose cwd is this tree; today's fault-arm child is this cell's own.
