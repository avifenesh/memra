# WP-B day 19 checkpoint: both prefix-cache capture sites on the GDN prime grid (memra#602)
- Branch lane/spill-b-20260919: 49d79b946 the spec-side fix (worker.rs capture_at/prime split/republish, spec.rs prompt-end republish only on the grid) plus the #379 and evict-reclaim gate restatements; fa5d7a014 the #379 site list correction, lane A's aligned equalities, SERVING/TESTING; day-19 receipts, DAY19.md, verify-day19.py, INDEX row and this record on top.
- Push: the first attempt at 49d79b946 was REFUSED by the pre-push hook's perf-ci freshness gate (engine files touched: spec.rs; `Run: tools/local-ci.sh --perf`); no skip variable, no --no-verify. `tools/local-ci.sh --perf` then ran on the local card (exit 0, `perf stage: 0 fail, 0 warn`, two perf-ci rows) and the push was allowed (`3e261c1c2..fa5d7a014`). The commits reached the target card as git bundles.
- Fix: one capture law for every site. Cold spec `capture_at` = `seed_capture_boundary(prompt, 0)`, its own prime stop beside the affinity boundary; restored-session republish at the stable boundary ahead of the restore or at the seed's grid boundary; the engine publishes a prompt-end entry only when that end is a grid multiple. cached_tokens reports the aligned restored length everywhere. No prime program moved.
- Completed fix on both cards: twin gate (day-16 shape) `identity_ok=12/12 grid_ok=31/31 off_grid_calls=0 ... -> PASS` (local and target; local digests equal day 18's 12/12), restore gate `identical=5/5 grid_ok=5/5 -> PASS` (both), serve-smoke `0 failed`, cache-meter `0 failed` (target).
- #379 gate, local card, first gate source: fix `1 FAILURE(S)` (only the `restore-suffix-feed` site this lane had wrongly required: the deferred restore draws in the prime path), base `3 FAILURE(S)` (the accounting clauses, entries at the prompt end); every identity clause green on the fix. Assertion removed the same day; reruns on the corrected gate: fix `SPEC-ON-CACHE-HIT GATE: ALL GREEN (qwen)` (61 ok), base `2 FAILURE(S)` (7 accounting clauses, every identity clause green). `tools/local-ci.sh` correctness stage exit 0 with the hit gate ALL GREEN in both postures.
- Evict-reclaim gate V3 now counts `pool_retained_bytes` as headroom (a gate bug; its docstring already said so): target card fix `... pool_retained_bytes=148013056 ... V3=ok -> PASS`, base `... pool_retained_bytes=140549120 ... V3=ok -> PASS`. Lane A's gate reads `capture_len` of the leader (its spec entries are `insert (spec-boundary): 64 tokens` for 83-token prompts, `hit: 64 of 96`): first run FAIL on the two whole-prompt equalities only, rerun PASS (28 ok).
- Local checks at fa5d7a014: fmt PASS, clippy (server + engine) -D warnings PASS, `cargo test -p memra-server` 758 passed, check-flags no uncovered names, docs-registry census PASS, `git diff --check` clean. `verify-day19.py`: `DAY19 REPLAY OK`. `local-ci.sh --perf` exit 0.
- Open: `prefix_fanout_groups` takes the raw in-batch LCP (inspection only). Decide-by 2026-10-04 for the --kv-allocator vmm door unchanged.
