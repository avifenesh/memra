# WP-B day 32 checkpoint: the admission-by-memory cell rerun on the fixed tree (main d544c6b82, memra#659), both cards; no crash anywhere, V-DOOR FAIL on V-ALLOC's arithmetic only, and a new prefill-OOM failure in the target card's 32768 burst
- Branch lane/spill-b-20260919 on origin/main 711be12c3 (#671, integ50). Pushes in the announced development mode (`UNQUALIFIED DEVELOPMENT`, logged; no qualification claimed). No engine change, no default moved, no open-output value picked. Every cell `executed-not-qualified`.
- Pre-registration `51c113659` committed 06:32:14Z and pushed; the first boot started 06:33:04Z. Arms, values, workload, orders, cards and drivers are day 31's (hashes in DAY32.md 1.3); the reader `day32-compare.py` closes DAY31.md 2.2's three gaps (V-CRASH, non-vacuous V-RETRY, every ON `length` row in one conserved term) with band G = v.
- Binaries from main `d544c6b82` in detached worktrees: local `d92b6cde...484ea7`, target `5bbe4857...b88a8`. Local chain 06:33 to 10:57Z; BOX3 07:46 to 13:40Z, started after lane E's `LANE-E-PRO-DONE` (lead ordering).
- Results (DAY32.md 2): V-CRASH PASS on all 16 boots (no panic, respawn, FATAL, NaN trap or spec refusal); V-ID PASS on all 12 ON arms; every ON `length` row at G = v; V-RETRY PASS on all 16. V-ALLOC FAIL on the 12 ON boots, every mismatch exactly -8: the cost line books `max(ctx_cap, P + budget + 64)` = `P + v + 64` on this tree, and 1.6 carried day 31's `P + v + 72`. V-DOOR FAIL on both cards on that term alone.
- SELECT as printed: 5090 R1 8192, R2 8192, R3 32768, R4 context; target R1 none (10, 10, 8 truncated), R2 none (max natural stop 193,178), R3 32768, R4 context.
- New failure: the target card's 32768 burst, both orders, admits 58, refuses 6 (waited 15.6 s), and 46/47 of 64 die in prefill with `CUDA_ERROR_OUT_OF_MEMORY` (503, Retry-After 5), with 13.1 GB of prefix cache resident and no reclaim line; first lines `pro-single-day32/box/boots/O1-on32768/server.log:6785`, `O2-on32768/server.log:6790`. Engine, the lead's; a lane-B repro cell is 0.2 agent-day plus about 25 minutes of card time.
- Owed: the door decision (owner, decide-by 2026-10-07); the prefill-OOM defect (lead); a later cell pre-registers V-ALLOC in the `need` form.
- Open from earlier days: `--kv-allocator vmm` decide-by 2026-10-04; `MEMRA_KV_PARK_COMPACT` decide-by 2026-10-06 with its deciding cell; memra#476 boot pre-grow booking point (owner); outstanding-only `W` release at prime completion; the enforcing door on the fuller charge; DAY24's step-OOM and client-disconnect fault arms; the `[spec-vg]` predictive gap on MoE + linear families; darklanes `lane/budget-journal-order-20260922` at `20f8140e8` (owner, money path) and memra#464 guard seed (0.5 agent-day).
- RULE (lead, 2026-09-21): never signal a process you did not start on this rig; the driver stops only a server it spawned whose cwd is this cell's scratch dir.
