# WP-B day 27 checkpoint: the prefix-cache budget's context term fixed, the park door's receipt for the owner
- Branch lane/spill-b-20260919 on origin/main dc192cd95 (#621, day 25 on main, merged at `9d6a0e282`). Pushes in the announced development mode (`UNQUALIFIED DEVELOPMENT`, logged; no qualification claimed). One engine change today: `crates/memra-server/src/worker.rs` (`prefix_budget_ctx`, `init_prefix_cache_budget`), tree `de2c781e6`.
- Fix (DAY27.md 1): the derived prefix-cache budget read a literal 8192 as its context term when `MEMRA_CTX` was unset while `request_ctx_cap` served the checkpoint's 262,144; on the target card 800,325,632 B of budget beside 8,271,167,488 B sessions, 43 of 45 spec-boundary entries evicted before their continuation (day 26). Now the served context per model through `resolve_ctx`; the two-entry count and the boot-free clamp unchanged; boot line names the source. Target card after: budget 15,883,042,816 B, 15 of 15 deferred continuations hit (`cached_tokens` 1440/3104/5760), P and G equal to day 26 on 45 of 45, 45 entries resident, 0 evictions; idle retention 30.1 -> 39.1 GB (the entries stay, as the budget intends). CPU test on the set and unset arms; clippy `-D warnings` clean. `MEMRA_CTX=65536` (the local shape) is a no-op by construction.
- Census correction (DAY27.md 2.1): the day-26 "four parked entries, two per pool" was wrong; `continuation_pool_entries=0` in every target cell, `spec_pool_entries=2`. A spec session parks in the spec pool only. The 30 GB at idle is pool reserved minus post-warmup used: two parked spec sessions at `ctx_cap` (up to 2 x 8.27 GB) plus the CUDA pool's cached free blocks (12 to 29 GB, the complement).
- Park cell (DAY27.md 2.5, target card, default vs `MEMRA_KV_PARK_COMPACT=1`, both orders, N=5 per arm per length): as predicted from the code, `park-compact lines: 0`, retained 39,090,913,280 B in every arm, `continuation_pool_hits=0`, `spec_pool_hits=0`, digests equal 45 of 45 across arms and boots. The door cannot reach the spec pool by design.
- Plain-path extra pair (DAY27.md 2.6, `MEMRA_SERVE_SPEC=0`, labelled): the door engages (46 `park-compact` lines, `2071..6459 of 262144 rows retained` in about 2 ms), retained 39,191,576,576 -> 22,649,241,600 B (the two 262,144-row plain caches), pools still 0 hits, digests equal 45 of 45 across arms and, across paths, plain vs spec 45 of 45.
- What the parked entries buy on days 20 and 26: nothing measured (0 pool hits; every `prompt_ids` replay `plain-affinity: declined`, every continuation `spec-affinity: declined`). `MEMRA_KV_PARK_COMPACT` row has no `decide-by:` (recorded on the row and on #539). No default changed; the park policy on the spec pool is the owner's.
- Local RTX 5090 Laptop GPU (DAY27.md 3): the canonical lock was held by another lane's server from before 00:42Z to 02:02Z; the chain waited on `flock -w 3600` (the after cell's first attempt expired, kept as a labelled failure, rerun after). All five local runs done: the after cell at `MEMRA_CTX=65536` is a no-op by construction (budget 2052MB unchanged, `cached=0` on all 15 as day 26, digests equal to the same-config boot 45 of 45); the park cell reads as the target card's (`park-compact lines: 0`, retained 7,449,083,904 B in every arm, both pools 0 hits, digests equal 45 of 45 across arms and orders).
- Open from earlier days: the admission-by-memory door decide-by 2026-09-23 (KV-RESIDENCY-DESIGN.md (a), the decision cell is 0.5 agent-day); `--kv-allocator vmm` door decide-by 2026-10-04 (the pool's cached blocks are its subject); outstanding-only `W` release at prime completion; the enforcing door on the fuller charge; DAY24's step-OOM and client-disconnect fault arms pre-registered only.
- RULE (lead, 2026-09-21): never signal a process you did not start on this rig; the driver stops only a server it spawned whose cwd is this tree.
