# WP-B day 20 checkpoint: the day-16 confirmation redone; the prefix-cache policy decision holds on both card classes (memra#523 item 2)
- Branch lane/spill-b-20260919 on origin/main 2a589903d (days 17 to 19 merged via #606): 778da9359 the day-20 pre-registration (before any GPU run), then the receipts, DAY20.md, verify-day20.py, the decision paragraph and the INDEX row on top.
- Binary: detached worktree at 9466b8912 (harness and two-arm binary) with 269178070, 49d79b946, fa5d7a014 cherry-picked (every crate file auto-merged; conflicts in gate scripts and docs only, resolved to the incoming version; spec.rs identical to the lane tip). SHA-256 bd1423b0... Worktree and target dir removed at close.
- Shape: the day-16 shape on the 32-token grid with the byte shares preserved (cohort 1248/1344/1440/1536, 73.8 %; loop 10912 + 160, entries 44.7 to 49.5 %; budget 1024 MiB, ctx 16384): the harness's own seed assertions require on-grid prompts under the capture law; nothing in the harness was changed.
- Verdict, verbatim: `PREFIX-POLICY-AB: budget_bytes=1073741824 cohort_tenants=4 cohort_bytes=792920064 turns=12 start_tokens=10912 grow=160 return_every=3 pairs_per_order=5 runs=20 requests_per_run=28 digests_identical=28/28 computed_tokens slru_median=31776 lru_median=29600 (N=10 each) pairs_slru_better=0/10 pairs_lru_better=10/10 ties=0/10 return_cached slru_median=0 lru_median=1696 loop_cold_after_1 slru_max=0 lru_max=0 refusals slru=0 lru=0 temp_c=66.0..74.0 power_limit_w=None -> WINNER=lru` (56 to 88 C over the cell, no power limit reported on the laptop part).
- Precondition: 28/28 digests in every run of all three cells (day 16 read 26/28); every cached_tokens row equals day20-predict.py (280/280 per arm in the scored cell); the same three mechanisms as the target card.
- Confound: at the default continuation pool the reclaim ladder fired 11 times per run (12 prefix entries), reproduced to the event by `effective free < cost + 1611 MB` over the day-16 /metrics rows; the pool is inert on prompt_ids replays (`plain-affinity: declined` on every row) and holds the VRAM. Scored cell with MEMRA_REUSE_POOL=0: 0 reclaim events, 0 admit-trim, 0 parked in 21 boots. Sizing the shape alone cannot remove it on this card (arithmetic in DAY20.md).
- Lock: attempt 0 at 15:06:53 UTC refused (busy 3 s after the previous cell's exit), one 90 s wait, attempt 1 held the lock for the whole cell; no holder signalled.
- Checks: fmt PASS, docs-registry census PASS, git diff --check clean, verify-day20.py `DAY20 REPLAY OK` x3, tier-battery --validate `CAPTURE INTEGRITY MATCH` x3.
- docs/decisions/PREFIX-CACHE-POLICY.md: the day-16 no-verdict paragraph replaced by the day-20 result; the Status line and the follow-up bullet no longer say the 5090 cell is owed.
- Open: `prefix_fanout_groups` takes the raw in-batch LCP (inspection only). Decide-by 2026-10-04 for the --kv-allocator vmm door unchanged.
- RULE (lead, 2026-09-21): never signal a process you did not start on this rig; identify by cwd (`readlink /proc/<pid>/cwd`) before any kill, and if it is not your worktree, leave it.
