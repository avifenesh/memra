# WP-B day 17 checkpoint: the day-16 digest divergence reproduced and classified on the local RTX 5090 (#523 thread)
- Branch lane/spill-b-20260919 on the merge of origin/main 9ef2f04d6 (ee208a4df, pushed): a348572ad day-17 receipts A/B/C, f65098c2b probe restore-points mode, then E/F/D receipts, DAY17.md, verify-day17.py, this record, the INDEX row, the decision-record paragraph.
- Binary: lane-tip memra-server release 5191591b (origin/main + 87d9e5963 plain-LRU only, pending #598; the prime, snapshot, restore and suffix-prime programs are main's), built under the CPU quota, receipt rtx5090-day17/build-tip/.
- Cell A (twin gate, default shape): `... V1=ok V2=ok V3=ok V4=ok -> PASS`, identity 8/8. Cell B (day-16 lru shape): `... -> PASS` mechanics, identity 11/12: turn 10, 12,350 = 12,200 restored + 150, restored 22f02397 vs cold 4415b7e3, all twelve digests equal day 16's lru run on a different binary.
- Probe C (texts): `RESTORE-VS-COLD: arm=batched ... identical=11/12 first_divergent_turn=10 ... first_diff_char=1 -> DIVERGENT`; restored `"_\t\t\"\t\t\"\t"` (8, length) vs cold `"_\n"` (3, stop); generated token 2 differs. Receipts: cold prime calls all `grid_off=0`; restored suffix call `start=12200 take=150 grid_off=8`.
- Classification: two programs named. A = cold prime, calls on the 32-token GDN WY grid; B = restore at the prompt-end seed boundary (arbitrary position) plus one off-grid prime call, which the engine's own law (`align_prime_ranges_to_gdn`) says is not bit-identical. Probe E: on-grid restores 12,288/12,320 identical, off-grid 12,250/12,300 flip to the chain's stream (`identical=3/5`). Probe F (`MEMRA_GDN_CHUNKED=0`, split-invariant scan): `identical=12/12 -> IDENTICAL`, the restore itself is exact; turns 6 and 10 (11,750 and 12,350, the day-16 slru and lru flip prompts) are greedy near-ties. Not the admission reclaim (it spared the restored entry and ran in every turn).
- Probe D (`MEMRA_PREFILL_TICK=8`, every prompt token through decode_step on both boots; `MEMRA_PRIME_TOKENWISE` does not reach the server's prime): `RESTORE-VS-COLD: arm=tokenwise turns=10 compared=1 identical=1/1 -> IDENTICAL`; nine restores deep the restored turn 10 is the cold turn 10 under one program.
- Verdict: a correctness defect (owner law: restored suffix == cold prime on every card class); the class is card-independent, which request flips is card-dependent, so the target card's 28/28 on day 15 was near-ties that did not flip, not identity. No engine or server code changed today; the fix (align the prompt-end seed capture to the grid, make the twin gate's identity column a verdict) is its own lane day. Filed as memra#602 (bug), linked from #523 by a comment (#523 left open).
- Local checks: fmt PASS, docs-registry census PASS, `git diff --check` clean; `verify-day17.py`: `DAY17 REPLAY OK`.
- Decide-by 2026-10-04 for the --kv-allocator vmm door unchanged.
