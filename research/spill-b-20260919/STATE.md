# WP-B day 16 checkpoint: the 5090 confirmation cell stopped on its precondition (#523 item 2)
- Branch lane/spill-b-20260919 on the merge of origin/main 34ed99dfc (0416a2e72): 6574ff5db pre-registration (DAY16.md, `build-day16.sh`, `run-day16-ab.sh`, `build-tip/` receipt), 358cf5a65 `day16-predict.py` (day-15 model with the restore-fence lease correction; reproduces day 15 exactly), then the smoke receipts, `verify-day16.py`, this record, the decision paragraph and the INDEX row.
- The cell: the day-15 harness and two-arm binary from history (`9466b8912`) in a detached worktree (removed at close), built under the CPU quota, run through the collector `--rig rtx5090` with the canonical `/tmp/memra-5090.lock`; shape scaled to 1024 MiB with the target's byte shares (cohort 73.9 %, loop 45.0 to 49.6 %, grow 150, ctx 16384), stated in DAY16.md.
- Smoke (one pair per order, 4 runs + cold, 386.7 s), verbatim: `PREFIX-POLICY-AB: budget_bytes=1073741824 ... digests_identical=26/28 computed_tokens slru_median=31700 lru_median=29550 (N=2 each) pairs_slru_better=0/2 pairs_lru_better=2/2 ties=0/2 return_cached slru_median=0 lru_median=1700 loop_cold_after_1 slru_max=0 lru_max=0 refusals slru=0 lru=0 temp_c=69.0..75.0 power_limit_w=None -> DIGEST-FAIL`; collector `failed`, exit 1.
- Agreed with the target card: the primary and secondary on the prediction to the token in all 4 runs (31,700 vs 29,550, lru better by 2,150; returns cached 0 vs 1,700), the day-15 mechanisms 2 and 3 in the policy's own evict lines.
- Disagreed: the precondition. One restored-suffix request per arm lineage differs from the cache-off boot (slru at request 14, lru at request 20), deterministic across runs; identity across runs held. And a confound: the admission reclaim ladder (`[admit-oom] reclaim-on-defer`, global LRU, both arms) evicted 12 prefix entries per run at loop turns 2 to 12 under VRAM pressure (peak 23,033 of 24,463 MiB); the target card logged none.
- The 20-run cell was NOT run (the brief: stop and report on disagreement; the rule was already failed). `verify-day16.py`: `DAY16 REPLAY OK: receipts consistent, precondition FAILED on this card, no verdict`.
- Decision text unchanged; one paragraph appended ("Confirmation on the second card class") stating the contrary result. The lead decides: the digest finding wants day 14's twin gate on the 5090, on its own lane; the shape cannot be replayed on this card without the reclaim confound at any budget the 80 % share allows.
- Decide-by 2026-10-04 for the --kv-allocator vmm door unchanged. Issue comment on #523 posted after the receipts land (the lead closes).
