# WP-B day 28 checkpoint: memra#476's graph growth term measured (floor covers it, booking point proposed), the park door dated
- Branch lane/spill-b-20260919 on origin/main 3df055601 (#626, day 26 on main, merged at `88552c5ef`). Pushes in the announced development mode (`UNQUALIFIED DEVELOPMENT`, logged; no qualification claimed). No engine change today; no default changed.
- memra#476 graph growth (DAY28.md 1, 3): the only shape-keyed, never-freed allocation on these models is the FA partial pool (`fa_part_pool`, retire-on-grow, `g_i = 4 x (o_i + 2 x m_i)`, doubling ladder); draft-graph state `D` is booked since day 24; the verify-graph pool is off on dense models (0 `[spec-vg]` lines). Shape walk (warm, S1, S2, S4, S8, L 23k, S8b) on both cards, one binary `3c9bdfeaa`, `executed-not-qualified`: growth after ready 38,638,080 B (target, floor 2,300,575,744) and 25,758,720 B (local, floor 1,610,612,736), two grows each (S8 at 8 in flight, L at 1), `delta_D = 0`. G1 PASS both (1.7 % / 1.6 % of floor against the 10 % line): no per-request booking lands. G3 `residual = 0` on 17 exact rows per card, B = 1, 2, 4, 8 (the day-24 fix holds on every shape). G2 FAIL both (in-flight under_real 7.44 GB / 3.14 GB): the pool's cached blocks plus the intended retention, not a graph term (its share 0.5 % / 0.8 %); stands as measured.
- Proposed booking point (owner): pre-grow the pool at boot through the existing `fa_dcw_pool_ensure` for the served context and the batch cap, before the probe's watermark reset, so the floor is measured with the pool final and `grows after ready = 0` (about 0.3 agent-day); not landed (moves the boot footprint by the final rung).
- `MEMRA_KV_PARK_COMPACT` (DAY28.md 2, `docs/FLAGS.md` row, `KV-RESIDENCY-DESIGN.md` addendum): decide-by 2026-10-06 set; the deciding cell is the plain-path resume byte-identity gate on both resume shapes plus the step-OOM adjacency replay and the copy-cost pair on both cards (about 1 agent-day); promotion moves only plain-pool retained bytes, deletion loses nothing measured, the spec pool is a separate owner decision (#539).
- Local RTX 5090: attempt 1 died at boot behind another session's 22 GB outside the lock (`CUDA_ERROR_OUT_OF_MEMORY`, kept as a labelled failure); the cell ran after a bounded wait. Target card attempt 1 was a runner bug (`env ... run`), fixed, no data.
- Open from earlier days: the admission-by-memory door decide-by 2026-09-23 (KV-RESIDENCY-DESIGN.md (a), 0.5 agent-day); `--kv-allocator vmm` decide-by 2026-10-04; outstanding-only `W` release at prime completion; the enforcing door on the fuller charge; DAY24's step-OOM and client-disconnect fault arms; the `[spec-vg]` predictive gap on MoE + linear families (named for the lead).
- RULE (lead, 2026-09-21): never signal a process you did not start on this rig; the driver stops only a server it spawned whose cwd is this tree.
