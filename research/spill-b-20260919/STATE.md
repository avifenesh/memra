# WP-B day 31 checkpoint: the admission-by-memory door measured, OFF against ON at 2048, 8192 and 32768, on the 5090 and the target card; the open-output bound reaches an engine defect under default spec
- Branch lane/spill-b-20260919 on origin/main 5f1b0eda4 (#656). Pushes in the announced development mode (`UNQUALIFIED DEVELOPMENT`, logged; no qualification claimed). No engine change, no default moved, no open-output value picked. Every cell `executed-not-qualified`.
- Pre-registration `ee53f7117` committed 2026-09-22T23:09:14Z; the first boot started 23:09:33Z. Arms, bound values, orders and reader unchanged after any result (DAY31.md 1).
- D1 survey (`OPEN-OUTPUT-SURVEY.md`, `3d160ffc4`): vLLM, SGLang, llama.cpp and TensorRT-LLM bound an omitted output by the remaining context; OpenAI documents the same contract. R4 = `context`.
- D3 cell, both orders on both rigs (DAY31.md 2): local RTX 5090 on Qwen3.5-9B at 65,536 (23:09 to 03:08Z); target card, one RTX PRO 6000 Blackwell on Qwen3.8-27B at 262,144 (00:06 to 05:57Z). V-BOOT, V-ALLOC and V-ID PASS on all 16 boots; V-ID `differ=0` everywhere.
- The open-output bound reaches an engine defect under default spec. `spec.rs:4590 mtp_kv_fill: scratch overflow` panics on the tick after a request ends `length` exactly at its allocation; the second panic in a process is FATAL. FATAL at 2048 and 8192 locally and at 8192 on the target card, in both orders; one panic and respawn at 32768 on the target card and at the full context on the local OFF boots; #87 NaN 500s 1 to 3 rows below the allocation on every ON arm. First panic: local `rtx5090-day31/boots/O1-off/server.log.gz` line 10286, first local ON `rtx5090-day31/boots/O1-on2048/server.log` line 116, target `pro-single-day31/box/boots/O1-on8192/server.log` line 1423.
- The ON numbers were measured on the pre-fix program. The fix is memra#659 on `lane/spec-ctx-edge-20260923` at `54711e6b5` (not merged; ON budget becomes `v`). A rerun on the fixed tree is the arm the owner would decide on.
- SELECT, stated, not chosen (DAY31.md 2.10): local R1 8192 as printed (none by 1.6's text, gap rows), R2 8192, R3 32768, R4 context; target R1 2048 as printed (not a zero-truncation observation: every long request died at the bound), R2 none (max natural G 193,178), R3 32768, R4 context.
- D4 (`DAY31-D4.md`, `3f19e40a2`): (b) one reclaim flush demoted 1 of 17 entries; D4-C PASS (11 x 429 at Retry-After 60, both tiers exhausted); D4-ID PASS 49/49.
- Owed (DAY31.md 2.11): the whole cell rerun on the fixed tree with a fresh pre-registration, 0.5 agent-day plus about 4 h local and 6 h target rig time in parallel; the door decision (owner); stale comments `admit_memory.rs:48-51` and `lib.rs:3428-3429`, 0.05 agent-day (engine, lead).
- Local 5090 lock released at 03:42:51Z; no further local cell. A local rerun waits for `SPEC-CTX-EDGE-5090-DONE` in the spec-ctx-edge lane's `rtx5090/run.log`.
- Open from earlier days: `--kv-allocator vmm` decide-by 2026-10-04; `MEMRA_KV_PARK_COMPACT` decide-by 2026-10-06 with its deciding cell; memra#476 boot pre-grow booking point (owner); outstanding-only `W` release at prime completion; the enforcing door on the fuller charge; DAY24's step-OOM and client-disconnect fault arms; the `[spec-vg]` predictive gap on MoE + linear families; darklanes `lane/budget-journal-order-20260922` at `20f8140e8` (owner, money path) and memra#464 guard seed (0.5 agent-day).
- RULE (lead, 2026-09-21): never signal a process you did not start on this rig; the driver stops only a server it spawned whose cwd is this cell's scratch dir.
