# WP-B day 15 checkpoint: the prefix-cache policy, decided (#523 item 2)
- Branch lane/spill-b-20260919 on a8c79d896 (merge of origin/main 70038ed01, which carries day 14 via #596): 9466b8912 pre-registration (harness `tools/prefix-policy-ab.py`, DAY15.md rule, `day15-predict.py`), 87d9e5963 the landing, 85138fa21 and c1f9b0a71 the record and receipts, a63739afb the merge of origin/main dcaa5bdf5 (#597, lane A's host-tier tenant-share reclaim; one worker.rs hunk resolved: A's block kept intact, my host-tier doc comment kept), b38848e6f the last comment rewording; all pushed, the hook allowed every push. Battery on the merged tree: fmt PASS, 754 passed 0 failed 6 ignored (A's 7 tenant_share cells and the 27 prefix-cache/kv-flex cells among them), clippy PASS, check-flags PASS, docs-registry census PASS, diff-check PASS. A's `tools/kv-host-tenant-reclaim-gate.sh` matches only `[prefix-host]` lines and asserts the device `skip pinned host-promote insert` line absent; none of the lines it matches changed, so no GPU rerun is owed.
- Verdict, target card (one RTX PRO 6000 Blackwell, 600 W, collector-locked, `ab-full`, 20 runs, one lock hold), verbatim: `PREFIX-POLICY-AB: ... digests_identical=28/28 computed_tokens slru_median=132300 lru_median=122700 (N=10 each) pairs_slru_better=0/10 pairs_lru_better=10/10 ties=0/10 return_cached slru_median=0 lru_median=8700 loop_cold_after_1 slru_max=0 lru_max=0 refusals slru=0 lru=0 temp_c=46.0..50.0 power_limit_w=600.0 -> WINNER=lru`. 9,600 tokens per pair (7.3 %), deterministic: 900 from SLRU evicting the loop's newest entry after each cohort return, 8,700 from SLRU protecting the loop's dead last entry over the last cohort tenant's fresh one. `verify-day15.py`: `DAY15 REPLAY OK`.
- Landed (87d9e5963): plain global LRU is the only policy; `MEMRA_PREFIX_CACHE_POLICY` and `MEMRA_PREFIX_CACHE_PROTECTED_PCT` deleted with the segments, promotion, demotion, `room_victim_with`, the SLRU tests and the harness; FLAGS "Removed doors, 2026-09-21", SERVING eviction paragraph, TESTING section, `docs/decisions/PREFIX-CACHE-POLICY.md`. Twin gate asserts a cohort eviction and never a self-eviction. No new MEMRA_* read; bytes untouched.
- Local battery on the landing: fmt PASS, `cargo test -p memra-server` 748 passed 0 failed 6 ignored, clippy -D warnings PASS, check-flags PASS, docs-registry census PASS (58 tables, 901 rows), diff-check PASS, all under CPUQuota=1200% MemoryMax=28G.
- Landed binary on the card (build-landed, 87d9e5963, `bc668f48...`): first sitting's three cells REFUSED (`[Errno 11] Resource temporarily unavailable`, another lane's collector held the lock; kept under refused-lockbusy/); second sitting `run-landed2.sh` waited two 90 s retries and ran: twin gate `PREFIX-NEWEST-TURN-FITS ... cold_turns_after_1=0 cached_ok=7/7 lines_ok=8/8 evictions=9 cohort_evictions=3 self_evictions=0 ... V1=ok V2=ok V3=ok V4=ok -> PASS` with day 14's digests 8/8; `serve-smoke: 0 failed` (spec/gemma4/Q35 SKIP); `cache-meter-gate: 0 failed`; `--validate` rc 0 on all five cells.
- One card class: target-card verdict and decision record; the local RTX 5090 replay of the shape is the follow-up. Receipts mirrored to pro-single-day15/ (binaries and bundles excluded). Decide-by 2026-10-04 for the --kv-allocator vmm door unchanged.
- Issue comment on #523 posted after the receipts land (the lead closes).
