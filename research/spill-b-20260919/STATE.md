# WP-B day 25 checkpoint: memra#524 gated (`tools/health-fault-gate.sh` arms a-f, both cards green, wired into local-ci) and `phase=warming` landed
- Branch lane/spill-b-20260919 on origin/main 81d75c457 (#615 merged at day start as `ace752157`). Pushes in the announced development mode (`UNQUALIFIED DEVELOPMENT`, logged; no qualification claimed anywhere). Day 24 is in PR #619 (integ27).
- `phase=warming` (health.rs `PHASE_WARMING`, `mark_warming` LOADING->WARMING by compare_exchange, `live()` names it; worker.rs `run_boot_calibration(.., &health)` marks it after every skip return and before the probe generation, `mark_ready` ends it). CPU: `warming_sits_between_loading_and_ready` + the day-24 source-order test extended; 18 passed. SERVING.md phase table and FLAGS rows (`MEMRA_ADMIT_CALIBRATE`, `MEMRA_PANIC_AFTER`) updated. Observable over HTTP on the respawn window only (a first boot binds after the probe, by design).
- `tools/health-fault-gate.sh`: (a) log order probe-done < listening, pre-ready samples all 000; (b) ready then first request 200/finish_reason/completion_tokens; (c) three probe-skipped boots DOCUMENTED (ready without warmup, never warming); (d) `MEMRA_PANIC_AFTER=1` respawn, /health 503 quoted in ~195 ms, 0 untruthful samples, generation 1; (a2) respawn window reads dead>loading>warming; (e) PATH-shadowed nvidia-smi hangs one probe, latch holds after the shim answers (health_200_after_clear=0, one CRITICAL); (f) SIGTERM with a stream open: /readyz 503 draining + Retry-After, new request 503 code=draining, stream to [DONE], exit 0. No new fault hook; no `/healthz` route exists (uses `/health`).
- Verdicts: local 5090 run1 and run2 `pass=6 documented=3 fail=0`; target card (one RTX PRO 6000 Blackwell, collector `executed-not-qualified`, 34.9 s) `pass=6 documented=3 fail=0`, identical asserted fields and log line numbers. Receipts `rtx5090-day25/{run1,run2}`, `pro-single-day25/`; two harness attempts kept (uutils `date %3N` prints ns; `jget` list index).
- Wired into `tools/local-ci.sh` after serve-smoke (`MEMRA_CI_HEALTH_FAULT=0` skips, FLAGS row, TESTING.md rows) on two identical local runs; arms are boots and fixed-door faults, not timings.
- Pre-registered only (not in the day-25 list): DAY24's step-OOM arm (`MEMRA_STEP_OOM_FAULT`) and client-disconnect arm; the request path under a latched gpu fault served 200 (recorded, owner question).
- Open from earlier days: outstanding-only `W` release at prime completion; the physical book charges the shared prime slab per request; the enforcing door on the fuller charge needs its own cell; `prefix_fanout_groups` raw LCP (inspection only); decide-by 2026-10-04 for the --kv-allocator vmm door.
- RULE (lead, 2026-09-21): never signal a process you did not start on this rig; the gate signals only a server it spawned whose cwd is this tree.
