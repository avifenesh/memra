# WP-B day 18 checkpoint: the prompt-end seed on the GDN prime grid (memra#602), both cards
- Branch lane/spill-b-20260919 merged with origin/main 9a1a717ce then 1b354be59; 269178070 the fix + gates, 03beaa0b9 the merge the cells ran, 66c0aea89 docs + gate default + lane A equalities back + clippy; day-18 receipts, DAY18.md, verify-day18.py, INDEX row and this record on top, pushed.
- Fix (worker.rs `seed_capture_boundary`, `Session::seed_at`): the seed publishes at the largest grid-aligned length under the prompt end leaving >= PRIME_MIN_T, through the prefill tick's existing boundary stop; typed `seed REFUSED (grid)` + `prefix_cache_seed_grid_refusals` when the aligned length is under the 64-token floor; cached_tokens reports the aligned restored length. No prime program moved, no new MEMRA_* read. The spec session's `capture_at` (prompt end) is NOT moved.
- Twin gate V5 identity and V6 grid are verdict clauses; V1 is an equality on the previous turn's published length. New `tools/prefix-restore-identity-gate.py` (turn 10 of the day-16 chain, five points).
- Local RTX 5090, day-16 shape: base `... identity_ok=11/12 grid_ok=0/31 off_grid_calls=11 ... V5=FAIL V6=FAIL -> FAIL` (turn 10 = day 17's flip, twelve digests = day 17's), fix `... identity_ok=12/12 grid_ok=31/31 off_grid_calls=0 ... -> PASS`; restore gate base `identical=3/5 grid_ok=2/5 -> FAIL`, fix `identical=5/5 grid_ok=5/5 -> PASS`. Target card (one RTX PRO 6000), fix: twin `-> PASS` (identity 12/12, grid 31/31), restore `5/5 5/5 -> PASS`, serve-smoke `0 failed`, cache-meter `0 failed`.
- Moved on the target card: `PREFIX-EVICT-RECLAIM ... pool_retained_bytes=148013056 ... V3=FAIL` on the fix, `-> PASS` on base built there; cause: the 71-token busy peer's seed is refused on the fix (aligned 32 < 64), so the reclaim evicts E1 alone and V3 sees the pool's ~140-148 MB retention that base covered by also evicting that 159 MB seed. A finding about my day-13 gate's V3 (pool_retained not in its arithmetic); not changed after the result. Lane A's gate: my pre-run move of its equalities to the aligned capture was wrong (its entries are `insert (spec-boundary)` at the prompt end); restored, fix arm `PASS` on the rerun.
- BLOCKER for "must stay ALL GREEN": `tools/spec-on-cache-hit-gate.sh qwen` on the local card: fix `2 FAILURE(S)` (r3, g2: spec-on text != spec-off text), base `ALL GREEN`. Cause: spec-on restores the spec entry at the off-grid 106-token prompt end (13-token suffix, tokenwise), spec-off restores the aligned 64 and primes 55 from the grid (== cold); base agreed because both sides ran the same off-grid program. Fixing it means aligning the spec `capture_at` too and redesigning that gate's FULL-COVER cells and its `restore-full-cover` site check; named in #602 for the lead, not started, nothing relaxed.
- Local checks at 66c0aea89: fmt PASS, `cargo test -p memra-server` 758 passed, clippy -D warnings PASS, check-flags no uncovered names, docs-registry census PASS, `git diff --check` clean, `verify-day18.py`: `DAY18 REPLAY OK`.
- Open follow-ups named: spec-path capture alignment + #379 fixture; `prefix_fanout_groups` takes the raw in-batch LCP (inspection); evict-reclaim V3 and pool_retained.
- Decide-by 2026-10-04 for the --kv-allocator vmm door unchanged.
