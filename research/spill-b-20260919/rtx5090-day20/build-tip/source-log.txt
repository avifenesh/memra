a843686ce19ce2a4fe93117ab6033f4bd736fe96 gates(prefix-cache): the #379 gate's boundary-site list drops the restore-suffix-feed site its cells cannot reach (the deferred restore draws in the prime path; a day-19 addition removed the same day), lane A's promote equalities read capture_len of the leader's prompt now that the spec capture is on the grid (64 of 83 measured on the target card), SERVING and TESTING state the law for both capture sites
74ee484bca2ba10c43e4e03d452a35ffa4c6e0ce fix(prefix-cache): the spec session's capture lands on the GDN prime grid too (memra#602, day 19): the cold spec capture_at is the seed's grid boundary and its own prime stop beside the affinity boundary, the restored session's republish lands on the stable or seed grid boundary and the engine's prompt-end republish fires only on the grid; the #379 gate restates its accounting under the capture law with an on-grid full-cover cell; the evict-reclaim gate's V3 counts pool_retained_bytes as headroom
fe28c5c078ab06d961358a26b750b3e97d5e6898 fix(prefix-cache): the prompt-end seed lands on the GDN prime grid (memra#602): seed_capture_boundary publishes at the largest grid-aligned length under the prompt end through the prefill tick's existing boundary stop, refuses typed and counted when that length is under the entry floor, and cached_tokens reports the aligned restored length; twin gate V5 identity and V6 grid are verdict clauses, sibling gate prefix-restore-identity-gate.py on the day-17 five restore points, lane A's promote equalities read the aligned capture
9466b89122a5815dacef0dd54db30fb1476ed533 test(prefix-policy-ab): interleaved A/B harness for the prefix-cache policy on the incident's shape (slru vs lru, AB/BA pairs, digest identity precondition, computed tokens primary, cohort return cached secondary); day-15 pre-registration and offline predictor (#523 item 2)
