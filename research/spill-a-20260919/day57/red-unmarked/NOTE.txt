These ten runs failed at the guard, but the red arm's marker literal was elided from the binary (an unused let), so the grep check could not confirm the patch; re-run with a marker that prints, in ../red.
