# WP-A day 22 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged `origin/main` `5df11152f` as `d2530e362` and the lead's `lane/spill-integ37-20260922` (`40964560a`, the #638 fixes) as `ac1ac91c5`; pre-registration `241d175b5`; tier schedule `898a24b20`; engine, worker, gate cells `a95f1ac92` (`wip:` prefix, stands per DAY22.md); the card fix `bb1a2b212`; receipts and records `data:`/`docs:` after it. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed. Write-up `DAY22.md`.
- Move 2 slice 3 landed under `MEMRA_KV_HOST_CONTRACTS=1` (default OFF, decide-by 2026-10-05): both D2D classes carry a RECEIPT. `memra_tier::conformance::d2d_receipt_witnessed` and `d2d_receipt_refused` (the program `receipt_digest`: four wrapping u64 lanes over LE words, the byte count folded; contracts 81 passed); the engine's `cu/tier_receipt.cu` (`d2d_receipt_digest`, `tier_delay_spin`; its own fatbin, loaded by `new_with_copy_stream`), per batch a `ReceiptScratch` (lanes zeroed on the owner stream and fenced, a pinned twin, the receipt event), per item the source digest behind the producer fence, the copy, the destination digest, then one D2H and the receipt event; `progress` lands an item only with its lanes read (destination digest = checksum, source digest = expectation, `Completion::require`'s clause compares); `d2d_receipt`; `inject_d2d_early_reader`; the worker's `D2D capture receipt` and `D2D restore receipt` lines naming both digests, `ReceiptMismatch` arms (capture: planes back and dropped, nothing published; restore: cache dropped, pin released, nothing primed), tier and route latch; fault values `d2d-delay-capture` / `d2d-delay-restore`; fault gate cells `d2d-capture`, `d2d-restore`. No new flag beyond the two fault values; no new numeric program for the KV bytes; `unsafe` only at the two documented launches and the pinned zero-fill.
- Target card (BOX3, one RTX PRO 6000 Blackwell, 600 W; tree `bb1a2b212`, binary `2261af788b0a…`): identity `ALL GREEN (teeth=0)` default and plain, OFF and ON; failure `ALL GREEN` both arms; fault `ALL GREEN` (8 cells; both D2D cells refused with source `d11e5c4b614f3a68..` against the early reader's `1462aed093ef5fee..`); twin `-> PASS` both arms; hit `ALL GREEN (qwen)` both arms; unit cells `8 passed` plus the engine `d2d_*` cells `5 passed`. The green arm live in the plain identity ON arm: 4 receipt lines `require=ok`, equal digests, the restore's source digest equal to its entry's capture digest.
- First sitting (tree `ac1ac91c5`): every gate green; engine cells `3 passed; 2 failed` (the early-reader cell read the landed copy: cudarc's implicit event tracking in the plain test context, which the engine disables at `Engine::new`; the price cell's `elapsed_ms` on timing-disabled events). Fixed in `bb1a2b212` (the lanes' zero-fill fenced explicitly; the fixture disables tracking as the engine does; timing events); every gate re-run on the fixed tree.
- Cell (v), verbatim: one digest 0.168 ms against the copy's 0.158 ms on 158 MiB (1.06x), the pair 0.335 ms (2.12x to 2.15x), N=5 per order, both orders. By the day-19 rule the pair EXCEEDS the copy's own time: reported for the door review, nothing relaxed, nothing decided here; the cost sits on the copy stream, off the tick.
- The lead's #638 notes: the take-and-match audit found one instance (the lead's, fixed in integ37); slice 3 adds no pending or ready state, so the parked-only wait guard and the orphan grace gain no arm.
- CPU: clippy `-D warnings` clean on tier (all targets), engine and server (lib, tests); server lib 806 passed; fmt, diff-check, flags, marker and boundary censuses green.
- Local RTX 5090: not touched today; the door gates on this tree are owed with the lock (as on days 18 to 21).
- Move 2 still owes (`OWNER-THREAD-OFFLOAD.md`): the recurrent f32 state off the tick; the spec-boundary capture route and the draft-bearing restore; an isolating stall cell; the review's reading of cell (v). Move 1 still owes: the receipt hashes off the tick, the by-reference demote routes, the same-window decision cell.
- BOX3 `/root/wt-a` at `bb1a2b212` on `lane-a-day22`; `/root/spill-receipts/a-day22/` and `a-day22-run1/` mirrored to `pro-single-day22/box/` and `box-run1/` (bins not mirrored); bundles removed both ends; no server of mine running. #536 comment posted; issue open.
