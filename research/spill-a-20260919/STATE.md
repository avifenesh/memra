# WP-A day 14 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged `origin/main` `2a589903d` (`2b2191afe`). Commits: pre-registration `42f66f8d2`, 5090 first sitting `4e9a75c1c`, the per-device default `4488d83fe`, 5090 second sitting `8a60da279`; the records commit that names this state is the branch tip. Write-up `DAY14.md`.
- Default (lead ruling 22): `PinnedKind::for_device` is `Cached` on the RTX PRO 6000 Blackwell class (day 13 receipt) and `WriteCombined` on the RTX 5090 class and on every class without a receipt; keyed on the device name through `parallel::HardwareTarget::from_device_name` (`pub(crate)`), resolved once in `CudaTransfers::new`, `alloc_host` takes it, `alloc_host_kind` stays the gate's arm, no `MEMRA_*` read, flag bits per kind unchanged (4, 0). Record `docs/decisions/PINNED-DESTINATIONS.md`.
- 5090 cell (`rtx5090-day14/pinned-ab-160m`, one RTX 5090 Laptop GPU, no power limit reported, 55 to 57 C, 28 to 29 W, N=5 per arm per order): `cached_arm=inconclusive` (bind hash 37.5 against 1449 ms 10/10; D2H 7.34 against 7.27 ms, cached not above at 5/10, above in both orders' medians; byte exact 22/22). `pinned-ab-16m`: D2H 0.74 against 0.70 ms, 0/10, ranges disjoint. Replays `day14-wc-ab-*.log` agree with the binary.
- Second sittings through the new default, tree `4488d83fe`: 5090 (`rtx5090-day14/*-s2`) resolves write-combined, driver flags 6 at every size; target card (`pro-single-day14/*-s2`, one RTX PRO 6000 Blackwell at 600 W) resolves cached, driver flags 2 at every size; conformance 13 `PASS` and roundtrip six `byte_exact=true` on both, lines identical to the prior sittings; the four `tier_transfer` cells pass on both cards.
- CPU gates green (`day14/gates/`): fmt, engine 518 / kv 71 / tier 7 tests, clippy `-D warnings`, flags census, docs-registry census, diff-check.
- Push: `MEMRA_RELEASE_QUALIFICATION_MODE=development git push` at `5a89bd0ac` (announced `UNQUALIFIED DEVELOPMENT`, logged in `.git/memra-gate-skips.log`, claims no qualification; `DAY14.md` section Push); remote tip `5a89bd0ac` plus the push-record commit.
- BOX3 `/root/wt-a` on `lane-a-day14` at `4488d83fe`, clean; `/root/spill-receipts/a-day14/bins/` holds the gate binary (not mirrored). No tmux session left. Local `rtx5090-day14/bins/` is gitignored.
- Follow-ups, stated: a served long-entry cell under the door (lead or C); the ticket-lifecycle split inside C's 130 ms; the RTX 5090 class moves only on a rule-meeting cell or a lead ruling on the D2H clause. memra#552 gets the decision pointer.
