# WP-A day 23 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged the lead's `lane/spill-integ38-20260922` (`643ecbb28`) as `bee1ca332` (PR #639 was open, `origin/main` not merged separately); pre-registration `0717ab470`; tier rule `d16cb66e9`; engine seam and worker `daf9c30fc`; box scripts `01971da3d`; clippy fix `2b850b2b0` (the card's tree); records after it. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed. Write-up `DAY23.md`.
- Move 2 owed item 2, the restore half, landed under `MEMRA_KV_HOST_CONTRACTS=1` (default OFF, decide-by 2026-10-05): the MTP draft-bearing restore through the door, per census item 11. Tier `d2d_restore_draft_ready` plus the `draft_read_before_its_wait` red arm (contracts 84 passed); engine `RestoredDraftScratch` (`alloc_restored_draft_scratch`, `destinations`, `set_len`) and `spec_session_from_restored_ready` (no draft copy; the OFF constructor shares the alloc and the tail); worker: the draft decision at the probe before the submit (`host_restore_draft_decision` over the gate's spec estimate and `spec_restore_refusal`), the scratch allocated and `len`-set before the producer fence, the draft K and V rows as two more items of the SAME batch (same pin, same receipt, `items=34`), `PendingRestore` owns the scratch (dropped with the cache, forgotten under `Latched`), `take_ready` under `ready` only, `admit` builds over the ready scratch or names the arm it takes. No new flag; FLAGS.md door sentence extended.
- Target card (BOX3, one RTX PRO 6000 Blackwell, 600 W; tree `2b850b2b0`, binary `8725826875750242…`), one sitting 09:54Z to 10:08Z: identity `ALL GREEN (teeth=0)` default and plain, OFF and ON; failure `ALL GREEN` both arms; fault `ALL GREEN` (93 ok; `d2d-restore` still refuses `d11e5c4b614f3a68..` against `1462aed093ef5fee..`); twin `-> PASS` both arms; hit `ALL GREEN (qwen)` OFF (61 ok) and ON armed (68 ok, `19 route submission(s)`): the spec-on boot's 12 draft-bearing hits `restore submitted off the tick ... 34 planes ...; draft plane N rows (X KB) in the batch`, 13 receipts `require=ok`, 12 `draft plane ready`, 12 `spec restore: ... + draft plane from cache`, zero refused, latched, declined or disagreement lines, every `spec==plain byte identity` ok; unit `8 passed` (server), `5 passed` (engine `d2d_*`). Envelope 31.97 to 507.02 W, 33 to 60 C.
- Findings: the draft route engaged on every draft-bearing hit the gate serves (12 of 16 hits, item 11's count) with the identity clause holding; the draft plane's share is 118,784 B per 64-token entry on the 27B (1,856 B per token); cell (v) reproduced (2.11x, 2.13x). No finding against the engine or the gates.
- CPU: fmt, clippy `-D warnings` all targets (tier, engine, server) and the `DOCS_RS=1` cross-target pass clean; server lib 808 passed; engine lib 531 passed; tier 84 passed; flags, marker and diff checks green.
- Local RTX 5090: not touched today; the door gates on this tree are owed with the lock (as on days 18 to 22).
- Move 2 still owes (`OWNER-THREAD-OFFLOAD.md`): the recurrent f32 state off the tick; the spec-boundary capture route (the draft plane at publication); an isolating stall cell; the review's reading of cell (v). Move 1 still owes: the receipt hashes off the tick, the by-reference demote routes, the same-window decision cell.
- BOX3 `/root/wt-a` at `2b850b2b0` on `lane-a-day23`; `/root/spill-receipts/a-day23/` mirrored to `pro-single-day23/box/` (bins not mirrored); bundles removed both ends; no server of mine running. #536 comment posted; issue open.
