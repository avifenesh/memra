# WP-A day 31 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Fast-forward merged integ47 (`160929a92`, A day 30 plus C day 38). Code `33b1285e0`; pre-registration and records `a21a05f09`; sitting scripts `6d940a97c`; receipts `50db70373`, `9fcd4eacf`, `f28b1f57a`; records after. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed. Write-up `DAY31.md`.
- Landed: DAY30's four small owed items. 1a: a `StagedSpans` guard returns the staging to the context's set on every post-take refusal (11 `return Err(` after the take, all inside it). 1b: a fresh staging buffer is charged to the governor's pinned ledger on allocation and released at the latch's `clear()`; a refused charge refuses the span attach through the typed `tier D2H spans refused` arm. 1c: `MEMRA_KV_HOST_FAULT=contract-spans` (a new value of the existing door, FLAGS row updated) and the fault gate's `span-refusal` cell, byte-compared with a door-OFF boot. 1d: the copy-complete line's `by slot class:` byte tally. No new `MEMRA_*` name, no new numeric program.
- Target card, one sitting on `6d940a97c`: identity x4 `ALL GREEN (teeth=0)`, failure x2 `ALL GREEN`, contract-fault `ALL GREEN` (142 ok, eleven cells), twin x2 PASS, hit OFF/ON `ALL GREEN (qwen)` with the day-24 census, unit 13 + 6 + 13 + 3, double-park 20 of 20 replays, `DAY30 A2 ... median=0.63 -> PASS`, `DAY28 ... ALL PASS`. Local RTX 5090: identity default ON, fault default and plain, hit OFF/ON, all ALL GREEN.
- Readings: the `postpublish` cell's next demote `pre-submit 0.93` (day 30 28.71; DAY30 finding 4 closed); one staging fill per boot (156,893,184 bytes charged on the 27B); the SSM state is 95.6 percent of the 27B image's heap bytes, 93.7 to 93.8 percent of the 9B's.
- **Integrable** as 1a to 1d complete; the lead integrates from `origin/lane/spill-a-20260919`. The H2D half of Move 2 owed item 1 is pre-registered (DAY31 section 2, design H, acceptance B1 to B5) and NOT landed; nothing claims it.
- Owed: the H2D half (its ordered list in DAY31 section 2), the D2D half, the strong-form receipt. Move 1 items 1, 3, 4 unchanged.
- BOX3 `/root/wt-a` at `6d940a97c` on `lane-a-day31`, clean; `/root/spill-receipts/a-day31/` mirrored to `pro-single-day31/box/` (the binary excluded, its sha256 kept); my bundle removed; no process of mine on the box; nothing of other lanes touched.
