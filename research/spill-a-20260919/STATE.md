# WP-A day 24 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged the lead's `lane/spill-integ39-20260922` (`1d57f4a57`) as `540fd2d8c` (PR #642 was OPEN, `origin/main` not merged separately); pre-registration `dc5eb64bc`; tier rule `46eed44d0`; worker route and FLAGS.md `0a4ff4b0b`; box scripts `185c57b4f` (the tree the card ran); records after it. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed. Write-up `DAY24.md`.
- Move 2 owed item 2, the CAPTURE half, landed under `MEMRA_KV_HOST_CONTRACTS=1` (default OFF, decide-by 2026-10-05): the spec-boundary capture through the door, per census item 10. Tier `d2d_capture_draft_publish` plus the `publish with the draft unlanded` red arm (contracts 87 passed). Worker: slice 1's submit half factored into `host_capture_submit` over `CaptureSubmit` (the seed route's admission, clones and lines unchanged; the trunk loop copies rows `[0..pos)`); `prefix_spec_capture_off_tick` asked after the publisher's early returns and before the latent arm, answering `OnTick` with the capture handed back untouched; the live trunk planes and the spec session's draft scratch rows `[0..pos)` as ONE batch behind one producer event at the drain sweep (`items=34` on the 27B); the recurrent state, logits and `last_h` owned by the `SpecBoundaryCapture` (no copy at publish); `CapturePlaneClass` and `Done { kv, draft }` so the `Capturing` entry owns both classes and publishes both or neither; the OFF trace role riding the pending state; both demotion sweeps settling a pending capture `Block` before consuming the session whose scratch the copy reads (item 10's borrow-lifetime question answered). Refused by name to the OFF program: latent planes or capture tails, a TP cache, a trunk layer short of the boundary, a snapshot not at `pos`, a cache with no KV plane. No new flag, no new engine seam.
- Target card (BOX3, one RTX PRO 6000 Blackwell, 600 W; tree `185c57b4f`, binary `1a1185fb554dd47b…`), one sitting 10:59Z to 11:13Z, zero lock retries with lane C sharing the card: identity `ALL GREEN (teeth=0)` x4; failure `ALL GREEN` x2; fault `ALL GREEN` (93 ok, both D2D cells refusing `d11e5c4b614f3a68..` against `1462aed093ef5fee..`); twin `-> PASS` x2; hit `ALL GREEN (qwen)` OFF (61 ok) and ON armed (68 ok, `30 route submission(s)`): all 11 spec-boundary publishes routed with their draft plane, 12 receipts `require=ok` (11 `items=34`), 12 published, the day-23 restores still engaging (13 submitted, 12 with the draft plane, 12 `draft plane ready`, 12 `draft plane from cache`), zero refused, trunk-only, dropped or latched lines, every `spec==plain byte identity` ok; unit `8 passed` (server) and `5 passed` (engine `d2d_*`). Envelope 34.26 to 506.63 W, 33 to 60 C.
- Findings: every spec-boundary publish the gate makes routed and the identity clause now covers BOTH halves of the door's D2D program for draft-bearing entries; the draft plane's share at publication is 118,784 B per 64-token entry on the 27B (1,856 B per token), the restore side's figure exactly; on this gate's shape every spec-boundary capture settles at the SESSION RETIRE (106.3 to 204.6 ms, `after 1 poll(s)`) rather than a tick-top poll, which is the retire seam behaving as the slice requires and bounds the moved share for a session retiring in the same tick as its prime stop (pricing it is owed item 3). No finding against the engine or the gates.
- CPU: fmt, clippy `-D warnings` all targets (tier, engine, server) and the `DOCS_RS=1` cross-target pass rc=0 with zero errors; server lib 809 passed; engine lib 531 passed; tier contracts 87 passed; flags, marker and diff checks green.
- Local RTX 5090: not touched today; the door gates on this tree are owed there with the lock (as on days 18 to 23).
- Move 2 still owes (`OWNER-THREAD-OFFLOAD.md`): the recurrent f32 state off the tick (the restore side); the publishes still on the tick by name (the fanout leader's and pause sweep's `prefix_snapshot`, `dspark-boundary`, `glm5-boundary`, every `OnTick` refusal); the isolating stall cell's capture share (C day 28's subtraction came out with the wrong sign); the review's reading of cell (v). Move 1 still owes: the receipt hashes off the tick, the by-reference demote routes, the same-window decision cell.
- BOX3 `/root/wt-a` at `185c57b4f` on `lane-a-day24`; `/root/spill-receipts/a-day24/` mirrored to `pro-single-day24/box/` (bins not mirrored); bundle and mirror tarball removed both ends; no server of mine running; lane C's server never touched.
