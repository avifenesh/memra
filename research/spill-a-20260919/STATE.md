# WP-A day 32 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Fast-forward onto `origin/main` `7029cd67c`, then `origin/main` `db5929017` (#677) merged as `71d21057c`, no conflict. Code: `3cabdd4a3` (the owner-segment field, log only), `e30bd5cbf` (tier `conformance/h2d_span.rs`), `148dc4a29` (engine `submit_h2d_spans` / `take_h2d_spans`), `441ef6607` (`HostF32::Heap(Arc<Vec<f32>>)`), `6461a9106` (the Fill job, the parked state, the settle's take-back), `4d1335cd6` (`MEMRA_KV_HOST_FAULT=contract-promote-spans`, the gate cell, the FLAGS row); scripts and reader `a86a39d22`; TESTING line `5ae139734`; receipts `95995ba1a` (5090), `09de19475` (target card); records after. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed. Write-up `DAY32.md`.
- Landed: the H2D half of Move 2 owed item 1 (DAY31 section 2 design H), behind `MEMRA_KV_HOST_CONTRACTS`: the hash helper fills the promote's staging from the resident recurrent planes (owned `Arc` clones) while the request parks; the poll that lands the fill submits the KV batch and 96 typed f32 H2D spans on one ticket; the settle takes the spans back after the landing and the reader wait and before the retire, each staging buffer back to the set through a guard. Departure named: `Arc<Vec<f32>>`, not `Arc<[f32]>` (the latter copies on conversion, on the door-OFF demote's owner thread). No new `MEMRA_*` name, no new numeric program.
- Target card, one sitting (after lane B's chain, 13:52Z to 14:46Z): B1 ALL GREEN (identity x4, failure x2, contract-fault twelve cells 160 ok, twin x2, hit OFF/ON with the day-24 census, unit 16 + 7 + 2 + 17 + 6); B2 `before ... N=90 median=4.58` and `after ... N=90 median=0.70 min=0.65 max=1.22 ... -> PASS`; B3 reading promote in-ms +10.30 (fill 6.40 ms, landed at poll 1); B4 `receipts=109 bad=0 ... -> PASS`; B5 PASS. Local RTX 5090: identity default ON, fault default and plain (160 ok), hit OFF/ON, all ALL GREEN; B4 13 of 13.
- Finding: DAY28 clause 1b (e2e ON minus OFF `<=+20.0`) reads `+22.5` / `+22.2 -> FAIL` on the H2D binary, `+12.2` / `+12.1 -> PASS` on the pre-H2D binary in the same hold: the parked request pays the fill. The tenant stall ON minus OFF improves, -8.2 / -8.4 to -9.1 / -9.2. Not a day-32 clause; stated, nothing moved.
- **Integrable** as a complete H2D half against B1 to B5, carrying the finding above for the lead. Owed: the D2D half, the strong-form receipt; a same-tick fill landing named, not pre-registered. Move 1 items 1, 3, 4 unchanged.
- BOX3 `/root/wt-a` at `5ae139734` on `lane-a-day32-h2d`, clean; `/root/spill-receipts/a-day32/` mirrored to `pro-single-day32/box/` (binaries excluded, sha256 kept); the bundle removed; no process of mine on the box; `pro-single-day32/run.log` ends `LANE-A-PRO-DONE`.
