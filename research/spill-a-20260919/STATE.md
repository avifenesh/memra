# WP-A day 13 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged `origin/main` `1b354be59` (`c2f13f6cc`, pushed). Commits: census `febd54201` (`PINNED-FLAGS.md`), seam `33405a186` (`PinnedKind`, `PinnedBacking`, `alloc_host_kind`, `tier-transfer-gate pinned-ab`), pre-registration `c65f3c11e`, flags-bit fix and replay `abfc3fc32`; the receipts and docs commit that names this state is the branch tip. Write-up `DAY13.md`.
- Seam: `alloc_host` unchanged (write-combined, flag bits 4, today's cudarc constant); `alloc_host_kind(.., PinnedKind::Cached)` is the gate's arm; no `MEMRA_*` read; the copy sites untouched. CPU cells pin the default bits and the single `malloc_host` site; the GPU cell reads back `cuMemHostGetFlags` (6 and 2 on the card: `DEVICEMAP` is implied on UVA).
- Target card (one RTX PRO 6000 Blackwell, 600 W): decision cell `pro-single-day13/pinned-ab-160m-s2` `cached_arm=wins-on-this-card` under the pre-registered rule (bind hash 77.6 against 1711 ms, 10/10 pairs; D2H 3.00 against 3.01; byte exact 22/22; `wc-ab.py` PASS 15 checks); sitting 1 `pinned-ab-160m` `inconclusive` on the per-pair D2H clause (8/10, 1 and 4 us) and `failed` by the gate's own equality assert on the flags (kept as the record). `conformance`, `roundtrip`, `gputest-s2` green.
- Discrepancy with C's whole-demote line resolved: `host_kv_planes_through_contract` hashes `p.len x row` (committed prefix, 1.75 MB at 89 tokens), not the 160.7 MB capacity the `demote:` line prints; the write-combined penalty is 22x per byte (98 to 123 MB/s against 2.16 GB/s, gate and Python probe agree).
- No default moved. Follow-ups, stated: the local RTX 5090 cell (per-hardware law); a served long-entry cell under the door; the split of the ticket lifecycle inside C's 130 ms. Decide-by review 2026-10-05 is the lead's ruling.
- Push: refused by the perf-ci hook at `ccc02604c` (`pre-push: engine files touched after the last perf-ci battery.`, the two engine files this lane changes); not overridden; the lead runs `local-ci --perf` on the integ tree; remote tip `c2f13f6cc`. `DAY13.md` §Push.
- BOX3 `/root/wt-a` on `lane-a-day13` at `abfc3fc32`, clean; `/root/spill-receipts/a-day13/bins/` holds the gate binary (not mirrored). No tmux session left.
- memra#552 open, commented; memra#384 and #385 unchanged from day 12.
