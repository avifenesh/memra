# WP-A day 12 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged `origin/main` `be07f2d36` (`e5b1c10d0`). Code `405466cf7` (memra#384 reclaim, gate, harness), `2798e0b2e` (harness `--basis`), `9887b1c0d` and `a0ff733ba` (gate: metrics token, two-entry device budget, ERE matchers); the receipts commit is the branch tip. Write-up `DAY12.md`.
- memra#384 done: `HostPrefixCache::reclaim_tenant_share` (this tenant's own unleased entries, oldest first, twin spared, leased skipped, bounded refusal with nothing evicted), `IdentitySlot::leased` in memra-kv, hook order reclaim-then-refuse, `/metrics` `prefix_host_tenant_reclaims`. Five CPU cells green; gate `tools/kv-host-tenant-reclaim-gate.sh <base|fix>` PASS on both arms on the target card (N=1, executed-not-qualified, 600 W); `verify-day12.py` `DAY12 PASS`; collector `--validate` exit 0. Not closed; commented.
- memra#385: plan `HOST-ARENA-STARTUP.md` (law, three candidates with code sites); harness `tools/pinned-host-reserve-bench.py`; BOX3 cell `arena-r2` 21.87 GiB: single 3598.8 ms vs chunked 3583.8 ms medians, N=5 pairs per order, driver serializes concurrent `cuMemHostAlloc` here. Harness receipt only; decision cell is the 2x B200 pair. Not closed; commented.
- Receipts: `pro-single-day12/` (BOX3 mirror, three attempts kept, binaries excluded), `day12/gates/` (CPU battery), `day12/verify-day12.log`, `day12/collector-validate.log`.
- CPU gates green under the quota on the code tree: fmt, `memra-server` 748 passed, `memra-kv` 73 passed, clippy server and kv `-D warnings`; docs gates on the final tree in `DAY12.md`.
- Open, stated: handoff import (`insert` direct) still evaporates at the cap; hugepage arm needs a pool; #385 decision cell out of scope; no 5090 run (no 5090-facing default changed).
- BOX3 `/root/wt-a` on `lane-a-day12` at the gate-fix commit, clean; `/root/spill-receipts/a-day12/bins/` holds the two binaries (133M, not mirrored). No tmux session left.
- Day 11 state unchanged: native bindings for the two contract rules not yet run on a card; io_uring DEFERRED.
