# WP-A day 26 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. No integ41 PR existed; the lead's local `lane/spill-integ41-20260922` (`e14c270a6`, contains main #643 and my day 25) merged `--no-ff` as `71e46fabe`, clean. Pre-registration `5f7d9035e`; the code `e008bf502` (the tree the card ran); the test-only clippy fix `55c56e8ee`; records after. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed. Write-up `DAY26.md`.
- Proposal 1 landed per ruling 36: `host_restore_promoted_this_admission` in `host_restore_park_probe` between the lookup and the class check; typed line `[prefix-cache] restore not routed (contracts door): the entry was promoted for this admission (insertion pin id=P, N tokens, model M); the tick program copies it` (the promote's ticket seq is not on the pin, so not printed); no flag, no new state, no numeric change; table test and source census added (server lib 811 passed).
- Acceptance gate on the target card (four collector holds plus the hit gate's flock, 12:55Z to 13:36Z, zero lock retries, 20 of 20 replays PASS): clause 1 PASS (100 of 100 ON runs `parked` 1, `restore submitted` 0, typed line 1); clause 2 FAIL (e2e 221.4 to 206.8, `on_minus_off=+91.4` against the expected +15.8: the demote's two hashes, 74.8, still sit between the hit's prime on tick A and the token emitted in tick B's host half, `advance_sample_emit`; the refusal removed only tick A's decode plus the slack, 14.6 observed); clause 3 FINDING (stall 149.4 to 81.8, IQR 0.0, 3.4 BELOW OFF's 85.3, `isolated`: the prime left the hash tick; the two stretched ticks now 92.4 and 95.3, sum 187.8 against day 25's 185.4; the demote's `in` 97.2 to 172.3 is the poll waiting a longer tick, `in - completion` 74.8 unchanged); clause 4 PASS (restore arm 100 of 100 parked once, landed); clause 5 PASS (hit OFF 61 ok, ON 68 ok, census equal to day 24's: 12/12/13/13, 2/2/3/3, 30 routes, 11 draft-plane captures, 0 typed refusals). Identity x4, failure x2 `ALL GREEN`, fault `ALL GREEN` (93 ok), twin x2 PASS, unit 8 + 5 passed. Identity ON route lines `1, 1, 1, 2, 1, 2` as pre-registered.
- Nothing tuned or relaxed; the code stays on the branch; keeping the refusal with clause 2 re-derived from the token-emission reading is the lead's ruling.
- Move 2 owed item 3 closed (ruling 36: the retire seam stays, 0.4 ms recorded; C day 30 read the capture share). Still owed: the recurrent f32 state off the tick; the publishes still on the tick by name; the receipt's price read by the door review. Move 1: the receipt hashes off the tick, the by-reference demote routes.
- Local RTX 5090 (ruling 36's both cards): hit gate OFF `ALL GREEN (qwen)` 61 ok and ON `ALL GREEN (qwen)` 68 ok on the day-26 tree (`rtx5090-day26/`), the ON census equal to day 24's exactly (12/12/13/13, 2/2/3/3, 30 routes, 11 draft-plane captures, 0 typed refusals); the launcher's first pass was an rc=127 path bug of mine (no gate ran, recorded in `battery.log`), fixed and rerun after a bounded wait behind another lane's processes.
- BOX3 `/root/wt-a` at `e008bf502` on `lane-a-day26`, clean; `/root/spill-receipts/a-day26/` mirrored to `pro-single-day26/box/` (bins not mirrored); bundle and tarball removed both ends; no server or process of mine running; nothing of other lanes touched.
