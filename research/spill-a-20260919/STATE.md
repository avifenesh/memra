# WP-A day 28 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged `origin/main` `22f7489a3` and the lead's local `lane/spill-integ43-20260922` `7cd2fc95d` (`HOSTPREFIX-DOOR.md` conflict: the integ side) as `c3e417a12`. Pre-registration `122e4e247`; code `45f824a75`; scripts `f9284a711`; records after. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed. Write-up `DAY28.md`.
- Ruling 39 (lead): option (a) landed. `HostHashWorker`, one long-lived helper per `HostTierContext`; `PendingDemote` gains the `Hashing` phase and an owner-thread ledger; every settle path meets `Hashing` through `host_demote_settle_with_deadline` before the contract step (a `Block` settle continues into the hash wait); `bind_tier_image` consumes the handed-in digests after a byte-count check (same `checksum` program, same bytes); `hash-helper-gone` and `hash-never-lands` on the existing `MEMRA_KV_HOST_FAULT` row (10 s wall deadline `HOST_HASH_DEADLINE`); `disable` and shutdown join the helper. CPU cells green (server lib `830 passed`), the bitwise digest cell among them.
- Target card (`pro-single-day28/box/`, one sitting, zero lock retries, 20 of 20 replays PASS): clause 1a stall `on_minus_off=-3.4 unc=0.1 isolated` (81.8/81.9 against 85.2) PASS; 1b e2e `+16.9 / +16.9 isolated` (132.3 against 115.4; day 27 +91.3) PASS; 1c `owner in-completion median=7.40 min=7.17 max=45.01` (N=100; day 27 74.8) PASS; helper 73.2 ms per 157.9 MB, 98 payloads, 100 of 100 landings at the tick-top poll; wall in-completion 86.5; tenant gaps 95.3 and 19.0 (day 27: 95.3 and 92.4). `DAY28 VERDICT clauses_failed=0 -> ALL PASS`.
- Clause 2 FAILS on the target card in the default (spec) arm: `identity-default-on` `4 FAILURE(S)`, `failure-on` `1 FAILURE(S)` (the `digest` cell), `contract-fault` `23 FAILURE(S)` (the four promote cells; the two demote cells' `the next demote publishes`); everything else `ALL GREEN` (identity x3, failure-off, twin x2, hit gate x2 with the day-24 census, the two hash cells 14 ok each, d2d cells). One cause: a hit inside the `Hashing` window (73 ms plus a tick here) is a miss, and the default arm's boundary insert puts r3 there; on the day-26 tree the bind ran inside the completing tick top, before admission. Not tuned. For ruling: park the request one tick on a hit that names the `Hashing` entry (recommended) or settle it synchronously at the probe; the fault gate's demote cells also want the boot's last publication awaited before `stop`. Until then the day-28 code is not the door's serving path.
- Second finding: the pre-submit segment (t0 to the submission's return: 32 pinned allocs, registration, fence, submit, then the 100 f32 D2H copies one `synchronize` at a time) is about 6 ms at steady state and 39 to 45 ms on the first two demotes per boot (first touch), 149 ms under the verify arm; it is now the demote's whole remaining owner-thread cost (Move 2 owed item 1).
- Local RTX 5090 (`rtx5090-day28/`): `hit-on` `ALL GREEN (qwen)` (census equal to day 24); `fault-plain` `ALL GREEN` (121 ok); `fault-default` `23 FAILURE(S)` (the same shape); `hit-off` `server died during boot` (the gate's `flock -w 300` timed out behind another lane's gate; empty log; card untouched), re-run recorded in DAY28 section 6.
- BOX3 `/root/wt-a` at `f9284a711` on `lane-a-day28`, clean; `/root/spill-receipts/a-day28/` (bin `52756f946dca77042c7a8ce6…`) mirrored to `pro-single-day28/box/` (bins excluded); bundles removed both ends; no server of mine outside the collector's cells; nothing of other lanes touched.
