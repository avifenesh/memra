# WP-A day 33 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged `origin/main` `0afd88e1d` (integ52, #684, A day 32) as `ad1afe6aa`, no conflict. Pre-registration `6846a400c` (DAY33 section 2, before any code). Code: tier rule 6 `f814af107`, engine `submit_h2d_spans_filled` `82472ff12`, server `045ab57d4` (the probe submits its filled spans in its tick; the day-32 Filling phase removed), the owner-hold cell `aad1e1797`, the log-only promote timeline `d7d54c7a9`. Scripts `93c7c37f7`. Receipts `1f0d12152`, `ba6d76810`, `57023773c`. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode. Write-up `DAY33.md`.
- Design F: the promote's staging fill is one host function on the copy stream ahead of the span copies; the probe submits the KV batch and the spans at once; the owner waits for nothing. Options W, C, P, G priced and not taken (G is the fallback, not pre-registered).
- 5090 (9B): identity default ON, fault default and plain (160 ok each), hit OFF/ON ALL GREEN; B4 13 of 13; door cells 16 of 16, engine span cells 3 of 3; owner segment median 0.41 ms. The stall reading showed no gain (`promote_in` 16.90 day 32, 17.10 day 33). Cause placed (section 5a): the copy lands 6.3 ms after the probe, inside its tick; the landing poll then holds the owner about 8.4 ms checksumming 16 write-combined KV leases (the 5090's pinned kind). The owner-hold hypothesis was refuted by its cell (3.55 ms of owner work while a 200 ms host function sat on the copy stream).
- Not yet delivered: the target-card sitting of DAY33 section 2 (scripts `pro-single-day33/`), waiting for the box's restore; design F stays until it reads.
- D2D half pre-registered, no code (DAY33 section 6): capture refuted by construction; the restore's log-only price cell decides.
- Next (lead's day 34): the H2D completion checksum moved to the hash helper under the door, `DAY34.md` first.
