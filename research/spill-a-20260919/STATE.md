# WP-A day 27 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. No integ42 PR existed; the lead's local `lane/spill-integ42-20260922` (`af92791f6`, contains main `bc6e2f44d` and my day 26) merged `--no-ff` as `cb9fa5ef3`, clean. Pre-registration `ad4f229e0`; records after. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed. Write-up `DAY27.md`. No engine code today.
- Ruling 37 (lead): the day-26 code stays; the 74.8 ms hash tick is Move 1 owed item 2. Read from the code (DAY27 section 1): the two Move 1 receipt hashes cover the KV planes only (1.9 MB on the 27B, 0.9 ms each on the target host; hash 1 inside `from submission to completion`, the stamp follows the settle at worker.rs 12416); the 74.8 is `bind_tier_image`'s ONE SHA-256 pass over the whole image, about 157 MB of it recurrent f32 in pageable heap (`HostF32::Heap`, no arena under the door) with no receipt partner, plus the pre-submit f32 D2H and the insert; OFF never computes it (`hpx.tier` None). The 5090's 21 to 23 ms per 54.8 MB is the same pass at that host's heap rate plus a write-combined read of the one-to-two-percent KV share. C's section D item 6 answered in `HOSTPREFIX-DOOR.md`.
- Options pre-registered (DAY27 section 2): (a) bundle hash of the heap payloads on a helper thread, `Hashing` phase, receipt term unchanged, two fault arms; (b) slice-3 four-lane program for the D2H receipt term (moves 1.9 MB hashes; (b') the bundle program too); (c) copy-stream GPU digest of pinned bytes (unmeasured, needs engine code; device-side form behind Move 2 owed item 1). Recommendation: (a), acceptance gate written. Nothing implemented; the lead rules.
- Digest cell, target host (`pro-single-day27/box/digest-micro/`): `sha_ms=77.589 lanes_ms=70.737 lanes_over_sha=0.912`, disjoint ranges, both compute-bound near 2.2 to 2.4 GB/s: (b') is worth about 7 ms of the 74.8. 5090 host: see DAY27 2b (bounded wait on the card).
- Section 3 (the day-26 double-park cell on the day-27 tree as the baseline for the chosen option): launched on the box at 15:06Z under the collector (`/root/spill-receipts/a-day27/`, driver.sh); outcome in DAY27 section 3 once mirrored to `pro-single-day27/box/`.
- Move 1 owed (restated in OWNER-THREAD-OFFLOAD day 27): the H2D settle-time owner wait; the bundle hash off the tick (item 2, correctly named); the by-reference routes; decision cell (i). Move 2 owed: the recurrent f32 state off the tick; the named publishes; the receipt's price read by the door review; ruling on day 26's clause 2 (closed by ruling 37).
- BOX3 `/root/wt-a` at `ad4f229e0` on `lane-a-day27`; `/root/spill-receipts/a-day27/` (bins: memra-server `2396b7d3...`, day27-digest-micro `65ffd3cf...`); no server of mine outside the collector's cells; nothing of other lanes touched; the bundle removed both ends.
