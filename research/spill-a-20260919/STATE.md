# WP-A day 12 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged `origin/main` `be07f2d36` (`e5b1c10d0`; main had not moved at the review). Code `405466cf7` (memra#384 reclaim, gate, harness), `2798e0b2e` (harness `--basis`), `9887b1c0d` and `a0ff733ba` (gate), `f122468a5` (day-12 receipts), `8b29b2aa3` (integ15 review of PR #597: deferred reclaim, wasted counter, FLAGS/SERVING aligned); the review receipts commit is the branch tip. Write-up `DAY12.md` (review section near the end).
- memra#384 done: `tenant_share_reclaim_plan` (pure, before the copy) and `reclaim_tenant_share` (after bind, right before `insert`; this tenant's own unleased entries, oldest first, twin spared, leased skipped, bounded refusal with nothing evicted); `reclaim_pending` consumed by `insert` or booked into `tenant_reclaims_wasted`; fixed-arena path reclaims inside `reserve_image(.., reclaim=true)` (imports pass false). `/metrics`: `prefix_host_tenant_reclaims`, `prefix_host_tenant_reclaims_wasted`. Six CPU cells green. Gate PASS: base `tenant-base-r3`, fix `tenant-fix-r4` on the review build (`d47caec8…`), 4 reclaims, 0 wasted; `verify-day12.py` `DAY12 PASS`; collector `--validate` exit 0. Issue open, commented.
- memra#385: plan `HOST-ARENA-STARTUP.md`; harness `tools/pinned-host-reserve-bench.py`; BOX3 cell `arena-r2` 21.87 GiB: single 3598.8 ms vs chunked 3583.8 ms medians, N=5 pairs per order; harness receipt only, decision cell is the 2x B200 pair. Issue open, commented.
- Receipts: `pro-single-day12/` (BOX3 mirror, four attempts kept, binaries excluded), `day12/gates/` (CPU battery, review run), `day12/verify-day12.log`, `day12/collector-validate*.log`.
- Push: the perf-ci hook refuses with the upstream range (main's engine files through the merge); the merge-base-with-main range (`push-range.sh` with the upstream unset, then set back) passes the whole battery. Recorded in `DAY12.md` §Push.
- Open, stated: handoff import (`insert` direct) still evaporates at the cap; hugepage arm needs a pool; #385 decision cell out of scope; no 5090 run (no 5090-facing default changed).
- BOX3 `/root/wt-a` on `lane-a-day12` at `8b29b2aa3`, clean; `/root/spill-receipts/a-day12/bins/` holds three binaries (not mirrored). No tmux session left.
- Day 11 state unchanged: native bindings for the two contract rules not yet run on a card; io_uring DEFERRED.
