# WP-A day 15 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged `origin/main` `a51e29abb` (`261241786`, #605 to #610). Commits: pre-registration `ad11f2a70`, harness plus replay plus cell scripts `f1aa058da`; the records commit that names this state is the branch tip. Write-up `DAY15.md`. Every push today in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode (`day15/push-attempt*.log`); no qualification claimed.
- memra#385 on the target card class (BOX3, one RTX PRO 6000 Blackwell, 600 W; host 30 CPUs, one NUMA node, 88.4 GiB, THP `madvise`, no hugetlb pool), size 53 GiB by the engine's 32 GiB margin, N=5 per arm per order, both orders, correctness pass per arm (byte exact, flags 3 everywhere): `arena-chunked` `candidate_arm=inconclusive` (7/10 pairs, ratio 1.0044, chunk completions 1.1 s apart: the driver serializes); `arena-thp` `candidate_arm=void (hugepage-requested-not-granted)` (min huge fraction 0.6361 in the correctness pass and 0.8435 in one timed row; where granted 2886 against 8877 ms, 10/10, ratio 3.08; where not, 18.3 s and 6.6 s, slower than today's call). Replays `arena-ab.py` PASS on both.
- No engine change landed (rule kept as pre-registered). A `thp` landing needs a hugetlb pool or a pre-registered page-cache regime, `reserve` plus `Drop` plus a fall-back policy (two sites, not one function), and the 2x B200 decision cell with the same harness. Today's `cuMemHostAlloc(PORTABLE)` stays.
- Door pair on C's harness (`pro-single-day15/wc-pair`, C tip `a6b62c998`, binary from `ad11f2a70`, engine source equal to `main`'s): `WC PAIR REPLAY: PASS (12 checks)`; demote OFF 37.8 against ON 113.6 ms pooled (steady 6.1 to 6.9 against 81.8 to 83.0), promote minus inline demote 4.4 against 5.8; identity across the four boots ok; banked as the decide-by review's input (2026-10-05), no verdict.
- Receipts mirrored without binaries (`pro-single-day15/`, collector `--validate` rc=0 on all three cells). BOX3 `/root/wt-a` clean at `f1aa058da` on `lane-a-day15`; `/root/spill-receipts/a-day15/bins/` holds the server binary; no `tmux` session left; the sitting took the box's page cache from 57 to 17 GiB (stated in DAY15).
- CPU gates green on `f1aa058da` (`day15/gates/`): fmt, flags census, public boundary (0 new), docs registry, diff-check, py_compile, bash -n, em-dash scan.
- Follow-ups, stated: the B200 decision cell (another box); a `MAP_HUGETLB` arm where a pool exists; the hash-versus-ticket split under the door (C's micro-cell); #385 comment posted with the receipt, issue open.
