# WP-A day 16 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged `origin/main` `653c997f4` (#613, #614) as `21307b636`. Commits: census and pre-registration `1646d421b`, stall harness `0ea1fd6b1` (+ `bf248692d` word-list fix), cancellation point, gate, design note and receipts `dfbf71ee1` (did not compile the server lib; fixed at once in `47b03b901`), then the records commit that names this state as the branch tip. Write-up `DAY16.md`. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed.
- memra#536 census (`OWNER-THREAD-CENSUS.md`): every class runs on the owner thread inside the tick; the door's D2H and H2D ride the owner stream through `CudaTransfers` (constructor argument) with synchronous publication; the prime's only cancellation point at the start of the day was the tick-top sweep.
- Stall cell on BOX3 (one RTX PRO 6000 Blackwell, 600 W, `MEMRA_SERVE_SPEC=0`, N=5 per arm per order, both orders, five cells, validate rc=0, replays PASS, zero errors, tenant text identical in all 100 runs; idle tick 13.4 ms, idle p99 14.8): prime (5122 tokens) five stretched ticks of 286 to 316 ms per run (`stall_median=301.5`); demote OFF 126 to 132 ms tick with a 42 ms demote (`117.5`), ON 202 to 208 with a 118 ms demote (`193.5`); promote OFF 98 ms steady (`85.0`), ON 176 ms steady (`162.8`). Receipts `pro-single-day16/box/`.
- Cancellation point landed (`progress::PrimeCancelScope`, `prime_cancel_point` in the three sequential walks, typed `PrimeCancelled`, worker `prime_cancelled_abort` + `[prime] cancelled` receipt, `abort_log` gains `fed`). Not covered, stated: the pipelined PP walks and `prime_cache_batch` (tick-top sweep). `tools/prime-cancel-gate.sh` on the target card: `PRIME-CANCEL GATE: PASS` (cancel at chunk 2 after 768 of 7488 rows, 0.34 s; cold and warm digests `f243df4517b99525` equal to the control boot).
- Hit gate and continuation gate on the box on the changed binary: see `DAY16.md` (their verdict lines); local RTX 5090 runs as recorded there (lane B held the card for most of the sitting).
- Design note `OWNER-THREAD-OFFLOAD.md`: Move 1 (door's D2H/H2D on a second stream, tick-top `poll`, `Demoting`/`promoting` states, two agent-days) and Move 2 (D2D capture/restore, a new contract, four agent-days after Move 1) with their decision cells; nothing started.
- BOX3 `/root/wt-a` at the lane tip on `lane-a-day16`; `/root/spill-receipts/a-day16/bins/` holds the two server binaries and the continuation gate (not mirrored); no `tmux` session.
- Follow-ups: #536 comment posted with the census and receipts, issue open; the lead's ruling on Move 1; the stall cell's second stretched demote tick (88 ms, present without a demote) is not attributed.
