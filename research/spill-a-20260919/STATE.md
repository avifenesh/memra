# WP-A day 17 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged `origin/main` `e2e9e294a` (#616) as `71cdb1383` (INDEX.md hunk, markers dropped, census OK). Commits: pre-registration `c761d9c01`, the slice `ba10a1252` (`wip:`), the submission-line rewording plus box scripts `fc46e230d`, then the records commit that names this state as the tip. Write-up `DAY17.md`. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode (the #589 gate is content-bound); no qualification claimed.
- Move 1 first slice landed under `MEMRA_KV_HOST_CONTRACTS=1` (default OFF, decide-by 2026-10-05): `CudaTransfers::new_with_copy_stream` (a D2H rides the copy stream behind the producer fence's event, no owner wait at submit; an H2D keeps the owner stream), the demote route split into submit and settle halves, the worker's one `Demoting` entry (`HostPrefixCache::demoting`), the tick-top poll as the run loop's first statement (idle wait capped at 2 ms while pending), settle-first at the hook, the promote hook and the tenant purge, publication through `host_demote_publish`. Contract, states, per-path rules, failure paths and identity law in `DAY17.md` "Pre-registration". No new flag, no new numeric program, no new `unsafe`.
- CPU: 765 server lib tests (5 new: the state machine, the failure paths, the settle-first census), clippy `-D warnings` clean on both crates, fmt, diff-check, flags and marker censuses green.
- Target card (BOX3, one RTX PRO 6000 Blackwell, 600 W, binary `ceaf238f…` from `fc46e230d`): identity gate `ALL GREEN (teeth=0)` default and plain, OFF and ON; fault gate `ALL GREEN` (62 ok); hit gate `ALL GREEN (qwen)` OFF and ON; twin gate `-> PASS` OFF and ON; failure gate `1 FAILURE(S)` both arms (the pre-existing `pool-full` line). Receipts `pro-single-day17/box/`.
- Stall cell, demote arm, target card, N=5 per arm per order, both orders, replay PASS, errors 0, tenant text identical: `stall_median=149.6 arm_max=163.4` against day 16's ON `193.5`/`207.6` and OFF `117.5`/`132.0`; pre-registered rule -> `toward_off` (44 ms less, the copy time; the two receipt hashes remain on the tick). Promote arm recorded, not claimed (`86.4`).
- Local RTX 5090 (9B artifact, `MEMRA_HOSTGATE_CACHE_MB=64`): attempt 1 red on the fault gate because my submission line carried the door's refusal marker (reworded, kept as `attempt1-line-collision/`); attempt 2: identity `ALL GREEN` x4, hit gate `ALL GREEN` x2, failure `1 FAILURE(S)` x2 (pool-full), fault gate `5 FAILURE(S)` all in `promote-reject` (the gate hardcodes `1 of 34 items`, the 9B prints 18); twin gate and GPU unit cells: see `DAY17.md` (lock held by other lanes for most of the sitting).
- Move 1 still owes (`OWNER-THREAD-OFFLOAD.md`): the promote on the copy stream with a `Promoting` admission state, the receipt hashes off the tick, the by-reference routes, then decision cell (i) with both classes.
- BOX3 `/root/wt-a` at `fc46e230d` on `lane-a-day17`; `/root/spill-receipts/a-day17/bins/` holds the binary (not mirrored); no `tmux`, no server left running. #536 comment posted with the receipt; issue open.
