# WP-A day 20 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged `origin/main` `45c4c3cd2` as `a06a923b7`; tier schedule `4e57efa00`; engine capture class `1770c604c`; worker `Capturing` entry `1c3cbb0dc` (`wip:` prefix, stands per DAY20.md); offload note and clippy fix `c7a6d3a5b`; receipts `data:`; records. Write-up `DAY20.md`. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed.
- Move 2 slice 1 landed under `MEMRA_KV_HOST_CONTRACTS=1` (default OFF, decide-by 2026-10-05): `memra_tier::conformance::d2d_capture_publish` (CPU bindings, red arm; contracts 74 passed); the engine's capture class (`CopyDirection::DeviceToDevice`, `D2dCapture`, `CudaTransfers::submit_d2d_capture` on the copy stream behind the producer event, no owner wait, no checksum term, `capture_landed`); the worker's `HostPrefixCache::capturing` with `prefix_capture_off_tick` under the seed and LCP-split publishes, the recurrent state `clone_dtod` on the owner stream at the boundary, tick-top publication through `insert_demoting`, settle-first at a second capture, purge, reclaim, trims, shutdown; the ledger's in-flight term doubled for the capture batch; `docs/FLAGS.md` day-20 sentence. No new flag, no new numeric program, no new `unsafe`.
- Correction to the day-19 pre-registration, made before any cell: `TransferOp::D2d(ContiguousCopy)` needs owned leases on both sides and a capture's source is the live cache's plane (borrowed), so the capture op is the engine's `D2dCapture` (borrowed source, owned registered destination), not a `TransferOp` variant. Slice 2's restore has the mirror problem; decided in its pre-registration.
- Target card (BOX3, one RTX PRO 6000 Blackwell, 600 W; tree `c7a6d3a5b`, binary `263fe777…`): identity `ALL GREEN (teeth=0)` default and plain, OFF and ON; failure `ALL GREEN` both arms; fault `ALL GREEN`; twin `-> PASS` both arms; hit `ALL GREEN (qwen)` both arms; unit cells `8 passed` plus the engine `d2d_capture_*` cell `1 passed`. The route engaged in the plain ON arm (two `capture submitted/published off the tick` pairs, 32 planes 158.8 MB each, 1 poll); spec-boundary publishes (`prefix_insert_from_spec_boundary`) keep the tick program.
- Capture stall cell (pre-registered, one hold, OFF/ON/ON/OFF, N=5 per arm per order, both orders, all four admissible, every re-post hit at `cached_tokens=5088`): C1 `flat` both passes (ON 355.4 vs OFF 355.4; ON 354.0 vs OFF 353.9; bound 6.0); C2 ON `server_capture_ms` median 227.9 (14.5 before the cache fills). The prime dominates both arms; the capture's own share is under the cell's resolution; decides nothing about the door.
- CPU: clippy `-D warnings` clean on tier (all targets), engine and server (lib, tests); server lib 798 passed; fmt, diff-check (own commits), flags, marker and boundary censuses green.
- Local RTX 5090: not touched today (budget); the door gates on this tree are owed with the lock (as on days 18 and 19); the day-18 27B twin `V3=FAIL` repro for lane B still owed.
- Move 2 still owes: slice 2 (the restore behind rule 3, `Restoring`, cells ii, iii, iv), slice 3 (the receipt term by the pre-registered rule, cell v), a capture-isolating stall cell, the spec-boundary capture route. Move 1 still owes: the receipt hashes off the tick, the by-reference demote routes, the same-window decision cell.
- BOX3 `/root/wt-a` at `c7a6d3a5b` on `lane-a-day20`; `/root/spill-receipts/a-day20/` mirrored to `pro-single-day20/box/` (bins not mirrored); bundles removed both ends; no server of mine running. #536 comment posted; issue open.
