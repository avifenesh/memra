# WP-A day 21 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged `origin/main` `4bb2afb63` as `2c656a3d4` and the lead's `lane/spill-integ36-20260922` (`bd6584f9b`, the #634 fixes) as `14b2d7b0f`; pre-registration `a3c2e7d75`; tier schedule `2e55f3a9f`; engine restore class `d8e66af09`; worker `Restoring` request `ce2089638`, `c2874bd42`, `1350f118b` (`wip:` prefix, stands per DAY21.md); receipts `data:`; records. Write-up `DAY21.md`. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed.
- Move 2 slice 2 landed under `MEMRA_KV_HOST_CONTRACTS=1` (default OFF, decide-by 2026-10-05): `memra_tier::conformance::d2d_restore_ready` (CPU bindings, `prime_early` red arm; contracts 77 passed); the engine's `D2dRestore` and `submit_d2d_restore` (borrowed pinned source, borrowed destination, unfenced at submit; `install_consumer_wait` fences unfenced non-D2H items; `restore_landed`); the worker's `HostPrefixCache::restoring` with `host_restore_park_probe` after the promote probe (the OFF checks split out as `prefix_restore_validate`; the pin; recurrent state, `len`, `len_d` on the owner stream; one batch; the park on the requeue), the tick-top poll installing the owner-stream wait, `host_restore_take_ready` at admit's hit site, settle-first at trims, reclaim, purge, shutdown, fail-closed and `Latched` (cache forgotten, pin kept), drain-release-latch on a refused submission; the ledger's third in-flight term. No new flag, no new numeric program, no new `unsafe`.
- Design decision before any code: the restore's destination is the request's fresh cache and its source is a published entry's plane, neither registered; the class is borrowed on both sides with the device LRU's pin as the producer guarantee and rule 3's installed owner-stream wait at the settle as the reader fence. `Restoring` is a state of the parked request, one per worker.
- Target card (BOX3, one RTX PRO 6000 Blackwell, 600 W; tree `1350f118b`, binary `ddde2083…`): identity `ALL GREEN (teeth=0)` default and plain, OFF and ON; failure `ALL GREEN` both arms; fault `ALL GREEN`; twin `-> PASS` both arms; hit `ALL GREEN (qwen)` both arms; unit cells `8 passed` plus the engine `d2d_capture_*` and `d2d_restore_*` cells `2 passed`. The route engaged in the plain identity ON arm (two `restore submitted/landed off the tick` pairs, 64 tokens, 32 planes, 158.8 MB); the hit gate's entries are all spec-boundary (draft-bearing) publishes, which the route refuses by name: its hits kept the tick program in both arms.
- Restore stall cell (pre-registered, one hold, OFF/ON/ON/OFF, N=5 per arm per order, both orders, all four admissible, every intruder `cached_tokens=5088` of 5121): C1 `on_under_off` both passes (ON 80.1 vs OFF 88.1; ON 80.2 vs OFF 87.9; bound 6.0); ON `arm_p99` 22.1 vs OFF 15.5: a split of the intruder's admission work across two ticks (about 8 ms moved), not a removal; the rows' share under resolution; C2 `server_restore_ms` 14.5 (the tick period). Decides nothing about the door.
- CPU: clippy `-D warnings` clean on tier (all targets), engine and server (lib, tests); server lib 803 passed; fmt, diff-check (own commits), flags, marker and boundary censuses green.
- Local RTX 5090: not touched today (budget); the door gates on this tree are owed with the lock (as on days 18 to 20).
- Move 2 still owes: slice 3 (the receipt term, cell v) with the `d2d-delay` fault (cell iii), the recurrent f32 state off the tick (the restore's 157 MB fixed term), the spec-boundary capture route and the draft-bearing restore, an isolating stall cell. Move 1 still owes: the receipt hashes off the tick, the by-reference demote routes, the same-window decision cell.
- BOX3 `/root/wt-a` at `1350f118b` on `lane-a-day21`; `/root/spill-receipts/a-day21/` mirrored to `pro-single-day21/box/` (bins not mirrored); bundles removed both ends; no server of mine running. #536 comment posted; issue open.
