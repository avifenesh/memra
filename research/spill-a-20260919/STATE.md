# WP-A day 19 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Day-18 5090 receipts settled `3a5c3cb86` (`data:`); merged `origin/lane/spill-integ32-20260922` `1a351ebc6` as `1467435f5` (carries `origin/main` `3df055601`, #626, and the lead's two revuto fixes on #627); rule 3 `b27f33a2d`; Move 2 pre-registration `428b920eb`; the settle-time wait `c96d51862` (`wip:` prefix, stands per DAY19.md); receipts `e3a6acd67`; records. Write-up `DAY19.md`. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed.
- Rule 3 (`crates/memra-tier/src/conformance/reader_fence.rs`, unversioned beside the frozen schedules): `consumer_fenced` is the installed wait on the reader's stream, never a flag or the copy's landing; schedules `h2d_reader_fence` (AtSubmit, AtSettle) and `h2d_reader_issued_before_its_wait_is_unordered`; CPU bindings `tests/contracts/reader_fence_bindings.rs` (at-submit ok, at-settle-with-wait ok, red arm flag-only fails the schedule). `memra-tier` contracts 71 passed.
- Move 1's first owed item is done under `MEMRA_KV_HOST_CONTRACTS=1` (default OFF, decide-by 2026-10-05): an off-owner H2D leaves `submit_batch` unfenced; `CudaTransfers::install_consumer_wait` installs the owner wait and the fence at the settle (after the completion is observed, before the receipt check and `ready_view`), called by `host_kv_planes_settle_promote` under `Poll` and `Block`; censuses pin the order; `docs/FLAGS.md` door row day-19 sentence. Owner-stream program and every D2H unchanged. No new flag, no new numeric program, no new `unsafe`.
- CPU: clippy `-D warnings` clean on tier (all targets), engine and server (lib, tests); engine `tier_transfer` 4 passed; server lib 788 passed; fmt, diff-check, flags, marker and boundary censuses green.
- Target card (BOX3, one RTX PRO 6000 Blackwell, 600 W; binary `bf353fc7…`): identity `ALL GREEN (teeth=0)` default and plain, OFF and ON; failure `ALL GREEN` both arms; fault `ALL GREEN` (65 ok); twin `-> PASS` both arms (`evictions=9`); hit `ALL GREEN (qwen)` both arms; unit cells `8 passed`; the ON arms carry `promote published off the tick` and `H2D receipt .. require=ok` lines, zero `reader wait refused`. No stall cell (an ordering move; its cell is the same-window A/B).
- Local RTX 5090: not touched today. Day-18 receipts (settled): identity green in three arms, default OFF NOT RUN (lock); 27B twin `V3=FAIL` in BOTH arms identically (not a door delta; cause not established; repro with a process listing owed; lane B's gate). The 5090 door gates on today's tree are owed with the lock.
- Move 2 pre-registered in `OWNER-THREAD-OFFLOAD.md` (D2D capture and restore off the tick): byte census on the 27B (157.3 MB recurrent state fixed per entry, 29.6 KB per token), one-program law under event-ordered publication, `Capturing`/`Restoring` per path, failure paths, the D2D contract and its schedules `d2d_capture_publish`/`d2d_restore_reader_fence` (receipt checksum decision rule), cells, slices (1.5/1.5/1 agent-days). First slice named: the capture on the copy stream with an event-ordered publication; not started.
- Move 1 still owes: the receipt hashes off the tick, the by-reference demote routes, the same-window decision cell with both classes.
- BOX3 `/root/wt-a` at `c96d51862` on `lane-a-day19`; `/root/spill-receipts/a-day19/` mirrored to `pro-single-day19/box/` (bins not mirrored); no server of mine running. #536 comment posted; issue open.
