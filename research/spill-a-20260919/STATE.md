# WP-A day 25 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. No integ40 PR or branch existed on origin, so `origin/main` (`ebe3fe17d`, integ39) merged as `d14b0f3b2`; C day 29 read from `origin/lane/spill-c-20260919` by `git show`, not merged. Pre-registration `c82b36d97`; the typed settle line `71ee2a64d` (the tree the card ran); records after it. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed. Write-up `DAY25.md`.
- Task 1, the double park (C day 29's finding), target card, one hold, twenty interleaved boots, N=5 per arm per order, both orders, 33 to 52 C, 32 to 362 W, all 20 replays PASS: tenant stall ON 149.4 / 149.4 against OFF 85.4 / 85.3 (`on_minus_off=+64.0 / +64.2 unc=0.1 -> isolated`); the request's e2e ON 221.4 against OFF 115.5 / 115.4 (`+105.8 / +105.9 unc=1.2 / 1.1 -> isolated`). Decomposition, 100 of 100 ON runs `parked_per_run=[2]`: re-admission 90.1 (89.7 to 91.5) = tick A's decode 13.46 + the inline demote's two on-tick hashes 74.9 (`demote_in - demote_completion`) + residual 1.8; poll-to-re-admit slack 0.10 ms; the copy itself about 0.5 ms (cell (v)). No existing typed refusal fits the shape, so the pair is door ON against OFF, stated as such.
- Verdict (c): BOTH. By construction the restore route parks a whole-entry device hit whose planes the same request's promote landed 6.5 ms earlier (one tick top for a 0.5 ms copy, unearned); the 90.1 ms magnitude is the hash tick's scheduling artifact (the tick top polls demote before restore, so the demote's 74.9 ms of host hashes precede the restore's poll). The tenant's +64 ms is the demote's hashes on one tick (57.9 of 64.1 by arithmetic), not the second park (inside the +1.8 residual). Which slice moved the demote's landing (C's question) is not determined.
- Proposal 1 (not implemented): in `host_restore_park_probe`, refuse the route by shape when `hpx.promoted_pin` names the hit entry (promoted at this tick top for this admission), one typed line, the OFF device-hit copy on the tick; acceptance gate: this cell again with `parked` 2 -> 1 and `restore submitted` 0 (100 of 100), e2e drops by the re-admission median (about 221 -> 131), the tenant stall within IQR of 149.4, the day-21 restore arm still parks once and lands, hit and identity gates ALL GREEN both arms.
- Task 2, the retire-settle share (owed item 3): `PendingCapture` gained `settle_after_ms` and `settle_held_ms`, printed as `; the settle held the owner thread H ms, entered A ms after submission` inside the publish line's parenthesis (no parser splits inside it; no new `MEMRA_*` read). Hit gate ON on the target card, same binary: `ALL GREEN (qwen)`, 68 ok, 0 FAIL, day 24's census counts exactly. Reading: the retire seam held the owner thread 0.37 to 0.44 ms (median 0.41, N=11), the tick-top poll 0.37 to 0.38 (N=3); entered 106 to 204 ms after submission; share 0.2 to 0.4 percent. Day 24's "host wait for the 159 MB copy at the retire" is refuted: the copy had landed, the 0.4 ms is the settle's fixed cost.
- Proposal 2 (not implemented): no reordering earned; poll-then-retire would break nothing and remove nothing measurable here. If the lead wants the seam to distinguish, the smallest form is a source-session identity on `PendingCapture` (block only for the source's retire), with the hit gate ON `held` unchanged and a CPU test that a non-source retire leaves the capture `Pending`. Recommended: leave the seam, record 0.4 ms.
- CPU: fmt, clippy `-D warnings` all targets (tier, engine, server) and the `DOCS_RS=1` cross-target pass rc=0; server lib 809 passed; flags, marker and diff checks green.
- Local RTX 5090: not touched today; the door gates on this tree are owed there with the lock (as on days 18 to 24).
- Move 2 still owes (`OWNER-THREAD-OFFLOAD.md`): the recurrent f32 state off the tick (the restore side); the publishes still on the tick by name; the isolating stall cell's capture share; the review's reading of cell (v); the lead's rulings on proposals 1 and 2. Move 1 still owes: the receipt hashes off the tick, the by-reference demote routes.
- BOX3 `/root/wt-a` at `71ee2a64d` on `lane-a-day25`, clean; `/root/spill-receipts/a-day25/` mirrored to `pro-single-day25/box/` (bins not mirrored); bundle removed both ends; no server of mine running; lane C's server never touched.
