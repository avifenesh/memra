# WP-A day 18 resumable state

- Lane `lane/spill-a-20260919`; Linux worktree `wt-spill-a`. Merged `origin/main` `dc192cd95` (#621) as `e58a62d74`, `origin/lane/spill-c-20260919` `304e8235c` as `5ec390ca3`, `origin/lane/spill-integ30-20260922` `340e8a474` (the lead's fail-closed fix on my day-17 settle arm) as `ccbf002ed`; each INDEX.md hunk was the inherited marker block, dropped, census OK. Commits: pre-registration `1390e5c7d`, the slice `f3e6be867` (`wip:`), the hook fix `3df0cb2b3` (`wip:`), records. Write-up `DAY18.md`. Every push in the announced `MEMRA_RELEASE_QUALIFICATION_MODE=development` mode; no qualification claimed.
- Move 1 promote half landed under `MEMRA_KV_HOST_CONTRACTS=1` (default OFF, decide-by 2026-10-05): the engine issues every copy on the copy stream and keeps the submit-time owner wait for an H2D; the promote decision moved to the admission loop before `admit(..)` (`host_promote_park_probe`, the body's predicates shared); a host hit submits and the request PARKS on the requeue; the one `Promoting` entry (`HostPrefixCache::promoting`) is polled at the tick top and published through the shared day-16 tail with the insertion pin held one tick; every other path settles it first; failures keep the day-16 typed outcomes with a one-tick memo; the fail-closed arm mirrors #622. Contract, states, per-path rules, failure paths and identity law in `DAY18.md` "Pre-registration". No new flag, no new numeric program, no new `unsafe`.
- The hook's settle-first now runs only before its own submission (behind the candidate check): run 1's stall receipt showed the parked request's re-admission paying a demote copy synchronously (`server_demote_ms` at 82 ms, 10 `settled synchronously by a promote` lines); fixed in `3df0cb2b3`, censuses moved.
- CPU: 786 server lib tests (6 new: the probe decision, poll and Ready, the day-16 outcomes with the memo, the fail-closed arm, latched-off, the settle-first and wiring census) plus one engine source census; clippy `-D warnings` clean on both crates; fmt, diff-check, flags and marker censuses green.
- Target card (BOX3, one RTX PRO 6000 Blackwell, 600 W), run 2 on the fixed tree (binary `f67f763a…`): identity `ALL GREEN (teeth=0)` default and plain, OFF and ON; failure `ALL GREEN` both arms (C's day-21 gate); fault `ALL GREEN` (64 ok); hit `ALL GREEN (qwen)` both arms; twin `-> PASS` both arms; unit cells `8 passed`. Run 1 (`f3e6be867`) read the same gate lines (receipts `pro-single-day18/box-run1/`).
- Stall cell, promote arm, N=5 per arm per order, both orders, replay PASS, errors 0, tenant text identical, every intruder `cached_tokens=64`: run 2 `stall_median=81.9 arm_max=131.1` against day 16's ON `162.8`/`211.2` and OFF `85.0`/`133.6` -> `at_off` (3.1 ms under OFF); against day 17's recorded `86.4` -> `promote_half_flat` (moved 4.5 ms, the pre-fixed threshold was 6). Run 1 (before the hook fix) `157.8` -> `flat`, recorded. Demote arm `149.7` (day 17: 149.6).
- Local RTX 5090: NOT RUN; the canonical lock was held by another lane for the whole sitting (75+ min); my run-1 driver instance died on my live edit of its file (stated in DAY18.md); `rtx5090-day18/resume.sh` waits detached in bounded retries against binary `ccc1673e…`; its receipts land in `rtx5090-day18/` when the card frees and are uncommitted until then.
- Move 1 still owes (`OWNER-THREAD-OFFLOAD.md`): the settle-time owner wait for an H2D (needs the tier crate's conformance first), the receipt hashes off the tick, the by-reference routes, the same-window decision cell with both classes. Move 2 (D2D capture and restore) is next.
- BOX3 `/root/wt-a` at `3df0cb2b3` on `lane-a-day18`; `/root/spill-receipts/a-day18-run1/` and `a-day18/` hold the receipts (bins not mirrored); no server of mine running. #536 comment posted with the receipt; issue open.
