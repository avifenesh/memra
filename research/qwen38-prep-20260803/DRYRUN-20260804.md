# Qwen 3.8 prep — runbook dry-run + day-one readiness re-check (2026-08-04)

Train `restructure/public-split` @ 3b98ca63. Release watch re-checked same day (WATCH.md
top section): **not out, no arch leaks, timeline holds at week of 2026-08-10**.

## Dry-run verdicts (every mechanical step runnable without the release)

| Step | Verdict | Receipt |
|---|---|---|
| Arch-diff checklist on the 3.6 config (self-test) | **PASS** — all §2 fields extract, self-diff 0/0, exit 0 | `arch-diff-selftest-36.txt`, tool `arch-diff-fields.py` |
| Arch-diff on the 3.6-FP8 sibling vs BF16 ref (cross-check) | **PASS** — exactly the 4 expected `quantization_config` diffs (fp8/e4m3/[128,128]/dynamic), 0 STOP-class | same file, second section |
| Converter main-model command (3.6 NVFP4 ST source) | **PASS after fix** — `--dry-run` plans 1252 tensors / 28.2G | run log inline below |
| Converter `--mtp` sidecar command (same source) | **PASS** — 18 tensors / 2.5G (the blk.64 NextN block + head) | run log inline below |
| GGUF header sanity command (`gguf_dump.py --no-tensors` on the daily) | **PASS after fix** — block_count 65 (64+MTP), heads 24/4 | inline below |
| `tools/serve-st-gate.sh` on the merged state (qwen35-4b BF16 dir, flock'd, one burst) | **GREEN 0 failed** — /models, chat coherent, CLI-vs-server 64/64 ids identical, quarantine holds | `serve-st-gate-dryrun-20260804.log` |
| Every §4/§6/§7 referenced script/binary exists at HEAD | **PASS** — fast-gate.sh, probe.txt, models.tsv (k27 line 32), map.tsv, make-trimmed-draft.sh, serve-smoke.sh, serve-st-gate.sh, local-ci.sh, p3-agentic-long.txt, pp512.txt, DRAFT-REGIME.md, RELEASING.md, changelog.sh, update-perf-board.py, run-gen/run-spec/kernel-check/frspec-owngen/prime-gate/memra-server/fp8_mmq_check binaries; env/flags verified in binaries (MEMRA_PROMPT_FILE/MEMRA_NGEN/MEMRA_SPEC_K/MEMRA_DRAFT, frspec-owngen --corpus-out/--limit/--validate); `nvfp4-27b-shape` now kernel_check.rs:1877 (runbook said 1616 — drifted line number only, section intact); converter fork commit bb090d1f1 = fork HEAD | this file |

Converter dry-run transcript (the two §3 commands, flags validated end-to-end):

```
$ ~/projects/llama.cpp/.venv/bin/python3 convert_hf_to_gguf.py \
      /data/ai-ml/hf-models/nvidia-qwen36-27b-nvfp4 --dry-run --outfile /tmp/dryrun-q27.gguf
INFO:gguf.gguf_writer:/tmp/dryrun-q27.gguf: n_tensors = 1252, total_size = 28.2G
INFO:gguf.gguf_writer:Dry run, not writing files

$ ... convert_hf_to_gguf.py <src> --mtp --outtype q8_0 --dry-run --outfile /tmp/dryrun-q27-mtp.gguf
INFO:gguf.gguf_writer:/tmp/dryrun-q27-mtp.gguf: n_tensors = 18, total_size = 2.5G

$ ~/projects/llama.cpp/.venv/bin/python3 gguf-py/gguf/scripts/gguf_dump.py --no-tensors \
      /data/ai-ml/hf-models/qwen36-27b-nvfp4-mtp/Qwen3.6-27B-NVFP4-Q4_K_M-mtp.gguf | grep -iE "block_count|head_count"
qwen35.block_count = 65 ; attention.head_count = 24 ; head_count_kv = 4
```

## Failures found by the dry-run → fixed in the runbook

1. **`python3 convert_hf_to_gguf.py` does not run**: system python3 has no torch; the fork's
   `.venv` had torch missing too (ModuleNotFoundError). Fixed the environment (uv-installed
   torch 2.13 cpu + numpy + transformers + safetensors into `~/projects/llama.cpp/.venv`)
   and the runbook now pins `.venv/bin/python3` and adds the free `--dry-run` pre-flight.
2. **`python3 gguf-py` header dump** was not a command. Replaced with the exact
   `gguf-py/gguf/scripts/gguf_dump.py --no-tensors` invocation (validated live).
3. Download size estimate corrected: the 3.6-27B BF16 repo is **15 shards ≈ 55 GB** (HF
   tree API), not 22 GB; FP8 sibling adds ~28 GB.

## Runbook revisions beyond fixes (the (a)-(c) deltas)

- **(a) §7 doctrine**: verified current — the llama-benching-STOPPED amendment (frozen
  denominators, self-competition pairing) is already the §7 text; no further edit needed.
- **(b) FP8-ST leg added as §3b** (prod direction): official `Qwen/Qwen3.8-27B-FP8`
  download in §1 → §2 gains a `quantization_config` STOP/GO row (block-128 e4m3 = GO) →
  block-128 loader gates (default host dequant exact; `MEMRA_FP8_BLK_GPU` byte-identical;
  FOLD/MMQ explicitly kept OFF with their negative receipts) → `tools/serve-st-gate.sh`
  battery → ST serving cells vs the 3.6 NV-27B-ST standing config, same-session law.
  Spec stays leg-B(GGUF)-only: dir ckpts spec-quarantined (research/serve-st-20260803).
  Note: no `research/fp8ship/` dir exists at HEAD — the serving-cell protocol cited is
  the serve-st + fp8st receipts (the receipts that do exist).
- **(c) day-one serve smokes added to §4.6**: constrained decoding (json_schema +
  json_object), /v1/models OR-schema shape, graceful-drain probe
  (research/serve-tail-20260804/run-battery.sh reused with the new model path).

## Day-one readiness delta — the 3 audit blockers re-verified at HEAD (3b98ca63)

1. **Stale q27 spec row** — PARTIALLY superseded by vt-fixes (research/vt-fixes-20260803):
   q27 K=3 was re-measured same-session interleaved at HEAD-4 (code 106.1→109.4, prose
   101.7→105.4, +3.1-3.6%, N=3, self-consistency PASS, acceptance bit-identical), so
   "spec throughput at HEAD is unknown" is answered, and the K=4 upside question is
   CLOSED (does NOT re-open at +7-9%; K=3 stays optimal). **What remains**: those are
   research-protocol rows (256-gen, code/prose prompts), NOT the board protocol
   (p1/p2/p3, published row). current-board.json `speculative` still carries the
   2026-07-18 pairing → the §7 same-session 3.6 re-pair alongside the 3.8 rows is STILL
   REQUIRED before publish. Blocker stands, scope reduced to "board re-pair in the
   bring-up session" (which §7 already schedules).
2. **Arch unverified** — STANDS, inherently blocked on the release. Re-checked 2026-08-04:
   no HF repo/stub, no transformers/llama.cpp PRs, no config leak (WATCH.md top section).
   The arch-diff step is now mechanized + self-tested (`arch-diff-fields.py`), so the
   verification itself is minutes once config.json exists.
3. **No q27 perf-CI cell** — STANDS. perf-cells.json at HEAD is unchanged: 9 cells
   (gemma x8 + qwen9b-plain-short), no q27. §5 of the runbook already schedules closing
   it together with the q38 cell.

New blocker-adjacent facts logged: GPU state at dry-run time had the allowlisted
llama-server --embedding (260 MiB) + a memra-server (4.7 GB) resident — the serve-st-gate
ran green alongside them under flock.
