# Session D day 13 handoff
- Lane `lane/spill-d-20260919`: `1113b99fd` (ruling 12 seam), `af2d2d3eb` (#545 gate), `eacbd9030` (records); the lead merged main again and pushed the lane (`e78bf933b`, on PR #592 integ12). Day-13 review fix `0e9e30b31` (both revuto findings; see below) pushed by this lane: the hook let it through once the main merges carried fresh perf-ci rows (the earlier refusals were for `run_lockstep.rs` and `hybrid_forward.rs` inherited through the merges; no override was ever used). The lead merges it into integ12.
- #545 (open, comment posted with the criteria ticked): `tools/portable-suites.sh` runs `cargo test -p memra-tier -p memra-kv -p memra-cli --offline --no-fail-fast` through the skip census (budget 0, floor 300; 334 passed, 14 binaries); ci.yml job `portable-suites` (no CUDA install) runs it, its teeth `tools/test_portable_suites.sh` (18 arms: three planted failures, one per crate; an undeclared SKIP under `tests/` and under `src/`; wiring) and the collector suite with BOTH rig lock paths held; `local-ci.sh` `cpu_chain()` runs the wrapper (fatal, no door). Review fix 1: `skip-census.py` static census scans `crates/<crate>/tests` too (memra-tokenizer's four `llama_parity` skips declared; gate-template fixture copy carries that tree).
- Teeth law learned: the planted copy builds in `target/portable-suites-teeth`, never the tree's target (same metadata hash, preserved mtimes: a shared dir reused the copy's planted memra-cli binary on the real tree). `DAY13.md` has the fingerprint evidence.
- Ruling 12 done: `MEMRA_TIER_BATTERY_LOCK_DIR` re-roots the two lock NAMES under a private dir in `tools/tier-battery.py` (`lock_table`) and `tools/tier-rig-bootstrap.sh`; `tests/battery/private_lock.py` (`PrivateLockMixin`) in every lock-taking class; lock-proof and the two legacy gates keep their literals (fixture copies substituted). Review fix 2: the seam alone moves nothing: `--execute`/`--dry-run` and lock-holding bootstrap runs refuse under it without `--private-lock-dir-for-tests` (flag without seam refuses too), loud stderr line, `"seam"` in lock.json/manifest (`lock_seam` in BOOTSTRAP.json), `--validate` refuses a foreign seam; red arms in test_day10/test_collector. Both real paths held: 24 failed before, 87 passed after (`day13/review/pytest-lock-held.log`, bwrap private /tmp, rig locks untouched).
- `fault.rs`: the committed day-11/day-12 receipt replays are strict (no `.exists()` guard).
- Docs: `docs/TESTING.md` (#545 paragraph; Collector "Private lock path under test" bullet), `docs/FLAGS.md` section 4 row, `tools/skip-census.tsv` header, `research/INDEX.md` day13 row.
- Finding for the lead: `ci-change-class.sh` treats `research/**` as docs-only, but ten test-time `research/` reads now exist across lanes A/B/C/D (list in DAY13.md); not touched today.
- Checks green: fmt, wrapper x3, pytest (with and without locks held), teeth, check-flags, docs-registry census, action pins, diff-check, YAML load, shellcheck. No GPU cell, no engine file, no flag default, no numeric program touched.
- Scratch: `/tmp/spill-d-day13/` and `/tmp/spill-d-day13b/` removed at close; receipts mirrored under `research/spill-d-20260919/day13/` and `day13/review/`.
- About 5.5 agent-hours in total (4 before the review, 1.5 on the two findings) against the 5-hour budget.
