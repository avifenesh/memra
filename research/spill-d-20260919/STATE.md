# Session D day 13 handoff
- Lane `lane/spill-d-20260919`, local only: merged `origin/main` `b013885ba` (`7fdc9a013`), then `1113b99fd` (ruling 12 seam), `af2d2d3eb` (#545 gate) and the records commit at the tip. Push refused by the pre-push perf-ci arm, verbatim `pre-push: engine files touched after the last perf-ci battery.` with `crates/memra-engine/src/bin/run_lockstep.rs` and `crates/memra-engine/src/hybrid_forward.rs`, both inherited through the main merge; no override used. The lead pushes.
- #545 (open, comment posted with the criteria ticked): `tools/portable-suites.sh` runs `cargo test -p memra-tier -p memra-kv -p memra-cli --offline --no-fail-fast` through the skip census (budget 0, floor 300; 325 passed, 14 binaries); ci.yml job `portable-suites` (no CUDA install) runs it, its teeth `tools/test_portable_suites.sh` (15 arms: three planted failures, one per crate; an undeclared SKIP; wiring) and the collector suite with BOTH rig lock paths held; `local-ci.sh` `cpu_chain()` runs the wrapper (fatal, no door).
- Teeth law learned: the planted copy builds in `target/portable-suites-teeth`, never the tree's target (same metadata hash, preserved mtimes: a shared dir reused the copy's planted memra-cli binary on the real tree). `DAY13.md` has the fingerprint evidence.
- Ruling 12 done: `MEMRA_TIER_BATTERY_LOCK_DIR` re-roots the two lock NAMES under a private dir in `tools/tier-battery.py` (`lock_table`) and `tools/tier-rig-bootstrap.sh`; `tests/battery/private_lock.py` (`PrivateLockMixin`) in every lock-taking class; lock-proof and the two legacy gates keep their literals (fixture copies substituted). Both real paths held: 24 failed before, 86 passed after (`day13/pytest-lock-held.log`, bwrap private /tmp, rig locks untouched).
- `fault.rs`: the committed day-11/day-12 receipt replays are strict (no `.exists()` guard).
- Docs: `docs/TESTING.md` (#545 paragraph; Collector "Private lock path under test" bullet), `docs/FLAGS.md` section 4 row, `tools/skip-census.tsv` header, `research/INDEX.md` day13 row.
- Finding for the lead: `ci-change-class.sh` treats `research/**` as docs-only, but ten test-time `research/` reads now exist across lanes A/B/C/D (list in DAY13.md); not touched today.
- Checks green: fmt, wrapper x3, pytest (with and without locks held), teeth, check-flags, docs-registry census, action pins, diff-check, YAML load, shellcheck. No GPU cell, no engine file, no flag default, no numeric program touched.
- Scratch: `/tmp/spill-d-day13/` removed at close; receipts mirrored under `research/spill-d-20260919/day13/`.
- About 4 agent-hours against the 5-hour budget.
