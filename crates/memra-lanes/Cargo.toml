[package]
name = "memra-lanes"
description = "Serving-lane types, SLO admission policy, and engine-truth step stats for the memra inference engine"
version.workspace = true
license.workspace = true
repository.workspace = true
# Shared crates.io browse metadata; see the note in the workspace manifest.
categories.workspace = true
keywords.workspace = true
rust-version.workspace = true
edition = "2021"
# Shared lane machinery (ARCHITECTURE-H100.md B3/D): lane types, admission policy,
# engine-truth step stats, metrics — consumed by memra-server natively and reusable by
# out-of-process controllers (the sidecar shape).

[dependencies]
serde = { version = "1", features = ["derive"] }
